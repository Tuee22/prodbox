# Legacy Tracking

**Status**: Authoritative source
**Supersedes**: N/A
**Referenced by**: [README.md](README.md),
[development_plan_standards.md](development_plan_standards.md),
[system-components.md](system-components.md),
[substrates.md](substrates.md),
[phase-0-planning-documentation.md](phase-0-planning-documentation.md),
[phase-1-runtime-cli-aws-foundations.md](phase-1-runtime-cli-aws-foundations.md),
[phase-2-gateway-dns.md](phase-2-gateway-dns.md),
[phase-3-chart-platform-vscode.md](phase-3-chart-platform-vscode.md),
[phase-4-lifecycle-canonical-paths.md](phase-4-lifecycle-canonical-paths.md),
[phase-6-clean-room-handoff.md](phase-6-clean-room-handoff.md),
[phase-7-aws-substrate-foundations.md](phase-7-aws-substrate-foundations.md),
[phase-8-email-invite-auth.md](phase-8-email-invite-auth.md),
[the engineering doctrine docs](../documents/engineering/README.md)
**Generated sections**: none

> **Purpose**: Record completed removal work and any surviving compatibility helper, duplicate
> surface, or tooling residue that is slated for deletion on the supported Haskell path.

> **Authoritative Reference**: [development_plan_standards.md](development_plan_standards.md#i-explicit-cleanup-and-removal-ledger)

## Ledger Status

The cleanup ledger preserves completed removal history. Python-removal work remains closed, the
doctrine-deviation residue scheduled by the Phase `1`–`4` reopen is fully closed, the
substrate-aware helper-fallback residue introduced by Sprints `7.5.b.i` / `7.5.b.ii.a` is
closed under Sprint `7.5.c`'s code follow-up, the two AWS-teardown-residue defects discovered
on May 19, 2026 (residual "Elevated AWS …" prompt strings + harness-internal long-lived-
residue bypass bug) are closed under Sprint `7.7`, and the May 20, 2026 batch of code-side
follow-ups (Sprint `0.7` non-TTY guardrails; Sprint `1.2` revision replacing `dhall-to-json`
with the native `dhall` library; Sprint `7.5.c.v.c` harness preflight residue policy; Sprints
`7.5.c.v.d`/`7.5.c.v.e` operational IAM policy compaction + S3 + SES grants) are recorded
under `Completed`. The May 21, 2026 batch of code-side follow-ups
(Sprint `7.5.c.v.f` substrate-aware threading of `prodbox host public-edge
--substrate {home-local,aws}` plus stderr breadcrumbs that make silent
exit structurally impossible; Sprint `4.10` `PulumiStateBackendSection`
decoder + `LongLivedPulumiBackend` module + `aws-ses-migrate-backend`
CLI scaffold; Sprint `4.11` `Lifecycle/Preconditions` + `Lifecycle/TagSweep`
+ `rke2 delete --cascade`/`--allow-pulumi-residue` flag matrix with
mutual exclusion; Sprint `4.12` `Lifecycle/K8sDrain` module wired into
the cascade; Sprint `4.13` `prodbox nuke` CLI scaffold with TTY guard
+ typed-confirmation literal + dry-run plan renderer) is recorded
under `Completed`. The May 21, 2026 follow-on extends that batch with
Sprint `4.13`'s five-step in-process orchestration body landing, the
matching `destroyLongLivedPulumiStateBucket` helper that empties the
versioned bucket before deletion, Sprint `4.10`'s
`pulumi/aws-ses/Pulumi.yaml` long-lived S3 `backend.url`
declaration, Sprint `4.10`'s `loadAdminAwsCredentials` +
`pulumiSesAdminBaseEnv` admin-credential switch on the aws-ses
ensure/destroy paths, Sprint `4.10`'s in-process
`migrateAwsSesStackBackend` body (export-from-MinIO →
import-into-long-lived), Sprint `4.11`'s full predicate inventory
(`noLiveClusterTaggedAws`, `noUndrainedK8sAwsResources`,
`noLiveOperationalIamUser`, `noLeftoverDnsBootstrapRecords`), and
Sprint `8.5`'s credential-setup form parser scaffold — all under
`Completed`. The `applyAwsTeardown` library-refactor follow-up
(Sprint 4.11 deliverable) remains deferred for the verbatim-text
preservation reason noted in
`DEVELOPMENT_PLAN/phase-4-lifecycle-canonical-paths.md` Sprint
4.11 status block. Sprint `4.14` (operator vocabulary contract
enforcement) landed the same day from a live
`prodbox rke2 delete --help` review and is recorded under
`Completed`: every `Sprint <number>` leak in `src/Prodbox/CLI/Spec.hs`
string literals, in `documents/cli/commands.md`, in the manpages
under `share/man/`, in the shell completions under
`share/completion/`, and in the test goldens under
`test/golden/cli/` is removed; a new `checkOperatorVocabulary`
scan in `src/Prodbox/CheckCode.hs` fails the gate on any future
regression. Sprint `4.15` (cascade tolerates absent cluster)
landed the same day with a live verification on this host: a new
`DrainSkipped String` constructor on `DrainResult` and a
`clusterReachable` probe make `prodbox rke2 delete --cascade --yes`
on a host without a cluster skip the drain cleanly and proceed to
the per-run Pulumi destroys. Sprint `4.15` has no `Pending Removal`
row because it is new behavior, not a vocabulary leak. The May 23, 2026
reopen of Phases `2`, `3`, and `4` (Sprints `2.17`, `2.18`, `2.19`,
`3.13`, `4.16`, `4.17`, `4.18`) reintroduced doctrine-aligned residue
to the `Pending Removal` table — file-existence stack predicates, the
`.prodbox-state/` host-side cache, the host-side chart-secret cache
and `.patroni-anchor-volume` marker, and the remaining `curl`
shell-outs — each scoped to its owning sprint. Other remaining open
work is operator-driven live validation (Sprint `7.5.c.v` live AWS
run, the live AwsSesStack admin-credential switch + migration body
exercise for Sprint `4.10`, the live cascade exercise against a host
with a running cluster + AWS substrate for Sprints `4.11`/`4.12`, and
Sprints `8.5`/`8.6`). The May 28, 2026
reopen of Phase `4` introduced Sprints `4.17.a` (reorder cascade to
doctrine-canonical drain-before-destroys sequence) and `4.17.b` (substrate-aware
drain kubeconfig). Both code surfaces landed May 28, 2026 and the corresponding
Pending Removal rows are now in `Completed` below. The live AWS-substrate
re-verification (full `prodbox test all --substrate aws` cycle completes cleanly
including the cascade) remains as the operator-driven closure gate for both
sprints. The doctrine update at
`documents/engineering/lifecycle_reconciliation_doctrine.md §5b` plus the new
substrate-aware drain section at
`documents/engineering/aws_integration_environment_doctrine.md §5.5` make the
correct sequence the documented contract. The 2026-06-11 Vault secret-management
refactor (Phases `0`/`1`/`3`/`4`/`5`/`7`/`8` reopen; see [README.md](README.md)
Closure Status) adds the plaintext-secret, Secret-mounted-plaintext-Dhall,
unencrypted-master-seed, unencrypted-Pulumi-backend, plaintext-Keycloak-Secret,
and sensitive-MinIO-object-name rows below, each scoped to its owning Vault-adoption
sprint and replaced per
[vault_doctrine.md](../documents/engineering/vault_doctrine.md). These rows track an
encryption-at-rest layer added *beneath* the existing secret model — the master-seed
HMAC derivation, the single-Dhall contract, and the retained-PV teardown are extended,
not removed.

The 2026-06-13 retained-storage topology reorg (Phase `4` extended with Sprint `4.31`; see
[README.md](README.md) Closure Status) adds the `prodboxId`-prefix, over-nested-chart-path,
bitnami-MinIO-Deployment, `vscode`-Deployment, and hand-applied-Vault-PV rows below, all owned by
Sprint `4.31`. They record the collapse to one `.data/<namespace>/<StatefulSet>/<replica>` scheme
under a single reconciler with every retained workload a StatefulSet — a refinement of the canonical
retained-storage paths that extends, it does not reverse, the Phase `3` storage-binding model.

The 2026-06-14 Vault-root finalization + cluster federation (Phase `2` reopen + Phases
`0`/`1`/`3`/`4`/`5`/`7`/`8` finalization; see [README.md](README.md) Closure Status) **repoints the
Vault rows below to own their COMPLETE removal — there is no migration bridge.** The
`FileSecret` / Secret-mounted plaintext Dhall surfaces are **removed, not bridged** (the
`SecretRefFile` constructor and its resolver arm are deleted from `Prodbox.Settings.SecretRef`),
and the **master-seed HMAC derivation model is itself removed** under option B:
`Prodbox.Secret.{Derive,MasterSeed,Inventory}`, the gateway daemon `/v1/secret/derive` +
`/v1/secret/ensure-namespace` RPC, the `checkRawMasterSeedReadScope` lint, and
`selfBootstrapOwnSecrets` are gone, there is no `master-seed` object in MinIO, and every
previously-derived or chart-generated secret becomes a Vault KV object fetched via Vault Kubernetes
auth. The completed rows below record the master-seed derivation modules + daemon RPC + lint
(Sprint `3.19`) and chart-generated `lookup`+`randAlphaNum` Secrets (Sprint `3.19`); pending rows
still track the host-CLI repo-root-config-as-SSoT model (Sprint `1.38`) and the repo-root
`VAULT_REFACTOR.md` proposal doc (Sprint `0.13`, folded into `vault_doctrine.md` +
`cluster_federation_doctrine.md`). The replacement model is the finalized
Vault-root architecture per
[vault_doctrine.md](../documents/engineering/vault_doctrine.md) and
[cluster_federation_doctrine.md](../documents/engineering/cluster_federation_doctrine.md).

The 2026-06-15 Model-B refinement (Pulumi-under-Vault + MinIO encryption finalized to the prodbox
object-level Vault-Transit envelope + whole-system zero-child-info framing; see [README.md](README.md)
Closure Status) **repoints two Vault rows and adds the Model-B residue rows below.** The
"MinIO object names that encode sensitive meaning" row now owns its replacement via the **shared
object-store** (HMAC opaque IDs + Vault-encrypted index) under Sprint `4.30`, and the
"Unencrypted Pulumi backend state" row now owns the **decrypt-to-scratch interposition** under
Sprint `7.14` alone (the `1.37` passphrase owner is dropped — Pulumi's own secrets provider is
dropped, not derived from Vault). The 2026-06-16 Sprint `7.14` partial landing updates those rows:
main stack cycles, production stack reads, and first-touch raw checkpoint migration now use
`Prodbox.Pulumi.EncryptedBackend`; raw backend env is confined to `LegacyPulumiBackend`
first-touch export/delete, while live deletion proof and the decision to remove or keep the
now-wrapper-backed `aws-ses migrate-backend` compatibility alias remain pending. New rows below add
the Pulumi DIY raw-S3 backend access
(`PULUMI_BACKEND_URL`), the `aws-ses` AES256-SSE-only long-lived treatment, and the Vault-derived
Pulumi passphrase / Pulumi secrets provider (all → uniform Model B envelope under Sprint `7.14`); the
cleartext-AAD `prodbox-envelope-v1` → `prodbox-envelope-v2` hashed-AAD migration (Sprint `4.30`); the
log/output + exists-vs-`NoSuchKey`/`stackPresentInList` oracle sites (Sprint `4.33` closed the
Haskell-side gate/redaction surface; Sprint `5.8` owns the live cross-surface proof); the
substrate/child-named k8s namespaces + downstream-identity surfaces (Sprint `2.26` closed the
gateway-mediated Vault custody surface; Sprint `4.32` closed the direct parent-side writer; Sprint
`4.33` closed the Haskell-side opaque-namespace audit; Sprint `5.8` owns the live proof); and the
role-revealing MinIO bucket names `prodbox` +
`prodbox-test-pulumi-backends`
(→ one generically-named bucket, Sprint `4.30`). The replacement model is the Model-B object-store
and decrypt-to-scratch interposition per
[vault_doctrine.md §9/§10](../documents/engineering/vault_doctrine.md). This refines, it does not
reverse, the 2026-06-14 rows above; no new phase reopens.

The 2026-06-16 phase-independence doctrine adoption (Phase `5` reopen — Sprint `5.8`; Phase `7`
reopen — Sprints `7.14`/`7.16`; owned by the new Sprint `0.15`; see [README.md](README.md)
Closure Status) **retires the "Blocked-by-a-later-phase" dependency framing and the overloaded use
of ⏸️ Blocked for pending live-infrastructure proofs**, and is the SSoT for the new dependency
model in
[development_plan_standards.md](development_plan_standards.md) Standards N (Phase Independence) +
O (Code-Local vs Live-Infra Proof) plus the amendments to A/C/H/M; all other plan and engineering
docs defer to those standards. The change is purely structural — no objective, feature, or
validation changes; only the dependency framing, the status semantics, the new per-phase
`Independent Validation` lines, and the relocations below. The Pending Removal row added below owns
the retirement of the backward-`Blocked by` framing and the live-infra-proof overload of ⏸️
Blocked under Sprint `0.15`. Per Standard D (reopen/cleanup history lives here, not in phase
narratives), the relocated cross-phase **reopened-phase narrative** is recorded as historical
record: Phase `5` Sprint `5.8` is reopened to adopt the phase-independence doctrine — its
cross-surface live sealed-Vault red-team proof (the `4.33`/`2.26`/`4.32` Haskell-side surfaces it
follows) is reframed from a phase-gating ⏸️ Blocked dependency into a distinct, non-blocking
`Live-proof: pending` note that never reopens or gates the earlier phases whose code-owned
surfaces already closed; Phase `7` Sprints `7.14` and `7.16` are reopened to adopt the same
doctrine — their remaining live-infrastructure proofs (live deletion of old raw Pulumi
checkpoints / SSE-only `aws-ses` migration for `7.14`; the live operator-prompt admin-credential
exercise for `7.16`) become `Live-proof: pending` notes rather than ⏸️ Blocked, and the
`aws_admin_for_test_simulation`/`test-config.dhall` fixture relocation keeps its existing rows
above unchanged. `⏸️ Blocked` is henceforth reserved strictly for a genuine unmet
earlier-or-same-phase or external prerequisite; a backward `Blocked by` is a structural defect to
be re-scoped, and AWS-substrate coverage of a home-validated suite-content sprint is tracked only
in [substrates.md](substrates.md)'s parity table and never marks the suite-content sprint or its
phase Blocked. This relocation is framing/history only; it neither reverses the 2026-06-15 rows
above nor changes what any sprint builds or proves.

## Pending Removal

| Item | Owning Sprint | Notes |
|------|---------------|-------|
| Internal command-surface identifiers that still carry the pre-Phase-5 names after the user-facing rename: the `Rke2Command`/`NativeRke2` ADT (+ `src/Prodbox/CLI/Rke2.hs` module name) now driving `prodbox cluster ...`; `PulumiCommand`/`NativePulumi` (+ `src/Prodbox/CLI/Pulumi.hs`) now driving `prodbox aws stack ...`; `K8sCommand`/`NativeK8s` folded under `cluster`; `HostPublicEdge` now routing `edge status`; `NativeCheckCode`/`NativeTlaCheck`/`NativeLint`/`NativeDocs` now under `dev`. The user-facing surface, parser, goldens, docs, and prerequisite remedies were all renamed (Sprint `1.34`); only these internal Haskell identifiers/module names are deferred. | Sprint `1.34` (internal-rename follow-up) | Sprint `1.34` did a clean cut of the user-facing surface with NO compatibility aliases — `prodbox rke2`/`pulumi`/`check-code`/`k8s`/`host public-edge`/`charts deploy` no longer parse. The handlers and ADT constructors keep their old names purely to bound the diff; rename them (`Rke2Command`→`ClusterCommand`, `CLI/Rke2.hs`→`CLI/Cluster.hs`, `PulumiCommand`→`AwsStackCommand`, etc.) so the code reads like the surface. No behavior change; gated by `prodbox dev check` + the parser round-trip test. |
| The `Prodbox.CLI.Command.PulumiResiduePolicy` constructor `BypassPerRunResidueOnly` and its `applyAwsTeardown` case branch (`src/Prodbox/Aws.hs`). Harness-internal-but-unused after Sprint `7.9`. | Sprint `7.9` | Sprint 7.9 switched the only production caller (`runAwsIamHarnessTeardown` postflight) to `BypassAllResidueForHarnessRefresh`, because post-Sprint-4.10 `aws-ses` is admin-credentialed and clearing operational `aws.*` cannot strand it. The constructor stays a valid ADT member (it still refuses on long-lived residue, exercised by unit-test Scenarios E/F/G of the `Sprint 7.7 applyAwsTeardown residue policy` group) but has no production caller. Remove once the operator-driven teardown surface is confirmed to never need a long-lived-only refusal mode. |
| The ad-hoc residue-assembly helpers — `Prodbox.Aws.categorizePulumiResidue` / `partitionResidueByLifecycle` and the bespoke per-command teardown sequences in `runNativeDelete` / `applyAwsTeardown`. | Sprints `4.21`, `7.8`, `4.26` | Folded into one `reconcileAbsent` reconciler over a class subset of the registry; the per-command imperative sequences become registry reconciliations (idempotent on re-run). (`categorizePulumiResidue` already routes its gate decision through the Sprint 4.20 `residueBlocksTeardownGate` combinator; the remaining work is folding the assembly + imperative teardown sequences into `reconcileAbsent`.) Sprint `4.26` additionally retires `categorizePulumiResidue` and routes the `rke2 delete` (default + cascade) and `nuke` dispatch through `runPlanWithOptions` so `--dry-run`/`--plan-file` are honored — the bespoke teardown sequences fold under the shared dry-run plan engine rather than each command hand-rolling its own destroy ordering. Preserve the deliberate refuse-gate vs reconciler split: default `delete`/`teardown` stay refuse-gates, only the cascade reconciles. |
| `.prodbox-state/` directory entirely, along with the helpers `awsEksTestStateDir` / `awsEksTestSnapshotPath` / `saveAwsEksTestStackSnapshot` / `loadAwsEksTestStackSnapshot` / `clearAwsEksTestStackSnapshot` (and their per-stack siblings), plus the `awsEksTestKubeconfigPath` helper and the SSH-key paths under `.prodbox-state/aws-test/id_ed25519{,.pub}`. The host-side cache is the root cause that made the cascade-credentials failure class possible in the first place. **Status (2026-05-27 later session)**: every external `loadXxxStackSnapshot` consumer is migrated to the live `Prodbox.Lifecycle.LiveResidue.fetchPerRunStackOutputs` read — `resolveSubstrateHostedZoneId`, `verifyAwsEksSnapshot`, `verifyAwsTestSnapshot`, `verifyAwsTestSshReachability` (sharing a new `fetchAwsTestNodes` helper), and `ensureAwsSubstratePlatformRuntime` all read live Pulumi outputs via the new pure parsers `parseAwsTestNodesFromOutputs` / `parseAwsEksTestStackFromOutputs`. **The two per-run stacks the home `prodbox test all` exercises (`aws-eks-test`, `aws-test`) now have NO on-disk snapshot cache**: their internal consumers (`ensureXxxStackResources` pre-check, `destroyXxxStackStatus`, `assertNoXxxStackResidue`) read live via new `fetchAwsEksTestSnapshotFromBackend` / `fetchAwsTestSnapshotFromBackend` (+ new full-snapshot `parseAwsTestStackFromOutputs`), and the `save`/`load`/`clear`/`snapshotToJson`/`snapshotFromJson`/`nodeToJson`/`<stack>SnapshotPath` (+ EKS `optionalString`) file-IO helpers are deleted. Fourth chunk (2026-05-30): `aws-eks-subzone` and `aws-ses` stacks migrated to the same pattern (new `parseAwsEksSubzoneStackFromOutputs` / `parseAwsSesStackFromOutputs` + `fetchXxxSnapshotFromBackend`; `save`/`load`/`clear`/`<stack>StateDir`/`<stack>SnapshotPath`/`snapshotToJson`/`snapshotFromJson` removed; `assertNoXxxStackResidue` drops the unused `Maybe <Snapshot>` parameter). Fifth chunk (2026-05-30): the EKS kubeconfig migration is **done** — new `Prodbox.Infra.AwsEksTestStack.withEksKubeconfig :: FilePath -> (FilePath -> IO a) -> IO a` materializes the kubeconfig into a scoped temp file via `Control.Exception.bracket` + `aws eks update-kubeconfig --kubeconfig <openTempFile>`; `materializeAwsEksKubeconfig` + `awsEksTestKubeconfigPath` + `awsEksTestStateDir` removed; `PublicEdge.substrateKubeconfigPath` deleted; all 8 consumer sites (`drainAwsEksClusterBeforeDestroy`, `withSubstrateKubectlEnvironment`, `withSubstrateEnvironment`, `withSubstrateKubeconfigEnv`, `runCascadeDrainPhase`, etc.) migrated. Sixth chunk (2026-05-30): the HA-RKE2 SSH-key migration is **done** — `pulumi/aws-test/Main.yaml` flips SSH-keypair ownership to Pulumi via a new `tls:PrivateKey` resource + `ssh_private_key` secret output; new `Prodbox.Infra.AwsTestStack.withAwsTestSshPrivateKey` materializes the private key into a scoped temp file with `setFileMode 0600`; `ensureAwsTestSshKey` / `readSshPublicKey` / `awsTestPrivateKeyPath` / `awsTestPublicKeyPath` / `awsTestStateDir` removed; the `publicKey` config-set entry removed; the one consumer (`verifyAwsTestSshReachability`) wraps in the new bracket. **Remaining on this row**: only the `forbidDotProdboxState` lint and the host-side chart-secret cache cleanup, both owned by the separate Sprint `3.13` rows below; with `3.13` closed, every code-side `.prodbox-state` write goes away and the lint lands. Live closure gates: `prodbox test all` on the home substrate (full lifecycle exercise) and the eventual AWS-substrate live run for the kubeconfig + SSH bracket paths. | Sprints `4.16`, `4.18` | Output caches replaced by on-demand `pulumi stack output --show-secrets` reads via new `Prodbox.Infra.StackOutputs.fetch`; kubeconfig replaced by an on-demand `aws eks update-kubeconfig` bracket; SSH key fetched into `mktemp` via the Pulumi stack-output secret. `forbidDotProdboxState` lint in `prodbox check-code` refuses any new `.prodbox-state/*` write. |
| Host-side chart-secret cache `resolveChartSecrets` (`src/Prodbox/Lib/ChartPlatform.hs:1212-1280`) plus `recoverPatroniSecretValues`, `mergeChartSecretValues`, and `shouldResetPatroniStorage`'s silent-reset arm (line 1343). The cache was a host-side bypass of the cluster-as-source-of-truth invariant. **Status (2026-05-30)**: Sprint 3.13's cluster-side pipeline is fully wired and gated. Seven chunks landed: (1) `src/Prodbox/Secret/Inventory.hs` expresses the doctrine-§6 derived-secret inventory in code; (2) `src/Prodbox/K8s/InCluster.hs` exposes the in-pod credentials loader + REST-path / manifest helpers + `K8sSecretOps` capability; (3) `src/Prodbox/Secret/EnsureNamespace.hs` implements the `applyDerivedSecrets` pipeline; (4) `inClusterK8sSecretOps` is the TLS-backed `K8sSecretOps` constructor using `crypton-connection` + `tls 2.x` + the in-pod CA store; (5) `src/Prodbox/Gateway/Daemon.hs::handleSecretEnsureNamespace` replaces the 503 stub with the full request-body parse + master-seed gate + ServiceAccount load + TLS client construction + `applyDerivedSecrets` invocation + structured response; (6) `charts/gateway/templates/{serviceaccount,rbac,service-clusterip}.yaml` + `deployments.yaml::serviceAccountName` give the daemon Pod an RBAC'd ServiceAccount + the unsuffixed in-cluster ClusterIP at `gateway.gateway.svc.cluster.local:8443`; (7) `charts/{keycloak,keycloak-postgres}/templates/secret-bootstrap-job.yaml` are Helm pre-install hooks that POST to the endpoint via the canonical Harbor curl mirror (`127.0.0.1:30080/prodbox/curl-mirror:8.11.0`) before the chart's own Secret manifests render. Test count 600 → 626 (+26) covering the inventory, K8s helpers, and the `applyDerivedSecrets` pipeline against a mock `K8sSecretOps`. **Chunk 8 (2026-05-31)** closed the chart-vs-daemon multi-writer race for the data-bound Secrets the daemon now owns. `charts/keycloak/templates/secret.yaml` no longer renders `keycloak-runtime` (daemon is sole writer of `KEYCLOAK_ADMIN_PASSWORD`); `charts/keycloak/templates/deployment.yaml` hardcodes `KEYCLOAK_ADMIN` as a literal env var (`value: "admin"`); `charts/keycloak-postgres/templates/00-secrets.yaml` removed entirely (daemon owns the three Patroni Secrets the Crunchy operator watches). `Prodbox.Secret.Inventory.DerivedSecretEntry` extends with `derivedSecretEntryStaticFields :: [(Text, Text)]` so the daemon writes Patroni `username` (per-role static) alongside `password` (HMAC-derived) atomically. **Chunks 9 + 10 + 11 + 12 + 13 + 14 (2026-05-31 later session)** close the host-side cache eradication: `UsersAdmin.loadKeycloakAdminPassword` reads via `kubectl get secret`; `AwsSesStack.persistKeycloakSmtpChartSecrets` kubectl-applies the `keycloak-smtp` Secret directly (chart-template removed); the OAuth client secrets + demo-user password join the daemon Inventory in `keycloak-oidc-clients` (Inventory shape extended to `derivedSecretEntryDerivedFields :: [(Text, Text)]`); `resolveChartSecrets` reduces to `pure (Right Map.empty)` after every `requireMapValue` is dropped from `valuesForKeycloak`/`KeycloakPostgres`/`Vscode`/`Websocket`; the entire Patroni-reset+anchor scaffolding (`shouldResetPatroniStorage`, `writePatroniResetMarker`, `.patroni-reset-required`/`.patroni-anchor-volume` markers, `patroniClusterStatusIndicatesFailure`, `patroniStorageExists`, `requiredKeysPresent`/`requiredKeyPresent`, `readOptionalSecretPassword`, `readOptionalPatroniAnchorVolumeName`) is removed; the surviving anchor-discovery sites query live k8s state via `discoverPatroniAnchorPersistentVolumeName`. Charts read all migrated fields via Helm `lookup` (cross-namespace for vscode/websocket). **Sprint 4.18's `forbidDotProdboxState` lint landed 2026-05-31 (still later)** as `checkForbidDotProdboxState` in `Prodbox.CheckCode`, wired into `haskellStyleViolations` — narrow scan of `src/` + `app/` Haskell string literals for the closed `.secrets.json` filename, with `CheckCode.hs` allowlisted as the lint's self-reference host and `test/` allowlisted for legitimate regression coverage. **Chunk 16 (2026-05-31 still later still)** closes the host-side cache eradication completely. Gateway per-node event-keys join the daemon Inventory (`gateway-event-keys` Secret with three `NODE_<X>_EVENT_KEY` fields derived via `gatewayEventKeyContext`); the daemon's `selfBootstrapOwnSecrets` startup function writes its own Secret right after acquiring the master seed (gracefully degrading when not in k8s or when RBAC is missing); `rbac.targetNamespaces` extends to include `gateway` so the daemon's SA can write to its own namespace; `charts/gateway/templates/configmap-config.yaml` reads the keys via Helm `lookup` and renders the per-node `event_keys` list directly. The host-side `resolveGatewayEventKeys` is gone, and `resolveOrGenerateStringMap`/`writeGeneratedMap`/`mergeRequiredKeys`/`writeStringMap`/`readStringMap`/`randomHexString`/`byteToHex`/`chartStateRootRelative`/`chartStateDir`/`ensureChartStateDir`/`repairChartStateDir` all deleted with them. Sprint 4.18's `forbidDotProdboxState` lint **broadens in lockstep** from the closed `.secrets.json` filename to the whole `.prodbox-state/` prefix; a new unit test pins the broader regression-resistance contract. After chunk 16 a grep for `.prodbox-state` in `src/` + `app/` string literals returns zero hits (only comments mention it for historical context). The live four-block preserved-data exercise remains the full-sprint closure gate. | Sprint `3.13` | Chart secrets become k8s Secrets materialized by the in-cluster gateway service per [secret_derivation_doctrine.md](../documents/engineering/secret_derivation_doctrine.md). Data-bound values derive from the master seed at MinIO `prodbox/master-seed`; non-data-bound values use Helm `lookup` + `randAlphaNum`. `shouldResetPatroniStorage` becomes a loud-failure check (derived password vs `pg_authid` mismatch), never a silent reset. |
| `<namespace>/.patroni-anchor-volume` marker file path and the rebind-decision consumer that reads it. The marker was a host-side cache of state already available from Kubernetes PVC objects. | Sprint `3.13` | Patroni anchor decision derives from `kubectl get pvc` state alone; the marker file path is removed; `forbidDotProdboxState` covers regression. |
| Every `curl` shell-out in `src/`: `Prodbox.Gateway.queryGatewayState` at `src/Prodbox/Gateway.hs:285-317`, `Prodbox.Gateway.Daemon.fetchPublicIp` at `src/Prodbox/Gateway/Daemon.hs:1341-1360`, `Prodbox.Dns.fetchPublicIp` at `src/Prodbox/Dns.hs:108-124`, and 10 sites in `src/Prodbox/TestValidation.hs` (lines 512, 863, 888, 910, 1216, 1234, 1261, 1270, 1334, 2140). The `toolCurl` prerequisite registration is removed in the same change. **Status**: still **open** — these `curl` shell-outs and the `toolCurl` registration remain live in `src/`. Any doctrine/engineering doc prose that already describes the `curl` shell-outs or `toolCurl` as removed is **premature and is corrected to "scheduled under Sprint 2.17, not yet removed"** as part of the Sprint `0.9` documentation-harmony pass. The chart-side Harbor `curl-mirror` image used by the secret-bootstrap Helm hooks (Sprint `3.13` chunk 7) is a separate in-cluster artifact and is **not** covered by this row. | Sprint `2.17` | Replaced by `Prodbox.Http.Client` (wrapping `Network.HTTP.Client` + `Network.HTTP.Client.TLS`, both already declared in `prodbox.cabal:105-120`) plus `Prodbox.Gateway.Client` reusing `PeerEndpoint` from `src/Prodbox/Gateway/Types.hs:66-74`. New `prodbox lint files` rule refuses curl invocations in non-test sources. |
| Gateway daemon JSON `parseOrders` parser. The daemon-config side (`parseDaemonConfig`) was removed by Sprint 2.20 closure on May 24, 2026; `parseOrders` is retained because `Prodbox.TestValidation` still parses kubectl-extracted JSON-encoded Orders in places. Removing it requires the live `prodbox test all` cluster validation to migrate fully to the `loadOrders` Dhall path. | Sprint `2.22` (live closure pending) | Already-replaced: `Prodbox.Gateway.Settings.loadOrders` is the supported decoder. The legacy `parseOrders` ships alongside until the live exercise confirms TestValidation does not need it. |
| SIGHUP reload handler in `src/Prodbox/Gateway/Daemon.hs` (`installHandler sigHUP reloadHandler` near line 377 and the surrounding signal-handler scaffolding). SIGHUP returns to ordinary `drain + exit` semantics; the file watcher feeds the reload queue. | Sprint `2.21` (closed May 24, 2026) | Replaced by `configFileWatchLoop` in `src/Prodbox/Gateway/Daemon.hs` using `fsnotify` on the parent directory of the daemon's `--config` Dhall path per [config_doctrine.md §7](../documents/engineering/config_doctrine.md#7-file-watch-reload-trigger). |
| `forbidFsnotify` / `forbidInotify` / forbid-mtime-polling lint rules in `src/Prodbox/CheckCode.hs` and the matching marker set in `.hlint.yaml`. The rules are inverted by the new doctrine — file-watch primitives are required, not forbidden. | Sprint `2.21` (closed May 24, 2026) | The `reloadTriggerViolations` arm of `checkDaemonRuntimeImports` is removed, and `checkHlintDoctrineCoverage` no longer requires `System.INotify` / `Linux.INotify` / `getModificationTime` markers (the surviving `System.FSNotify` marker now records the required-and-mentioned file-watch primitive). |
| Env-var-sourced daemon credentials in `charts/gateway/templates/deployments.yaml`: `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_SESSION_TOKEN`, `MINIO_ACCESS_KEY_ID`, `MINIO_SECRET_ACCESS_KEY`, `GATEWAY_NODE_ID`. Under the Sprint `2.22` doctrine credentials first moved out of env vars and into Secret-mounted Dhall fragments; Sprint `3.18` then removed those fragments on the gateway credential path. | Sprint `2.22` (closed code-side May 24, 2026; superseded by Sprint `3.18` on June 15, 2026) | Superseded replacement: the gateway chart no longer renders or mounts `gateway-aws-credentials`, `gateway-minio-creds`, or `/etc/gateway/secrets/*.dhall`. `config.dhall` now carries `SecretRef.Vault` references for event keys, Route 53 AWS credentials, and gateway MinIO credentials; the daemon resolves them through Vault Kubernetes auth and projects AWS credentials only into the `aws` subprocess environment. |
| `MINIO_ENDPOINT_URL` env var attempted addition in `charts/gateway/values.yaml` and `charts/gateway/templates/deployments.yaml` (rolled back the same day the new doctrine landed, May 24, 2026). | Sprint `2.22` (no removal needed — never landed; replacement field landed in Sprint 2.19 later-session close) | Replaced by the `boot.minio_endpoint_url` field of the daemon's mounted Dhall config per [config_doctrine.md §6 "Non-secret service-endpoint fields"](../documents/engineering/config_doctrine.md#6-cluster-mount-contract) and [§10 Forbidden Surfaces](../documents/engineering/config_doctrine.md#10-forbidden-surfaces) where `MINIO_ENDPOINT_URL` is explicitly enumerated. This row exists so future readers can find the rollback decision and the canonical replacement field in the ledger rather than re-discovering the env-var path. |
| The in-cluster public-edge TLS retention mechanism in `src/Prodbox/Lib/ChartPlatform.hs`: `retainedPublicEdgeTlsSecretNamespace` (`"prodbox"`), `retainedPublicEdgeTlsSecretName` (`"public-edge-tls-retained"`), `publicEdgeTlsRetentionNamespaceManifest`, `retainedPublicEdgeTlsSecretManifest`, and the in-cluster `prodbox/public-edge-tls-retained` Secret it writes; plus the silent-success arm of `preservePublicEdgeTlsSecretBeforeDelete` that returns `Right ()` when the owned cert Secret is absent at preserve time. The in-cluster copy is destroyed by every cluster-wipe path (`rke2 delete`/`nuke`) and only protects the chart-delete→redeploy path, so it cannot keep the cert across the cluster-wipe rebuild cycle the canonical suite is designed to run, forcing a fresh ZeroSSL order each rebuild. | Sprint `8.7` | Replaced by the S3-backed `LongLived` retention store (Sprints `4.24`/`7.11`/`8.7`): the public-edge cert material is retained under a substrate-scoped key (`public-edge-tls/<substrate>/<fqdn>`) in the long-lived `pulumi_state_backend` S3 bucket, restored before every issuance on all rebuild paths, and registered as a managed resource. The silent-success arm is replaced by typed/logged preserve outcomes per [lifecycle_reconciliation_doctrine.md § 3](../documents/engineering/lifecycle_reconciliation_doctrine.md) soundness. Remove once the S3 retention is proven by a live production round-trip on both substrates (Sprint `8.8`). |
| `Prodbox.Gateway.Types.eventTypeOrdersPromoted` and `extractOrdersVersionFromEvent` — the in-process Orders-promotion machinery. Dead under the restart-based promotion model: `stateOrdersVersionUtc` never advances in-process. | Sprint `2.25` | Orders promotion is restart-based per [config_doctrine.md §8 step 4](../documents/engineering/config_doctrine.md) and the D4 rewrite of [distributed_gateway_architecture.md §7.5](../documents/engineering/distributed_gateway_architecture.md); the `orders_promoted` event-key encoding and the in-process version-extraction helper are removed while the refuse-to-reclaim-while-behind gate is kept. |
| The false "agree by construction" event-key comment at `src/Prodbox/Gateway/Daemon.hs:354-357`. The comment claims peers' event-key encodings agree by construction, which is not guaranteed; the typed event-key encoding must be derived and round-trip-checked instead. | Sprint `2.25` | Replaced by one typed event-key encoding with a derive-context encode/decode round-trip per the D4 [distributed_gateway_architecture.md §7.5](../documents/engineering/distributed_gateway_architecture.md) rewrite; the misleading comment is removed when the typed encoding lands. |
| The hand-rolled Pulumi-stack list in `src/Prodbox/CLI/Rke2.hs::runNativeDelete` that omits `aws-eks-subzone` from the default-delete sweep. The bespoke literal list drifted out of sync with the managed-resource registry. | Sprint `4.26` | The default-delete sweep is derived from `perRunManagedResources` (closing the `aws-eks-subzone` omission); the hand-rolled list is removed. Default `delete` stays a refuse-gate, not a reconciler, per the deliberate refuse-gate vs reconciler split. |
| Dead `Prodbox.Lifecycle.Preconditions.noLiveLongLivedPulumiStacks` — defined but not wired into any teardown preflight. | Sprint `4.26` | Wired into the `aws teardown` preflight (the long-lived-residue refuse gate); the dead-until-wired status is closed when the call site lands. |
| Stale comments in `src/Prodbox/Lib/AwsSubstratePlatform.hs`: the "no Harbor on EKS" prose and the "Keep in lockstep" per-substrate-version comment. Both contradict the substrate-equivalence invariant — Harbor + MinIO + Percona run on **both** substrates, and chart/image versions must be pinned once across substrates, not kept in manual lockstep. | Sprint `7.12` | Substrate equivalence becomes a structural invariant: one `Prodbox.ContainerImage` Envoy release value (chart + control-plane + data-plane pinned together), a lint forbidding per-substrate chart-version/image re-pinning, and a shared `[PlatformComponent]` inventory with a coverage test asserting both installers cover it. The stale prose is corrected per [CLAUDE.md Substrate Equivalence](../CLAUDE.md). |
| The misleading HTTP-01-claiming issuer name in `src/Prodbox/PublicEdge.hs`, the matching strings in `charts/keycloak/values.yaml` + `charts/vscode/values.yaml`, and the doc/test sites that reference it. The name claimed HTTP-01 but the issuer uses DNS-01 via cert-manager. | Sprint `7.13` | Renamed to the DNS-01-honest `zerossl-dns01` from one SSoT constant (`publicEdgeClusterIssuerName`) in `src/Prodbox/PublicEdge.hs`, propagated to both chart `values.yaml` and all doc/test sites, on a wipe-and-rebuild boundary. ZeroSSL remains the sole ACME provider per [project memory](../README.md). |
| The PATH-dropping `awsCliEnvironment` (bare-`aws`) helper used by `src/Prodbox/Dns.hs` — it drops `PATH`/`HOME` from the subprocess env so a bare `aws` invocation cannot resolve the binary. | Sprint `1.30` | Replaced by one PATH/HOME-preserving `awsCliSubprocessEnvironment` shared across all aws-CLI subprocess sites, fixing the `Dns.hs` PATH drop. Part of the D2 [haskell_code_guide.md](../documents/engineering/haskell_code_guide.md) capability/error rewrite. |
| The `normalizeManagedAwsHarness` `substrate=aws` blanket override — it forces the full IAM-harness tier for every AWS-substrate validation regardless of what the validation actually needs. | Sprint `5.6` | Replaced by a capability-derived IAM-harness tier (the harness tier is derived from each validation's declared capabilities, not a blanket substrate override); `infra_ready` / `public_edge_ready` split so charts-* gate on an AWS-cred-free readiness. |
| The untyped `[String]` per-validation prerequisite helpers — prerequisites are expressed as bare string lists rather than a typed `PrerequisiteId` ADT. | Sprint `5.6` | Replaced by a typed `PrerequisiteId` ADT with minimal-and-precise per-validation prerequisites; goldens (the three destructive `--dry-run` goldens + registry-generated golden coverage) pin the result. |
| The `PRODBOX_AWS_SUBSTRATE_HOSTED_ZONE_ID` `lookupEnv` read in `src/Prodbox/PublicEdge.hs`. A surviving env-var config read on the public-edge surface that `checkEnvVarConfigReads` does not yet cover. | Sprint `7.13` | The hosted-zone ID flows from the mounted Dhall config (or the live Pulumi stack output for the AWS subzone) instead of an env var; `checkEnvVarConfigReads` is extended to scan `PublicEdge.hs` so the lint enforces the removal. |
| The over-claim in `documents/documentation_standards.md` §6 ("Committed Dhall Imports") that `prodbox check-code` ENFORCES a `sha256:` freeze on `prodbox-config.dhall` (and any future `types.dhall` / `defaults.dhall`). No such check ever existed in `src/Prodbox/CheckCode.hs`; the prose described an "intended" Sprint `0.9` implement-or-strike check as if it were settled enforcement. **Corrected in Sprint `0.9`**: the sha256 Dhall-freeze decision was **STRUCK** (no phantom check to re-derive). The sole current local import is `prodbox-config.dhall` → `./prodbox-config-types.dhall`, a co-edited sibling whose freeze adds re-freeze friction with zero integrity benefit. §6 is reframed to "freezes apply to any future remote/untrusted committed import; the sole current local import is a co-edited sibling and is intentionally not frozen." No code-side removal is needed — there is nothing to delete because the claimed check never landed. | Sprint `0.9` | Documentation-only correction. The `**Generated sections**` header↔markers↔registry reconciler and the relative-link checker (the other two Sprint `0.9` deliverables) DID land in `runGeneratedArtifactLint` (`src/Prodbox/CheckCode.hs`); only the sha256-freeze leg of the implement-or-strike decision was struck. This row records the corrected claim so future readers do not re-derive a non-existent freeze check. |
| Plaintext secret values in `prodbox-config.dhall`: the historical `aws.access_key_id`, `aws.secret_access_key`, `aws.session_token`, `acme.eab_key_id`, and `acme.eab_hmac_key` fields held raw secret material in the operator-authored Dhall. | Sprints `7.14`, `7.15` | Replaced by typed `SecretRef.Vault` references resolved through Vault KV per [vault_doctrine.md §3](../documents/engineering/vault_doctrine.md). **Haskell-side foundation landed (2026-06-16, Sprints `1.35`/`1.38`)**: the SecretRef union is finalized without a `FileSecret` arm and has a Vault KV resolver seam (`resolveSecretRefWithVault` / `resolveSecretRefFromVault`), and the host loader no longer treats repo-root Dhall as the in-force SSoT once basics exist. **Sprint `7.14` AWS credential landing**: root `aws.*` now decodes as a mandatory `SecretRef.Vault` reference (the *generated operational* `prodbox` identity, minted into Vault after Vault is unsealed and referenced — never stored plaintext — from `prodbox-config.dhall`); `prodbox aws setup` / `config setup` write generated operational provider keys to `secret/gateway/gateway/aws`, teardown clears that Vault object, Pulumi provider credentials require the same Vault object through `Prodbox.Infra.AwsProviderCredentials`, and the raw config fallback is removed. The `aws_admin_for_test_simulation.*` *test-simulation* admin block is a separate, distinct role: it is **not** a `SecretRef.Vault` reference and is **not** a `prodbox-config.dhall` section — it is a `TestPlaintext` test-harness fixture that moves to `test-config.dhall` (Sprint `7.16`) to simulate the operator's interactive elevated-credential prompt, never read by any production binary and never stored in Vault, per [vault_doctrine.md §3/§4/§13](../documents/engineering/vault_doctrine.md) and [aws_admin_credentials.md](../documents/engineering/aws_admin_credentials.md). The remaining plaintext-config migration is Sprint `7.15` for ACME EAB/TLS key material; remove this row once those fields also move to `SecretRef.Vault` and validation enforces it. |
| The `aws_admin_for_test_simulation` plaintext block in `prodbox-config.dhall` / `prodbox-config-types.dhall`. **Location**: the test-simulation admin section authored into the production config files. **Why**: it is a test-harness-only fixture that simulates the operator's interactive elevated-credential prompt, not a production-config section — its home is `test-config.dhall` (`TestPlaintext`), never `prodbox-config.dhall`, never Vault, never read by any production binary. The field name `aws_admin_for_test_simulation` is unchanged; only its home and framing move. | Sprint `7.16` | Replaced by the `test-config.dhall` `TestPlaintext` fixture consumed only by the suite-level IAM harness. `prodbox config validate` rejects any plaintext admin/operational AWS key in `prodbox-config.dhall`. Per [vault_doctrine.md §3/§4/§13](../documents/engineering/vault_doctrine.md) and [aws_admin_credentials.md](../documents/engineering/aws_admin_credentials.md). |
| The config-backed admin path for `aws-ses` / `prodbox nuke`: `loadAdminAwsCredentials` (`src/Prodbox/Infra/LongLivedPulumiBackend.hs`), `pulumiSesProviderBaseEnv` / `pulumiSesAdminBaseEnv` (`src/Prodbox/Infra/AwsSesStack.hs`) reading the stored `aws_admin_for_test_simulation.*` block. **Location**: the aws-ses reconcile/destroy/migrate-backend and nuke admin-credential resolution sites. **Why**: real operator ops must acquire the ephemeral elevated credential through the interactive `SecretRef.Prompt` (prompt-use-discard); there is no production config-backed admin path that reads a stored admin section. | Sprint `7.16` | Replaced by unifying `aws-ses` reconcile/destroy/migrate-backend and `prodbox nuke` admin acquisition on the interactive `SecretRef.Prompt`, with the test harness simulating that prompt from the `test-config.dhall` fixture. Per [vault_doctrine.md §3/§4/§13](../documents/engineering/vault_doctrine.md), [aws_admin_credentials.md](../documents/engineering/aws_admin_credentials.md), and [lifecycle_reconciliation_doctrine.md §2](../documents/engineering/lifecycle_reconciliation_doctrine.md). |
| The intermediate `test-config.dhall` naming (this Item heading retains the old token solely as the renamed-from record). **Location**: Sprint `7.16` renamed the fixture `test-secrets.dhall` → `test-config.dhall`; **Sprint `1.43` (2026-06-20) renamed it back to `test-secrets.dhall`** and made it the SOLE durable-secret fixture file (operator decision 2026-06-19) — the single home for all test-only plaintext that simulates operator prompts and seeds fixtures (the Vault unlock-bundle password, `aws_admin_for_test_simulation.*`, ACME/EAB), never imported by `prodbox-config.dhall`, never in Vault. **Why**: the fixture carried no non-secret toggles, so there is no separate non-secret `test-config.dhall`. **Done (Sprint `1.43`)**: `TestConfig`→`TestSecrets`, `loadTestSecrets`/`testSecretsPath`, the generated schema is `test-secrets-types.dhall`, `.gitignore` covers `test-secrets.dhall`, and all `src`/`test`/`documents`/golden references use the current name; the Dhall field identifier `aws_admin_for_test_simulation` is unchanged. | Sprints `7.16`, `1.43` ✅ | Code-side rename complete (Sprint `1.43`). The doc references and goldens use `test-secrets.dhall`. Per [vault_doctrine.md §4](../documents/engineering/vault_doctrine.md) and [config_doctrine.md §0](../documents/engineering/config_doctrine.md#0-three-tier-config-model). Move to Completed once this row is migrated. |
| Pre-existing unencrypted Pulumi backend state in the per-run MinIO backend (`prodbox-state` raw Pulumi checkpoint objects). Resource names, account IDs, cluster names, endpoints, and dependency topology are readable as plaintext until first-touch migration rewrites them. | Sprint `7.14` | Main stack cycles now use the **decrypt-to-scratch Pulumi interposition** (`Prodbox.Pulumi.EncryptedBackend`, `withDecryptedStack` / `withMigratedDecryptedStackEnvironment` on a RAM tmpfs): each op hydrates the stack into a `file://` RAM-tmpfs backend, runs `pulumi`, then re-envelopes + opaque-names back to MinIO through the Model-B object-store, so Pulumi never touches MinIO on the supported path (Sprint `7.14`) per [vault_doctrine.md §10](../documents/engineering/vault_doctrine.md). First touch now imports legacy raw checkpoint bytes when the encrypted object is absent and deletes the legacy stack only after encrypted persistence succeeds. Sprint `4.30` already retired the role-revealing bucket name. Remaining cleanup: live-prove old raw checkpoint deletion across both substrates and remove this row once no plaintext-backend residue can survive. |
| Keycloak and chart data-bound k8s Secrets recoverable from Secret plaintext without Vault — including `keycloak-smtp`, the Keycloak admin/runtime Secret, and the invite-flow OIDC client secrets. | Sprints `3.18`, `3.19`, `8.9` | Become **pure Vault KV objects with no derivation** — generated once, persisted on Vault's durable storage, and consumed via Vault Kubernetes auth with fail-closed bootstrap when Vault is sealed per [vault_doctrine.md §11–§12](../documents/engineering/vault_doctrine.md). The Sprint `3.18` policy/role/service-account foundation, Kubernetes-auth backend config, Vault TokenReview RBAC, automatic generated/static KV seed bootstrap, direct websocket OIDC `SecretRef.Vault` consumer, Keycloak / MinIO chart Vault-login init consumers, MinIO admin bootstrap Job Vault-login init consumers, VS Code Envoy `SecurityPolicy` client-Secret materializer, gateway event/AWS/MinIO Vault consumers, Patroni Vault materializer hook, externally-owned SMTP Vault writer, host/admin Vault helper reads, and sealed-startup structural proof have landed. Sprint `3.19` removed the old derivation/RPC and generated-secret cleanup path; any invite-auth-specific remaining Vault migration remains Sprint `8.9`. The master-seed derivation model is retired (Sprint `3.19`); the secret inventory in [secret_derivation_doctrine.md §6](../documents/engineering/secret_derivation_doctrine.md) maps each secret to its Vault KV/PKI/Transit path. Remove the plaintext-recoverable path once Vault KV is the sole source for each secret. |
| Raw Pulumi object names and prefixes that encode stack identities inside `prodbox-state` (for example stack keys that reveal `aws-eks`). | Sprint `7.14` | Main writes are replaced by the decrypt-to-scratch interposition, which stores checkpoints as Model-B `objects/<vault-keyed-HMAC>.enc` objects. Sprint `4.30` removed the literal in-force config key and added the shared object-store. First-touch migration/delete is code-owned for old raw names. Remaining cleanup: live sealed-Vault proof plus host-disk verification that the old raw names are gone after migration. |
| Pulumi DIY raw-S3 backend access: `PULUMI_BACKEND_URL = pulumiBackendUrl` hands Pulumi raw S3, so `pulumi up` writes `aws-eks`-named cleartext keys directly to MinIO. | Sprint `7.14` | Main stack actions now run through `Prodbox.Pulumi.EncryptedBackend`; `fileBackendEnvironment` rewrites `PULUMI_BACKEND_URL` to a scratch `file://` backend and strips raw backend credentials before Pulumi starts. The per-run raw backend env constructors are now legacy-only inputs to `LegacyPulumiBackend` for first-touch checkpoint export/delete; supported Pulumi action env is provider-only before the scratch rewrite. The `aws-ses migrate-backend` compatibility command is also wrapper-backed and no longer performs raw MinIO-to-S3 export/import. Remaining cleanup: live-prove the first-touch raw checkpoint deletion and decide whether to remove the compatibility alias after live proof. |
| The `aws-ses` AES256-SSE-only long-lived-backend treatment (the long-lived S3 backend protected by bucket-level SSE instead of the prodbox envelope). | Sprint `7.14` | Main `aws-ses` reconcile/destroy/read paths now use the **uniform Model B envelope**: per-run and long-lived (`aws-ses`) checkpoints are enveloped + opaque-named through the object-store and hydrated via the same decrypt-to-scratch interposition per [vault_doctrine.md §10](../documents/engineering/vault_doctrine.md), so a sealed Vault is opaque uniformly on the supported path. `aws-ses` paths construct an optional legacy long-lived S3 backend only for first-touch export/delete when encrypted state is absent, including the wrapper-backed `aws-ses migrate-backend` compatibility command. Remaining cleanup: live-prove migration from the former long-lived S3 backend and remove this row once no SSE-only checkpoint path survives. |
| The Vault-derived Pulumi passphrase / Pulumi's own secrets provider (old Sprint `1.37` "Vault-Derived Secrets Provider" Option B — `PULUMI_CONFIG_PASSPHRASE` derived from Vault). | Sprint `7.14` | Superseded by the Model B object envelope: **Pulumi's own secrets provider is dropped** (the prodbox envelope is the encryption) per [vault_doctrine.md §10](../documents/engineering/vault_doctrine.md). Sprint `1.37` is reframed to own the Vault-readiness gate (`vaultGateOutcome`) plus the production Vault-Transit `DekCipher` (`Prodbox.Vault.TransitCipher`), not a Pulumi passphrase. Remove the passphrase-derivation deliverable from the schedule (no code landed it); this row records the dropped fork. |
| Log/output sites + the exists-vs-`NoSuchKey`/`stackPresentInList` oracle that leak bucket/key/stack names: `MinioBackend.hs`, `LongLivedPulumiBackend.hs`, `LiveResidue.hs` (`residueStatusFromMinioListing`, `bucketObjectCount`, the `isAwsCliNoSuchKeyMessage`/`stackPresentInList` discriminators), `StackOutputs.hs`. A sealed-Vault caller can distinguish "stack exists" from "absent" via the error path, and structured logs emit child/stack/bucket names. | Sprints `4.33`, `5.8` | Sprint `4.33` landed the Haskell-side replacement: residue queries and the retained-object `NoSuchKey` classifier are **gated behind the Vault-readiness check** and return `vault_status=... result=unobservable`, while token-bearing `VaultToken` / federation custody values get redacted `Show` instances per [vault_doctrine.md §14/§19](../documents/engineering/vault_doctrine.md) and [streaming_doctrine.md](../documents/engineering/streaming_doctrine.md). Keep this row pending until Sprint `5.8` runs the deployed sealed-Vault cross-surface red-team and proves no leaking log line or ungated oracle remains. |
| Substrate/child-named Kubernetes namespaces + downstream-identity surfaces: child-named namespaces and downstream kubeconfig/identity custodied in k8s Secrets reveal a cluster's children on a host-disk/k8s dump even while the parent Vault is sealed. | Sprints `2.26`, `4.32`, `4.33`, `5.8` | Replaced by **opaque k8s namespaces** (child-named namespaces use opaque IDs) and **downstream identity custodied in the parent's Vault KV** (`secret/clusters/<child-id>/*`, never a k8s Secret) per [vault_doctrine.md §9](../documents/engineering/vault_doctrine.md) and [cluster_federation_doctrine.md §3–§4](../documents/engineering/cluster_federation_doctrine.md). Sprint `2.26` landed the gateway-mediated downstream-inventory and bootstrap-reference Vault custody surface; Sprint `4.32` landed the direct parent-side writer and child lifecycle consumer; Sprint `4.33` landed the Haskell-side opaque-namespace audit and redaction gate. Keep this row pending until Sprint `5.8` proves the deployed k8s/log dump leaks no child identity. |
| The repo-root `VAULT_REFACTOR.md` proposal doc. | Sprint `0.13` | Its substance is folded into [vault_doctrine.md](../documents/engineering/vault_doctrine.md) + [cluster_federation_doctrine.md](../documents/engineering/cluster_federation_doctrine.md); the file is DELETED in Sprint `0.13`. Remove once the doctrine docs are the sole home for the Vault-root + federation architecture. |
| The "Blocked-by-a-later-phase" dependency framing and the overloaded use of ⏸️ Blocked for pending live-infrastructure proofs. **Location**: backward `Blocked by` references (a later phase or a higher-numbered sprint named as a blocker) and ⏸️ Blocked status markers used for live-infra-pending proofs across the phase documents (`phase-5-*.md`, `phase-7-*.md`, and any sprint whose `Blocked by` names a later phase) and the standards/overview prose that condones them. **Why**: superseded by the phase-independence doctrine — [development_plan_standards.md](development_plan_standards.md) Standards N (Phase Independence) + O (Code-Local vs Live-Infra Proof) make an earlier phase validatable on its owned surface independent of any incomplete later phase; a backward `Blocked by` is a structural defect (re-scope so the owned surface is validatable now), and live-infrastructure proofs become distinct, non-blocking `Live-proof: pending` notes that never gate an earlier phase or a sprint's code-owned closure. `⏸️ Blocked` is reserved strictly for a genuine unmet earlier-or-same-phase or external prerequisite. | Sprint `0.15` | Replaced by the forward-only blocking model, the per-phase `Independent Validation` line, the `Live-proof: pending` non-blocking note, and the [substrates.md](substrates.md) parity table for orthogonal AWS-substrate coverage, all per Standards N/O. No objective, feature, or validation changes — only dependency framing and status semantics. Remove this row once every backward `Blocked by` is re-scoped and no ⏸️ Blocked marker is carrying a live-infra-pending proof. |
| The hand-maintained, version-controlled config-schema Dhall files `prodbox-config-types.dhall` and the former `test-config-types.dhall`. **Location**: committed repo-root schema files imported by the operator's (git-ignored) `prodbox-config.dhall` / `test-secrets.dhall`. **Why**: the config schema is duplicated — the Haskell `ConfigFile` / `defaultConfigFile` / `SecretRef` types (decoded via `Dhall.inputFile auto`) are the real source of truth the binary enforces, and the committed `*-types.dhall` files are a second, hand-maintained encoding kept in sync by hand (drift risk, no integrity benefit). | Sprints `1.41`, `7.17`, `1.43` | Replaced by binary-generated schema files emitted from the Haskell source of truth and **removed from version control** (git-ignored). Sprint `7.17` landed the on-demand renderer + drift guard (`prodbox config schema` / `config setup` / `config validate`). **Sprint `1.43`** renamed the test schema `test-config-types.dhall` → `test-secrets-types.dhall` and `git rm --cached`-removed the old `test-config-types.dhall` (the now-empty `test-config.dhall` having been removed outright); the test schema is git-ignored and regenerated. **Remaining**: the one-time operator `git rm --cached prodbox-config-types.dhall` still untracks the production schema (it stays generated + git-ignored, not deleted from disk). Per [config_doctrine.md §0](../documents/engineering/config_doctrine.md#0-three-tier-config-model) and [code_quality.md](../documents/engineering/code_quality.md). Remove this row once `prodbox-config-types.dhall` is no longer git-tracked. |
| The legacy operator seed/propose input `prodbox-config.dhall`. **Location**: the operator-authored (git-ignored) repo-root config file — the first-bring-up seed input and proposed-update vehicle. **Why**: once the in-force MinIO SSoT is seeded (Sprint `1.42` wires `storeInForceConfigWith`), the cluster reads its config from the encrypted SSoT object, not the filesystem seed-fallback, so the file is retired — its non-secret operator config (Route 53 zone, SES domains, ACME email, Pulumi bucket) lives in the SSoT. **Secret-safety verified**: it carries **no plaintext secrets** — only `SecretRef.Vault` pointers (`aws.*` → `secret/gateway/gateway/aws`; `acme.eab_*` → `secret/acme/eab`), and the test secrets already live in `test-config.dhall`. **Code retirement landed (Sprint `1.42`, 2026-06-19)**: no production code path reads the repo-root `prodbox-config.dhall` any more — `Settings.loadConfigFile` decodes `( prodbox.dhall ).parameters`, the in-force SSoT payload decode uses `Settings.decodeConfigFileAtPath` (an internal temp basename only), and `config setup` / `aws setup` / `vault init` author into `prodbox.dhall` (`Tier0.writeOperatorParametersToTier0` / `writeTier0FloorPreservingParameters`). The operator non-secret config now lives in the binary-generated, git-ignored `prodbox.dhall` `parameters`, per the [config_doctrine.md §0 three-tier model](../documents/engineering/config_doctrine.md#0-three-tier-config-model) and [vault_doctrine.md §10](../documents/engineering/vault_doctrine.md). **Remaining (both non-blocking, Standard O)**: delete any vestigial on-disk `prodbox-config.dhall` (no code reads it), and the live-proof — a from-scratch home bring-up reads config with no repo-root `prodbox-config.dhall` present. | Sprint `1.42` ✅ | Code retirement complete. The `decodeConfigFileAtPath` SSoT decoder still materialises a TEMP `prodbox-config.dhall` beside `prodbox-config-types.dhall` for the encrypted in-force payload shape — that internal basename + schema are retained intentionally and are NOT the retired repo-root file. Closure gate (live-proof) = a rebuild reads the in-force config with no repo-root `prodbox-config.dhall` present. Move this row to Completed once the live-proof lands. |
| The host-disk `.data/prodbox/vault-unlock-bundle.age` unlock bundle. **Location**: the on-disk age-encrypted Vault unlock material (Shamir unseal keys, recovery keys, initial root token) read at unseal time. **Why**: the Tier-1 bootstrap secret cannot live on host disk — it must be the password-AEAD-sealed unlock-bundle object in the durable MinIO bucket, read via a password-derived bootstrap MinIO credential before Vault is reachable, per the [config_doctrine.md §0 three-tier model](../documents/engineering/config_doctrine.md#0-three-tier-config-model). | Sprint `7.19` | Replaced by the password-AEAD-sealed (Argon2id + ChaCha20-Poly1305) unlock-bundle object in the durable MinIO bucket, read via the password-derived bootstrap MinIO credential. Removed after the MinIO write + unseal validation; closure gate = a live home reconcile/unseal/rebuild reads the bundle from MinIO with the operator password. |
| The host-disk `.data/prodbox/unencrypted-basics.json` file. **Location**: the standalone unencrypted-basics JSON read as the sealed-Vault bootstrap floor. **Why**: there is **no JSON bootstrap floor at all** — the Tier-0 non-secret basics live in the binary-owned, self-contained (no-imports) `prodbox.dhall`, and the dependency-free bootstrap floor is read directly from it (the binary decodes `prodbox.dhall` and projects the basics via `projectBasics`); **both** the legacy hand-written standalone `unencrypted-basics.json` **and** the derived `prodbox-basics.json` (the earlier Sprint `1.39`/`1.40` framing that projected a JSON floor from the Tier-0 Dhall) are eliminated, per the [config_doctrine.md §0 three-tier model](../documents/engineering/config_doctrine.md#0-three-tier-config-model). | Sprint `1.41` | Replaced by reading the sealed-Vault bootstrap floor directly from the self-contained Tier-0 `prodbox.dhall` (`projectBasics`); `configBasicsDerivedPath` and the legacy unencrypted-basics.json fallback are both dropped. Closure gate = a live cluster reconcile reads the floor from `prodbox.dhall` with no JSON floor on disk. |
| The derived `prodbox-basics.json` bootstrap floor. **Location**: the JSON floor projected from the Tier-0 `prodbox.dhall` by the earlier Sprint `1.39`/`1.40` framing, read as the sealed-Vault bootstrap floor before Vault is reachable. **Why**: there is no separate JSON floor — the sealed-Vault bootstrap floor is read directly from the self-contained (no-imports) Tier-0 `prodbox.dhall` (the binary decodes it + projects the basics via `projectBasics`), so the intermediate derived JSON is eliminated, per the [config_doctrine.md §0 three-tier model](../documents/engineering/config_doctrine.md#0-three-tier-config-model). | Sprint `1.41` | Replaced by reading the floor directly from `prodbox.dhall` via `projectBasics`; `configBasicsDerivedPath` and the derive-to-JSON step are dropped. Closure gate = a live cluster reconcile reads the floor from `prodbox.dhall` with no `prodbox-basics.json` on disk. |
| The committed, generated `docker/default-prodbox.dhall` daemon-container default config registered as a `TrackedGeneratedPath`. **Location**: the version-controlled generated Dhall baked into the daemon image build context, registered for the `prodbox check-code` generated-artifact lint. **Why**: no `.dhall` is version-controlled on the supported path — the daemon-container default is GENERATED at image-build time (rendered from `renderDaemonContainerDefaultDhall` into the build context immediately before `docker build`) and git-ignored, so its committed `TrackedGeneratedPath` registration in `src/Prodbox/CheckCode.hs` is superseded, per the [config_doctrine.md §0 three-tier model](../documents/engineering/config_doctrine.md#0-three-tier-config-model). | Sprint `1.41` | Replaced by build-time generation (`renderDaemonContainerDefaultDhall` → build context before `docker build`) + git-ignore; the committed file and its `CheckCode.hs` `TrackedGeneratedPath` registration are removed. Net with the rows above: zero version-controlled `.dhall`. Closure gate = `prodbox dev check` green with no committed `docker/default-prodbox.dhall` and the image build rendering it on demand. |

## Pending Removal Notes

Pending-removal rows move here only when the owning sprint closes and the doctrine-required
replacement is verified.

**2026-06-17 — Sprints `7.14`/`7.15`/`7.16` closed; these Pending Removal rows are verified landed
and ready to relocate to Completed:** (a) the plaintext `prodbox-config.dhall` secret fields — `aws.*`
(via `7.14`) and `acme.eab_*` (via `7.15`) are now `SecretRef.Vault`, validation-enforced; (b) the
`aws_admin_for_test_simulation` plaintext block — removed from `prodbox-config(-types).dhall` and the
`ConfigFile` record by `7.16`; (c) the config-backed admin path — `loadAdminAwsCredentials` /
`pulumiSes*BaseEnv` replaced by the interactive `SecretRef.Prompt` seam
`Prodbox.Aws.AdminCredentials.acquireAdminAwsCredentials`, harness-simulated from `test-config.dhall`;
(d) the `test-secrets.dhall` → `test-config.dhall` rename (`.gitignore` + code). Each was
code-validated (`dev check` 0, `test unit` 0/954, `test integration cli`/`env` 0/38) with the
SecretRef golden tests and sealed-state oracle intact (no leak-guard weakened). The live-infra proofs
(sealed-Vault opacity, live AWS first-touch, live invite) remain the non-blocking
`Live-proof: pending` axis (Standard O), tracked in the phase rows. Physical relocation of these
rows into `## Completed` is a trivial follow-up.

## Completed

| Item | Removed In | Notes |
|------|------------|-------|
| The **host-disk** Vault unlock bundle and the MinIO chart's Vault coupling: the host-disk bundle write in `initFreshVault` + `runVaultRotateUnlockBundle`, `loadAndDecryptDiskBundle` + the `BootstrapFallBackToDisk` arm + `classifyBootstrapMinioSource`/`BootstrapSourceDecision`/`renderBootstrapSourceWarning` (`Vault/Host.hs`); the `charts/minio` `vault-secrets` init container + `vault-materialized` tmpfs + `MINIO_ROOT_*_FILE` indirection + `vault.*` values; `restartMinioIfVaultRootChanged` + `vaultLifecycleMinioRootChanged` / `reconcileStepsMinioRootChanged` (`CLI/Rke2.hs`). | Sprint `7.25` disk-free unseal on 2026-06-22 | The unlock bundle now lives ENTIRELY in the durable MinIO bucket (`initFreshVault` makes the MinIO write REQUIRED via `writeBootstrapBundleToMinio`; `loadAndDecryptBundle`/`fetchBootstrapBundleEnvelope` read MinIO-only; rotate writes to MinIO). MinIO is **cluster-only** — its chart takes the static `Prodbox.Minio.RootCredential` directly (`renderMinioChartArgs --set rootUser/rootPassword`), no Vault init container — and `applyNativeInstallPlan` brings it up BEFORE Vault. NOTE: `vaultUnlockBundlePath`/`vaultUnlockBundleRelPath` were not deleted but **renamed/repurposed** to the NON-SECRET `clusterEstablishedMarkerPath`/`…RelPath` (`.data/prodbox/.cluster-established`) — the only on-disk artifact, a bare establishment flag that unseals nothing. A `PRODBOX_TEST_BOOTSTRAP_BUNDLE_DIR` test seam backs the bundle with a local file for the host-only vault-lifecycle integration test (production always uses MinIO). `secret/minio/root` stays in Vault for the post-startup gateway/Harbor bootstrap Jobs. Gates: `dev check` 0, `test unit` 1053/1053, the disk-free `vault lifecycle` integration test passes; live wipe-rebuild proof is a non-blocking 🧪 axis (blocked on restoring the env's operator config). |
| The **password-derived** MinIO credentials and their support machinery: `deriveMinioRootPassword` + `deriveBootstrapMinioCredential` + `BootstrapMinioCredential` + `bootstrapSaltForClusterId` + `encodeCredentialHalf` + the derivation contexts (`Prodbox.Vault.BootstrapBundle`); the `SuppliedSecretValues` / `noSuppliedSecretValues` / `minioRootSuppliedValue` override + the `vaultReconcileSuppliedSecretValues` plan field + the supplied-value path threaded through `runVaultSecretBootstrapWith` / `ensureFields` / `materializeMissingFields` / `reconcileSecretObjects`; `resolveMinioRootSuppliedValues` (`CLI/Vault`) and `resolveBootstrapClusterId` (`Vault/Host`). (These had briefly replaced the even-earlier *random* `secret/minio/root.rootPassword` generation on 2026-06-21.) | Sprint `7.19` static-credential simplification on 2026-06-22 | Replaced by a single **static** credential, `Prodbox.Minio.RootCredential` (`minioRootUser` + `minioRootPassword`), per the operator decision that the MinIO access credential is not the security boundary (Vault Transit + the bundle's password-AEAD seal are) — so deriving key/value pairs from a human-memorized password was theatre. `secret/minio/root.{rootUser,rootPassword}` are now `staticField`s; `bootstrapObjectStoreConfig :: Int -> ObjectStoreConfig` builds the unlock-bundle I/O config from the static root; `dualWriteBootstrapBundle` / `fetchBootstrapBundleEnvelope` drop the derive step. A static credential is stable across rebuilds (fixes the original mismatch) AND is one MinIO accepts (the bundle round-trips through MinIO — resolves the `InvalidAccessKeyId` axis). The secret-bootstrap reverts to its pre-7.19 signatures. Gates: `dev check` 0, derivation tests removed + a static-field/static-config test added, full `test unit` 1058/1058. 🧪 Live-proof (non-blocking): a wipe-and-rebuild brings MinIO up on the static cred with the bundle round-tripping through MinIO and no `InvalidAccessKeyId`. |
| The Sprint `1.46` **persistent** Harbor-login isolation mechanism: the `<repoRoot>/.docker` config dir, `ensureHarborDockerLogin` / `harborLoginRetryPolicy` / `isRetryableHarborLoginFailure` in `src/Prodbox/CLI/Rke2.hs`, the persistent `DockerConfig` helpers (`prodboxDockerConfigDir` / `ensureProdboxDockerConfig` / `seedProdboxDockerConfig` / `dockerSubprocessEnvironment`), and the `.docker/` entries in `.gitignore` / `.dockerignore`. | Sprint `1.47` on 2026-06-20 | Replaced by the **ephemeral** hostbootstrap-`Registry` pattern: `src/Prodbox/DockerConfig.hs` rewritten to `dockerHubAuthFromConfig` (PURE `docker.io`-only projection) + `renderEphemeralDockerConfig` (host `docker.io` auth + inline Harbor `127.0.0.1:30080` entry, `base64 admin:Harbor12345`, no `credsStore`) + `withEphemeralDockerConfig` (throwaway `withSystemTempDirectory` `DOCKER_CONFIG`, `bracket`-scrubbed). `mirrorClusterImagesOnce`, `ensureCustomImageVariantsHomeLocal`, and the AWS host build wrap their docker calls in it; the curl-based home Harbor-project creation drops its login gate (readiness stays on the `waitForHarbor*` probes). **No `docker login` runs anywhere**, the host `~/.docker` is only read, nothing persists in `~/prodbox`. `captureDockerToolOutput` / `dockerSubprocessFor` are retained but no longer inject env (the bracket provides `DOCKER_CONFIG` process-wide). The `docker.io` discovery/projection is the seam to swap onto `HostBootstrap.Registry` at the planned hostbootstrap refactor. Gates: `dev check` 0, six new `DockerConfig` unit tests (incl. exact base64), the CliSuite reconcile asserting no `docker login` + an ephemeral `prodbox-docker-config` `DOCKER_CONFIG`, full `test unit` 1061/1061. |
| The per-host `prodboxId` (`prodbox-<machine-id>`) directory prefix on MinIO's retained PV host path and the legacy hand-named `prodbox-minio-pv-0` / `prodbox-vault-pv-0` retained PVs. | Sprint `4.31` on 2026-06-16 | Replaced by the unified `.data/<namespace>/<StatefulSet>/<replica>` scheme and deterministic `prodbox-retained-<namespace>-<statefulset>-<ordinal>` PV names derived by `retainedStatefulSetPersistentVolumeName`. `ensureRetainedLocalStorage` now walks a typed always-on inventory for MinIO and Vault, creates/chowns `.data/prodbox/minio/0` and `.data/vault/vault/0`, and applies claimRef-bound PVs for `data-minio-0` / `data-vault-0`. |
| The over-nested chart-storage host path `.data/<namespace>/<release>/<workload>/<ordinal>/<claim>` and release/claim-derived chart PV names. | Sprint `4.31` on 2026-06-16 | `storageBinding` now produces `.data/<namespace>/<StatefulSet>/<ordinal>` and PV names derive only from namespace, StatefulSet, and ordinal. Unit tests pin the `vscode` PV as `prodbox-retained-vscode-vscode-0`, and CLI integration pins the Patroni cleanup PV as `prodbox-retained-vscode-prodbox-vscode-pg-0`. |
| The bitnami MinIO **Deployment** chart install in `ensureMinioRuntime` / `renderMinioChartArgs`: `mode: standalone`, a single-replica Deployment with a standalone `minio` PVC, and the steady-state Harbor-mirrored MinIO image switch. | Sprint `4.31` on 2026-06-16 | Replaced by a prodbox-owned `charts/minio/` single-replica StatefulSet with volumeClaimTemplate `data`, PVC `data-minio-0` bound to `.data/prodbox/minio/0`, MinIO root credential files materialized from Vault KV by a Vault-login init container, and the public `quay.io/minio/minio` image for both bootstrap and steady state to avoid the Harbor storage-backend deadlock. |
| The `vscode` **Deployment** with standalone `vscode-data-0` PVC. | Sprint `4.31` on 2026-06-16 | Replaced by `charts/vscode/templates/statefulset.yaml`, a single-replica StatefulSet with volumeClaimTemplate `data`, PVC `data-vscode-0`, and the unified `.data/vscode/vscode/0` host path. The chart-plan unit and CLI integration delete proof pin the new storage binding. |
| The hand-applied Vault PV at `.data/vault/0/data` created outside the retained-storage reconciler during the live Vault bring-up. | Sprint `4.31` on 2026-06-16 | Vault's durable PV is now reconciler-managed as `.data/vault/vault/0` with deterministic PV `prodbox-retained-vault-vault-0` and StatefulSet PVC `data-vault-0`; the RKE2 fake-kubectl integration proof asserts the generated retained-storage manifest. |
| The host-CLI "repo-root `prodbox-config.dhall` read directly as the config SSoT" model in `src/Prodbox/Settings.hs`. | Sprint `1.38` on 2026-06-16 | `validateAndLoadSettings` now routes through `loadConfigForSettingsWith`: without `.data/prodbox/unencrypted-basics.json`, repo-root Dhall remains only the first-bring-up seed input; once basics exist, settings loads validate basics, recover a ready Vault root token, read `secret/minio/root`, build the production Vault-Transit `DekCipher`, port-forward to MinIO, fetch the in-force envelope, and decode the Dhall payload. Targeted Sprint `1.38` tests passed 31/31; full validation passed `./.build/prodbox test unit` 910/910, `./.build/prodbox test integration cli` 36/36, docs checks, chart lint, and `./.build/prodbox dev check`. |
| Secret-mounted **plaintext** Dhall credential fragments: `/etc/gateway/secrets/aws.dhall`, `/etc/gateway/secrets/minio.dhall`, and the `as Text` opaque-import pattern in `gateway-config-<nodeId>`. | Sprints `3.18`/`3.19`/`1.38` completed by 2026-06-16 | Removed outright, not bridged. The gateway chart renders event keys, AWS credentials, and MinIO credentials as Vault SecretRefs; gateway MinIO bootstrap materializes root and gateway-user credentials via Vault auth; `SecretRefFile` and the disk resolver arm are gone; Sprint `3.19` removed the old derivation/RPC machinery; and the Sprint `1.38` host-loader proof no longer needs any Secret-mounted Dhall config-authority fallback. Code/chart search shows no live `/etc/gateway/secrets/*.dhall` mount or `as Text` credential import on the supported path; remaining hits are historical documentation. |
| The role-revealing MinIO bucket names `prodbox` and `prodbox-test-pulumi-backends`. | Sprint `4.30` on 2026-06-16 | Collapsed into the generic `prodbox-state` bucket. `minioBackendBucket`, the Pulumi YAML stack `backend_bucket` outputs, the gateway MinIO bootstrap bucket/policy, and unit fixtures now use `prodbox-state`; the focused 4.30 unit block pins that the old bucket name is not the shared object-store/Pulumi bucket. |
| Cleartext AAD writes in `prodbox-envelope-v1`: `sealEnvelope` wrote `base64("clusterId|objectName")` (for example `aws-eks`) into new object bodies. | Sprint `4.30` on 2026-06-16 | New writes emit `prodbox-envelope-v2`, store `base64(SHA256(aad))`, and include `transit_key` / `created_at` / `key_version`; v1 decode compatibility remains only to read already-written historical envelopes. The focused 4.30 unit block proves a sealed envelope body does not contain `cluster1|aws-eks`. |
| The literal in-force config MinIO key `active-config/in-force-config.prodbox-envelope-v1` and the backend helpers `Prodbox.Infra.MinioBackend.fetchInForceConfig` / `storeInForceConfig`. | Sprint `4.30` on 2026-06-16 | Removed from the supported backend API. Production settings loads now read `secret/object-store/hmac`, derive the opaque key for `LogicalInForceConfig`, fetch from `prodbox-state` through `Prodbox.Minio.ObjectStore`, and decrypt with the Vault-Transit `DekCipher`. |
| The master-seed object at MinIO `prodbox/master-seed`. | Sprint `3.19` on 2026-06-16 | Removed outright, not enveloped. The HMAC-derivation model is retired: there is no `master-seed` object in MinIO, no gateway seed acquisition path, and no code path reads or writes it. Every secret that was previously HMAC-derived is a Vault KV object generated once and consumed via Vault Kubernetes auth. Validated by `cabal build --builddir=.build exe:prodbox` and `./.build/prodbox test unit` 898/898, including the Sprint `3.19` absence proof for retired modules/RPC strings. |
| The master-seed HMAC derivation modules + gateway daemon RPC + lint: `Prodbox.Secret.{Derive,MasterSeed,Inventory}`, the gateway daemon `/v1/secret/derive` + `/v1/secret/ensure-namespace` endpoints, the `checkRawMasterSeedReadScope` lint, and `selfBootstrapOwnSecrets`. | Sprint `3.19` on 2026-06-16 | Deleted the derivation/wire/inventory/master-seed modules, removed their `prodbox.cabal` exposure, removed gateway daemon handlers and host client functions for `/v1/secret/*`, deleted the host `GatewayDeriveMode` test seam, and removed the raw-seed lint from `runDoctrineAlignmentCheck`. Host/admin helpers now read Vault KV through `Prodbox.Vault.Host`; chart deploy no longer pre-applies derived Secrets. |
| Chart-generated `lookup`+`randAlphaNum` Secrets — formerly the MinIO root credential in `charts/minio/templates/secret.yaml` and OIDC client secrets generated at chart-render time. | Sprint `3.19` on 2026-06-16 | Sprint `3.18` moved the direct chart consumers to Vault; Sprint `3.19` removed the surrounding generated-secret assumptions and pins the no-regression shape. The gateway chart no longer grants cross-namespace secret-writer RBAC or mounts a master-seed scratch volume, and the unit suite keeps the sealed-startup / no-fallback structural proof plus the retired-RPC absence proof. |
| Duplicated CLI-verb / project-subdir / stack-name string literals scattered across `src/Prodbox/Aws.hs`, `src/Prodbox/Lifecycle/ResourceClass.hs`, `src/Prodbox/CLI/Pulumi.hs`, and `src/Prodbox/CLI/Spec.hs` describing each Pulumi-managed substrate stack (registry name, Pulumi stack id, project subdir, CLI verb, lifecycle class). | Sprint `4.27` on 2026-06-09 | Collapsed into one `StackDescriptor` SSoT record (`stackRegistryName` / `stackPulumiStackId` / `stackProjectSubdir` / `stackCliVerb` / `stackLifecycleClass`) in the new `Prodbox.Infra.StackDescriptor` module. The per-run name list (`Prodbox.Aws.perRunStackNames` now `= perRunStackDescriptorNames`), the CLI verbs (`stackResourcesCliVerb` / `stackDestroyCliVerb` derive `<stem>-resources` / `<stem>-destroy`), and the project dirs (`stackProjectSubdirs`) all derive from `stackDescriptors`. A new registry-name↔CLI-command `GeneratedSectionRule` (`stack-command-surface`, `renderStackCommandSurfaceMarkdown`) renders the table into `DEVELOPMENT_PLAN/substrates.md` so `prodbox docs generate`/`docs check` keep it in sync — the typed source Sprint `0.10` (table generation) and `5.6` (registry-generated golden coverage) consume. Unit tests pin the derived per-run names equal to both the prior literal and the `PerRun` registry slice, the CLI-verb derivation, and the EKS registry-name↔stack-id difference (`aws-eks` vs `aws-eks-test`). Validated `prodbox check-code` exit 0, `prodbox test unit` 802/802, `prodbox docs generate`/`docs check` round-trip exit 0, `prodbox test integration cli` pass, `prodbox lint docs` exit 0. |
| The misnamed `longLivedStackNames` (the value lists long-lived **resource** names, not only Pulumi stack names). | Sprint `4.27` on 2026-06-09 | Renamed `longLivedStackNames` → `longLivedResourceNames` (`src/Prodbox/Aws.hs`), all call sites updated (`Prodbox.Aws`, the unit tests). It stays derived from the `LongLived`-class managed-resource registry facts (`ResourceClass.resourceNamesOfClass LongLived`) rather than the `StackDescriptor` Pulumi-stack list, because the long-lived class spans more than Pulumi stacks — it includes the non-stack `public-edge-tls` certificate (Sprint `4.24`). Behavior-preserving (a unit test pins `longLivedResourceNames == ["aws-ses", "public-edge-tls"]`). |
| The leak-prone create→delete in `src/Prodbox/EffectInterpreter.hs::requireRoute53LifecycleCapability` — the Route 53 capability-proof record is created and deleted without exception-safe cleanup, so a failure between create and delete leaks the proof record (audit C66). | Sprint `4.27` on 2026-06-09 | After a successful `create-hosted-zone` the rest of the probe is wrapped in `Control.Exception.bracketOnError` so any exception thrown after the create (async, or a future step inserted between create and delete) always triggers a best-effort `delete-hosted-zone` of the proof zone. On the normal path the success body deletes once. It is deliberately **not** registered as a `ManagedResource` (the probe has no steady state to discover/reconcile, so the §3.1 totality registry stays correct without it), and the create-site lint preserves its deliberate carve-out (`create-hosted-zone` is in `awsCreateProbeVerbs`, never in `awsCreateVerbs`). The live `bracketOnError` cleanup exercise (`prodbox test integration public-dns`/`aws-iam`) is operator-driven. |
| The `resetPatroniStorageIfRequested` no-op guard in `src/Prodbox/Lib/ChartPlatform.hs`. | Sprint `3.16` | Landed as the doctrine-prescribed loud-failure guard, not deleted. `resetPatroniStorageIfRequested` now derives the expected Patroni app-role password via `Gateway.Client.derive` (host never reads the raw seed) and probes it against the preserved `pg_authid` through a probe-only Postgres connection (`probePatroniAppRoleAuth` / `probePatroniPsqlAuth`). The pure decision `patroniSeedMismatchDecision` keeps the loud-failure policy separate from the boundary probe and is unit-tested: a definite `PatroniAuthRejected` is the only path to a loud failure naming the namespace/role pair + resolution options; `PatroniAuthMatches` and `PatroniAuthUnobservable` (fresh install / no primary Pod / transient psql failure) both proceed — never a silent destructive reset. [secret_derivation_doctrine.md §8](../documents/engineering/secret_derivation_doctrine.md) updated to "Implemented (Sprint 3.16)". The live probe is operator-driven (requires a running cluster with preserved data). |
| The `/tmp` master-seed file writes plus `resolveSeedViaMinio`, the `readKeycloakVscodeClientSecret` MinIO branch, and the `PRODBOX_TEST_HOST_MASTER_SEED_HEX` env-var override (`src/Prodbox/Secret/MasterSeed.hs`, `src/Prodbox/Secret/HostBootstrap.hs`, `src/Prodbox/CLI/Rke2.hs`). | Sprint `3.16` | Daemon-only master-seed boundary closed. The fixed `/tmp/prodbox-master-seed{,-put}.bin` round-trip is deleted — `Prodbox.Secret.MasterSeed` now transits the seed only through a randomized, single-use, bracket-deleted temp file (`withManagedSeedTempFile`). `HostBootstrap.preApplyDerivedSecretsForRelease` triggers in-cluster materialization via `Gateway.Client.ensureNamespace`; `Rke2.readKeycloakVscodeClientSecret` obtains the OIDC client secret via `Gateway.Client.derive` — both over the loopback NodePort (`hostLoopbackGatewayEndpoint`), neither reads the raw seed. `resolveSeedViaMinio` is removed. The `PRODBOX_TEST_HOST_MASTER_SEED_HEX` host-side seam is replaced by the gateway-client-boundary `PRODBOX_TEST_GATEWAY_DERIVE_SEED_HEX` seam (`Prodbox.TestSeam.GatewayDerive`) that injects a *derived* value, never the raw seed. A redacting `Show MinioMasterSeedConfig` lands. The new `checkRawMasterSeedReadScope` lint (wired into `runDoctrineAlignmentCheck`) forbids the raw-seed read outside the in-cluster daemon module set, so the boundary cannot regress. Per [secret_derivation_doctrine.md §2/§5](../documents/engineering/secret_derivation_doctrine.md). |
| The `PRODBOX_*` env-var fallback ladder on the public-workload binary in `src/Prodbox/Workload.hs`: the `resolveWorkloadMode` (`PRODBOX_WORKLOAD_MODE`), `resolveHttpPortWithDhall` (`PRODBOX_PORT` / `PRODBOX_HTTP_PORT`), `resolveWorkloadLogLevelWithDhall` (`PRODBOX_LOG_LEVEL`), `resolveRedisConfig` (`PRODBOX_REDIS_HOST` / `PRODBOX_REDIS_PORT`), and `resolveOidcConfig` (`PRODBOX_OIDC_*`) env-var arms, plus the `resolveWorkloadDhallConfig` / `resolveWorkloadModeFromDhall` "config optional" indirection and the now-dead `resolveHttpPort` / `resolveWorkloadLogLevel` exports (and their three `test/daemon-lifecycle/Main.hs` env-ladder tests). | Sprint `3.15` on 2026-06-09 (chart-side env-vars had already been removed code-side May 24, 2026 under the original Sprint `3.14` row) | The mounted Dhall config (`--config /etc/workload/config.dhall`, decoded via `Dhall.inputFile auto`) is now the SOLE workload config source; a missing or unparseable `--config` is a fast structured failure, never a silent env fallback. The workload gained the gateway daemon's Boot/Live split (`WorkloadBootConfig` = `mode` + listen `port` → drain-and-exit on change; `WorkloadLiveConfig` = `log_level` + Redis/OIDC tunables → in-process reload) via `workloadBootConfigFromDhall` / `workloadLiveConfigFromDhall` / `workloadBootFieldsChanged`, plus an `fsnotify` `configFileWatchLoop` + `reloadLoop` + `drainCoordinator` mirroring `Prodbox.Gateway.Daemon`. The api/websocket chart ConfigMaps are now mounted as a directory (no `subPath`) so the kubelet `..data` symlink swap fires the watch (Sprint 2.21 chunk-47 gotcha). `src/Prodbox/Workload.hs` joined `checkEnvVarConfigReads.scopedPaths` so any reintroduced `PRODBOX_*` config read fails `prodbox check-code`; the one legitimate k8s-runtime-metadata read (`HOSTNAME` pod name) moved to the out-of-scope `Prodbox.Workload.PodIdentity` module. Validated with `prodbox check-code` exit 0 (lint proven to fire on an injected `lookupEnv`), `prodbox test unit` 769/769 (8 new Boot/Live + missing-`--config` tests), `prodbox test integration cli` 35/35, `prodbox lint docs` exit 0, `prodbox docs check` exit 0, and `helm template api charts/api` + `helm template websocket charts/websocket` rendering with zero `PRODBOX_*` env vars on the Deployments. The live in-cluster reload exercise (a `log_level` ConfigMap edit reloads in-process; a `mode`/port edit drains+exits) is the operator-driven closure gate. The env-var ladder is replaced by a `workload.mode = Api \| Websocket` field plus the full config-as-data record in the workload Pod's mounted Dhall config per [config_doctrine.md](../documents/engineering/config_doctrine.md). |
| The dead in-process Orders-promotion machinery in the gateway daemon: the `eventTypeOrdersPromoted` constant and `extractOrdersVersionFromEvent` recovery function in `src/Prodbox/Gateway/Types.hs` (and their export-list entries), the `updateOrdersAdvert` fold over accepted peer events in `src/Prodbox/Gateway/Daemon.hs`, and the `applyAcceptedEvents` advance of `stateLatestObservedOrdersVersion` from event payloads. Also the Sprint 2.21 chunk-48 reload workaround in `reloadLiveConfig` (reapplying the in-memory event-key derivation onto the freshly-decoded config before `daemonBootFieldsChanged` compared), the divergent in-memory `deriveHex` event-key re-derivation in `deriveOwnGatewayEventKeys`, and the false "the in-memory and on-cluster values agree by construction" comment near `Daemon.hs:354-357`. | Sprint `2.25` on 2026-06-09 | Doctrine D4: `stateOrdersVersionUtc` never advances in-process; a newer Orders document is adopted only by restarting the daemon against the new config ([config_doctrine.md §8](../documents/engineering/config_doctrine.md#8-boot-vs-live-split-and-the-restart-contract)). Nothing ever emitted an `orders_promoted` event, so the recovery/advance machinery was unreachable. The refuse-to-reclaim-while-behind gate is **kept** in `gatewayLoop` (`stateLatestObservedOrdersVersion > stateOrdersVersionUtc` ⇒ owner = `Nothing`), now fed only by the sender's advertised `orders_version_utc` on each peer push via `noteSenderOrdersAdvert`. Event-key handling collapsed onto one canonical encoding (base64url-unpadded) used by both the `gateway-event-keys` Secret write/read and the HMAC signing/verification path, so the chunk-48 hex-vs-base64url overlay was no longer needed. [distributed_gateway_architecture.md §7.5](../documents/engineering/distributed_gateway_architecture.md) and [tla_modelling_assumptions.md](../documents/engineering/tla_modelling_assumptions.md) already described restart-based promotion + single-host shared-fate topology (Sprint 0.9 D4 rewrite); verified no surviving in-process-promotion claim. Validated with `prodbox check-code` exit 0, `prodbox test unit` 760/760 (incl. the derive-context `decode . encode == id` property and the `markEventProcessed` IS-NULL-guard test), `prodbox test integration cli`, `cabal test prodbox-daemon-lifecycle` 14/14, `prodbox lint docs` exit 0, `prodbox docs check` exit 0. |
| The `--log-level`, `--port`, and `--foreground` runtime-override flags on the daemon-launching commands `prodbox gateway start` and `prodbox workload start` (`daemonLaunchOptionsParser` and `workloadOptionsParser` in `src/Prodbox/CLI/Spec.hs`), the `foregroundParser = flag True True` no-op parser, and the matching override threading through `src/Prodbox/Gateway.hs` (`resolveGatewayLogLevel` / `resolveGatewayPortOverride` / the `portOverride` + `foreground` plumbing into `applyGatewayStartPlan` / `runGatewayDaemon`), `src/Prodbox/Gateway/Daemon.hs` (`runGatewayDaemon`'s `Maybe Int` REST-port-override + `String` log-level parameters and the `serveGatewayDaemon` / `daemonWorkers` / `restServerLoop` `restPortOverride` chain), and the `WorkloadOptions` CLI-flag arms in `src/Prodbox/Workload.hs` (`workloadPort` / `workloadLogLevel` in `resolveHttpPortWithDhall` / `resolveWorkloadLogLevelWithDhall`). The `DaemonLaunchOptions` / `WorkloadOptions` records (`src/Prodbox/CLI/Command.hs`) drop their `daemonLogLevel` / `daemonPort` / `daemonForeground` and `workloadLogLevel` / `workloadPort` / `workloadForeground` fields. The workload parser was included in this removal (it shared the same `--log-level` / `--port` / `--foreground` shape). | Sprint `2.24` on 2026-06-09 (supersedes the original Sprint `2.20` ledger removal, which closed the daemon-config side only) | `--config` is the sole startup-time CLI knob per the pure-Dhall config doctrine ([config_doctrine.md §10](../documents/engineering/config_doctrine.md#10-forbidden-surfaces)); `prodbox gateway start` additionally keeps only the universal Plan/Apply `--dry-run` / `--plan-file` renderers. The gateway daemon now sources `log_level` from the mounted Dhall (`live.log_level`, defaulting to `info`) and its REST port from the Orders file exclusively; the workload daemon sources its port and log level from its mounted Dhall config. The `PRODBOX_*` env-var ladder in `src/Prodbox/Workload.hs` is intentionally retained (owned by Sprint `3.15`); this row is the CLI-flag removal only. The `prodbox docs generate` command regenerated the `cli_command_surface.md` §2/§3 command matrix to drop the removed flags, and `test/daemon-lifecycle/Main.hs` was rewritten to inject the daemon REST port via the generated Orders Dhall instead of `--port`. Validated with `prodbox check-code` exit 0, `prodbox test unit`, `prodbox test integration cli`, `prodbox-daemon-lifecycle`, `prodbox lint docs` exit 0, and `prodbox docs check` exit 0. |
| The entire `src/Prodbox/StateMachine.hs` module plus its lone typecheck-only test in `test/unit/Main.hs`. The GADT-indexed state-machine experiment was never adopted by any production code path; no `src/` or `app/` module imported it (grep-confirmed: only the one typecheck test consumed it). Its `Some*` existential wrappers lacked singleton witnesses, the exact pattern [pure_fp_standards.md](../documents/engineering/pure_fp_standards.md) Forbidden list rejects. | Sprint `1.32` on 2026-06-09 | The GADT doctrine in [pure_fp_standards.md](../documents/engineering/pure_fp_standards.md) was already realigned (D1) in Sprint `0.9` to permit a flat exhaustive ADT for externally-authoritative / log-reconciled state (e.g. gateway ownership as a `Disposition` fold over the append-only commit log), keeping GADTs only for authoritative in-process transitions; Sprint 1.32 verified that softening reads consistently (the Forbidden list scopes the GADT requirement to in-process authority, exempts log-reconciled state to an exhaustive ADT, and still forbids raw-`String` state + still requires exhaustive matching). The un-adopted module, its `Prodbox.StateMachine` import + typecheck-only test block in `test/unit/Main.hs`, and the `prodbox.cabal` `exposed-modules` entry were deleted in the same change. |
| The fail-open boolean residue helpers `Prodbox.Lifecycle.ResidueStatus.isResiduePresentOrUnknownPerRun` / `isResiduePresentOrUnknownLongLived`, and the hand-maintained `Prodbox.Aws.perRunStackNames` / `longLivedStackNames` string literals. | Sprint `4.20` on 2026-05-28 | The two per-class booleans (which had drifted to identical implementations) are replaced by the single soundness combinator `residueBlocksTeardownGate :: ResidueStatus -> Bool` ("present OR unreachable → block"); `categorizePulumiResidue` and `noLiveLongLivedPulumiStacks` migrated to it. New low-level `Prodbox.Lifecycle.ResourceClass` holds the SSoT facts `resourceLifecycleClasses :: [(String, LifecycleClass)]`; `perRunStackNames` / `longLivedStackNames` are now `resourceNamesOfClass PerRun` / `LongLived` (a unit test asserts they equal the prior literals). Validated `prodbox check-code` exit 0, `prodbox test unit` 583/583, `prodbox test integration cli` 30/30, `prodbox test integration env` 30/30. Behavior-preserving; no live re-run needed (the registry is not yet wired into a teardown reconciler — that is Sprint 4.21). |
| File-existence residue predicates `awsEksTestStackHasLiveResources`, `awsEksSubzoneStackHasLiveResources`, `awsTestStackHasLiveResources`, `awsSesStackHasLiveResources` (and the underlying `<stack>HasLiveResources :: FilePath -> IO Bool` shape) across the four `src/Prodbox/Infra/Aws*Stack.hs` modules, plus the matching unit-test fixture `writeFakeStackSnapshot` in `test/unit/Main.hs`. The file-existence approximation was the doctrine-violating piece the May 22, 2026 cascade failure exposed. | Sprint `4.16` source-of-truth swap on 2026-05-27 | New `src/Prodbox/Lifecycle/LiveResidue.hs` exposes `PerRunResidueStatuses`, `queryPerRunResidueStatuses :: FilePath -> IO PerRunResidueStatuses` (one shared MinIO port-forward bracket across the three per-run stacks), and `queryAwsSesResidueStatus :: FilePath -> IO ResidueStatus` (admin AWS credentials + long-lived S3 backend), plus pure helpers `residueStatusFromListing` / `residueReasonFromMinioError` / `residueReasonFromS3Error` and the canonical stack-name constants. Each per-stack `<stack>ResidueStatus` now delegates to the live module; the four `<stack>HasLiveResources` boolean predicates are removed. `Prodbox.Aws.checkPulumiResidueBeforeTeardown` gains a pure half `categorizePulumiResidue :: PerRunResidueStatuses -> ResidueStatus -> [(String, String)]` that the unit suite exercises with synthetic statuses; the IO half batches one MinIO query + one S3 query. `Prodbox.Lifecycle.Preconditions.noLive{PerRun,LongLived}PulumiStacks` and `Prodbox.CLI.Rke2.runNativeDeleteCascade` likewise share the batch. New test-only env var `PRODBOX_TEST_RESIDUE_ABSENT=1` (documented at the test-fixture boundary, set by `fakeAwsEnvironment` / `fakeAwsHarnessEnvironment`) short-circuits both queries to `ResidueAbsent` so the fake-AWS-CLI integration tests do not need a running MinIO or a configured long-lived S3 backend. 17 unit tests rewritten from `writeFakeStackSnapshot tmpDir "<stack>"` + `checkPulumiResidueBeforeTeardown` to synthetic `PerRunResidueStatuses` + `categorizePulumiResidue`; 13 new tests cover the LiveResidue pure helpers and the per-lifecycle-class doctrine asymmetry (per-run unreachable → absent; long-lived unreachable → still-present). Validated with `prodbox check-code` exit 0, `prodbox test unit` 567/567 (up from 554), `prodbox test integration cli` 28/28, `prodbox test integration env` 28/28, `prodbox-daemon-lifecycle` 14/14. The live AWS-substrate re-run (`prodbox test all --substrate aws` produces zero `.prodbox-state/aws-*/` snapshot writes during cascade refusal paths) is the residual operator-driven closure gate. |
| Orphan AWS resources left by killed `prodbox test all --substrate aws` runs on May 27/28, 2026: 3 `aws-test-node-*` EC2 instances (`i-0749e286348bb7d01`, `i-0d7cdc07479c2d854`, `i-01fd4207bac68d579`), 2 VPCs (`vpc-0cb620c5f7ed597cf` = `aws-eks-test-vpc`, `vpc-0f03c105e027d0421` = `aws-test-vpc`), 1 orphan ENI in `subnet-00db2adeb400ca61f` (`eni-00a9aa7ff3261b740`, AWS K8s CNI residue from the destroyed EKS node group), and the associated subnets / IGW / route tables / security groups / EBS volumes. The harness lost Pulumi state for these stacks because the operator killed the test all process before postflight; the file-existence residue predicate then reported "nothing to destroy" while the AWS resources persisted. | Sprint `4.16` (predicate fix planned) | Operator-driven cleanup completed May 28, 2026 via `aws ec2 terminate-instances` + `aws ec2 delete-network-interface` + `aws ec2 delete-subnet` + `aws ec2 detach-internet-gateway` + `aws ec2 delete-internet-gateway` + `aws ec2 delete-route-table` + `aws ec2 delete-security-group` + `aws ec2 delete-vpc` in dependency order. EBS volumes auto-deleted with instances (root-volume flag). This one-time deviation from CLAUDE.md's "harness is the exclusive AWS owner" rule was the bounded escape hatch the doctrine doesn't provide for cases where Pulumi state has diverged from live AWS state; the Sprint 4.16 source-of-truth swap (replace file-existence predicates with live `pulumi stack ls --json` queries against MinIO / S3) is the structural fix that makes this divergence impossible. Local `.prodbox-state/aws-eks-test/`, `.prodbox-state/aws-test/`, and `.prodbox-state/aws-eks-subzone/` directories were also removed in the same session. Final AWS state verified: zero non-default VPCs, zero EC2 instances, zero ENIs, zero ELBs, zero EKS clusters. |
| Inverted cascade phase order in `src/Prodbox/CLI/Rke2.hs::runNativeDeleteCascade`. The pre-Sprint-4.17.a cascade ran `confirm-MinIO → per-run destroys → drain → uninstall → sweep`, contradicting both `documents/engineering/lifecycle_reconciliation_doctrine.md §5b` (now correct) and §1's `drain runs before any Pulumi destroy` prose. Harmless on the home substrate (no in-cluster AWS controllers) but fatal on the AWS substrate: AWS Load Balancer Controller + EBS CSI driver were still alive on the EKS cluster when per-run destroys began, so their ENIs / ALBs / EBS volumes blocked subnet deletion with `DependencyViolation: The subnet '<id>' has dependencies and cannot be deleted`. Surfaced by the May 27/28 AWS-substrate live exercise on Bathurst. | Sprint `4.17.a` on May 28, 2026 | `runNativeDeleteCascade` orchestration reordered to the doctrine-canonical `confirm-MinIO → drain → per-run destroys → uninstall → sweep`; docstring rewritten with the correct rationale (the substrate-aware narrative); new top-level constant `cascadeOrderNarration` exposes the operator-facing phrase as a stable pin. 5 new unit tests in `test/unit/Main.hs::"Sprint 4.17.a canonical cascade phase order"` cover: drain-before-destroys assertion, confirm-MinIO-first assertion, uninstall-between-destroys-and-sweep assertion, explicit refusal of the inverted phrase, and full-string identity. Validated with `prodbox check-code` exit 0, `prodbox test unit` 549/549, `prodbox test integration cli` 28/28. The live home-substrate verification and live AWS-substrate verification (the latter rolling up with Sprint 4.17.b) remain the operator-driven closure gates. |
| Hardcoded local-cluster kubeconfig path `KUBECONFIG=/etc/rancher/rke2/rke2.yaml` in `src/Prodbox/CLI/Rke2.hs::runCascadeDrainPhase`. On the AWS substrate the drain walked the wrong cluster, reported nothing to drain, and let the per-run destroy phase fail with `DependencyViolation` because EKS-side LBC + EBS CSI ENIs were never released. | Sprint `4.17.b` on May 28, 2026 | `runCascadeDrainPhase` now takes a `Substrate` argument; new helper `buildDrainEnvironment` constructs the env-var list per substrate (home-local: optional `KUBECONFIG=rke2.yaml`; AWS: `KUBECONFIG=.prodbox-state/aws-eks-test/kubeconfig` + `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY` / `AWS_DEFAULT_REGION` / `AWS_REGION` / `AWS_SESSION_TOKEN` from `settings.aws`). The cascade infers substrate from per-run residue via new pure helper `inferCascadeSubstrate` — any AWS per-run stack with `ResiduePresent` → `SubstrateAws`. `DrainSkipped` on `SubstrateAws` is now a hard failure with an explanatory message naming `DependencyViolation` as the downstream symptom (skipping the drain guarantees the next phase will fail); `DrainSkipped` on `SubstrateHomeLocal` remains success-with-reason per Sprint 4.15. 6 new unit tests in `test/unit/Main.hs::"Sprint 4.17.b cascade substrate inference"` pin every substrate-inference combination. `Prodbox.PublicEdge.withSubstrateKubectlEnvironment` is not used here because `K8sDrain.K8sDrainEnv` takes an explicit env-var list rather than mutating process env. Validated with `prodbox check-code` exit 0, `prodbox test unit` 554/554, `prodbox test integration cli` 28/28. The live AWS-substrate verification (full `prodbox test all --substrate aws` cycle completes cleanly including the cascade) remains the operator-driven closure gate. |
| Three latent live-path bugs surfaced by the May 27, 2026 `prodbox test all` cycle on Bathurst. (1) `BS.readFile "/dev/urandom"` in `Prodbox.CLI.Rke2.resolveGatewayMinioCredentials` (Sprint 2.19): the strict-ByteString `readFile` reads until EOF, which the infinite character device never emits, so `prodbox rke2 reconcile` hung indefinitely whenever the chart-side `gateway-minio-creds` Secret was absent and reconcile had to bootstrap fresh credentials. (2) UTF-8 decode failure in the gateway daemon Pod (Sprint 2.20/2.22): chart-rendered `config.dhall` contains `§` (`0xC2 0xA7`) in the Sprint 2.22 comment block, and the daemon's Dhall decoder fails under the container's default C/POSIX locale (no `LANG` set). The three `gateway-node-{a,b,c}` Pods crash-looped on every reconcile. (3) JSON-format validation config in `Prodbox.TestValidation.runGatewayDaemonValidation` (Sprint 2.20): `renderGatewayValidationConfig` / `renderGatewayValidationOrders` still emitted JSON, but the post-Sprint-2.20 Dhall-only `Prodbox.Gateway.Settings.loadDaemonConfig` rejects JSON with `Invalid input: unexpected '"'`, so the `gateway-daemon` canonical validation failed on every AWS-substrate and home-substrate run. | May 27, 2026 follow-ups to Sprints 2.19, 2.20, 2.22 | (1) `resolveGatewayMinioCredentials` switched from `BS.readFile` to `openBinaryFile "/dev/urandom" ReadMode` + `BS.hGet handle 34` so it reads exactly the 34 bytes the suffix + password derivation needs. (2) `setLocaleEncoding utf8` added at the top of `src/Prodbox/App.hs::main` (defense-in-depth at the binary layer), plus `ENV LANG=C.UTF-8` and `ENV LC_ALL=C.UTF-8` in `docker/gateway.Dockerfile` and `docker/prodbox.Dockerfile` (defense-in-depth at the container layer); the Dockerfile ENV changes also invalidate the Docker COPY-src layer cache so the binary inside the gateway/workload images is always rebuilt against the current source. (3) `renderGatewayValidationOrders` and `renderGatewayValidationConfig` replaced with `renderGatewayValidationOrdersDhall` and `renderGatewayValidationConfigDhall` emitting the canonical `{ schemaVersion = 1, boot = {…}, live = {…} }` shape that matches `Prodbox.Gateway.Settings.DaemonConfigDhall`; temp file extensions renamed from `.json` to `.dhall`; new `dhallText` helper escapes string literals safely; unused `encode`/`object`/`.=` imports from `Data.Aeson` dropped. Validated live in `prodbox test all` run-4 (~1h35m on home substrate): all 16 named canonical validations recorded `body exit=ExitSuccess` (including the `gateway-daemon` validation that had been failing on run-3), and the destructive `lifecycle` validation (`rke2 delete + rke2 reconcile` in one run) succeeded. Only `keycloak-invite` failed, which is the documented Sprint 8.5 follow-up requiring live Keycloak credential-setup form-structure capture. The gateway daemon master-seed acquisition emits a 403 from MinIO and falls back to structured-503 responses per [secret_derivation_doctrine.md §8](../documents/engineering/secret_derivation_doctrine.md) — the doctrine fallback is preserved; full Sprint 2.19 closure requires diagnosing why the dedicated `prodbox-gateway` MinIO user lacks the HeadObject grant the master-seed read needs. |
| Sprint `7.5.c.v.d` operational IAM policy gap: the May 20, 2026 Sprint `7.5.c.v.d` policy compaction collapsed `ec2:*` and `eks:*` action lists to service wildcards but left individual IAM actions explicit — and dropped the IAM customer-managed-policy lifecycle (`iam:CreatePolicy`/`DeletePolicy`/`GetPolicy`/etc.) and the IAM OIDC-provider lifecycle (`iam:CreateOpenIDConnectProvider`/`DeleteOpenIDConnectProvider`/`UpdateOpenIDConnectProviderThumbprint`/etc.) that the `aws-eks-test` Pulumi program needs. Live runs surfaced both gaps: run 3 hit `iam:CreatePolicy` AccessDenied on the AWS LB Controller policy creation, then run 4 hit `iam:CreateOpenIDConnectProvider` AccessDenied on the EKS OIDC IRSA setup. | Sprint `7.5.c.v.d` follow-up grants on May 21, 2026 | The `IamEksRoleLifecycle` statement in `src/Prodbox/Aws.hs` extended with 10 new actions: `iam:CreateOpenIDConnectProvider`, `iam:CreatePolicy`, `iam:CreatePolicyVersion`, `iam:DeleteOpenIDConnectProvider`, `iam:DeletePolicy`, `iam:DeletePolicyVersion`, `iam:GetOpenIDConnectProvider`, `iam:GetPolicy`, `iam:GetPolicyVersion`, `iam:ListEntitiesForPolicy`, `iam:ListOpenIDConnectProviders`, `iam:ListPolicyVersions`, `iam:TagOpenIDConnectProvider`, `iam:TagPolicy`, `iam:UntagOpenIDConnectProvider`, `iam:UntagPolicy`, `iam:UpdateOpenIDConnectProviderThumbprint`. Verified live in run 4 — the EKS cluster + node group + LB controller IRSA + EBS CSI driver IRSA + OIDC provider all provision cleanly, the `aws-eks` canonical validation passes, and the postflight destroy unwinds the full stack. Sprint `7.5.c.v.d` test pin in `test/unit/Main.hs` only checks the Sid name, not the action list, so the existing tests still pass. |
| `prodbox test all` `lifecycle` validation calling `rke2 delete --yes` without `--allow-pulumi-residue`: the Sprint `4.11` `noLivePerRunPulumiStacks` predicate (correctly) refuses to delete the local RKE2 cluster while the `aws-eks-test` / `aws-test` Pulumi stacks (provisioned earlier by `aws-eks` / `ha-rke2-aws` validations) still own AWS resources, but the canonical suite's `lifecycle` validation runs BEFORE the postflight destroy phase, so the suite always hit the refusal. | Sprint `4.11` follow-up on May 21, 2026 | `runNativeValidation` in `src/Prodbox/TestValidation.hs` extended the `ValidationLifecycle` arm to pass `--allow-pulumi-residue` to `rke2 delete --yes`. The bypass is documented in-code with a haddock comment explaining the suite-internal ownership semantics (the test harness knows its own postflight will clean up the residue). The change preserves the operator-facing `rke2 delete` refusal contract for ad-hoc invocations (no `--allow-pulumi-residue` flag at the operator-CLI layer). |
| Integration test Dhall fixtures missing `pulumi_state_backend` block (Sprint `4.10` schema extension): the cabal `prodbox-integration` test suite carried 4 inline Dhall fixtures (1 in `test/integration/EnvSuite.hs`, 3 in `test/integration/CliSuite.hs`) that listed every section of `ConfigFile` except the new `pulumi_state_backend` field added by Sprint `4.10`. The Dhall decoder rejects with a type-mismatch error, breaking 13/28 integration tests. | Sprint `4.10` fixture follow-up on May 21, 2026 | `pulumi_state_backend = { bucket_name = "", region = "", key_prefix = "" }` added to all 4 fixtures. Sprint `7.5.c.v.f`-related stderr breadcrumb assertions in 2 integration tests (`runs native gateway-partition through the built frontend without delegating to tla-check` + `runs native aws-iam validation through the shared harness and clears leaked operational credentials`) updated from `stderrText `shouldBe` ""` to `stderrText `shouldContain` "[validation=… entering body"` so the integration suite expects the runner's new breadcrumb output. Validated with `cabal test prodbox-integration` passing 28/28. |
| Sprint `4.10` AwsSesStack admin-credential switch + migrate-backend body; Sprint `4.11` full predicate inventory (`noLiveClusterTaggedAws`, `noUndrainedK8sAwsResources`, `noLiveOperationalIamUser`, `noLeftoverDnsBootstrapRecords`); Sprint `8.5` credential-setup form parser scaffold. The previous shape required the operational `aws.*` block for `prodbox pulumi aws-ses-resources` / `aws-ses-destroy` and ran them against the in-cluster MinIO Pulumi backend, so the long-lived `aws-ses` state was tied to the in-cluster MinIO lifetime — incompatible with `rke2 delete + rke2 reconcile` cycles. The migrate-backend command was a runbook-only emitter. The Sprint 4.11 predicate library had only the two Pulumi-stack predicates. Sprint 8.5's credential-setup form POST step was operator-driven with no Haskell parser to hand off to. | Sprint `4.10` admin-cred switch + migrate-body + Sprint `4.11` predicate-library full inventory + Sprint `8.5` parser scaffold on May 21, 2026 | New `loadAdminAwsCredentials` + `adminCredentialsConfigured` in `src/Prodbox/Infra/LongLivedPulumiBackend.hs`; new `pulumiSesAdminBaseEnv` in `src/Prodbox/Infra/AwsSesStack.hs`; `ensureAwsSesStackResources` + `destroyAwsSesStackStatus` were rewritten to authenticate with `aws_admin_for_test_simulation.*` and use the then-current long-lived S3 backend directly (no MinIO port-forward, no operational `aws.*` read). The Sprint `4.10` `migrateAwsSesStackBackend` body implemented the historical doctrine recipe end-to-end (idempotent short-circuit when the long-lived backend already carried the stack; export from MinIO → import into S3 otherwise) bracketed around a temporary JSON file; Sprint `7.14` later superseded that body with the encrypted-wrapper-backed first-touch migration path. New `noLiveClusterTaggedAws :: TagSweepInput -> Precondition` in `src/Prodbox/Lifecycle/Preconditions.hs` wraps `discoverClusterTaggedAwsResources` from `Prodbox.Lifecycle.TagSweep`; new `noUndrainedK8sAwsResources :: K8sDrainEnv -> Precondition` wraps the newly-exposed `collectSurvivors` from `Prodbox.Lifecycle.K8sDrain`; new `noLiveOperationalIamUser :: FilePath -> Credentials -> Precondition` wraps the new `operationalIamUserExists :: FilePath -> Credentials -> IO (Either String Bool)` IAM probe in `src/Prodbox/Aws.hs`; new `noLeftoverDnsBootstrapRecords :: FilePath -> Credentials -> Precondition` wraps the new `operationalBootstrapDnsRecordExists :: FilePath -> Credentials -> IO (Either String Bool)` Route 53 probe. New `src/Prodbox/Keycloak/CredentialSetupForm.hs` exposes `CredentialSetupForm`, `parseCredentialSetupForm` (rejects HTML missing `kc-passwd-update-form` id, missing password input pair, malformed attributes), and `renderCredentialSetupFormPost` (application/x-www-form-urlencoded body with hidden-field preservation). New synthetic fixture at `test/unit/fixtures/keycloak-credential-setup-form.html` for offline development; the live form-structure capture step replaces this fixture verbatim. Six new unit tests in `test/unit/Main.hs::"Sprint 8.5 Keycloak credential-setup form parser"`, four in `"Sprint 4.11 predicate-library labels"`, five in `"Sprint 4.10 admin-credential predicate"`. The `applyAwsTeardown` library refactor (compose `checkAll [noLivePerRunPulumiStacks, noLiveLongLivedPulumiStacks]`) is deliberately deferred — runtime behavior is identical to the existing `checkPulumiResidueBeforeTeardown` + `renderPulumiResidueRefusal` pair, and switching would require either fragile golden-text pinning or doctrine-driven refusal-text alignment. Validated with `prodbox check-code` (exit 0) and `prodbox test unit` (434/434, up from 420). The remaining live form-structure capture + parser wire-in into `runKeycloakInviteValidation` and live `--substrate aws` canonical-suite re-run are operator-driven and tracked as remaining work in `phase-8-email-invite-auth.md`; the live end-to-end `nuke` destructive cycle closed on June 3, 2026. |
| Sprint `4.13` `prodbox nuke` orchestration body: the operator-only total-teardown command previously emitted the dry-run plan even after typed-confirmation acceptance, leaving the destructive sequence as a runbook for the operator to compose by hand. The Sprint `4.10` `pulumi/aws-ses/Pulumi.yaml` file also lacked the long-lived `backend.url` declaration, so operators running `pulumi` directly outside the harness fell back to the local in-cluster MinIO backend even when `pulumi_state_backend` was configured in Dhall. | Sprint `4.13` orchestration-body landing + Sprint `4.10` `Pulumi.yaml` backend URL on May 21, 2026; credential-source and live nuke closure refreshed June 3, 2026; Sprint `7.14` encrypted-backend order refresh on June 16, 2026 | `src/Prodbox/CLI/Nuke.hs` now wires a five-step in-process orchestration that acquires the ephemeral elevated admin credential through the interactive `SecretRef.Prompt` (the test harness simulates that prompt from the `aws_admin_for_test_simulation.*` `TestPlaintext` fixture in `test-config.dhall`; there is no stored admin block in `prodbox-config.dhall`) and runs `aws-ses` destroy while Vault/MinIO are still live → cascade arm → operational IAM teardown → postflight tag sweep → long-lived state-bucket destroy. (Sprint `7.16` retires the earlier config-backed admin path; see the Pending Removal row.) The tag sweep at step 4 is non-fatal (reports residue, proceeds to bucket destroy) because failures after that point cannot be unwound. New `Prodbox.Infra.LongLivedPulumiBackend.destroyLongLivedPulumiStateBucket` empties the versioned bucket (`aws s3 rm --recursive` + paginated `list-object-versions` → `delete-objects` loop) then deletes the bucket; the Haskell-side JSON parsing replaces an earlier `python3` shell helper draft. New export from `Prodbox.CLI.Rke2` (`runNativeDeleteCascade`) plus `Prodbox.Aws.adminAwsEnvironment` lets the orchestration body compose existing helpers without subprocess invocations of `prodbox` itself for steps that need the already-loaded admin credential. `pulumi/aws-ses/Pulumi.yaml` gained a top-level `backend:` block declaring `s3://prodbox-pulumi-state-long-lived?region=us-west-2&awssdk=v2&prefix=pulumi/` so operators running pulumi directly pick up the long-lived backend. Eight unit tests in `test/unit/Main.hs` cover the canonical `delete-objects` payload shape (`renderDeletePayload`), the nuke confirmation literal, the five-step ordering of `renderNukePlan`, and the long-lived S3 `NoSuchBucket` classification; the June 3 refresh also pins the rendered admin credential source. Pre-existing formatter blockers fixed in passing: haddock comment placement in `Prodbox.Lifecycle.K8sDrain` `CascadeDecision` constructor; import-order rebalance in `test/unit/Main.hs`. Validated with `prodbox check-code` (exit 0), `prodbox test unit` (634/634), `prodbox nuke --dry-run` (exit 0), and live `prodbox nuke` (exit 0) on June 3, 2026. Remaining live operator validations for the AwsSesStack migrate body are tracked in `DEVELOPMENT_PLAN/phase-4-lifecycle-canonical-paths.md`; the live end-to-end `nuke` destructive cycle is closed. |
| Sprint identifiers in operator-facing strings: `src/Prodbox/CLI/Spec.hs:672` (`--cascade` flag help, parser-side), `:683` (`--allow-pulumi-residue` flag help), `:1277` (`aws-ses-migrate-backend` leaf description), `:1331` (`rke2 delete` leaf description), `:1337` (`--cascade` flag help, leaf-side), `:1349` (example help), `:1444` (`nukeLeaf` description), plus sprint labels in `src/Prodbox/CLI/Spec.hs::awsTeardownFlagsParser` flag-help strings at `:735` / `:748`. The leak had propagated into `documents/cli/commands.md`, `share/man/man1/prodbox-{nuke,pulumi,rke2}.1`, `share/completion/{bash,zsh,fish}/...`, and `test/golden/cli/{commands-tree.txt,commands.json,help-all.txt}`. | Sprint `4.14` on May 21, 2026 | Each string rewritten to operator vocabulary (behavioral prose preserved, sprint labels dropped). New `checkOperatorVocabulary` scan in `src/Prodbox/CheckCode.hs` enforces the [cli_command_surface.md § 2A](../documents/engineering/cli_command_surface.md#2a-operator-vocabulary-contract) contract by refusing any adjacent `Sprint <digit>` / `Sprints <digit>` token pair in `Spec.hs` string literals or in any file under `share/man/`, `share/completion/`, `documents/cli/`, `test/golden/cli/`. New exports `extractStringLiterals` + `matchesSprintToken` plus seven pure unit tests covering the boundary cases (parenthesised, colon-suffixed, plural form, plain English `Sprint` word without digit). Validated with `prodbox check-code` (exit 0, with the new scan wired), `prodbox test unit` (410/410, up from 403), `prodbox lint docs` (exit 0), `prodbox docs check` (exit 0), and `grep -rE 'Sprint [0-9]' documents/cli/ share/man/ share/completion/ test/golden/cli/` returning empty. |
| Silent-exit failure mode in `Prodbox.TestValidation.runChartsVscodeValidation` and its `runCharts*Validation` / `runAdminRoutesValidation` / `runPublicDnsValidation` siblings under `substrate=aws`. | Sprint `7.5.c.v.f` on May 21, 2026 | Closed by threading `Substrate` end-to-end through `prodbox host public-edge --substrate {home-local,aws}` (new `HostCommand.HostPublicEdge` carries `Substrate`; `src/Prodbox/Host.hs::runHostPublicEdge` uses `substratePublicFqdn` / `substrateHostedZoneId` instead of the home-substrate `publicFqdn` / hardcoded `route53.zone_id`; new `src/Prodbox/Dns.hs::queryRoute53RecordInZone` takes an explicit hosted-zone id; the legacy `queryRoute53Record` is now a home-substrate adapter), through `src/Prodbox/TestValidation.hs::waitForPublicEdgeReady` (now takes `Substrate` and passes `--substrate <id>` to the spawned subprocess), through the five sibling validation bodies, and through `src/Prodbox/TestRunner.hs::runWaitForPublicEdgeReady`. `runNativeValidation` now emits stderr breadcrumbs `[validation=<id> substrate=<id>] entering body` before the body and `... body exit=<ExitCode>` after, so a silent exit is structurally impossible at the runner level. Validated with `prodbox check-code` (exit 0) and `prodbox test unit` (399/399). Live `--substrate aws` re-run is rolled up into Sprint `7.5.c.v`. |
| Sprint `4.10`/`4.11`/`4.12`/`4.13` code frameworks: long-lived Pulumi backend decouple + lifecycle predicate library + K8s drain + `prodbox nuke` total teardown scaffold. | Sprints `4.10`–`4.13` code frameworks on May 21, 2026 | Sprint `4.10`: `prodbox-config-types.dhall` + `prodbox-config.dhall` `pulumi_state_backend` block already in place; new `PulumiStateBackendSection` Haskell record in `src/Prodbox/Settings.hs` with a custom `FromDhall` instance that strips the `psb` Haskell-side prefix; new `src/Prodbox/Infra/LongLivedPulumiBackend.hs` exports the URL renderer + `ensureLongLivedPulumiStateBucket` + `withLongLivedPulumiBackendEnv` bracket; new `prodbox pulumi aws-ses-migrate-backend` CLI scaffold with TTY guard, later superseded by Sprint `7.14`'s encrypted-wrapper-backed first-touch migration body. Sprint `4.11`: new `src/Prodbox/Lifecycle/Preconditions.hs` (`Precondition`, `StructuredError`, `checkAll`, `noLivePerRunPulumiStacks`, `noLiveLongLivedPulumiStacks`); new `src/Prodbox/Lifecycle/TagSweep.hs` (`discoverClusterTaggedAwsResources` against the AWS Resource Tagging API); new `Rke2DeleteFlags` record with `--cascade` xor `--allow-pulumi-residue` enforced at parse time via `flag' <|> flag' <|> pure`; `prodbox rke2 delete` (default) opens with `checkAll [noLivePerRunPulumiStacks]`. Sprint `4.12`: new `src/Prodbox/Lifecycle/K8sDrain.hs` (`DrainTimeout`, `DrainResult`, `drainAwsAffectingK8sResources` deletes LoadBalancer Services + ALB Ingresses + Delete-reclaim PVCs cluster-wide then polls every 10s with bounded timeout). Sprint `4.13`: new `src/Prodbox/CLI/Nuke.hs` + new `NativeNuke NukeOptions` constructor; `prodbox nuke` is TTY-only, refuses non-interactive, requires typed confirmation literal `NUKE EVERYTHING`, supports `--dry-run` / `--plan-file`. Validated with `prodbox check-code` (exit 0) and `prodbox test unit` (403/403, up from 387 by adding URL-renderer + lifecycle-flag + nuke-parser tests). The remaining live cascade exercise is operator-driven and tracked in `DEVELOPMENT_PLAN/phase-4-lifecycle-canonical-paths.md`; the live `nuke` exercise closed on June 3, 2026. |
| The redundant `settings_loaded` prerequisite node in `src/Prodbox/Prerequisite.hs` (the `settingsLoaded` `EffectNode` + its `allPrerequisites` entry). It modelled the exact same `Validate RequireSettings` satisfied condition as `settings_object`, so the two-node split carried no extra information. | Sprint `1.31` on June 9, 2026 | Collapsed into the single surviving `settings_object` node. The `settingsLoaded` definition and its registry entry are deleted; the one dependent edge (`aws_credentials_valid.effectNodePrerequisites`) is re-pointed from `["settings_loaded", "tool_aws"]` to `["settings_object", "tool_aws"]`; `TestPlan.clusterPrerequisites` already named `settings_object`. Done in the same Sprint 1.31 change that made acyclicity a construction-time invariant: `Prodbox.EffectDAG.transitiveClosureIds`/`fromRootIds` now reject a back-edge (a node transitively depending on itself) with `Left "Prerequisite cycle detected: a -> b -> a"` in the same `Either String` expansion path that already rejected missing ids — a cyclic registry can never produce an `EffectDAG` — and `Prodbox.EffectInterpreter.runEffectDAG` threads a boundary-only `SatisfiedEffectMemo` so an already-satisfied node effect runs its probe at most once per run. Unit tests added under `test/unit/Main.hs`: `"construction-time acyclicity (Sprint 1.31)"` (back-edge, self-edge, diamond-still-constructs, missing-id-still-rejected), `"collapsed settings node (Sprint 1.31)"` (registry no longer carries `settings_loaded`; closure resolves through the survivor), and `"interpreter satisfied-node memo (Sprint 1.31)"` (two distinct nodes sharing one counting command increment the counter once). The static `hasCycle`-over-registry assertion is demoted to a regression guard. Validated with `prodbox check-code` (exit 0), `prodbox test unit` (757/757), `prodbox lint docs` (exit 0), `prodbox docs check` (exit 0), and `prodbox test integration cli` (the prerequisite DAG path still passes). |
| Dead `Prodbox.Retry` exports `retryAppError` and `defaultRetryPolicy` — exported from the module but with zero `src/` callers. | Sprint `1.30` on June 9, 2026 | Removed as part of the retry/error doctrine rewrite. `serviceErrorRetryable` is now real: `ServiceError` is a classified constructor sum (`SEConnectionFailed`/`SETimeout`/`SEConflict`/`SEInternalError` retryable; `SENotFound`/`SEPermissionDenied` not), classified once at the single subprocess boundary by `classifyServiceError` in `src/Prodbox/Service.hs`; `serviceErrorRetryable :: ServiceError -> Bool` is a total function of the constructor and no call site pins a literal Bool. The retrier (`retryServiceAction`) is split from the new readiness poller (`pollUntilReady` + `PollOutcome` in `src/Prodbox/Retry.hs`); the ChartPlatform Patroni "wait until ready" sites and the daemon-lifecycle HTTP-status wait migrated to the poller, transient-failure retries stay on the retrier. New `checkServiceErrorRetryableLiteral` doctrine-alignment scan (pure `serviceErrorRetryableLiteralViolations`) wired into `runDoctrineAlignmentCheck` fails on any hand-set retryable literal outside the classifier boundary. The dead `retryAppError` / `defaultRetryPolicy` exports (no tests referenced them) are deleted. See [haskell_code_guide.md](../documents/engineering/haskell_code_guide.md). |
| Non-TTY-safe interactive-command contract: every `prodbox` interactive entry point (`prodbox config setup`, `prodbox aws setup`, `prodbox aws teardown`, `prodbox aws check-quotas`, `prodbox aws request-quotas`, `prodbox charts delete` confirmation) refuses to prompt when stdin is not a TTY and emits a stderr message naming the automation equivalent. Plus a doctrine-aware command-mapping table added to `CLAUDE.md` and `AGENTS.md`. Closes a chronic LLM/CI failure mode where agents would run `prodbox aws setup`, hit the prompt, and report it as a blocker — the new error message names `prodbox test all --substrate aws` (or the targeted integration command) as the harness equivalent. | Sprint `0.7` (LLM/automation guardrails) on May 20, 2026 | New module `src/Prodbox/CLI/Interactive.hs` exposes `InteractiveGuard` + `requireInteractiveTty` + per-command guard values (`awsSetupGuard`, `awsTeardownGuard`, `awsCheckQuotasGuard`, `awsRequestQuotasGuard`, `configSetupGuard`, `chartsDeleteGuard`); wired at the head of every `interactive*Input` in `src/Prodbox/Aws.hs` and into `promptForDelete` in `src/Prodbox/CLI/Charts.hs`. Test-only env var `PRODBOX_ALLOW_NON_TTY_INTERACTIVE=1` (documented in `documents/engineering/cli_command_surface.md` § 3A as test-only) is set by `fakeAwsEnvironment` / `fakeAwsHarnessEnvironment` in `test/integration/CliSuite.hs` so the existing interactive-flow integration tests still exercise the prompt with piped stdin. New section "Interactive vs Non-Interactive Surfaces" in `documents/engineering/cli_command_surface.md` and one-line cross-reference in `documents/engineering/aws_integration_environment_doctrine.md` § 0. The exception-display bug in `runAwsCommand` / `runInteractiveConfigSetup` was fixed in the same commit: `try @SomeException` on the dispatch was treating `exitWith` as a crash and printing `ExitFailure 1` after the guard's own message; the fix is `fromException @ExitCode` → re-throw. Validated with `prodbox check-code` (exit 0), `prodbox test unit` (386/386 — 6 new guard tests + 1 env-var-name test), and `prodbox test integration cli` (28/28 — all previously-failing interactive tests pass after the env-var-set in the fake-env helpers). |
| `Ec2HaTestStackLifecycle` Sid in the operational IAM policy (PolicyFull tier) compressed from a 24-action explicit list to `ec2:*`; `EksTestStackLifecycle` similarly compressed to `eks:*`. New `SesCaptureBucketRead` / `SesCaptureObjectRead` (S3 grants on `prodbox-ses-capture`) and `SesReadOnly` (`ses:Describe*` / `Get*` / `List*`) statements added. Inline policy submission switched from pretty-printed `AesonPretty` to compact `Data.Aeson.encode` so the policy fits within AWS's 2048-byte inline-user-policy cap. | Sprint `7.5.c.v.d` + `7.5.c.v.e` on May 20, 2026 | The May 20 live run (run3) exposed two architectural gaps: (a) the Sprint 8.4 `ses_receive_bucket_accessible` prereq calls `aws s3api head-bucket` using operational `aws.*`, but the operational user had no S3 access; (b) the `ses_receive_rule_set_active` and `ses_sending_identity_verified` prereqs call `aws ses describe-active-receipt-rule-set` / `get-identity-verification-attributes` and the operational user had no SES access. Adding both grants pushed the pretty-printed policy past AWS's 2048-byte inline-policy cap; the explicit Ec2/Eks action lists were the biggest contributors, so they collapsed to service wildcards (the operational user creates and destroys whole VPCs / clusters by design via `aws-test` / `aws-eks` Pulumi stacks, so wildcards are operationally equivalent). Compact-encoded policy size: 1509 bytes. Unit test extended at `test/unit/Main.hs:506` and integration test at `test/integration/CliSuite.hs:105` updated for the renamed Sid + new Sids. Verified live in run4: the prereqs pass and the harness IAM-user creation succeeds. | 
| Sprint 7.7 `BypassPerRunResidueOnly` policy applied to `runAwsIamHarnessSetup` preflight teardown caused every harness-driven test run to refuse on the long-lived `aws-ses` stack (which is the intended steady state). The preflight is paired with an immediate aws.* re-materialization in the same function call, so the residue refusal that protects operator-driven teardowns is misapplied to the harness preflight refresh. | Sprint `7.5.c.v.c` on May 20, 2026 | New `PulumiResiduePolicy` constructor `BypassAllResidueForHarnessRefresh` (harness-internal only, never CLI-settable) bypasses both per-run AND long-lived residue refusal. `applyAwsTeardown` adds the constructor to its case-of with a straight `runTeardown` branch. `runAwsIamHarnessSetup` preflight switched from `BypassPerRunResidueOnly` to `BypassAllResidueForHarnessRefresh`; `runAwsIamHarnessTeardown` (postflight) keeps `BypassPerRunResidueOnly` because at end-of-run the operator may legitimately need aws.* preserved to destroy aws-ses. Two new unit tests in `test/unit/Main.hs::"Sprint 7.7 applyAwsTeardown residue policy"` (Scenarios M and N) cover aws-ses-only and all-four-stacks-present cases. Verified live in run5: the harness preflight now proceeds and materializes operational aws.* successfully on every run regardless of aws-ses state. |
| External `dhall-to-json` subprocess decode bridge in `src/Prodbox/Settings.loadConfigFile`, the `toolDhall` / `tool_dhall` external-tool prerequisite in `src/Prodbox/Prerequisite.hs`, the matching prerequisite-registry assertion in `test/unit/Main.hs`, and the `FromJSON` / `ToJSON` derivations on the settings record types that existed solely to receive the bridge's JSON output. | Sprint `1.2` revision on May 20, 2026 | Replaced by in-process decoding through the native Haskell `dhall` library (`Dhall.inputFile auto`). `src/Prodbox/Settings.hs` now derives `FromDhall` on `Credentials`, `Route53Section`, `AwsSubstrateSection`, `SesSection`, `DomainSection`, `MetallbBgpPeer`, `AcmeSection`, `DeploymentSection`, `StorageSection`, and `ConfigFile`; `loadConfigFile` wraps `Dhall.inputFile` with `Control.Exception.try` and keeps the historical `IO (Either String ConfigFile)` contract and the "`Failed to decode Dhall config \`<path>\`: ...`" error shape. `prodbox.cabal` library `build-depends` gained `dhall ^>=1.42` and the `aeson` / `Subprocess` / `System.Exit` / `Data.ByteString.Lazy.Char8` imports were dropped from `Prodbox.Settings`. `Prodbox.Prerequisite` lost the `toolDhall` definition and its `allPrerequisites` entry; the corresponding `lookupPrerequisiteEffect "tool_dhall"` assertion in `test/unit/Main.hs` was deleted alongside the `"tool_dhall"` registry-list line. Fixture strings `validConfig` / `invalidZeroSslConfig` were extended with explicit `None Text` / `None Natural` / `None (List ...)` values for all `Optional` deployment fields because the native Dhall decoder requires the value to satisfy the full record type (aeson's `FromJSON` defaulted missing `Maybe` fields to `Nothing`; Dhall does not). Validated with `cabal build --builddir=.build exe:prodbox` (clean), `./.build/prodbox test unit` (377/377), and `./.build/prodbox check-code` (exit 0). |
| Unconditional `awsTeardownAllowPulumiResidue = True` in `src/Prodbox/Aws.hs::runAwsIamHarnessSetup` (line 774) and `src/Prodbox/Aws.hs::runAwsIamHarnessTeardown` (line 858), plus the residual "Elevated AWS access key ID" / "Elevated AWS secret access key" / "Elevated AWS session token" / "AWS region for elevated operations" prompt strings in `src/Prodbox/Aws.hs::promptAdminCredentials` (lines 743 / 744 / 746 / 749). Both defects reproduced May 19, 2026: a `prodbox test integration aws-iam` run cleared operational `aws.*` from `prodbox-config.dhall` even though `aws-ses` was alive from the Sprint `8.1` closure, stranding `aws-ses` from the supported destroy surface; the prompt structure also made it unclear whether operators should fill the session-token field (treated as "optional" but actually required for STS-derived `ASIA…` keys). Six aws-ses AWS resources verified alive during diagnosis: SES sending identity `test.resolvefintech.com` (Verified + DKIM Success), active receipt rule set `prodbox-receive-rule-set`, S3 bucket `prodbox-ses-capture`, IAM user `prodbox-ses-smtp`, Route 53 MX `inbox.test.resolvefintech.com.`, and the `.prodbox-state/aws-ses/stack-snapshot.json` predicate file. | Sprint `7.7` closure on May 19, 2026 | Replaced the `Bool` bypass with a `PulumiResiduePolicy` ADT (`RefuseOnAnyResidue` / `DestroyPulumiResidueFirst` / `AcceptOrphanResidue` / `BypassPerRunResidueOnly`) plus a per-run vs long-lived partition (`perRunStackNames` / `longLivedStackNames` / `partitionResidueByLifecycle`). `runAwsIamHarnessSetup` and `runAwsIamHarnessTeardown` now use `BypassPerRunResidueOnly` so the harness refuses on long-lived (`aws-ses`) residue while bypassing per-run residue that `awsPostflightDestroyActions` handles. New `--destroy-pulumi-residue` operator flag (mutually exclusive with `--allow-pulumi-residue` via the `flag' <|> flag' <|> pure RefuseOnAnyResidue` idiom, rejected at parse time with "Invalid option" exit 1) dispatches `prodbox pulumi <stack>-destroy --yes` for each live stack in canonical order via new `dispatchPulumiDestroysForResidue`, with a stderr warning before the `aws-ses` destroy. `interactiveAwsTeardownInput` refactored to `IO (Either String (Maybe AwsTeardownInput))` shape: file-based residue check runs before any prompt, residue + empty `aws.*` short-circuits to "Nothing to do." exit 0, refusal returns `Left` without ever prompting. `promptAdminCredentials` refactored to use pure helper `sessionTokenPromptShape :: Text -> SessionTokenPromptShape` that returns `SkipPrompt` for `AKIA…` (long-lived IAM user keys), `PromptRequiredHidden` for `ASIA…` (STS-derived), and `PromptOptionalWithHint` for any other prefix as defensive fallback. All four user-facing prompt strings renamed from "Elevated AWS …" / "elevated operations" to "Temporary admin AWS …" / "admin operations". `showAdminCredentialsGuidance` body updated to explain both `AKIA` and `ASIA` credential shapes in plain language. The historical-marker parentheticals "(historically called 'elevated credential')" in `documents/engineering/aws_admin_credentials.md` / `aws_account_setup_guide.md` / `aws_integration_environment_doctrine.md` / `DEVELOPMENT_PLAN/README.md` / `DEVELOPMENT_PLAN/00-overview.md` / `README.md` remain as compatibility markers for one release cycle. 24 new unit tests across `test/unit/Main.hs::"Sprint 7.7 residue lifecycle partition"`, `"Sprint 7.7 applyAwsTeardown residue policy (Scenarios E/F/G/H/I)"`, `"Sprint 7.7 DestroyPulumiResidueFirst dispatch plan (Scenarios J/K/L)"`, `"Sprint 7.7 promptAdminCredentials UX (sessionTokenPromptShape)"`, and `"Sprint 7.7 awsTeardownPolicyFromFlags mutual exclusion"`. Validated with `prodbox check-code` (exit 0), `prodbox lint docs` (exit 0), `prodbox docs check` (exit 0), and `prodbox test unit` (378/378, up from 354). CLI smoke verified live: `prodbox aws teardown --help` shows `[--destroy-pulumi-residue | --allow-pulumi-residue]` mutual-exclusion bracket; `prodbox aws teardown --allow-pulumi-residue --destroy-pulumi-residue` exits 1 with "Invalid option" before any other work. |
| Host-Docker `docker push` + `sudo ctr image import` paths in `Prodbox.CLI.Rke2.ensureCustomImageVariants` had no AWS-substrate analog. `docker push 127.0.0.1:30080/prodbox-gateway/...` from the operator host reaches the home RKE2 Harbor (NodePort 30080 on `127.0.0.1`), not the EKS-side Harbor; and `sudo ctr -a /run/rke2/containerd/containerd.sock` is unavailable from outside an EKS node. Without an AWS path, `prodbox-gateway` + `prodbox-public-edge-workload` could not land in EKS-side Harbor, so the gateway chart pods went into `ImagePullBackOff` in the Sprint `7.5.c.v` first live run. | Sprint `7.5.c.v.b` closure on May 19, 2026 | New `src/Prodbox/Lib/EksCustomImagePush.hs` renders a long-running `gcr.io/go-containerregistry/crane:debug` Pod with a 4 GiB `emptyDir` at `/data` as the `kubectl cp` target. New `Prodbox.CLI.Rke2.ensureCustomImageVariantsAws` builds the image via operator-host Docker, `docker save`'s to `.prodbox-state/tmp/prodbox-custom-image.tar`, applies the crane pod, `kubectl wait`'s for Ready, `kubectl cp`'s the tarball, and `kubectl exec`'s `/ko-app/crane push /data/image.tar <rewritten-target> --insecure` for each requested tag. The `ctr` import step is intentionally omitted (EKS nodes pull from in-cluster Harbor via the Sprint 7.5.c.ii containerd registry-mirror DaemonSet). `ensureCustomImageVariantsForSubstrate :: Substrate -> ...` dispatches; `SubstrateHomeLocal` keeps the existing host-Docker login + push + `ctr` flow unchanged. New `ensureGatewayImagesForSubstrate` / `ensurePublicEdgeWorkloadImageForSubstrate` exports thread the substrate parameter; both modules' legacy non-substrate names remain as `SubstrateHomeLocal` aliases for backward compatibility with `ensureNativeInstallation` and other home-substrate call sites. `ensureAwsSubstratePlatformRuntime` orchestration extended with two new steps between `applyEksImageMirrorJob` and `ensurePostgresOperatorRuntime`, yielding the canonical thirteen-step sequence. Six new unit tests in `test/unit/Main.hs::"Sprint 7.5.c.v.b EKS custom-image push pod"` cover the renderer + the chart-ref rewrite; the extended orchestration-ordering block adds the thirteen-step golden + the mirror→gateway→workload→Percona invariant. Validated with `prodbox check-code` (exit 0), `prodbox lint docs` (exit 0), `prodbox docs check` (exit 0), and `prodbox test unit` (354/354, up from 348). Live verification of the crane push pod end-to-end is deferred to the Sprint 7.5.c.v re-run. |
| Six unstated home-substrate assumptions in the AWS-substrate platform install path, surfaced by the first live `prodbox charts deploy gateway --substrate aws` run against EKS on May 19, 2026: (1) the Pulumi `aws-eks` program did not install the EBS CSI driver (newer EKS clusters do not ship the in-tree `kubernetes.io/aws-ebs` provisioner, so PVCs against `gp2` stay `Pending` forever); (2) `awsLbControllerRole` and the new `ebsCsiDriverRole` trust-policy condition keys included `https://` prefix that STS rejects (per AWS IRSA docs the condition key uses the OIDC issuer URL without the scheme); (3) `Prodbox.Lib.EksImageMirror.defaultEksImageMirrorConfig` referenced `gcr.io/go-containerregistry/crane:v0.20.2` which has never been published on gcr.io; (4) the `:debug` variant of crane uses distroless static-debian12:debug as its base which ships only `/busybox/sh`, not `/bin/sh`; (5) `crane copy` defaulted to HTTPS:443 against the in-cluster Harbor service which exposes HTTP only (`expose.tls.enabled=false`); (6) `Prodbox.CLI.Rke2.ensureHarborProject` issued `curl` POSTs to `http://127.0.0.1:30080/api/v2.0/projects` which only resolves to Harbor on RKE2 (the AWS substrate operator host has no network path into the EKS Harbor NodePort). | Sprint `7.5.c.v` in-flight closure on May 19, 2026 (for items 1–5 and item 6); Sprint `7.5.c.v.b` covers the remaining `ensureGatewayImages` / `ensurePublicEdgeWorkloadImage` host-Docker gap | (1) `pulumi/aws-eks/Main.yaml` extended with `ebsCsiDriverRole` (IRSA), `AmazonEBSCSIDriverPolicy` attachment, and the `aws-ebs-csi-driver` managed addon; verified live by manual `aws sts assume-role-with-web-identity` plus PVCs binding to `gp2`-backed EBS volumes. (2) `pulumi/aws-eks/Main.yaml` `variables` block now derives `oidcIssuerHostPath` via `fn::split` on `/` + `fn::join` of segments 2–4 of `cluster.identities[0].oidcs[0].issuer`, then references `${oidcIssuerHostPath}:sub` / `:aud` in both LB controller and EBS CSI trust policies; verified live by STS returning valid creds where it had returned `AccessDenied` before. (3) `Prodbox.Lib.EksImageMirror.defaultEksImageMirrorConfig.mirrorJobImage` updated to `gcr.io/go-containerregistry/crane:debug`. (4) `eksImageMirrorCopyScript` shebang switched to `#!/busybox/sh`; `eksImageMirrorJobManifest` container command updated to `["/busybox/sh", "-c", …]`. (5) `crane copy` invocations rendered by `renderCopyCommand` now append `--insecure`. (6) `createHarborProjectsAws` in `Prodbox.CLI.Rke2` now runs project creation by `kubectl exec` into the already-running `harbor-core` deployment and calling `http://harbor.harbor.svc.cluster.local/api/v2.0/projects`, avoiding both the operator-host NodePort assumption and a pre-mirror curl-pod image dependency; `ensureHarborProjectsForSubstrate` dispatches `SubstrateHomeLocal -> createHarborProjectsHomeLocal` (the existing host-curl path) and `SubstrateAws -> createHarborProjectsAws`. Verified live: full 11-step `ensureAwsSubstratePlatformRuntime` completes on EKS — AWS LB Controller + Envoy Gateway + cert-manager + ACME ClusterIssuer + containerd registry-mirror DaemonSet + MinIO (gp2 EBS) + Harbor S3 backend Job + Harbor (NodePort 30080) + image-mirror Job (21 images via `crane copy`, 5m02s) + Percona operator + steady-state MinIO reconcile. Tested with `prodbox check-code` (exit 0), `prodbox lint docs` (exit 0), `prodbox docs check` (exit 0), and `prodbox test unit` (348/348). The remaining `ImagePullBackOff` on gateway pods is tracked by Sprint `7.5.c.v.b`. |
| Host-Docker + host-`ctr` image-mirror loop in `Prodbox.CLI.Rke2.mirrorClusterImagesOnce` had no AWS-substrate analog. On EKS the operator host has no network path into the EKS-side Harbor NodePort and no `ctr` socket access into EKS node containerd sockets, so the home-substrate image-mirror flow does not apply. Without an in-cluster equivalent, the chart-image refs the canonical chart set consumes (`127.0.0.1:30080/prodbox/...`) cannot be populated in EKS-side Harbor, and downstream Helm releases (Percona operator, steady-state MinIO, gateway/keycloak/vscode/api/websocket charts) fail with `ImagePullBackOff`. | Sprint `7.5.c.iv` closure on May 19, 2026 | New `src/Prodbox/Lib/EksImageMirror.hs` exposes `EksImageMirrorConfig`, `defaultEksImageMirrorConfig`, `eksImageMirrorJobManifest`, and `eksImageMirrorCopyScript`. The renderer emits a `batch/v1` Job in `harbor` namespace running `gcr.io/go-containerregistry/crane:v0.20.2`; the container script authenticates to Harbor via the in-cluster DNS endpoint `harbor.harbor.svc.cluster.local`, then `crane copy`'s every `(upstream-source, chart-target)` pair from `Prodbox.ContainerImage.requiredPublicImagePairs` (rewriting `127.0.0.1:30080/...` chart-targets to the in-cluster endpoint for the push so in-pod-network `127.0.0.1` does not resolve to the pod itself). `crane copy` is idempotent on already-pushed digests; the Job's `backoffLimit=2` retries transient upstream registry failures within the single Job. `applyEksImageMirrorJob :: FilePath -> IO ExitCode` in `Prodbox.Lib.AwsSubstratePlatform` wraps the manifest in a `v1/List`, applies via `kubectl apply -f`, then blocks on `kubectl wait --for=condition=complete job/prodbox-image-mirror -n harbor --timeout=20m`. The `ensureAwsSubstratePlatformRuntime` orchestration is extended with three new steps after Harbor install: image-mirror Job, `ensurePostgresOperatorRuntime` (now able to pull the Percona operator image from Harbor via the Sprint 7.5.c.ii containerd mirror), and `ensureMinioRuntime SubstrateAws MinioSteadyStateHarbor` (reconcile MinIO with Harbor-mirrored images for steady state). Seven new unit tests in `test/unit/Main.hs::"Sprint 7.5.c.iv EKS image-mirror Job"` cover manifest + copy-script structure; three new tests in the extended orchestration block verify the Harbor→mirror→Percona ordering and the Percona→steady-state-MinIO ordering against the now-eleven-step canonical sequence. Validated with `prodbox check-code` (exit 0), `prodbox lint docs` (exit 0), `prodbox docs check` (exit 0), and `prodbox test unit` (348/348, up from 341). |
| Home-substrate-only Harbor + MinIO install path in `Prodbox.CLI.Rke2.ensureClusterPlatformRuntime`. The AWS-substrate platform install (`Prodbox.Lib.AwsSubstratePlatform.ensureAwsSubstratePlatformRuntime`) stopped at AWS LB Controller + Envoy Gateway + cert-manager + ACME — chart pods on EKS could not resolve the doctrine-mandated `127.0.0.1:30080/prodbox/...` refs because (a) no Harbor was installed on EKS and (b) no node-level containerd registry-mirror config existed. Additionally `ensureHarborRegistryRuntime` baked in a host-Docker `docker login` step that has no analog on EKS (operator host has no network path into the EKS-side Harbor NodePort). | Sprint `7.5.c.iii` closure on May 19, 2026 | `Prodbox.CLI.Rke2.ensureHarborRegistryRuntime` now takes a `Substrate` parameter and delegates the docker-login + bootstrap-project-creation tail to the new `ensureHarborProjectsForSubstrate` helper. On `SubstrateHomeLocal` the existing `ensureHarborDockerLogin` + `createHarborProjects` sequence runs unchanged; on `SubstrateAws` the docker-login step is skipped (the in-cluster image-mirror Job from Sprint 7.5.c.iv replaces the host-Docker push path) and bootstrap-project creation runs via the Harbor REST API (routed through the EKS API server's `kubectl port-forward` subprocess that `ensureHarborProject` already uses). `ensureMinioRuntime`, `ensureHarborRegistryStorageBackend`, `ensureHarborRegistryRuntime`, and `MinioImageSource` are now exposed from `Prodbox.CLI.Rke2`'s module export list so `Prodbox.Lib.AwsSubstratePlatform` can call them directly. The orchestrator `ensureAwsSubstratePlatformRuntime` is extended with four new steps after the existing ACME step: `applyEksContainerdMirrorDaemonSet` (applies the Sprint 7.5.c.ii DaemonSet via `kubectl apply -f` of a `v1/List` wrapper), `ensureMinioRuntime SubstrateAws MinioBootstrapPublic`, `ensureHarborRegistryStorageBackend`, `ensureHarborRegistryRuntime SubstrateAws`. The pure helper `awsSubstratePlatformRuntimeStepDescriptions :: [String]` is exported alongside the orchestrator so unit tests verify the eight-step ordering, the mirror-before-Harbor invariant, and the MinIO-before-Harbor-storage-backend invariant. Three new unit tests in `test/unit/Main.hs::"Sprint 7.5.c.iii AWS-substrate platform orchestration"` cover the ordering contract. Validated with `prodbox check-code` (exit 0), `prodbox lint docs` (exit 0), `prodbox docs check` (exit 0), and `prodbox test unit` (341/341, up from 338). |
| Absence of an EKS-side route from `127.0.0.1:30080/prodbox/...` chart-image refs to the in-cluster Harbor NodePort. The home substrate routes these via RKE2's `/etc/rancher/rke2/registries.yaml` mechanism written by `Prodbox.CLI.Rke2.ensureRke2RegistriesConfig`; EKS exposes no equivalent registries-yaml hook. Without a node-level containerd registry-mirror drop-in, EKS chart pods using the doctrine-mandated `127.0.0.1:30080/prodbox/...` refs fail with `ImagePullBackOff`. | Sprint `7.5.c.ii` closure on May 19, 2026 | New `src/Prodbox/Lib/EksContainerdMirror.hs` exposes `ContainerdMirrorConfig`, `defaultProdboxMirrorConfig` (matching the home substrate's `127.0.0.1:30080` + `prodbox/` rewrite contract), `eksContainerdMirrorBootstrapScript`, and `eksContainerdMirrorDaemonSetManifest`. The renderer emits an `apps/v1` DaemonSet in `kube-system` with `hostNetwork=true`, `hostPID=true`, a privileged init container, and a `hostPath` mount of `/etc` so the bootstrap script can read/write the host's containerd config. The init container script: (1) enables `config_path = "/etc/containerd/certs.d"` under `[plugins."io.containerd.grpc.v1.cri".registry]` in `/etc/containerd/config.toml` when missing — Amazon Linux 2023 EKS AMIs from late 2024 onward already have it; (2) writes a `hosts.toml` drop-in declaring `capabilities = ["pull", "resolve"]` plus `skip_verify = true` for the in-cluster Harbor NodePort; (3) restarts containerd via `nsenter --target 1 ... systemctl restart containerd` **only when** the on-disk content actually changed (`RESTART_NEEDED` flag), so subsequent DaemonSet rollouts are idempotent. The long-running pause container keeps the pod alive across containerd restarts. Eight new unit tests in `test/unit/Main.hs::"Sprint 7.5.c.ii EKS containerd registry-mirror DaemonSet"` cover structural assertions on the manifest + bootstrap script. Effectful wiring into `ensureAwsSubstratePlatformRuntime` lands as part of Sprint `7.5.c.iii` alongside the Harbor NodePort install. Validated with `prodbox check-code` (exit 0), `prodbox lint docs` (exit 0), `prodbox docs check` (exit 0), and `prodbox test unit` (338/338, up from 330). |
| Substrate-unaware MinIO chart values in `Prodbox.CLI.Rke2.ensureMinioRuntime` — always emitted `persistence.existingClaim=minio` (hostPath-bound PVC) + `persistence.size=200Gi`, which only works on the home substrate where `ensureRetainedLocalStorage` pre-creates the matching hostPath-backed PVC. AWS-substrate EKS pods have no hostPath story; nodes are ephemeral and `gp2`-backed dynamic EBS provisioning is required. | Sprint `7.5.c.i` closure on May 19, 2026 | Refactored `ensureMinioRuntime` to take a `Substrate` parameter and route through a pure `renderMinioChartArgs :: Substrate -> MinioImageSource -> [String]` helper. The substrate-agnostic core (`mode=standalone`, `replicas=1`, image refs, service type, resource requests) is shared; only the persistence block diverges. `SubstrateHomeLocal` keeps the existing `persistence.existingClaim=minio` + `persistence.size=200Gi` shape exactly. `SubstrateAws` emits `persistence.storageClass=gp2` + `persistence.size=20Gi` with no `existingClaim` so the chart dynamically provisions EBS against EKS's default storage class (smaller 20 GiB size keeps test-substrate EBS cost predictable). Both home-substrate call sites in `ensureNativeInstallation` pass `SubstrateHomeLocal`. `MinioImageSource` derives `Eq`/`Show` and is exported so unit fixtures can build comparison tables. Four new unit tests in `test/unit/Main.hs::"Sprint 7.5.c.i substrate-aware MinIO chart values"` cover home/AWS × bootstrap/steady-state image-source variants. Validated with `prodbox check-code` (exit 0), `prodbox lint docs` (exit 0), `prodbox docs check` (exit 0), and `prodbox test unit` (330/330, up from 326). |
| Four AWS-resource orphan footguns in the `prodbox aws teardown` / `prodbox test all` postflight contract (Scenario A — `prodbox pulumi eks-resources` → `prodbox aws teardown` → operational IAM user deleted while EKS still up; Scenario B — `prodbox test all` Ctrl-C mid-suite leaving per-run Pulumi stacks alive while `aws.*` cleared; Scenario C — Phase-2 test failure short-circuiting the postflight before `pulumi eks-destroy` / `pulumi test-destroy` ran; Scenario D — cross-substrate SES stack stranded after `prodbox aws teardown` cleared operational creds) | Sprint `7.6` closure on May 19, 2026 | New `src/Prodbox/Aws.hs::checkPulumiResidueBeforeTeardown` queries all four `<stack>HasLiveResources` predicates exposed by `src/Prodbox/Infra/Aws{EksTest,EksSubzone,Test,Ses}Stack.hs` and returns the offending stacks paired with their canonical destroy command; `applyAwsTeardown` returns `Left (renderPulumiResidueRefusal …)` on non-empty residue and `Right …` after a successful teardown. The new `--allow-pulumi-residue` flag (parsed via `AwsTeardownFlags` in `src/Prodbox/CLI/Command.hs` + `src/Prodbox/CLI/Spec.hs::awsTeardownFlagsParser`) bypasses the check as an operator-acknowledged escape hatch. `src/Prodbox/TestRunner.hs::runWithAwsHarnessCleanup` wraps the suite body with `Control.Exception.try` so synchronous suite failures **and** async exceptions (Ctrl-C / SIGTERM) both flow through `awsPostflightDestroyActions` (`pulumi aws-subzone-destroy --yes` → `eks-destroy --yes` → `test-destroy --yes`, idempotent on empty stacks) before `runManagedAwsHarnessTeardown` clears operational `aws.*`. The `aws-ses` stack is **explicitly excluded** from auto-destroy per the long-lived cross-substrate shared-infrastructure class in `DEVELOPMENT_PLAN/substrates.md` § Resource Lifecycle Classes. Six new unit tests in `test/unit/Main.hs::"Sprint 7.6 AWS harness orphan-safety"` cover the regression matrix (A / B / C / D / all-four-present). Validated with `prodbox check-code` (exit 0), `prodbox lint docs` (exit 0), `prodbox docs check` (exit 0), and `prodbox test unit` (326/326, up from 320). |
| Sprint `8.1` SES Pulumi program initially relied on the `aws:region` provider config in string interpolations (`inbound-smtp.${aws:region}.amazonaws.com`, `email-smtp.${aws:region}.amazonaws.com`) — Pulumi YAML does not expand provider-config keys in those positions, so the stack failed at preview with `resource, variable, or config value "aws:region" not found`. Sprint `7.5.c` substrate-aware kubectl/helm auth fix; Sprint `7.5.c` AWS-LB-Controller `extractRegionFromArn` empty-segment + helm `--set-string` fix; Sprint `7.5.c` AWS-substrate ACME manifest `v1/List` wrap | Sprint `7.5.c` code fixes on May 18, 2026 | Three real bugs surfaced by the live Bucket B operator workflow. (1) `Prodbox.CLI.Charts.withSubstrateEnvironment` + `Prodbox.TestValidation.withSubstrateKubeconfigEnv` now bracket-set `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY` / `AWS_DEFAULT_REGION` / `AWS_REGION` (+ optional `AWS_SESSION_TOKEN`) from `settings.aws.*` so EKS's `aws eks get-token` kubeconfig exec provider authenticates. (2) `Prodbox.Lib.AwsSubstratePlatform.extractRegionFromArn` now preserves empty ARN segments and the caller passes the configured `aws.region` as the default; helm string args are now `--set-string` so the chart's string-typed `region` field stops being parsed as `int64`. (3) `Prodbox.Lib.AwsSubstratePlatform.ensureAwsSubstrateAcmeRuntime` wraps the `[Value]` manifest list as a `v1/List` before `kubectl apply -f`. Verified live: AWS LB Controller + Envoy Gateway + cert-manager + ACME ClusterIssuer install cleanly on EKS after the fixes. |
| Sprint `8.1` Pulumi program missing the `_amazonses` TXT verification record + DKIM `_domainkey` CNAME records in the parent Route 53 zone, leaving the SES sending identity stuck in `Pending` forever | Sprint `8.1` live-validation closure on May 18, 2026 | `pulumi/aws-ses/Main.yaml` extended with one `aws:route53:Record` for the verification TXT and three for the DKIM CNAMEs; a new `awsRegion` config input (now synced from `aws_admin_for_test_simulation.region` by `Prodbox.Infra.AwsSesStack.syncAwsSesStackConfig`) replaces the `${aws:region}` interpolation in the MX target + SMTP endpoint hostnames. Sending identity `VerificationStatus` flipped to `Success` immediately after the new records propagated; `aws ses send-email` proof landed an inbound capture in S3 within seconds. |
| `inviteUser` / `listUsers` / `revokeUser` remedy-hint stubs in `src/Prodbox/UsersAdmin.hs`; `runKeycloakInviteValidation` remedy-hint stub in `src/Prodbox/TestValidation.hs` | Sprint `8.3` / Sprint `8.5` live HTTP integration on May 18, 2026 | Replaced with the live Keycloak admin API HTTP client in new module `src/Prodbox/Keycloak/Admin.hs` (token acquisition, user CRUD, `execute-actions-email`), the SES capture-bucket polling helper in new module `src/Prodbox/Ses/Capture.hs`, and the RFC-822 invite-link parser in new module `src/Prodbox/Keycloak/Email.hs`. `prodbox.cabal` library `build-depends` gained `http-client`, `http-client-tls`, `http-types`, and `temporary`. `runKeycloakInviteValidation` now drives invite → S3 capture → link follow → cleanup; credential-setup form POST + fresh OIDC login + claim assertions remain as a future Sprint 8.5 follow-up. Validated with `prodbox check-code` (exit 0), `prodbox lint docs` (exit 0), `prodbox docs check` (exit 0), and `prodbox test unit` (315/315). |
| Static `change-me-ses-smtp-user` / `change-me-ses-smtp-password` placeholders in `charts/keycloak/values.yaml::smtp` consumed by the chart at deploy time regardless of SES Pulumi reconcile state; absence of any code path deriving the SES SMTP password from the IAM secret access key | Sprint `8.5` SES SMTP password derivation closure on May 18, 2026 | New `src/Prodbox/Ses/SmtpPassword.hs` exposes `derivedSesSmtpPassword :: Text -> Text -> Text` implementing the AWS-published IAM-to-SMTP credentials algorithm (fixed date `11111111`, HMAC-SHA256 signing chain through `region` / `ses` / `aws4_request`, then HMAC-SHA256 signs `SendRawEmail`, version byte `0x04` prefixed, base64-encoded). After `pulumi up` succeeds for the SES stack, `Prodbox.Infra.AwsSesStack::persistKeycloakSmtpChartSecrets` fetches `smtp_iam_secret_access_key` via `pulumi stack output --show-secrets`, derives the password against the SES stack region sourced from `aws_admin_for_test_simulation.region`, and merges `ses_smtp_endpoint` / `ses_smtp_user` / `ses_smtp_password` / `ses_smtp_from` into `.prodbox-state/charts/keycloak/.secrets.json`. The IAM secret access key is never written to disk. `Prodbox.Lib.ChartPlatform::keycloakSmtpValues` renders the chart's `smtp` block from those four keys when present; absent them it falls through to a disabled-SMTP placeholder block so home-substrate deploy still functions before SES is provisioned. Five new unit tests cross-check the derivation against the AWS-published algorithm (us-west-2 / us-east-1 / eu-west-1 vectors plus region-sensitivity and determinism invariants). Validated with `prodbox check-code` (exit 0), `prodbox lint docs` (exit 0), `prodbox docs check` (exit 0), and `prodbox test unit` (320/320, up from 315). |
| `substratePublicFqdn` and `substrateHostedZoneId` home-substrate fallback branches in `src/Prodbox/PublicEdge.hs`; conflated `hosted_zone_id`+`subzone_name` pre-provision gate in `src/Prodbox/Infra/AwsEksSubzoneStack.hs::resolveAwsEksSubzoneStackConfig`; now-unused `isAwsSubstrateConfigured` helper in `src/Prodbox/Settings.hs` | Sprint `7.5.c` code follow-up on May 18, 2026 | Sprint `7.5.b.iii` recorded the substrate-independence doctrine and reclassified the shipped helper fallback as doctrine-violating residue. Sprint `7.5.c`'s code follow-up replaced `substratePublicFqdn` and `substrateHostedZoneId` AWS-substrate fallback branches with fail-fast `error` calls naming `development_plan_standards.md` § M (no fallback), loosened `resolveAwsEksSubzoneStackConfig` so it requires only `subzone_name` at pre-provision time, and removed the now-unused `isAwsSubstrateConfigured` helper that conflated the two fields. Validated with `prodbox check-code` (exit 0) and `prodbox test unit` (300/300). |
| Plan-wide framing that split validation surfaces into "home-cluster validation" and "AWS validation" as parallel categories | Sprint `0.6` closure on 2026-05-16 | Replaced by the substrate doctrine introduced in [development_plan_standards.md → M. Test Suite Substrates](development_plan_standards.md#m-test-suite-substrates) and the per-substrate parity tracker in [substrates.md](substrates.md). The plan now describes one canonical test suite that runs against substrates, with substrate provision/teardown as a separate concern from suite content. Phase docs phase-5 (renamed to `phase-5-canonical-test-suite.md`) and phase-7 (renamed to `phase-7-aws-substrate-foundations.md`) reflect this. The historical Sprints `5.1`–`5.5` and `7.1`–`7.4` sprint blocks remain intact under the renamed files as accurate records of when each surface was implemented. The `phase-5-public-host-validation.md` and `phase-7-aws-iam-quota-automation.md` paths no longer exist. |
| Benign upstream uninstall-script chatter on successful `prodbox rke2 delete --yes` runs | Sprint `4.8` closure on 2026-05-16 | Closed by extending `isIgnorableRke2DeleteNoiseLine` in `src/Prodbox/CLI/Rke2.hs` to classify `Failed to allocate directory watch` and `Too many open files` as benign noise alongside the existing inotify- and uninstall-script residue, and by adding the hermetic-success and summarized-failure integration cases in `test/integration/CliSuite.hs`. The governed docs in `documents/engineering/cli_command_surface.md`, `documents/engineering/streaming_doctrine.md`, and `documents/engineering/storage_lifecycle_doctrine.md` now name the contract. |
| Remaining doctrine-adoption cleanup rows from the Phase `1`–`4` reopen | Sprints `1.12`–`1.14`, `1.16`–`1.18`, `1.22`, `2.9`, `2.11`, `2.13`, `2.14`, and `4.7` closure on May 14, 2026 | Closed the remaining service-boundary, retry-policy, error-kind, state-machine, output-boundary, one-shot `App`, dependency-audit, daemon-lifecycle, live-config, test-hook, lifecycle-stanza, and Pulumi-harness rows. The pending-removal ledger is now empty. |
| `prodbox rke2 install` command alias | Sprint `4.5` cleanup on May 14, 2026 | Removed the `Rke2Install` command constructor, parser branch, command-registry leaf, runner delegation, generated CLI docs, shell completions, manpage entry, root command map, and governed CLI surface reference. `prodbox rke2 install` now fails at parse time as a forbidden lifecycle sister command. |
| Pre-doctrine subprocess compatibility names | Sprint `1.8` closure on May 14, 2026 | Removed the old subprocess `CommandSpec` alias and the `runStreamingCommand` / `captureCommand` compatibility exports, migrated call sites to the doctrine-shaped `Subprocess` record with `subprocessPath` / `subprocessArguments` fields, and kept validation-only `Result` consumers behind explicitly named `captureSubprocessResult` / `runSubprocessStreaming` adapters. |
| Direct Redis / Postgres call sites outside the capability classes | Sprint `3.9` closure on May 14, 2026 | Closed the chart-platform Redis/Postgres service boundary by adding IO-backed `HasRedis` / `HasPg` instances, routing Patroni discovery, readiness, restore, retained-claim, and cleanup paths through `runPg` plus `retryServiceAction`, and covering the absence of direct `redis-cli` / raw Postgres subprocesses outside the capability class. The broader MinIO service migration closed with Sprint `1.12`. |
| Direct `System.Process` / `typed-process` smart-constructor call sites outside the subprocess interpreter | Sprint `1.8` partial closure on May 14, 2026 | Closed by extending `src/Prodbox/CheckCode.hs` to reject `System.Process` / `System.Process.Typed` imports and the `callProcess`, `readCreateProcess`, `readCreateProcessWithExitCode`, `createProcess`, `proc`, and `shell` constructor symbols in `src/Prodbox/` outside `src/Prodbox/Subprocess.hs`, while stripping string literals before the token scan so Linux `/proc` paths do not false-positive. `.hlint.yaml` now carries the matching doctrine marker set. |
| Ad-hoc daemon `putStrLn` / direct stderr logging and non-`co-log` structured output | Sprint `2.12` closure on May 14, 2026 | Closed by routing gateway and workload daemon entrypoint logs through `src/Prodbox/Gateway/Logging.hs`, backed by `co-log` / `co-log-core`, and by extending `src/Prodbox/CheckCode.hs`, `.hlint.yaml`, and `test/haskell-style/Main.hs` to reject direct terminal writes and inline `Aeson.object` / `Aeson.fromList` log payloads in daemon paths. |
| Daemon log level not refreshed from `LiveConfig` on hot reload | Sprint `2.12` closure on May 14, 2026 | Closed by making gateway daemon log calls read `envLiveConfig` at emission time, adding threshold-aware logging helpers, and covering SIGHUP reload of `log_level` plus structured stderr JSON shape in `test/daemon-lifecycle/Main.hs`. |
| `print` / `exitFailure` / direct terminal formatting in non-boundary code | Sprint `1.26` closure on May 13, 2026 | Closed by routing supported-path terminal output through `src/Prodbox/CLI/Output.hs`, extending `src/Prodbox/CheckCode.hs` and `test/haskell-style/Main.hs` to reject `print`, `exitFailure`, `putStr`, `putStrLn`, and direct stderr writes outside the output layer and daemon structured-logging module, and keeping `renderError` / `writeError` as the CLI error-rendering boundary. |
| In-memory gateway peer-gossip commit log lacked explicit at-least-once doctrine correspondence | Sprint `2.16` partial closure on May 13, 2026 | Closed by introducing `src/Prodbox/Daemon/Events.hs` for durable at-least-once consumers, unit-covering its record / mark / fetch / process behavior, and documenting the gateway's intentional in-memory anti-entropy variant in `documents/engineering/distributed_gateway_architecture.md` and `documents/engineering/pure_fp_standards.md`. |
| Remaining daemon-lifecycle stanza process-driver and endpoint-golden gaps after process-drain coverage | Sprint `2.14` / Sprint `2.16` partial closure on May 13, 2026 | Closed by routing `test/daemon-lifecycle/Main.hs` through `src/Prodbox/Subprocess.hs` background-process helpers, removing the direct test-stanza `process` dependency, adding `/healthz`, ready/draining `/readyz`, and normalized `/metrics` goldens under `test/golden/daemon-health/`, and extending the Haskell style guard with readiness-marker and reload-trigger checks. |
| Hardcoded or absent default for the daemon graceful-drain deadline | Sprint `2.9` partial closure on May 13, 2026 | Closed by defaulting the gateway daemon drain deadline to 30 seconds, sourcing the active value from `envLiveConfig`, and exercising first- and second-SIGTERM behavior in the daemon lifecycle stanza. The later Sprint `2.9` closure added prerequisite-registry acquire gating and worker-failure classification. |
| Module-local mutable counter state behind `/metrics` instead of `envMetrics :: MetricsRegistry` | Sprint `2.10` partial closure on May 13, 2026 | Closed by adding `envMetrics :: MetricsRegistry`, rendering `/metrics` from that field plus daemon state, golden-testing the endpoint shape, and extending the Haskell style guard to reject `newIORef` / `newMVar` daemon counter state. |
| Absent STM broadcast channel for `LiveConfig` subscribers on hot reload | Sprint `2.11` partial closure on May 13, 2026 | Closed by exposing `envLiveConfigReloads :: TChan LiveConfig` on the daemon `Env` and publishing the refreshed `LiveConfig` on successful reload. Subscriber-specific coverage and the prescribed Dhall reload shape closed with Sprint `2.11`. |
| Hand-constructed PV / PVC pairs and resource names outside the smart constructor and naming helpers | Sprint `1.15` / Sprint `3.8` closure on May 12, 2026 | Closed by routing deterministic storage pairing through `storageBinding`, moving Patroni service-name, PVC-name, and storage-spec inventory into `src/Prodbox/PostgresPlatform.hs`, and updating `src/Prodbox/Lib/ChartPlatform.hs` to consume those shared helpers instead of open-coded related-name construction. |
| Monolithic pre-`CommandSpec` CLI parser shape | Sprint `1.6` closure on May 12, 2026 | Closed by routing the public optparse parser through `src/Prodbox/CLI/Spec.hs` via `commandRequestParser`, while the same `CommandSpec` registry now drives `prodbox commands`, `prodbox help <path>`, the JSON and tree introspection surfaces, and the durable generated CLI artifacts. |
| Inline prerequisite checks outside the authoritative registry on supported prerequisite-aware flows | Sprint `1.9` closure on May 12, 2026 | Closed by consolidating the supported prerequisite DAG under `src/Prodbox/Prerequisite.hs`, rejecting missing node IDs during `fromRootIds` expansion in `src/Prodbox/EffectDAG.hs`, and routing supported prerequisite-aware flows through the node-id / description / remedy-hint contract owned by `src/Prodbox/EffectInterpreter.hs`, `src/Prodbox/TestRunner.hs`, `src/Prodbox/Host.hs`, and `src/Prodbox/K8s.hs`. |
| Partial `hspec`-era test-stanza migration and split built-frontend integration suites | Sprint `1.11` closure on May 12, 2026 | Closed by consolidating the built-frontend Haskell proof into the doctrinal single `prodbox-integration` stanza (`test/integration/Main.hs`, `CliSuite.hs`, `EnvSuite.hs`) and by covering the Sprint `1.11` property-test inventory across the repo-root Dhall config, persisted gateway JSON shapes, saved AWS snapshot state, deterministic renderers, and parser roundtrips. The deeper daemon-lifecycle and ephemeral-stack behaviors later closed under Sprints `2.14` and `4.7`. |
| Absent durable CLI documentation artifacts beyond the generated Markdown command reference | Sprint `1.24` closure on May 12, 2026 | Closed by generating the doctrine-owned manpages under `share/man/man1/`, shell completion scripts under `share/completion/`, and tracked-generated-path enforcement through `src/Prodbox/CheckCode.hs`, with golden coverage in the haskell-style suite. |
| Renderer determinism violations and absent `trackingGeneratedPaths` registry | Sprint `1.21` closure on May 12, 2026 | Closed by renaming the fully generated-file registry in `src/Prodbox/CheckCode.hs` to the doctrine-owned `trackingGeneratedPaths`, storing pure render functions plus source-module ownership, and extending `test/haskell-style/Main.hs` with repeated-render stability checks and synthetic forbidden-input regression cases for timestamps, random IDs, locale-dependent ordering, terminal-width state, and environment-derived paths. |
| Missing `prodbox lint chart` Helm-chart structural-invariants linter | Sprint `3.12` closure on May 12, 2026 | Closed by extending `src/Prodbox/CheckCode.hs` so `prodbox lint chart` validates `Chart.yaml` metadata, required chart-label helpers, and chart-side generated-section drift. |
| Route catalog rendered only as a Haskell value, not as marker-delimited content inside chart manifests | Sprint `3.12` closure on May 12, 2026 | Closed by generating the canonical `route-registry` sections from `src/Prodbox/PublicEdge.hs` into the consuming `keycloak`, `vscode`, `api`, and `websocket` chart templates via `prodbox docs generate`. |
| Missing repeated-deploy idempotence proof on the public chart reconciler surface | Sprint `3.10` closure on May 12, 2026 | Closed by extending `test/integration/CliSuite.hs` so the fake-Helm integration harness runs `prodbox charts deploy vscode` twice in succession, injects the already-deployed Helm snapshot on the second pass, and proves that no additional Helm upgrade or kubectl apply mutations occur. |
| Host-installed `fourmolu` / `hlint` use and missing doctrine-specific nesting or daemon negative-space rules | Sprint `1.19` closure on May 12, 2026 | Closed by adding `src/Prodbox/Lint.hs` with pinned formatter GHC `9.12.4`, Cabal `3.16.1.0`, Fourmolu `0.19.0.1`, and HLint `3.10`, bootstrapping the tools through `ghcup run --install`, invoking the sandboxed binaries by absolute path, and adding governed scans for `Avoid case inside lambda body`, `Refactor nested case`, daemon-path `forkIO`, `unsafePerformIO`, and module-level `IORef`. |
| Pre-doctrine ad-hoc `fourmolu` invocations outside the doctrine lint surface | Sprint `1.10` / Sprint `1.11` / Sprint `1.19` closure on May 12, 2026 | Replaced by the canonical `prodbox lint haskell` surface, the `prodbox-haskell-style` Cabal stanza, the pinned repo-root `fourmolu.yaml`, the shared byte-equality `cabal format` check path, and the sandboxed `src/Prodbox/Lint.hs` style-tool bootstrap. |
| Partial daemon CLI plumbing after the `--config` rename | Sprint `2.15` closure on May 11, 2026 | Closed by wiring `--config`, `--log-level`, `--port`, and `--foreground` through the gateway and workload command paths and by implementing the `PRODBOX_CONFIG_PATH`, `PRODBOX_LOG_LEVEL`, and `PRODBOX_PORT` precedence helpers exercised by the daemon-lifecycle stanza. |
| Missing two-SIGTERM lifecycle assertion in the daemon stanza | Sprint `2.14` closure on May 14, 2026 | Closed by extending `test/daemon-lifecycle/Main.hs` to spawn `prodbox gateway start`, poll `/readyz`, send a first SIGTERM to observe `503 draining`, assert exit `0` after the configured drain deadline, exercise the second-SIGTERM branch in a distinct test, and keep the lifecycle-stanza doctrine gaps closed in the completed ledger. |
| Partial parser-test category despite inline `execParserPure` coverage | Sprint `1.25` closure on May 11, 2026 | Closed by splitting the parser tests into `test/unit/Parser.hs`, covering leaf happy and unhappy paths from the `CommandSpec` registry, and adding the `typed-process` regression guard to the haskell-style surface. |
| `cabal format` invoked without temp-file round-trip / byte-equality check during the check pass | Sprint `1.10` closure on May 11, 2026 | Closed by the doctrine-owned `prodbox lint haskell` path in `src/Prodbox/CheckCode.hs`, which formats through a temp file, compares bytes during check mode, and reserves in-place rewrite for `--write`. |
| Legacy `timedatectl` field fallback in `src/Prodbox/Host.hs` (`parseTimedatectlNtpDisposition` accepted `NTP synchronized: yes/no`) | Sprint `2.8` closure on May 6, 2026 | Removed so the supported host baseline closes only on Ubuntu 24.04's `System clock synchronized: yes/no` field, with unit coverage now making the legacy field explicitly unsupported on the supported path. |
| Stale `Referenced by` header backlinks to the deleted `METALLB_ENVOY_KEYCLOAK_REDIS_WEBSOCKETS.md` planning doc in `documents/engineering/envoy_gateway_edge_doctrine.md`, `documents/engineering/unit_testing_policy.md`, and `documents/engineering/helm_chart_platform_doctrine.md` | Phase `6` doc-harmony follow-up closure on May 4, 2026 | Removed after the `METALLB_ENVOY_KEYCLOAK_REDIS_WEBSOCKETS.md` planning doc was deleted in favor of the canonical architecture in `DEVELOPMENT_PLAN/` and the governed engineering docs themselves. The three governed-doc headers no longer reference the missing target. |
| Unused supported-runtime cleanup residue in `src/Prodbox/SupportedRuntime.hs` (`SupportedRuntimeContext`, `removePublicHostHostsOverride`, `removeFqdnFromHostsText`, and `removeDeletePendingAwsResources`) | Sprint `6.3` closure on May 4, 2026 | Deleted from the library surface, Cabal module manifest, and unit suite after the aggregate rerun, IAM harness, direct-Dhall config contract, and public-edge restore paths were confirmed to live elsewhere on the supported command surface. |
| `example.com` placeholder public domains in `prodbox-config-types.dhall`, `src/Prodbox/Settings.hs`, `src/Prodbox/Aws.hs`, `prodbox-config.dhall`, and related test fixtures | Sprint `1.5` / Sprint `7.4` closure on May 2, 2026 | Removed in favor of the one supported public hostname `test.resolvefintech.com` across schema defaults, onboarding, validation, fixtures, and the operator-authored repo-root config. |
| Dedicated public-FQDN config fields such as `domain.keycloak_fqdn`, `domain.vscode_fqdn`, `domain.api_fqdn`, and `domain.websocket_fqdn` plus matching validator assumptions | Sprint `1.5` / Sprint `7.4` closure on May 2, 2026 | Replaced by one canonical config field, `domain.demo_fqdn`, with supported public destinations derived from shared-host path prefixes. |
| Explicit per-FQDN Route 53, `dns_write_gate`, bootstrap-DNS, and public-certificate assumptions across DNS inspection, lifecycle, and proof surfaces | Sprint `2.3` / Sprint `4.4` / Sprint `5.3` closure on May 2, 2026 | Replaced by one Route 53 record, one listener certificate, one `dns_write_gate` public hostname, and shared-host proof on `test.resolvefintech.com`. |
| Dedicated identity, browser, API, and WebSocket public-host routing doctrine across charts, validations, and host diagnostics | Sprint `3.5` / Sprint `3.6` / Sprint `3.7` / Sprint `5.3` / Sprint `5.4` closure on May 2, 2026 | Replaced by shared-host path routing on `/auth`, `/vscode`, `/api`, `/ws`, `/harbor`, and `/minio`, with Keycloak-backed Envoy auth and RBAC for the supported application and admin surfaces. |
| Cross-arch `docker buildx --platform linux/amd64,linux/arm64 --push` custom-image publication and mixed-arch container assumptions in lifecycle code, tests, and doctrine docs | Sprint `4.1` closure on May 2, 2026 | Replaced by native-host-architecture Docker publication only: `amd64` hosts publish `amd64` images, `arm64` hosts publish `arm64` images, and no supported lifecycle path uses `docker buildx`, cross-arch emulation, or mixed-arch cluster closure. **Host-state residue swept 2026-06-20**: the `docker-container`-driver buildx builder `prodbox-multiarch-hostnet` (created May 2, 2026 by the pre-Sprint-4.1 path, persisted in `~/.docker/buildx/` and remained the selected builder, so plain `docker build` was still routed through buildx) was removed (`docker buildx use default` + `docker buildx rm prodbox-multiarch-hostnet`), and both `docker/*.Dockerfile` had their BuildKit-only features stripped (`# syntax=docker/dockerfile:1.7` frontend pin and `RUN --mount=type=cache` mounts) so the build runs on the daemon's default builder with basic `docker build`/`docker push` only. |
| Frontend Haskell-build container doctrine in `docker/prodbox.Dockerfile` and related tests/docs that mounted `haskell:9.6.7-slim` and created symlinked GHC tool shims | Sprint `1.1` implementation closure on April 26, 2026 | Replaced with a single-stage `ubuntu:24.04` frontend image that installs `ghcup` in-image, pins GHC `9.12.4`, preserves `/opt/build`, and removes the symlinked tool-shim path. |
| Gateway Haskell-build container doctrine in `docker/gateway.Dockerfile` and related tests/docs that mounted `haskell:9.6.7-slim` and created symlinked GHC tool shims | Sprint `2.1` implementation closure on April 26, 2026 | Replaced with a single-stage `ubuntu:24.04` gateway image that installs `ghcup` in-image, pins GHC `9.12.4`, preserves the official AWS CLI bundle keyed by the native Debian host architecture, and removes the symlinked tool-shim path. |
| The two separate custom images — `docker/gateway.Dockerfile` → `prodbox-gateway` (with a baked `tini -- prodbox gateway start` ENTRYPOINT) and `docker/prodbox.Dockerfile` → `prodbox-public-edge-workload` — plus the `ensureGatewayImages*` / `ensurePublicEdgeWorkloadImage*` build functions, the `resolveGatewayChartImageForSubstrate` / `resolvePublicEdgeWorkloadChartImageForSubstrate` resolvers, and the `harborGateway*` / `harborPublicEdgeWorkload*` repo constants | Sprint `1.45` closure on 2026-06-20 | Consolidated into ONE union runtime image `prodbox/prodbox-runtime` built from a single `docker/prodbox.Dockerfile` (the same `prodbox` binary; `tini` + AWS CLI; bare `ENTRYPOINT ["/usr/bin/tini","--","/usr/local/bin/prodbox"]`). `docker/gateway.Dockerfile` deleted; `ensureRuntimeImageForSubstrate` + `resolveRuntimeChartImageForSubstrate` + `harborRuntime{,Image}Repository` replace the pairs; each chart now selects its role via the pod `args:` (`gateway start` vs `workload start`). The gateway daemon's Vault identity (`prodbox-gateway-state` / `prodbox-gateway-daemon` / Vault policy `prodbox-gateway`) was intentionally NOT renamed — it is unrelated to the image. |
| Host `docker` CLI calls (Harbor `docker login` + mirror pulls/build/push) running with NO `DOCKER_CONFIG`, writing the Harbor `127.0.0.1:30080` credential into the operator's global `~/.docker/config.json` (the file holding their fixed-token Docker Hub login) | Sprint `1.46` closure on 2026-06-20 | Every prodbox `docker` subprocess now runs with a prodbox-local `DOCKER_CONFIG` (`<repoRoot>/.docker`, git-ignored, `Prodbox.DockerConfig` via `captureDockerToolOutput`/`dockerSubprocessFor`). Harbor login lands inside `~/prodbox`; the prodbox config is re-seeded read-only from `~/.docker` (keeping the Docker Hub `index.docker.io` auth, dropping the Harbor entry + `credsStore`) so public pulls keep the fixed-token login; prodbox never writes `~/.docker/config.json`. **Operator follow-up:** the stale `127.0.0.1:30080` entry already in `~/.docker/config.json` can be removed once via `docker logout 127.0.0.1:30080` (leaves `index.docker.io` untouched); prodbox will not touch the global file. |
| Cluster-wide Zalando `postgres-operator` Helm release, `ghcr.io/zalando/postgres-operator`, `ghcr.io/zalando/spilo-17`, and `postgresqls.acid.zalan.do` chart/runtime assumptions | Sprint `3.3` implementation closure on April 26, 2026 | Replaced by the Percona operator surface in `src/Prodbox/CLI/Rke2.hs`, `src/Prodbox/ContainerImage.hs`, `src/Prodbox/PostgresPlatform.hs`, `charts/keycloak-postgres/`, and related tests. |
| Lifecycle-managed Haskell-build custom-image publication in `src/Prodbox/CLI/Rke2.hs` and related tests that injected the named BuildKit `haskell-toolchain` context pinned to `haskell:9.6.7-slim` | Sprint `4.1` implementation closure on April 26, 2026 | Replaced by direct publication from the repo-owned Dockerfiles with no mounted Haskell toolchain context, the explicit `ghc-9.12.4` repo upgrade in place, and final host-native Docker build or push closure on May 2, 2026. |
| Local-cluster supported ownership in `pulumi/home/Main.yaml` and the public `prodbox pulumi up|destroy|preview|refresh|stack-init` surface | Sprint `4.2` closure on April 23, 2026 | Pulumi is no longer the supported public operator surface for local-cluster platform ownership. The retained public Pulumi command family is limited to the AWS validation stacks, while the local lifecycle and chart runtime own the supported cluster path. The residual root `Pulumi.yaml` and `Pulumi.home.yaml` files were deleted in the Phase `6` cleanup closure on April 23, 2026. |
| Shared Bitnami `postgresql-ha` / `postgresql-repmgr` plus `pgpool` application-database doctrine | Sprint `3.3` closure on April 23, 2026 | Replaced by namespace-local Patroni-based Helm-managed PostgreSQL HA with exactly three replicas, synchronous replication, retained credentials, and no embedded chart-local PostgreSQL subcharts. The residual Bitnami Docker build artifacts were deleted in the Phase `6` cleanup closure on April 23, 2026. |
| Broader-than-target direct-public bootstrap image set in the lifecycle/runtime/docs | Sprint `4.1` closure on April 23, 2026 | The bootstrap exception now covers Harbor and Harbor's storage backend only before later Helm deployments switch to Harbor-backed image refs. |
| Harbor-first bootstrap ordering in `src/Prodbox/CLI/Rke2.hs` that mirrored required public images before the backend was healthy and deployed MinIO from Harbor-backed refs | Sprint 4.1 implementation closure on April 21, 2026 | Replaced by public-registry MinIO bootstrap, post-bootstrap Harbor populate, and a Harbor-backed MinIO steady-state reconcile |
| Python-era clean-room backlog through April 15, 2026 | Pre-rewrite baseline | Closed before the Haskell rewrite reopened this ledger on April 16, 2026 |
| `src/prodbox/**/*.py` Python implementation modules and `src/prodbox/py.typed` package marker | Sprint 4.3 | Deleted after Haskell parity reached for all supported surfaces |
| `tests/**/*.py` Python unit and integration harnesses | Sprint 4.3 | Replaced by Haskell test suites under `test/` |
| `typings/` Python type-stub inventory | Sprint 4.3 | Removed with Python runtime and mypy |
| `pyproject.toml`, `poetry.toml`, `.python-version` | Sprint 4.3 | Root Python packaging and Poetry ownership removed |
| Python-specific quality-tool ownership (`check_code.py`, `lint/`, ruff, mypy) | Sprint 4.3 | Replaced by Haskell `CheckCode.hs` |
| Python Pulumi runtimes and stack programs (`Pulumi.yaml` Python runtime, `src/prodbox/infra/`, `pulumi/aws-eks/__main__.py`, `pulumi/aws-test/__main__.py`) | Sprint 4.2 | Replaced with YAML Pulumi definitions for the retained AWS IaC surfaces: `pulumi/aws-eks/Main.yaml` and `pulumi/aws-test/Main.yaml` |
| Python CLI entrypoint and command-group ownership (`src/prodbox/cli/main.py`, `src/prodbox/cli/*.py`) | Sprint 4.3 | Haskell frontend owns the full command surface |
| Python settings, ADT, DAG, interpreter, and subprocess ownership | Sprint 4.3 | Replaced by Haskell modules under `src/Prodbox/` |
| Python local lifecycle and Harbor/local-registry helpers | Sprint 4.1 | `src/Prodbox/CLI/Rke2.hs` owns all lifecycle paths |
| Python gateway runtime and container entrypoint (`gateway_daemon.py`, `gateway.py`, Python Dockerfile entrypoint) | Sprint 2.1 | Replaced by the Haskell gateway runtime in `src/Prodbox/Gateway.hs`, `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/Gateway/Types.hs`, and the canonical single-stage `docker/gateway.Dockerfile` build |
| Python DNS inspection command (`dns.py`) | Sprint 2.1 | `src/Prodbox/Dns.hs` owns the surface |
| Python TLA+ CLI wrapper and proof-check helper (`tla.py`, `tla_check.py`) | Sprint 2.2 | `src/Prodbox/Tla.hs` owns the surface |
| Python chart orchestration and retained-state helpers (`charts.py`, `chart_platform.py`, Python chart test suites) | Sprint 3.1 | `src/Prodbox/CLI/Charts.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, and `src/Prodbox/Lib/Storage.hs` own the runtime |
| Python public-edge diagnostic helpers (`host.py`, public-edge report logic in `dag_builders.py`) | Sprint 5.1 | `src/Prodbox/Host.hs` owns the surface |
| Python interactive onboarding helper (`config_cmd.py`) | Sprint 7.1 | `src/Prodbox/Aws.hs` owns `config setup` |
| Python standalone IAM and quota commands (`aws_cmd.py`) | Sprint 7.2 | `src/Prodbox/Aws.hs` owns `aws setup|teardown|check-quotas|request-quotas` |
| Python temporary-admin-credential IAM helper (`aws_admin.py`) | Sprint 7.3 | Haskell `aws_admin_for_test_simulation` harness owns real IAM lifecycle proof |
| Public `aws_admin.*` fallback in `src/Prodbox/Aws.hs` (`configuredAdminCredentials`, `resolveAdminCredentials`, `resolveAdminCredentialsWithRegionChoice`, and the non-interactive fallback guidance) | Sprint `7.1` / Sprint `7.2` | Removed so public `prodbox config setup` and public `prodbox aws ...` now prompt for one temporary admin credential set instead of reading stored `aws_admin_for_test_simulation.*`. |
| Non-test `aws_admin.*` recovery path in `src/Prodbox/SupportedRuntime.hs` | Sprint `7.3` | Removed so public onboarding and public `aws ...` paths no longer consume stored admin credentials. Long-lived stack / `prodbox nuke` admin acquisition unifies on the interactive `SecretRef.Prompt` (the harness simulates the prompt from the `test-config.dhall` `aws_admin_for_test_simulation.*` fixture); the former config-backed admin path is retired by Sprint `7.16`. |
| Ambiguous stored-admin-credential field name `aws_admin.*` in `prodbox-config.dhall`, the governed docs, and native IAM harness diagnostics | Phase `7` cleanup closure on April 27, 2026 | Renamed to `aws_admin_for_test_simulation.*` so the contract is explicit: it is a test-harness-only fixture that simulates the operator's interactive elevated-credential prompt for suite-driven destructive validation. Sprint `7.16` moves the field to the `test-config.dhall` `TestPlaintext` home (the field name is unchanged) and unifies all real admin acquisition on the interactive `SecretRef.Prompt`. |
| Python bridge modules (`Backend/Python.hs`, `PythonEnv.hs`) | Sprint 4.3 | `Main.hs` no longer imports `Backend.Python` |
| Residual Python-era command delegation and supported-runtime field naming (`DelegateToPython`, `supportedRuntimePythonPath`) | Sprint 6.2 audit closure on April 19, 2026 | The frontend now dispatches directly to native Haskell commands, and supported-runtime helpers no longer expose Python-named context fields |
| Phase-owned engineering docs presenting Python as supported architecture | Sprint 6.2 | Doctrine aligned with the Haskell-only architecture |
| Surviving Python-era architecture references in supported-path docs | Sprint 6.2 | Final cleanup sweep completed |
| `prodbox-config.json` compatibility path and public `prodbox config compile` surface | Sprint 1.2 runtime closure on April 18, 2026 | `src/Prodbox/Settings.hs`, `src/Prodbox/CLI/Parser.hs`, `src/Prodbox/Native.hs`, `src/Prodbox/Repo.hs`, `src/Prodbox/Aws.hs`, `test/unit/Main.hs`, `test/integration/CliSuite.hs`, and `test/integration/EnvSuite.hs` now close on direct `Dhall -> Haskell types`; no supported command materializes the JSON artifact |
| Stale mixed-baseline or removed-tooling wording in root guidance and governed docs such as `README.md`, `AGENTS.md`, `CLAUDE.md`, `documents/engineering/README.md`, `documents/engineering/cli_command_surface.md`, `documents/engineering/code_quality.md`, `documents/engineering/dependency_management.md`, and `documents/engineering/unit_testing_policy.md` | Sprint 1.2 doc-harmony closure on April 18, 2026 | Root guidance and the governed docs listed by Sprint `1.2` now describe the Haskell-only repository, direct-Dhall config contract, and native validation harness |
| Repository-root `Dockerfile` and root-level references that treated it as canonical | Sprint 1.1 closure on April 18, 2026 | The canonical frontend image now lives at `docker/prodbox.Dockerfile`, and supported references point to `docker/` only |
| Multi-stage frontend image build in the former root `Dockerfile` (`haskell:9.6.7 -> debian:bookworm-slim`) | Sprint 1.1 closure on April 18, 2026 | Replaced with a single-stage `ubuntu:24.04` frontend image while preserving `/opt/build` |
| Multi-stage gateway image build in `docker/gateway.Dockerfile` (`haskell:9.6.7 -> debian:bookworm-slim`) | Sprint 2.1 closure on April 18, 2026 | Replaced with a single-stage `ubuntu:24.04` gateway image |
| Supported doctrine that framed Harbor as a local mirror or allowed arbitrary non-Harbor workload pulls | Sprint 4.1 closure on April 18, 2026 | Supported charts and Pulumi home-stack workloads reference Harbor-backed images in steady state; the only supported public-image exception is the narrow Harbor/bootstrap path owned by Sprint `4.1` |
| Arch-implicit container population doctrine without explicit architecture-handling rules | Sprint `4.1` closure on April 18, 2026; final native-host-architecture closure on May 2, 2026 | The supported lifecycle now defines image architecture explicitly and closes on host-native Docker builds and Harbor publication only. |
| Traefik lifecycle ownership, `traefik-system` inventory, and Traefik mirror assumptions in lifecycle code, tests, plan control docs, and registry doctrine | Sprint `1.4` closure on April 29, 2026 | Replaced by Envoy Gateway lifecycle ownership, `envoy-gateway-system`, and the Harbor-backed Envoy Gateway control-plane plus Envoy data-plane image set. |
| Legacy Traefik uninstall shim in `src/Prodbox/CLI/Rke2.hs` (`removeLegacyTraefikIfPresent`, `deleteLegacyTraefikNamespace`) | Phase `4` cleanup closure on April 29, 2026 | Removed once the supported clean-room lifecycle no longer needed to migrate pre-Envoy clusters. |
| Legacy incompatible PostgreSQL operator uninstall shim in `src/Prodbox/CLI/Rke2.hs` (`removeLegacyPostgresOperatorIfPresent`, `removeLegacyPostgresOperator`, `deleteLegacyOperatorNamespace`) | Phase `4` cleanup closure on April 29, 2026 | Removed once the supported clean-room lifecycle no longer needed to migrate pre-Percona clusters. |
| Legacy Pulumi AWS provider-config cleanup in `src/Prodbox/Infra/AwsTestStack.hs` and `src/Prodbox/Infra/AwsEksTestStack.hs` (`clearLegacyAwsProviderConfig`) | Phase `4` cleanup closure on April 29, 2026 | Removed once the supported retained AWS-validation stacks no longer needed migration from the previous provider-key layout. |
| `Ingress`-based public-edge classification in host diagnostics, chart templates, native validation flows, and doctrine docs | Sprint `5.2` closure on April 29, 2026 | Replaced by Gateway API, Envoy Gateway `SecurityPolicy`, certificate, and explicit Route 53 readiness classification. |
| App-local `vscode-nginx` browser-auth proxy surfaces, its Harbor image, and `keycloak_nginx_client_secret` | Sprint `3.4` closure on April 29, 2026 | Removed from charts, lifecycle image publication, retained chart-secret state, and public-edge doctrine after Envoy Gateway `SecurityPolicy` took over the browser-auth path. |
| Shared-host `domain.vscode_fqdn` plus `/auth` public-host doctrine | Sprint `1.4` / Sprint `3.4` closure on April 29, 2026; final one-host closure on May 2, 2026 | The intermediate shared-host browser path was superseded by the final single-host doctrine on `test.resolvefintech.com` with all supported application and admin surfaces routed by explicit path prefixes. |
| `vscode-nginx` delivery gap for Harbor-only browser-auth image publication | Sprint `3.4` closure on April 29, 2026 | Closed by removing `docker/nginx-oidc.Dockerfile` and the remaining nginx-based browser-auth path from the supported stack. |

## Related Documents

- [phase-4-lifecycle-canonical-paths.md](phase-4-lifecycle-canonical-paths.md)
- [phase-6-clean-room-handoff.md](phase-6-clean-room-handoff.md)
- [phase-7-aws-substrate-foundations.md](phase-7-aws-substrate-foundations.md)
- [phase-8-email-invite-auth.md](phase-8-email-invite-auth.md)
- [substrates.md](substrates.md)
