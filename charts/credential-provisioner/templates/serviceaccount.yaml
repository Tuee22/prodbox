{{- if .Values.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "credential-provisioner.serviceAccount" . }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "credential-provisioner.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
