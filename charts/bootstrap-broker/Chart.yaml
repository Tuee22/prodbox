apiVersion: v2
name: bootstrap-broker
version: 0.1.0
appVersion: "1.0.0"
type: application
description: >-
  Physically separate pre-Vault Bootstrap Broker workload. It runs the
  `bootstrap-broker` runtime role with its own ServiceAccount / bootstrap-only
  Vault Kubernetes-auth role and failure domain, distinct from the Gateway
  Runtime. Rendered from the compiled `Prodbox.Bootstrap.Broker.ChartStatics`
  source of truth (Sprint 3.26).
