# System Components

**Status**: Authoritative source
**Supersedes**: N/A
**Generated sections**: none

> **Purpose**: Document the authoritative target component inventory for infrastructure, runtime
> control surfaces, validation surfaces, and state or authority boundaries in the Haskell rewrite.

The inventory documents the authoritative Haskell-only target end state. The supported public edge
closes on one public hostname, while bootstrap, lifecycle authority, target-secret delivery, and
gateway mesh are separate failure domains. Status and blockers live only in
[README.md → Resume Here](README.md#resume-here); deployment qualification follows
[Standard P](development_plan_standards.md#p-deployment-qualification-and-counterexample-closure).
[legacy-tracking-for-deletion.md](legacy-tracking-for-deletion.md)
preserves cleanup history separately from this canonical inventory.

**Inventory interpretation:** the rows below describe the canonical target, evidence-backed current
source correspondence, and (where useful) historical sprint provenance. Under
[Standard J](development_plan_standards.md#j-documentation-harmony), those facts must agree with the
phase files, but they do not originate or carry live sprint state. Blockers, remaining work,
qualification, and removal ownership live only in the
[Development Plan status](README.md#resume-here) and deletion ledger.

The retained local RKE2 deployment is the mandatory control plane. AWS is an optional target
substrate reached only through authenticated Lifecycle Authority submissions and exact
worker/adapter interpreters; no component row creates a host-direct AWS mutation fallback.

## Haskell-Only Architecture

| Surface | Owner | Paths |
|---------|-------|-------|
| One-shot secret-worker capacity | One compiled Guaranteed-QoS envelope (`250m / 256Mi / 256Mi` per worker) feeds Bootstrap, AWS-admin, external-material, and Target worker manifests. The resource plan represents one Bootstrap slot and the co-resident credential-plus-Target pair in one exclusive window, so its peak is `500m / 512Mi / 512Mi`. The corrected host-only RKE2 reservation plus that peak equals the superseded reservation on every axis; systemd containment stays at its established full-core/2GiB base and 3GiB maximum. Deployment config validation rejects a missing or drifted profile, and the harness refreshes its own stale canonical capacity projection without overwriting operator-owned config. | `src/Prodbox/Capacity/{Config,Allocation}.hs`, `src/Prodbox/{Bootstrap/Broker/KubernetesWorker,ControlPlane/TargetSecretWorkerKubernetes,Lifecycle/CredentialProvisioner/{AwsAdminKubernetes,KubernetesJob}}.hs`, `src/Prodbox/{Settings,Aws}.hs`, `src/Prodbox/CLI/Rke2.hs` |
| Retained-material delivery response budget | One typed capacity constant binds the Lifecycle Authority's five-minute persisted delivery lifetime to a finite 30-second authenticated-response overhead. Only the host EAB retained-delivery client and the AWS-admin worker's SES retained-delivery client consume the resulting 330-second HTTP timeout; generic and unrelated control-plane clients retain their narrower budgets. | `src/Prodbox/Capacity/RetainedMaterialDeliveryBudget.hs`, `src/Prodbox/ControlPlane/RetainedMaterialDeliveryCoordinator.hs`, `src/Prodbox/ControlPlane/LocalClient.hs`, `src/Prodbox/Lifecycle/CredentialProvisioner/AwsAdminWorker.hs` |
| Target-worker observation terminal fold | The bounded Pod observer retains its last typed validation failure across retries. A final clean absence reports `WorkloadAbsent` only when every prior observation was also clean absence; deadline-driven Pod deletion cannot erase a reached-workload failure. | `src/Prodbox/ControlPlane/TargetSecretWorkerProduction.hs`, `src/Prodbox/ControlPlane/TargetSecretWorkerCoordinator.hs`, `test/unit/ControlPlaneTargetSecretWorker.hs` |
| Target-worker pulled-manifest identity | The registered Target Agent identity used by the downstream `Always`-pull materialization Job is derived from the independently resolved repository-manifest digest in both deployed control-plane values and first-reconcile permit construction. Docker's host-local image ID remains only a Pod rollout trigger, the freshly observed declared tag remains only the pull address, and Kubernetes runtime image evidence must equal the signed manifest identity. | `src/Prodbox/Lib/ChartPlatform.hs`, `src/Prodbox/CLI/Rke2.hs`, `src/Prodbox/ControlPlane/{Runtime,TargetSecretWorkerProduction}.hs`, focused image-mapping regression in `test/unit/Main.hs` |
| CLI frontend and command surface | Haskell parser, native-only request ADT, native command entrypoints for the full supported command matrix, and the operator-interactive non-TTY guard module (refuses to prompt when stdin is not a TTY and emits structured guidance naming the automation equivalent) | `app/prodbox/Main.hs`, `src/Prodbox/CLI/Command.hs`, `src/Prodbox/CLI/Parser.hs`, `src/Prodbox/CLI/Interactive.hs`, `src/Prodbox/Native.hs`, `prodbox.cabal`, `cabal.project` |
| Configuration, validated deployment context, and settings | Haskell-owned native `dhall` decode of the operator-authored binary-sibling Tier-0 boot/proposal payload plus typed role-scoped projections from Lifecycle Authority. The authority aggregate owns schema/generation/digest/blob reference; no component reads the object store or another role's projection directly. `config generate` emits an intentionally unauthored skeleton: deployment-varying served host, ACME contact, cluster/machine identities, Vault address, MinIO endpoint, and `aws_substrate.profile` stay absent or raw until authored. `validateConfig`, the sole production minter of `ValidatedSettings`, narrows deployment coordinates once into `ValidatedCoordinates` and an opaque `ValidatedDeploymentContext`; the profile has its own hidden-constructor `AwsSubstrateProfile` decode seam, and every AWS provisioning flow requires it explicitly. A missing authored config has no compiled daemon fallback. Host probes and Pulumi preflight project the Vault address from that context; retained root-lifecycle binding compares its exact cluster id and Vault address, while lower gates consume sealed Tier-0 basics. The ZeroSSL directory and generic `prodbox-state` bucket are single compiled protocol/product identities and have no Dhall fields. `checkTier0CoordinateReads`, `checkDeploymentConfigOwnership`, and `checkHostLifecycleContextOwnership` enforce the boundary and reached-projection registries. | `src/Prodbox/Settings.hs`, `src/Prodbox/Settings/AwsSubstrateProfile.hs`, `src/Prodbox/Settings/Coordinate.hs`, `src/Prodbox/Config/Tier0.hs`, `src/Prodbox/Cluster/Topology.hs`, `src/Prodbox/Vault/Host.hs`, `src/Prodbox/Lifecycle/Authority/Config.hs`, `src/Prodbox/Minio/ObjectStoreTypes.hs`, `prodbox-config-types.dhall` |
| Settings and command runtime | Pure capability/program, component-graph, absolute-deadline, bounded-admission, service-capacity, Plan/Apply, and interpreter foundations. Operation-indexed references bind observation, admission, and execution to one service identity and authority scope; externally authoritative state remains a flat exhaustive observation consumed by total folds. | `src/Prodbox/ControlPlane/`, `src/Prodbox/Config/ComponentGraph.hs`, `src/Prodbox/EffectDAG.hs`, `src/Prodbox/EffectInterpreter.hs` |
| Host and Kubernetes helpers | Haskell host/k8s modules plus a substrate-neutral prerequisite boundary: `K8sClusterReachable` requires `ToolKubectl` and proves the selected cluster with authoritative `kubectl cluster-info`; local RKE2 kubeconfig/install/service facts remain explicit home-local nodes rather than generic Kubernetes dependencies. Host Vault/lifecycle selection has no compiled address, cluster-id fallback, or endpoint-changing environment seam; integration fixtures inject typed Tier-0 coordinates. | `src/Prodbox/Host.hs`, `src/Prodbox/CLI/{Rke2,Pulumi}.hs`, `src/Prodbox/K8s.hs`, `src/Prodbox/Prerequisite.hs`, `src/Prodbox/EffectInterpreter.hs`, `test/unit/Main.hs`, `test/integration/CliSuite.hs` |
| Home RKE2 compiled reconcile plan | The validated component DAG derives order from exact capability identities. It contains Bootstrap Broker, home Target Agent, frozen Lifecycle Authority, private Authority Backup and TLS Retention Adapters, fenced Provider Worker, on-demand mode-indexed Credential Provisioner, schema-indexed External Material Ingress, explicit Admin Action Runner, Gateway emitter journals, and platform/workload steps. Authority Backup desired-state apply and listener liveness precede its genesis credential establishment; that pre-seed establishment consumes the already validated Tier-0 proposal rather than asking the in-force loader for a generation that config CAS has not created. Because its Helm apply is intentionally no-wait, each pending listener observation brackets and cleans a fresh Deployment port-forward instead of retrying HTTP against a child that exited before the Pod existed. Inside that bracket, HTTP begins only after a bounded exact kubectl loopback-forward acknowledgement; EOF, timeout, or an unexpected acknowledgement stays pending liveness and retires the child. The exact Adapter Deployment uses `Recreate`, so this pre-readiness route selects no overlapping old generation. The readiness-filtered Service remains the steady route and is not weakened to publish a credential-less endpoint. Store readiness and every S3 operation remain closed until the exact Vault credential exists, and the post-CAS component-readiness step loads and observes the in-force generation and established store. Every host Lifecycle Authority client uses an exact self-token identity and a hidden-constructor Kubernetes TokenRequest lifetime of at least 600 seconds; the operator's separate Vault-issued session may remain five minutes. API validation rejection and RBAC denial are distinct closed causes. Normal admission waits for backup genesis; private workers have distinct references/envelopes. The post-export Decommission Runner is outside the live control-plane graph. | `src/Prodbox/CLI/Rke2.hs`, `src/Prodbox/Config/ComponentGraph.hs`, `src/Prodbox/ControlPlane/`, `src/Prodbox/Lifecycle/AnchoredReconcile.hs`, plan goldens |
| Container packaging and registry doctrine | Dockerfiles under `docker/`, Haskell-build containers that stay single-stage `ubuntu:24.04`, install `ghcup` in-image, pin GHC `9.12.4`, avoid symlinked Haskell tool shims, install the resolved binary and remove only `.build` plus Cabal's package-cache/state roots before that same build step commits, registry-backed steady-state image sourcing with a registry-plus-storage-backend bootstrap exception only, ordered public-image candidate retry during registry mirror publish including `mirror.gcr.io` fallbacks for the Docker Hub-hosted Percona and Envoy images used on the supported path, and native-host-architecture image publication only with basic `docker build`/`docker push` on the daemon's default builder — no supported `docker buildx`, `docker-container` builder, BuildKit-only Dockerfile features (`# syntax=` frontend pin or `RUN --mount` cache/bind mounts), cross-arch emulation, or mixed-arch cluster closure. Host-Docker runtime-generation retention runs before cluster readiness and after publication/import; it can select only canonical full IDs from `dangling=true` machine inventory under the exact managed union-runtime repository, deletes them individually, and requires empty read-back. The pinned toolchain, repository source, YAML Provider programs, installed binary, and tagged/foreign images remain; broad prune and global build-cache deletion are unrepresentable. Sprint `4.44` makes the registry S3 input a `RegistryStorageBackend` with required `RedirectPolicy`; the canonical MinIO-backed value is `RedirectDisabled`. `registryConfigYaml` remains an `unlines` renderer over the typed value, preserving the golden output and existing resource ownership. | `docker/`, `src/Prodbox/ContainerImage.hs`, `src/Prodbox/CLI/Rke2.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `test/golden/config/registry-config.yaml` |
| Pulumi orchestration and YAML stack programs | Haskell Plan/Apply orchestration over the public AWS substrate YAML stacks. The validated `aws_substrate.profile` and positive EKS topology desired size are sealed into appended provider-stack configurations, revalidated at execution, and projected through `pulumi config set`; the pre-existing constructor order and request digests remain unchanged. Legacy stack configurations are observation/destroy compatible but cannot apply desired presence. A separately resourced fenced provider worker hydrates immutable encrypted checkpoint blobs into bounded scratch; one Lifecycle Authority aggregate CAS publishes checkpoint references and workflow state. Gateway object APIs and host-direct fallback have no post-cutover authority. | `src/Prodbox/Pulumi/`, `src/Prodbox/Lifecycle/Authority/`, `src/Prodbox/Lifecycle/ProviderWorker/ProviderWork.hs`, `src/Prodbox/ControlPlane/ProviderProduction.hs`, `src/Prodbox/Infra/`, `pulumi/aws-eks/`, `pulumi/aws-eks-subzone/`, `pulumi/aws-ses/`, `pulumi/aws-test/`, MinIO `prodbox-state` bucket |
| DNS inspection | Haskell DNS check module | `src/Prodbox/Dns.hs` |
| Shared public-edge route catalog | Haskell-owned single-host route catalog, path-prefix constants, canonical public-route URL construction, and Keycloak issuer derivation consumed by lifecycle, DNS, chart, host-diagnostic, and native validation surfaces | `src/Prodbox/PublicEdge.hs` |
| Gateway legacy continuity endpoint projection | The pre-cutover Model-B continuity client decodes `boot.minio_endpoint_url` into an absent-or-validated `GatewayMinioEndpoint`. Missing configuration remains explicitly unavailable and refuses at the object-store consumer; malformed configured values refuse at decode; configured HTTP(S) endpoints reach the client exactly. No Gateway production module contains a compiled MinIO service address. Chart-side projection from the validated deployment context is separately owned by the chart rendering surface. | `src/Prodbox/Gateway/Types.hs`, `src/Prodbox/Gateway/Settings.hs`, `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/CheckCode.hs` |
| Gateway runtime | Mesh- and DNS-only Haskell daemon. One actor owns each emitter's bounded mailbox and encrypted identity-bound retained journal through stage/fsync/publish/commit/fsync; heartbeats coalesce while claim/yield/rotation remain ordered. Journal-first admission, the OS lock, durable incarnation, current Lease, recovery rewind, prior-Orders evidence, peer acknowledgements, and checkpoint floor/suffix are bounded and fail closed. Local restart rebuilds from that journal; peer repair updates only a remote replica. Peer state, framing, diagnostics, and DNS authority remain bounded. Bootstrap, lifecycle, Pulumi, generic object-store, authority-clock/CAS, and target-secret routes are absent from the target role. The code-local target exists, while Standard P keeps production on the mutually exclusive `LegacyModelBEmitter` until qualification. | `src/Prodbox/Gateway/Emitter/`, `src/Prodbox/Gateway/State.hs`, `Orders.hs`, `Peer.hs`, `DnsAuthority.hs`, `Daemon.hs`, identity-bound retained journal volumes |
| Bootstrap Broker | Minimal same-binary pre-Vault process owning bounded initialize/unseal/status/rotation plans over flat Vault observations; no generic Vault/MinIO or mesh API. Vault initialization admits the API's dual share encoding only when the canonical hex and base64 projections decode to the same encrypted bytes, then retains only opaque redacting custody values. Applied-without-response failures, baseline physical refusals, generated-root PGP workflow refusals, root-accessor inventory/revocation/absence, provisioner-accessor cleanup, and provisioner-policy application carry only closed payload-free causes/stages to their protected route diagnostics. Inventory distinguishes projected-token/auditor cleanup, list/policy-lookup HTTP, malformed-accessor, and inventory-construction causes and independently normalizes only Vault's empty-collection LIST HTTP 404. Root-accessor revocation distinguishes projected-token/auditor cleanup, revoke HTTP, list read-back HTTP, and exact target-presence causes. Accessor proof calls distinguish global stable zero from exact journaled-target absence and likewise normalize only Vault's empty-collection LIST HTTP 404 to an empty inventory; a later explicit post-baseline inventory owns global cleanup after the current generated-root target is proven absent. Provisioner cleanup retains exact initial and repeated audit operations plus visibility and stable-absence outcomes, shares the exact LIST-404 empty-collection decision across both list sites, and keeps revoke replies provisional so only later authoritative observations decide terminal closure. Provisioner-policy application distinguishes missing process-local token state, every core Vault reconcile outcome, and every PKI reconcile/read-back outcome through the same exhaustive payload-free core/PKI classifiers as generated-root baseline; its public response remains generic. The subsequent provisioner-session revocation distinguishes bounded auditor acquisition/cleanup, initial and post-revocation LIST, target and post-revocation lookup, revoke, subject mismatch, and exact target/role presence through a closed payload-free cause while preserving the prior generic public reply classes. Exact HTTP 404 supplies an empty inventory only at the two accessor-LIST operations; non-list operations and every other failure remain closed. [Sprints `2.75`](phase-2-gateway-dns.md#sprint-275-provisioner-accessor-revocation-needs-an-exact-cause) and [`2.81`](phase-2-gateway-dns.md#sprint-281-empty-post-revocation-provisioner-inventory-is-list-404) own the independently observed corrections. The later durable final absence proof has a separate closed cause for bounded-auditor acquisition/cleanup, role-wide LIST and lookup HTTP classes, exact role presence, and inventory construction; exact role-wide LIST HTTP 404 alone supplies its empty inventory, while lookup and every other failure remain closed. Sprint [`2.82`](phase-2-gateway-dns.md#sprint-282-final-provisioner-accessor-absence-needs-an-exact-cause) owns its diagnostic-first crossing and correction. A closed baseline receipt is current only when its append-only target identity includes every semantic policy revision, including the exact standing roles, the Lifecycle Authority AWS-admin journal observer, and the Credential Provisioner completion principal/key with both derived exact policies; the exact preceding terminal set is admitted only long enough to restart under a fresh root-session ID and cannot suppress the repaired projection or read-only grant. Current execution status remains solely in [README.md → Resume Here](README.md#resume-here). Every constructor classified as a secret-worker operation, including generated-root ciphertext completion, reaches Vault only through the fenced one-shot driver; the direct physical interpreter refuses it. Unseal and baseline each close on their exact receipt; the separate post-unseal handoff reconcile is eligible only after the Lifecycle Authority consumer is ready and persists its acceptance read-back in the fixed handoff journal. Terminal shutdown is proof-carrying: `Stopped` requires joined accept/worker children, resolved replay waiters, and empty queue/active/idempotency residue; timeout is explicit `ShutdownIncomplete`. | `src/Prodbox/Bootstrap/Broker/`, dedicated Deployment/Service/ServiceAccount/Vault policy |
| Control-plane role transport | The accept path every standing role server shares. Sprint `4.68`: a bounded `TBQueue` sized at the compiled `ServiceCapacityPlan`'s queue capacity, a fixed worker pool, and the pure decide/evolve `AdmissionQueue` from `Prodbox.ControlPlane.Capacity`, so in-process concurrency is finite and a saturated lane answers `429` rather than spawning a thread. Each accepted connection carries an absolute deadline from the instant it was accepted, enforced inside the response obligation so an expiry answers `408` instead of the cancellation refusal; a refusal drains the request through the bounded reader before closing so the reply is not discarded by an RST. Every reply is a closed `ReplyStatus` (Sprint `4.67`), and `prodbox dev check` fails on a raw status literal or an unbuildable capacity plan. | `src/Prodbox/ControlPlane/Runtime.hs`, `src/Prodbox/ControlPlane/Capacity.hs`, `src/Prodbox/ControlPlane/Deadline.hs`, `src/Prodbox/Http/ReplyStatus.hs`, `src/Prodbox/Http/ResponseObligation.hs` |
| Lifecycle Authority | Retained same-binary control-plane process owning authority epoch/time, durable operations, fences, immutable checkpoint references, provider revisions, credential generations, schema-specific retained source catalogs, and per-registered-target cross-cluster delivery outboxes. Reconciliation of an exact receipt-backed source into a catalog/outbox is a fresh metadata-only Authority transaction with its own five-minute deadline and predecessor-grace bound; it never reuses the expired ingress/provisioning deadline or reauthorizes the source effect. The sole v1 compatibility correction replaces only a legacy `receipt == operation` current-source field from an authenticated exact source observation, refuses field drift or a completed legacy delivery, is response-loss idempotent, and leaves any pending envelope immutable until exact Target absence and strict expiry admit its corrected-receipt successor. An expired unobserved delivery is atomically replaced by its Authority-derived deterministic successor: the exact source/target/generation/attestation binding is preserved, while a fresh ephemeral key and Authority-time five-minute deadline are required; exact response-loss replay resumes that persisted successor and callers cannot select one. The authenticated SMTP/EAB route persists before effect, observes Target metadata before recovery, and reaches plaintext only through the selected Agent's closed rewrap plus attested one-shot worker. Obtaining that worker's Target execution intent deliberately traverses the authenticated Lifecycle Authority Service route even from the Authority-hosted interpreter; the default-deny policy admits only the exact namespace/name/release-bound self Pod on the control-plane port, and that physical lane grants no application capability. The Target-intent receipt digest is the delivery's exact prepared-custody attestation; rewrap-envelope and destination-opening digests remain distinct response-validation and worker-material identities and cannot substitute for it. The intent deadline is likewise selected from the sole exact pending delivery outbox after registered-Agent, target, source-receipt, generation, operation, and attestation validation; the completed ingress deadline is historical and cannot substitute for the fresh successor deadline. Its protected startup diagnostic closes configuration, typed Vault/authentication, exact known primary-store read/field, coordinate, every interpreter-construction substage, and fallback stages without retaining boundary payload or changing exit results; initial admission separately distinguishes registration-coordinate construction, corrupt/unready/unobservable observations, and clean-install/migration construction. Registration unobservability is further divided into closed coordinate-authority, native request/HTTP, missing-version, envelope-open, invalid-Model-B-version, and unknown fail-closed causes; true S3 absence remains `ModelBMissing`. Native store HTTP is nested into closed authentication, authorization, other-client, server, unexpected-non-error, and unknown status classes while the raw internal error remains unchanged. Authorization is further nested into closed access-denied, invalid-access-key, signature-mismatch, request-time-skewed, authorization-header-malformed, expired-token, other, and malformed-or-unknown S3 code classes. The clean generation-58 deployment proves the live class is `store-http/authorization/invalid-access-key`; both anchored substrate plans now run the unchanged unified MinIO IAM reconcile after Vault unseal/Target Agent and before Authority initial admission, with home execution in the transition phase rather than later steady application. Generation 59 proves that Job completes before Authority Helm admission; its first attempt then removes the inherited failed generation-58 release as non-proof before the clean retry. Authority readiness owns only its signed object-store LIST and bootstrap-handoff Vault observation; Target-worker child-custody HMAC/KV remain foreign narrow capabilities and are not granted to the Authority role. Each standing control-plane Vault role binds its exact chart ServiceAccount in the matching workload namespace; no separately deployed worker/adapter inherits `gateway`. The signed Target-intent trust-install boundary classifies generic authenticated-client failures plus exact Target endpoint refusal, observation-unavailable, CAS-unavailable, and read-back causes before private detail crosses the Authority endpoint; observation further distinguishes session acquisition/relogin, exact 401/403, other HTTP/transport, and invalid or identity-mismatched stored records, while only request 404 is missing. A decoded prior-rollout record is observable only for the same exact target and Agent cluster; CAS still requires the desired record to name the endpoint's exact current rollout and pass all issuer, epoch, fence, version, and read-back checks. Foreign-cluster state remains fail-closed. The worker terminal retains only the canonical value-free cause below `intent/unavailable/trust-install`, with unknown detail collapsed to `other`. The Authority role is also an append-only root-baseline target so a retained pre-correction receipt can trigger only the native fresh-session repair path, never suppress the update. Current execution status and exact identities remain solely in [README.md → Resume Here](README.md#resume-here). | `src/Prodbox/Lifecycle/Authority/`, `src/Prodbox/ControlPlane/RetainedMaterialDelivery*.hs`, dedicated StatefulSet/Service/ServiceAccount/projected controller identity/Vault policy, MinIO `prodbox-state` |
| Retained authenticated request replay | Each standing role retains its accepted caller nonce, request digest, deadline, and exact response through the replay horizon. Lifecycle Authority capacity is the derived `2 * (56 + 4) = 120` envelope for one complete hard first-reconcile plus config observe/propose, retained-root-marker observation, and final in-force load followed by one immediate retry. Authority Backup has a distinct `2 * (3 + 6) = 18` envelope: at most three aggregate repair calls and six config-backup calls per complete attempt. Target Secret Agent derives `2 * (1 + 1 + 3) = 10`: one provider-credential Target observation, one committed-source recovery observation, then retained delivery's Target observation, rewrap, and trust installation, plus one immediate retry; its encoded projection is independently bounded at 24 MiB so ten accepted 2 MiB responses plus metadata fit. TLS Retention and Provider Worker retain the generic capacity four and 12 MiB bound. A capacity upgrade admits only canonical v2/v3/v4/v5/v6 bytes with identical response-size and clock-skew limits and capacity no greater than the role's compiled target, preserves every entry, and rewrites v7 on the next CAS. Its encoded-byte ceiling may only widen to the role's new compiled finite bound; capacity shrink, response/skew drift, corruption, or entry clearing refuses. | `src/Prodbox/ControlPlane/RequestReplay.hs`, `src/Prodbox/ControlPlane/Runtime.hs`, each role's Vault-backed request-replay coordinate |
| AWS-admin prepared-target diagnostic | The Lifecycle Authority's protected prepare route distinguishes closed payload-free failures at Authority-time, admission, first-reconcile-journal, deadline, intent-canonicalization, admission-confirmation, coordinate, and prepared-outbox stages. Private store/decoder detail is never serialized, and diagnostic projection changes no unavailable response class, retry, state transition, journal/outbox behavior, permit, or credential authority. Current execution status remains solely in [README.md → Resume Here](README.md#resume-here). | `src/Prodbox/ControlPlane/AwsAdminPreparedTargetProduction.hs`, `src/Prodbox/ControlPlane/AwsAdminProvisionerEndpoint.hs` |
| External EAB committed-effect recovery | Lifecycle Authority asks the authenticated retained-home Target Agent for an exact schema/operation/generation source observation after an expired committed permit loses its Job. A present source reconstructs and CAS-commits the original receipt, including its exact custody HMAC source receipt; delivery never invents that binding from the permit id. A valid completed state-v2 receipt omitted the custody receipt, so state v3 migrates it only to its exact signed permit-committed phase and requires that same authoritative observation before delivery. Typed positive absence may instead reset only an immutable-equivalent request/permit binding to a fresh strictly later deadline; the workflow separately proves stable exact-Job absence before successor creation and fresh authorization. The worker transports its canonical binary receipt only inside the distinct fixed-version `prodbox-external-material-target-receipt-v2:` canonical-base64 record envelope: attach requires exactly one leading LF plus the envelope, and only an empty attach admits the exact terminal-LF Pod-log grammar. A failure renders one closed value-free stdin/frame/identity/permit/clock/Vault/key/custody/session/exception terminal cause; the controller classifies only one unique exact whole prefixed attach-stderr or Pod-log line and never retains raw stderr or variable detail. Its protected behavior-neutral transport observation retains only source, process success/failure, each stream's empty/nonempty shape, and closed terminal disposition—never exit integers, bytes, lines, sizes, or subprocess text. Cross-schema envelopes, generic normalization, non-empty-attach fallback, unknown or multiple terminal lines, active permits, binding/deadline drift, mismatch, corruption, and unobservability refuse. Authority receives no custody Vault capability and callers cannot assert source or workload absence. | `src/Prodbox/ControlPlane/ExternalMaterialIngressEndpoint.hs`, `src/Prodbox/ControlPlane/TargetRetainedMaterialSourceClient.hs`, `src/Prodbox/Lifecycle/CredentialProvisioner/{ExternalIngress,ExternalMaterialWorker,KubernetesJob}.hs` |
| AWS-admin Job heartbeat boundary | Each permit-created Job attempt samples one fresh host heartbeat before create and carries that exact value through manifest render, Job/Pod observation, Authority attestation, and pre-delete exact-object read-back. Completed-permit recovery uses the heartbeat already bound into its signed Job attestation and never resamples. Clock failure creates no Job; a fresh sample changes neither the retained absolute deadline nor the Authority's freshness bound. | `src/Prodbox/Lifecycle/CredentialProvisioner/AwsAdminCoordinator.hs`, `src/Prodbox/Lifecycle/CredentialProvisioner/AwsAdminKubernetes.hs`, `src/Prodbox/CLI/Rke2.hs` |
| Authenticated-role plaintext response classifier | The authenticated-role interpreter owns one total projection of its 21 static status/body pairs across authentication, replay, readiness, and handler failures. Its bodyless observer recognizes only an exact pair and maps wrong-status text, prefixes, suffixes, private bodies, and all other responses to `other`. The Target-material and Target-trust decoders apply that observer only to codec-invalid server-class responses and otherwise retain only the closed non-server status class; the aggregate-backup and config-backup decoders apply it only to their codec-invalid responses. All retain only the closed result, checkpoint-backup decoding is untouched, and decoding, observation, projection, reconciliation, retry, and response behavior do not change. | `src/Prodbox/ControlPlane/AuthenticatedRoleInterpreter.hs`, `src/Prodbox/ControlPlane/{TargetMaterialClient,TargetAuthorityTrustClient,AuthorityBackupClient,ConfigBackupClient}.hs` |
| First-reconcile journal and active-session admission | The Lifecycle Authority creates the compiled plan only after a definitive missing observation. Every retry adopts an observed journal unchanged after validating the compiled action topology against the journal's own origin deadline; it never compares that digest with a fresh caller deadline, reinitializes an occupied coordinate, clears receipts, or resets the cursor. Each prompt/permit attempt carries a separate bounded active deadline. An expired exact `Prepared` state may renew under immutable request/plan equality. An expired exact `Authorized` state can recover only after an opaque proof binds authenticated 404 observations for its exact Job and Pod plus either authenticated absence of its permit-derived execution journal or the exact embedded-permit-equal `intent-committed/initial-attempt` phase. That phase is before the durable create-attempt fence: replay may repeat only idempotent bucket/IAM prerequisite reconciliation, retains the predecessor journal, and starts a binding-equivalent replacement with its own permit-derived journal. Every later phase, remint intent, invalid/mismatched bytes, presence outside that one phase, and session/request unobservability refuse. Both replacements CAS-read-back the outbox before the Authority state CAS, and exact replacement replay closes response loss. Attested/Completed phases and every drift refuse. | `src/Prodbox/ControlPlane/AwsAdminAuthorizedRecoveryProduction.hs`, `src/Prodbox/ControlPlane/AwsAdminPreparedTargetProduction.hs`, `src/Prodbox/Lifecycle/CredentialProvisioner/FirstReconcileJournal.hs`, `src/Prodbox/Lifecycle/CredentialProvisioner/AwsAdminAuthority.hs` |
| Credential Provisioner execution substrate | Non-secret retained-local prerequisite for permit-created one-shot Jobs: fixed namespace, distinct AWS-admin and external-material ServiceAccounts, schema-selecting default-deny egress, and narrow Job/Pod/attach/read-back controller permissions. A disjoint Lifecycle Authority observer Role permits only GET on Jobs and Pods for expired-Authorized recovery proofs; it grants no list/watch/log/attach or mutation. The matching Vault policy grants the Authority read only on the execution-journal prefix while the worker remains its sole create/update owner. Exact absence or `intent-committed/initial-attempt` selects no-effect renewal. A closed pre-target journal set selects only a mode-indexed Genesis-or-normal cleanup-continuation permit bound to the exact predecessor signed-permit digest; its distinct journal starts cleanup-required and must commit stable IAM-key absence before its one remint. Backup repair remains outside expired-Authorized renewal. Target-committed, complete, invalid, mismatched, and unobservable observations refuse. The mandatory local control-plane plan reconciles and independently observes this substrate after unsealed Vault/Target Agent and before genesis; AWS target selection neither duplicates nor moves it onto EKS. No chart consumer implicitly owns it and no standing worker Pod exists. Each one-shot Pod uses the same explicit numeric non-root UID/GID/fsGroup projection; it never asks Kubernetes to infer non-root identity from the role-neutral union image. Automount stays disabled. AWS-admin alone receives a Kubernetes-API-audience token, projected root CA, and downward namespace at the standard in-cluster client path, independently of its existing `prodbox-control-plane`-audience Vault token; its NetworkPolicy consumes the compiled post-DNAT API endpoint coordinate. External EAB receives no Kubernetes client capability. After create, a bounded observer may wait only on the exact owned Pod's absent phase or Pending/not-yet-ready runtime; identity drift, deletion, terminal phase, API failure, or a foreign image refuses immediately. On this `Always`-pull path, its target attestation identity is the registry-manifest digest independently resolved into the permit and read back from Kubernetes runtime evidence; the validated pullable tag remains only the addressing hint, and neither a config digest nor a digest assembled into a pull reference can substitute. Its accessor-bearing session uses the stable delivery caller and Transit key only for Target observation/intent and retained delivery; only after that session is absent may the accessor-free batch session use the appended completion caller and distinct key for the AWS-admin completion route. Neither caller can exercise the other's routes or signing key. Native S3 HEAD/GET/PUT requests send the exact body hash in `x-amz-content-sha256` and sign that header; empty-body calls and hardening documents therefore have request-specific authentication evidence. A worker execution refusal renders only an exhaustive value-free subcause derived from the closed execution-error constructor; journal/cleanup/material/delivery/identity/receipt payloads and counts are discarded before the terminal Pod-log line. IAM prerequisite errors are classified before private text projection into their finite constructor and, for AWS failures, an authored operation stage plus closed client/service category with explicit unknown fallbacks; credentials, IAM names, provider messages, request IDs, and bodies remain absent. Pre-delivery Target observation likewise classifies every authenticated-provider, endpoint, HTTP status/transport, response-codec/status, authored remote-refusal, generation, receipt, and retained-custody arm before terminal rendering; response bodies, status numbers, Vault detail, receipt values, and failed-delivery text are unrepresentable. Current execution status remains solely in [README.md → Resume Here](README.md#resume-here). | Typed substrate renderer/observer and the retained-local reconcile graph; shared Credential Provisioner runtime-security projection, predecessor-bound cleanup continuation, pulled-manifest attestation identity, bounded convergence, disjoint delivery/completion authentication, payload-free execution diagnostics, and payload-hash-bound native S3 requests; `charts/credential-provisioner/` remains the workload-shape reference, while each Job is created and removed only by its permit-bound coordinator |
| One-remint worker diagnostic | An exact one-remint refusal retains only its closed cleanup trigger: resumed cleanup-proven journal, nonempty inventory at intent, prepared-inventory divergence, create-dispatch ambiguity, lost successful create result, predecessor collision, unavailable created material, invalid material, Target delivery failure, or Target receipt mismatch. The Target-delivery arm nests only a closed direct-preflight stage, authenticated Target-intent transport/codec/HTTP/refusal/unavailability/decoder class, exact Target-worker coordinator constructor, or retained-custody boundary. The selected-Agent rollout observation further closes subprocess acquisition, missing in-cluster Kubernetes client configuration, authorization refusal, Deployment absence, other Kubernetes exit, and exact response/identity/digest/UID/generation validation stages. Session revocation further separates auditor login/role cleanup from fenced journal, binding, pre-clean, cleaned-login-ambiguity, cleanup, cleanup-exception, and cleanup-journal stages; audit causes retain only identity, observation, classification, visibility-wait, or stable-zero failure. Journal unavailability separately identifies binding allocation, pre-action acquisition, or post-entry finalization through a closed two-state action-progress latch, then retains only authentication rejection, authorization rejection, absence, timeout, transport, decode, invalid state, or other. The accessor-free, non-renewable auditor's one compiled 300-second maximum feeds both its Vault role and exact issued-lease validation and strictly contains the 180-second child-worker runtime plus finalization margin; an otherwise-valid shorter lease refuses before action entry. Provider/Vault text, HTTP status integers, tokens/accessors, journal coordinates, action time/results, Kubernetes errors, inventory, annotation values, identities, key/material/receipt values, and retained detail are unrepresentable, direct revoke status remains provisional, and the projection changes no exact-lease success path or delivery, cleanup, remint, permit, journal, observation, completion, or receipt authority. | `TargetAgentRolloutObservationCause` in `src/Prodbox/ControlPlane/TargetSecretAgentExecution.hs`; `AwsAdminRecoveryRemintCause`, `AwsAdminTargetDeliveryCause`, `AwsAdminWorkerSessionClosureCause`, `AwsAdminWorkerActionProgress`, and `AwsAdminWorkerJournalUnavailableCause` in `src/Prodbox/Lifecycle/CredentialProvisioner/AwsAdminExecution.hs`; auditor lifetime in `AuthenticationRegistry.hs`; exact issued-lease validation in `VaultAccessorAudit.hs`; production classification in `src/Prodbox/ControlPlane/TargetMaterializationProduction.hs` and `AwsAdminWorker.hs`; terminal cause rendering and exhaustiveness/containment tests |
| Fenced Provider Worker request diagnostic and measured child lane | One protected payload-free trace spans owned-route socket ingress, fresh authenticated handler admission, intent admission, trust/time revalidation, Vault-backed narrow-session acquisition, opaque credential binding, Provider capability execution, Authority projection, response encoding, and the server-owned socket-write completion. Eleven exhaustive stage tokens pair only with `started`, `completed`, or `refused`; request/response bodies, operation/resource coordinates, credentials, Vault/AWS/subprocess/transport details, exceptions, and request identifiers are unrepresentable. Replay may answer without a fresh inner-handler event, socket completion is not a Provider completion receipt, synchronous observer failure is discarded, and async cancellation remains cancellation. Its four request workers and independent readiness observer share one physical subprocess permit across AWS/Pulumi launch sites. The typed plan rounds the measured 74,052 KiB AWS CLI peak to one 80 MiB slot inside a `100m / 176Mi` Guaranteed-QoS envelope, projects `-M67108864`, and retains the complete default host fit; AWS CLI execution also has a 30-second/output bound. One capacity-level budget caps the configurable child schedule at 300 seconds and derives only Lifecycle Authority's Provider HTTP response timeout as that maximum plus 30 seconds of bounded protocol overhead; capacity validation rejects a larger configured schedule, while the generic ten-second default and all sibling clients remain unchanged. Its NetworkPolicy retains the namespace-local lane and admits the named Provider port from the registered Lifecycle Authority only through the exact `lifecycle-authority` namespace plus `prodbox-lifecycle-authority` Pod identity; neither selector is sufficient alone. The Lifecycle Authority's reciprocal default-deny policy selects only the `provider-worker` namespace plus `prodbox-provider-worker` Pod identity on its control-plane port. Current execution status remains solely in [README.md → Resume Here](README.md#resume-here). | `src/Prodbox/ControlPlane/ProviderWorkerDiagnostic.hs`, `ProviderWorkerClient.hs`, `ProviderWorkerExecution.hs`, `ProviderProduction.hs`, and `Runtime.hs`; typed owners in `src/Prodbox/Capacity/{Config,ProviderWorkerBudget}.hs`; chart projection in `src/Prodbox/Lib/ChartPlatform.hs`; paired policies in `charts/{provider-worker,lifecycle-authority}/templates/networkpolicy.yaml`; focused regressions in `test/unit/{Main,ControlPlaneProviderWorkerExecution}.hs` |
| Ordinary teardown recovery profile | Minimal RKE2/API, retained MinIO/Vault bindings, Bootstrap Broker unseal path, bootstrap-core CLI identity, Lifecycle Authority, Backup Adapter, Provider Worker, and only conditionally required Target Agent. Derived from teardown capabilities; excludes Gateway and applications. Incomplete cleanup carries `RecoveryPlaneDisposition` as `Established`, `NotEstablished`, or `Lost` and claims a live profile only for `Established`. | Target component-graph projection, chart renderers, and lifecycle interpreter; no host-direct authority |
| Recovery repair rendering and retained artifact authority | Pure projection of what repairing that profile does from each observed local substrate state, and which retained bytes it may read. A versioned, architecture-specific inventory over a closed artifact-kind universe validates pinned version, canonical `sha256:` digest, and normalized retained-root-relative location; the obligation is derived from the recovery closure, and a closure member with no declared policy refuses. The stopped/absent/healthy matrix differs only in its substrate arm, and an uncovered obligation refuses with the complete missing set instead of rendering a network fetch. The deletion-survivor projection resolves each recovery-plane resource's owning release and namespace from the one chart-name registry. | `src/Prodbox/Config/OrdinaryTeardownRepair.hs`; no effect, no host-direct authority, no Registry pull source |
| Retained artifact custody | The lifecycle execution of the bytes that repair rendering names. Custody derives one plan from the validated inventory and one exact store listing, classifying every inventory entry and every observed member exactly once: present-and-matching is retained, absent is acquired, mismatched or unreadable is replaced with the reason preserved, and a member no entry names is collected. The inventory is the authority and a pinned-archive source is only a transport — delivered bytes are refused and discarded unless they hash to the pinned digest, both at catalog binding and again at execution. Deliveries stage in a sibling directory and are admitted by rename, so a partial or rejected delivery is never a store member. Convergence is read back from a fresh listing alone; an unobservable store closes nothing. The store is phantom-indexed by how its root was obtained, so a bootstrap-located root can locate retained bytes while only an Authority-bound one can mutate them. A rendered repair is checked against a listing before it runs — the artifacts derived from the plan's own steps, so plan and check cannot disagree — and a member the repair never reads never blocks it. | `src/Prodbox/Lifecycle/Teardown/RetainedArtifactCustody.hs`, `src/Prodbox/Http/Client.hs` (bounded digesting download); `.data/prodbox/artifacts` plus its `.staging` sibling |
| Host cleanup recovery-plane arms | The join between the Sprint-`3.41` repair renderer, the Sprint-`4.86` repair admission and execution, and the host cleanup runner's bootstrap recovery-plane effects. The re-establishment reports only what the attempt did — a repair applied, a repair stopped at a step with its unattempted tail, a repair nothing could admit, a substrate nothing could observe — and never that the plane is available, which is decided solely by the separate read-back from a fresh observation. An unobserved substrate selects no repair at all, because a plan is rendered for an observed state; stopped, absent, and unread stay three answers because the runner reports unavailability and unobservability as different errors. Admission precedes every boundary call, so a repair whose retained bytes are missing is refused rather than half-run. The check's scope comes from the running host-cleanup record. Host mutations stay behind the injected repair boundary. | `src/Prodbox/Lifecycle/HostCleanupRecoveryPlane.hs` over `Prodbox.Config.LocalRke2RecoveryState` and `Prodbox.Lifecycle.Teardown.RecoveryRepairExecution` |
| Host cleanup Lifecycle-Authority arms | The production answer to the host cleanup runner's three Authority effect pairs: accepting the pre-uninstall readiness into the Authority's retained namespace, reading it back, and reconciling the durable cleanup run. The readiness slot is named from the `CleanupRunId` alone and the only write the repository can issue is into an empty slot, so a second, different readiness under one run is a conflict rather than a second key, while an exact replay is a success. The acceptance read-back and the post-re-establishment read-back are the same observation asked at two times, so an Authority that was restored without the readiness it accepted cannot satisfy the runner. Re-establishment reports only what the attempt did and never that the readiness survived, and admission is awaited only after a successful restore. The cleanup-run reconciliation begins and completes the local-uninstall node under the one deterministic attempt derivation shared with the durable cleanup driver, using the lease the durable host intent already carries, and reports a transport failure as a lost response because the run read-back decides. Restoring the aggregate from the independent backup domain and awaiting admission are the injected restore boundary, which `Prodbox.ControlPlane.LifecycleAuthorityRestoreProduction` answers. | `src/Prodbox/Lifecycle/HostCleanupAuthorityArms.hs` over `Prodbox.ControlPlane.HostCleanupReadinessRepository` and `Prodbox.ControlPlane.CleanupRunClient` |
| Lifecycle-Authority restore boundary | The production answer to the host cleanup runner's re-establishment arm. Restoration succeeds only from an established aggregate whose exact `BackupReceipt` the physically separate Authority Backup Adapter confirms healthy, so a cascade never advances genesis on its own behalf and the Authority is not the only surface answering for its own aggregate. The bounded wait covers a control plane that is not yet routable and nothing else: a decided refusal and a terminal observation failure are answers rather than states to retry. Restoration and admission stay separate arms over separate fresh observations, and neither reaches an object store from the host. | `src/Prodbox/ControlPlane/LifecycleAuthorityRestoreProduction.hs` over `Prodbox.ControlPlane.AuthorityObservationClient` and `Prodbox.ControlPlane.AuthorityBackupClient` |
| Closed cascade host runtime | The descriptor-bound dispatcher's fourth route. The four compiled cascade host nodes — uninstall, absence read-back, completion commit, completion read-back — each drive the durable host-cleanup runner to the one phase that performs its effect and stop, so a resume attributes a failure to the node that failed. A node whose phase is already reached performs nothing, which keeps the destructive uninstall issued exactly once across an interrupted cascade, and a runner observation failure is reported as an unconfirmed effect rather than a refusal because the mutation may still have landed. The runtime is closed: it carries a durable record and a production effects record and takes no caller-supplied continuation. | `src/Prodbox/ControlPlane/CascadeHostRuntime/Internal.hs` over `Prodbox.Lifecycle.HostCleanupRunner` and `Prodbox.Lifecycle.HostCleanupIntent` |
| Closed cloud runtime composition | The production constructor of the descriptor-bound dispatcher's cloud route, which had none: `mkCloudRuntime` normalizes four interpreters and nothing in the repository built one, so the dispatcher admitted a closed cascade host runtime it could construct and a closed cloud runtime it could not. Exactly one registered-target interpreter is built and normalized into every Provider-observing component, and the recursion this creates is deliberate: the stack reader's checkpoint input is the same checkpoint interpreter the graph's own recovery nodes use, so a stack decision and a recovery read-back cannot observe different authorities. Every durable input — decision inputs, Provider binding, post-recovery checkpoint pair, ownership manifest, creation binding — is read back at the moment its node executes and none is carried between nodes, so the record holds no mutable state. The two records whose authority identity only their repository module can derive are reached through scope-keyed derivations that module now owes its host-reachable client. The two observation revisions an EKS selection needs are derived from the attempt's own operation identity under two distinct dispatch purposes, because a resumed run must re-derive the same durable intent; the only thing a clock decides is the drain deadline, bounded at construction by the session ceiling. Every arm reaches the Lifecycle Authority through the closed authenticated protocol; the one process it causes to run is `kubectl` against a Provider-issued projection inside the boundary that owns that ephemerality. | `src/Prodbox/Lifecycle/Teardown/CloudRuntimeProduction.hs` over `Prodbox.Lifecycle.Teardown.CloudRuntime`, the authenticated Authority transport clients, and `Prodbox.Lifecycle.Teardown.ProviderDispatch` |
| Durable cascade entry protocol | What any caller of the descriptor-bound cleanup protocol must do before and after a run: bind a stable run id to the compiled program and its declared initial run, capture their canonical program descriptor, commit the matching host intent before the first mutation, observe-or-create the run so a lost create response is resolved by observing the same id, claim it, attach the primary outcome, and read the terminal report back independently before compacting. None of it is validation-specific; it was named as harness-owned until the first production caller could not reach it. | `src/Prodbox/Lifecycle/CleanupRunEntry.hs` over `Prodbox.ControlPlane.CleanupRunClient` and `Prodbox.Lifecycle.HostCleanupIntent` |
| Non-public cascade candidate entrypoint | The drive that composes both closed runtimes the descriptor-bound dispatcher takes. Everything decidable without a session — the compiled program, the declared initial run, the program descriptor, the host scope, the optional operator-authored AWS DNS zone, and the terminal identity — is one transport-free value; only then are the two authenticated sessions opened, both halves reading one repository root and one caller so a cloud half cannot authenticate as a different identity than the host half. The plan is a function of the declared cascade identity, so re-entering replays the run the Authority already holds instead of admitting a second one for the same host; the initial lease is therefore a declared window rather than a clock sample. The terminal operation the durable host record carries is the compiled program's local-uninstall operation id. The entrypoint is Cabal-hidden behind four non-authorizing booleans and nothing in the repository calls it: activating it as the sole public writer is Sprint `6.5`'s qualified cutover. | `src/Prodbox/Lifecycle/Teardown/CascadeCandidate/Internal.hs` over `Prodbox.ControlPlane.DescriptorBoundLifecycleRuntime.Internal`, `Prodbox.Lifecycle.HostCleanupCompositionRoot`, and `Prodbox.Lifecycle.Teardown.CloudRuntimeProduction` |
| Cascade terminal node — exact host-absence read-back | The only join between the canonical local-RKE2 marker observation and the private constructor that mints local uninstall evidence. The marker observer is forbidden from naming that proof and the constructor is inside the cascade-evidence ownership set, so nothing could turn an exact host observation into the terminal node's read-back. The observation is scoped by the running host-cleanup record rather than by the readiness, keeping the constructor's scope comparison a comparison of two independent sources; still-installed and unobservable stay distinct answers rather than collapsing into one; and a present marker outranks an unrelated read failure. Supplies the production implementation of the runner's local-absence effect arm. The durable host-completion record it feeds is located through the one prodbox-owned control directory from either the non-authorizing bootstrap locator or the Authority-bound root, so the record a run writes before uninstall is the record a recovery reads after it; it carries no durability index, because it must be mutated while the Authority is absent. | `src/Prodbox/Lifecycle/HostCleanupLocalAbsence.hs` over `Prodbox.Lifecycle.HostCleanupRke2` and `Prodbox.Lifecycle.Teardown.CascadeEvidence.Internal` |
| Cascade terminal node — local completion journal | The receipt half of the same terminal node, and the only surface that appends a local-completion entry or observes one back. It binds the signed permit and the observed uninstall evidence to the stable local-completion operation reference, appends that entry to the preserved non-secret cleanup journal, and separately observes it back into the read-back the host runner validates. The entry is keyed by the digest of its reference rather than by its bytes, so a rerun after a lost append response finds its own entry already present instead of writing a second one, and an entry that is present and differs is a conflict rather than an overwrite. The observation reads the journal rather than the append's answer, so a successful append response over a journal holding nothing observable produces no receipt, and a journal with no entry stays distinct from a journal that could not be read. Every field of the read-back is decoded from the durable bytes and none is taken from the running context, keeping the runner's binding comparison a comparison of two independent sources. Located through the one prodbox-owned control directory from either the bootstrap locator or the Authority-bound root, with no durability index, because it is written at the moment the Authority may already be gone. | `src/Prodbox/Lifecycle/HostCleanupCompletion.hs` over `Prodbox.Lifecycle.HostCleanupRunner` and `Prodbox.Lifecycle.Teardown.CascadeEvidence.Internal` |
| Cascade terminal escape audit | The producer of the audit observation the cascade's terminal arm consumes. The retained matcher catalog, the query catalog, the classification, and the region-bounded verdict all existed and nothing assembled them, so there was no path from issued queries to an observation. The queries are issued separately and unioned by ARN, because the Resource Groups Tagging API intersects the filters inside one call — the Sprint-`4.77` defect, not reintroduced. A query that went unanswered is a blind spot: rows that came back are still classified, since a discovered escapee is an escapee, but a would-be-clean verdict is downgraded to unobservable carrying that failure. Two returned rows disagreeing about one ARN refuse the audit outright rather than resolving the disagreement by preferring a row. The audit scope is derived from the compiled run's own observation scope through the same function the evidence constructor checks against. | `src/Prodbox/Lifecycle/Teardown/CascadeTerminalAudit.hs` over `Prodbox.Lifecycle.Teardown.RetainedInventory` and `Prodbox.Lifecycle.Teardown.CascadeEvidence.Internal` |
| Cascade credential revocation | Ending the cascade's Provider credential, ordered behind the audit that licensed it. The transition is reachable only from the recorded-audit state and only over a clean verdict, so "fence, audit, record, revoke" cannot be reordered by a caller: revoking from the frozen state would destroy the only credential that could re-run the audit with no record of what it found, and revoking over an escape or a blind spot would remove what an investigation needs. The receipt binds two independent read-backs — the joint IAM family disposition and the retained Target generation's revocation — and refuses one digest standing for both. The revoked state carries the audit receipt alongside the revocation's own, so the durable statement that the surface was clean outlives the credential that proved it. It is reached through its own authenticated control route, and the route relation is a projection over the closed command vocabulary rather than an authored claim. | `src/Prodbox/Lifecycle/Authority/ProviderAdmissionEpoch/Internal.hs` and `src/Prodbox/Lifecycle/Authority/Admission.hs`, routed by `src/Prodbox/ControlPlane/AuthorityAdmissionEndpoint.hs` |
| Joint IAM family disposition | Destroying one managed credential class's IAM objects as a single disposition rather than a sequence of deletes. The family is a closed enumeration — access keys, inline policies, principal, and for a class that owns one the assumed role and its inline policy — and the principal and declared policy name are derived from the registered credential descriptor rather than accepted from a caller. Every member is attempted rather than abandoned on the first failure; the inline policies are enumerated from the provider rather than addressed by an authored name; completion is minted only from a read-back in which every member is independently absent; the refusal names every surviving member, which is what a retry resumes from; and an unobservable member refuses distinctly from a surviving one. Both destroy paths — the Credential Provisioner's production IAM session and the SES SMTP decommission node — decide through it, and the SES read-back is total over the family rather than asking only whether the principal is present. | `src/Prodbox/Lifecycle/CredentialProvisioner/JointIamDisposition.hs`, consumed by `src/Prodbox/Lifecycle/CredentialProvisioner/ProductionIam.hs` and `src/Prodbox/Infra/AwsSesDecommission.hs` |
| Terminal-audit durable receipt | The audit's verdict as a durable record the Authority may not discard. Compaction deletes a settled submission's ledger record, its epoch binding, and its provider operation together, so a verdict kept where the audit's own query ran is one capacity pressure may erase; the receipt therefore lives in the retained admission epoch, carrying the audit's scope, query-catalog, and retained-set digests with its verdict. Recording is its own authenticated control route, separate from the freeze that reserved the audit and from the revocation that may follow it. All three verdicts record — a run that found an escape or could not see everything must be able to say so durably without that statement authorizing anything — while a zero-count escape or blind-spot verdict is refused as the clean verdict wearing another constructor. Recording is idempotent on the identical receipt, refuses a different receipt for the same reservation, does not lift the fence, and cannot be undone by a retried freeze. The reservation and the receipt derive their scope digest from one function over every field of the evidence scope. The independent read-back re-reads the durable object and confirms what is there against what was committed. | `src/Prodbox/Lifecycle/Authority/ProviderAdmissionEpoch/Internal.hs` and `src/Prodbox/Lifecycle/Authority/Admission.hs`, minted from an audit observation by `src/Prodbox/Lifecycle/Teardown/CascadeTerminalAuditAdapter.hs` |
| Provider resource allowlist | The registered-resource set the accepted authority carries, and the boundary a committed intent is refused at. It is authored rather than projected from the intent type on purpose: its three stack entries name exactly the stacks the Provider Worker may touch, and deriving the family would widen that authority to any stack reference a caller composed. What it may not be is a second statement joined to nothing, which is what it was — two registered families had no entry, so every committed intent for either would have been refused while their adapter tables stayed green. It now sits beside the intents it bounds and is joined to the exhaustive intent fixture by a measurement. | `productionRegisteredProviderResources` in `src/Prodbox/Lifecycle/ProviderWorker/ProviderWork.hs`, consumed by `src/Prodbox/ControlPlane/Runtime.hs` |
| Terminal-audit Provider query boundary | The production boundary the audit kernel takes on the AWS substrate. One `ObserveOwnedResourceTags` intent carries exactly one tag filter, so the catalog stays a union of separately issued queries at the provider seam too, and the key-only and exact-pair forms take distinct provider coordinates so a narrower listing cannot answer for the wider family. It dispatches at its own `ProviderTerminalAudit` purpose rather than at a registered-target purpose, so a global tag listing can never stand in for an exact family read-back and a registered observation can never close the audit. The listing follows the Tagging API's own cursor to exhaustion, refuses a cursor it has already been handed, and fails at its page bound rather than returning a prefix; an entry with no readable ARN makes the whole page unreadable. The evidence codec is shared by the renderer and the parser so they cannot drift: it echoes the query, states completion as a terminator carrying the page count, and distinguishes a resource returned carrying no tags from one not returned. Resource type and coordinate are derived from the ARN, and a mis-derived type can only fail to match a family matcher, which classifies the row as an escapee and fails the audit closed. Every inability becomes that query's own `ObservationFailure`; none produces an empty row set. | `src/Prodbox/Lifecycle/Teardown/CascadeTerminalAuditAdapter.hs`, `src/Prodbox/Lifecycle/OwnedResourceTagEvidence.hs`, and `src/Prodbox/Lifecycle/TaggedResourceQuery.hs` over `Prodbox.ControlPlane.ProviderProduction` |
| Cascade credential disposition | The producer of the credential-disposition observation the cascade's readiness proof composes. The disposal set is derived from the credential inventory — exactly the run-scoped classes — and the retained set is its complement rather than a second list, so a class cannot be absent from both. The Lifecycle-provider credential is retained through ordinary cascade convergence so it can run the terminal audit; after a clean recorded verdict, Sprint `7.36`'s terminal lane separately revokes it through one joint IAM disposition and exact retained-generation read-back. A credential observed still present is outstanding whatever the other observations did, and only when nothing is outstanding does an unanswered class decide — as unobservable, never as disposition. An empty disposal set is a refusal rather than a vacuous proof. | `src/Prodbox/Lifecycle/Teardown/CascadeCredentialDisposition.hs`, `src/Prodbox/Lifecycle/Teardown/CascadeTerminalAuditLane.hs`, and `Prodbox.Lifecycle.CredentialProvisioner.JointIamDisposition` |
| Cascade Stage C — pre-uninstall readiness | The durability protocol that turns exact convergence into `ReadyToUninstallEvidence`: commit the pre-uninstall report at the Authority, read it back through the independent Backup Adapter, obtain the one-shot local-completion permit, and compose readiness from those two plus the absence, credential, and terminal-audit evidences the earlier nodes produced. The read-back runs after every commit outcome, including a reported refusal, so an applied-but-response-lost write and a success that left nothing durable are separated by the observation rather than by the response. Writer and reader are separate injected boundaries because the surface claiming the write must not decide durability. A report identity that drifts between commit and observation refuses before a permit is requested. The module owns the protocol only — report content, the permit's one-shot semantics, and the uninstall itself belong elsewhere — and it wires no production client, so it activates no writer. **Both boundaries now have production surfaces.** The writer is the Authority's retained cascade-report namespace: one report slot and one permit slot per run, each named from the `CleanupRunId` alone and admitting one write into an empty slot, which is where the permit's one-shot semantics live — a second and different permit under one run is a conflict, an exact replay is a success. The commit checks the report bytes against the identity it is asked to commit, writes the Authority record before the independent copy, reports a copy that does not confirm as a lost response rather than a refusal, and hands back the permit read out of the durable slot. The reader is an authenticated client restricted to the **cleanup-report** Authority-backup blob class, reached by class and digest inside the prefix the adapter's grant already covers; a present observation reports the adapter's own digest, and an absent object stays distinct from a domain that answered nothing. The report itself is admitted rather than merely rendered: it is built only from a compiled program together with the three convergence evidences and only when all three bind to it, its enumeration is the exact-absence targets the program compiled (the same set the absence evidence required the observations to equal, so it cannot name more than was proven), and its canonical bytes give one converged run one identity — computed the way the independent adapter computes the name it stores them under, so renderer, writer, and reader are never told each other's answers. Nothing is composed into a run yet. | `src/Prodbox/Lifecycle/Teardown/PreUninstallReadiness.hs` over `Prodbox.Lifecycle.Teardown.CascadeEvidence.Internal`; `Prodbox.Lifecycle.Teardown.PreUninstallReport`; `Prodbox.Lifecycle.Teardown.PreUninstallReportCommit` over `Prodbox.ControlPlane.CascadeReportRepository`; `Prodbox.Lifecycle.Teardown.PreUninstallReportBackup` over `Prodbox.ControlPlane.CleanupReportBackupClient` |
| Recovery repair admission and execution | The only consumer of a rendered ordinary-teardown repair, and the reason retained-artifact custody is load-bearing rather than advisory. An admitted repair has no constructor reachable from a rendered plan alone: the plan is joined against an observation of the retained store, and only a join in which every artifact the plan names is present and hashes to its pinned digest yields one. A refusal carries the custody plan that would close it, and reports explicitly when no acquisition can — an artifact the inventory never names has no pinned digest to check a delivery against, and an unlistable store decides neither retention nor drift. Execution steps are handed the store-relative path and verified digest rather than an inventory reference, and because repair steps are sequentially dependent the run stops at the first failure and carries the unattempted tail. Substrate convergence is read back from a fresh observation alone and is scoped to the substrate arm; chart convergence is the descriptor-bound component observer's question. No production boundary is wired, so this composition activates no writer. | `src/Prodbox/Lifecycle/Teardown/RecoveryRepairExecution.hs`; pure kernel plus an injected boundary, no host mutation |
| Authority Backup Adapter | Home-only private worker owning closed S3 backup prepare/read-back/restore/GC programs and the sole Vault policy for `secret/aws/authority-backup-store`; core Authority receives typed receipts only. Its listener starts before genesis materializes that credential, and the establishment client waits only on `/healthz`; the deferred exact-role binding reports `/readyz` not-ready and permits no S3 effect until a current Vault read supplies the credential. No plaintext credential is cached across operations. | `src/Prodbox/Lifecycle/AuthorityBackup/`, `src/Prodbox/ControlPlane/{DedicatedAdapterStore,LocalClient}.hs`, separate Deployment/ServiceAccount/Vault role/queue/envelope |
| TLS Retention Adapter | Home-only private ciphertext-store worker owning the exact public-edge TLS S3 prefix and `secret/aws/tls-retention-store`; selected Agents own Kubernetes Secret plaintext, while retained-home Target Agent owns DEK wrap/unwrap. | target TLS retention runtime, separate Deployment/ServiceAccount/Vault role/queue/envelope |
| Fenced Provider Worker | Home-only private worker owning committed normal provider/Pulumi effects and the sole Vault policy for `secret/aws/lifecycle-provider`; its `aws-ses` program is restricted to the sending identity, DKIM, MX, receipt rules, and capture bucket and cannot describe an SMTP IAM principal, policy, or key. It is not admitted during genesis or backup repair and cannot accept provisioning/admin permits. The native component graph makes its exact rollout/readiness barrier a dependency of the full Gateway orchestration group because that group contains the operational-AWS namespace guardrail's first Provider dispatch; a topological sibling tie cannot put that dispatch before the worker Service exists. **Current correspondence:** the pure `ProviderIntent` fence, narrow-session admission/idempotency/recovery algebra, control-plane `apply`/`observe` endpoint/interpreter, and graph-owned standing runtime exist. | `src/Prodbox/Lifecycle/ProviderWorker/ProviderWork.hs`, `src/Prodbox/ControlPlane/ProviderWorkEndpoint.hs`, `src/Prodbox/ControlPlane/RoleInterpreters.hs`, `src/Prodbox/Lifecycle/ProviderWorker/ChartStatics.hs`; separate Deployment/ServiceAccount/Vault role/queue/envelope |
| Credential Provisioner | On-demand ephemeral home Job that accepts exactly one active signed `GenesisBackup`, `BackupRepair`, or normal `OperatorMaterial` permit at a time. During first reconcile only, one attested deadline-bounded AWS-admin prompt session may survive backup genesis to process the finite, separately backup-receipted identity permits sequentially. The Genesis permit binds a secret-free exact action/coordinate/count/deadline plan digest; every action must be its next unconsumed member and have its own permit, so the plan grants no batch authority. Every later rotation starts a new Job/prompt. Both native ingress-schema renderers consume one explicit nonzero numeric UID/GID/fsGroup security projection; admission never infers non-root identity from the union image. It alone uses raw AWS-admin bytes for permit-scoped identity setup/rotation/repair and direct Target-Agent handoff. For the deterministic `LongLived` SES-SMTP descriptor it is the sole create/rotate/remint and repair-time key-delete interpreter; explicit total SES teardown belongs to `AdminActionRunner DestroyAwsSes`. Admin Action and post-export Decommission Runners share only the authenticated stdin mechanism under disjoint proof families. | `src/Prodbox/Lifecycle/CredentialProvisioner/OperatorMaterial.hs`, `RuntimeSecurity.hs`, `Kubernetes.hs`, `KubernetesJob.hs`, `AwsAdminKubernetes.hs`, `TargetMaterial.hs`, `Execution.hs`; `charts/credential-provisioner/` |
| External Material Ingress | On-demand ephemeral attested home Job for exactly one schema-indexed non-AWS external-material permit, initially ACME EAB. It hands the bounded value directly to the retained-home Target Agent's matching custody constructor and receives only its source-ingest receipt, whose v2 canonical record includes the exact custody HMAC source receipt required by later rewrap. Its one-shot Pod binds `stdin: true` plus `stdinOnce: true`, so the first attested attach disconnect closes the stream and releases the bounded reader into the effect. Its Kubernetes carrier uses an external-material-specific canonical-base64 record envelope around the canonical binary receipt, admits only exact attach/empty-attach-log grammars, and cannot accept the AWS-admin envelope. Its failure carrier is an exhaustive value-free terminal-cause vocabulary recognized only from one unique exact whole prefixed line; raw stderr and variable failure detail are never retained. If that post-effect response is lost, expired permit replay asks the authenticated Agent for only the exact schema/permit-derived-operation/generation source metadata under a fresh bounded deadline, reconstructs the existing receipt, and CAS-read-backs normal completion; a completed legacy receipt lacking the source binding must take this same recovery path. Absence, mismatch, corruption, or unobservability refuses without a successor Job or direct Authority custody access. It cannot accept an AWS-admin/identity plan, reuse the first-reconcile admin session, expose arbitrary bytes/paths, or make `config setup` secret-bearing. | `src/Prodbox/ControlPlane/ExternalMaterialIngressEndpoint.hs`, `TargetRetainedMaterialSourceClient.hs`, `TargetRetainedMaterialRewrapProduction.hs`; separate ephemeral Job/ServiceAccount/network policy and closed schema-indexed permit interpreter |
| Admin Action Runner | On-demand ephemeral home Job for one backup-receipted `AdminActionPermit`: `DestroyAwsSes`, legacy-backend migration/compatibility, or quota request/status read-back. The `DestroyAwsSes` DAG first proves consumers quiescent and obtains the Provider Worker's provider-stack absence receipt; the Runner then deletes/read-backs the registered SMTP key family, least-privilege policy, and principal and asks live Target Agents to tombstone/read-back target generations and retained-home custody. The always-run DAG aggregates every failure. It cannot create/deliver credentials, execute normal provider work, or perform total decommission. | target admin-action runtime; ephemeral Job/ServiceAccount/network policy and closed admin-action interpreter |
| Decommission Runner | Standalone post-export interpreter for an authenticated external `DecommissionManifest` after the Authority is permanently stopped. It verifies the complete manifest/inventory and pinned artifact metadata, journals fsync-ordered stable node/attempt intent and authoritative read-back frames, re-observes before retry after response loss, and executes only the manifest's closed dependency-ordered programs. An ordered terminal phase follows the last resource deletion: the final no-retention escape audit, the home-substrate uninstall and its marker read-back, the operator's signed `.data` retain-or-delete disposition and its read-back, and the terminal receipt — a node that refuses unless the receipt's own committed frames already record every other plan node as durably terminal, so its success frame is the record's declaration that the run converged. Node cardinality is one derived classification over mandatory singletons, the mandatory parameterized choice, and per-Agent work. `prodbox nuke` composes this external-receipt path; it has no generic shell/provider surface. | `src/Prodbox/Lifecycle/Decommission/`, `src/Prodbox/Lifecycle/HostCleanupRke2.hs`, `src/Prodbox/Lifecycle/HostCleanupLocalData.hs`, `src/Prodbox/CLI/Nuke.hs` |
| Target Secret Agent | Per-substrate same-binary process owning allowlisted, generation-checked Vault KV observe/seal/CAS/read-back for its attested target identity plus a distinct exact Kubernetes TLS-Secret lane. The retained home Agent alone owns the TLS Transit DEK exchange and the physically bound, schema-closed SES-SMTP/ACME-EAB custody rewrap handlers. The family has no generic export/decrypt/path constructor; the Lifecycle Authority transports only ciphertext, an ephemeral destination public key, and receipts, while selected one-shot workers open and materialize the destination envelope. Each permit-created Target worker owns exact numeric UID/GID/fsGroup `65532` plus `OnRootMismatch`, `runAsNonRoot`, and RuntimeDefault seccomp at the Pod boundary; it never infers admission identity from the role-neutral union image. Startup remains exit-1 fail-closed across config, Vault, mounted-authentication, and handler stages; typed initial-session, relogin, and Transit-read provenance projects onto a closed payload-free protected Pod cause. Handler construction separately names target-sink compilation, trusted-sink construction, tombstone boundary/binding/registry construction, and retained-custody construction. The tombstone registry reference comes from the exact compiled sink identity; the independent deployment cluster ID cannot be substituted. Its composed cached readiness similarly projects starting/stale and the closed target-material, authority-clock, projected-token, retained-epoch, replay-projection, or other unavailable/identity-rejected family to a protected payload-free cause while returning the original readiness state and HTTP contract unchanged. The target-material family is further refined to a closed compiled target and metadata-read/metadata-validation stage before any behavior change. Neither projection can carry an underlying decoder, HTTP/Vault body/detail, arbitrary identity/path/token, or handler error text. Its standing Vault role binds the exact `target-secret-agent` namespace and `prodbox-target-secret-agent` ServiceAccount rather than a foreign standing-role namespace. | `src/Prodbox/ControlPlane/Runtime.hs`, `TargetMaterialEndpoint.hs`, `TargetRetainedMaterialRewrapProduction.hs`, `RetainedMaterialDeliveryProduction.hs`, `TargetSecretWorkerRuntime.hs`; per-substrate Deployment/Service/ServiceAccount/Vault and exact Kubernetes RBAC policies |
| Deployment qualification recorder | Separate frozen/replacement secret-safe source/config identities plus substrate/fault/cleanup evidence required by Standard P before seamless/current-architecture claims or operational legacy cutover. Each identity records/digests its source-manifest exclusion-policy version; secret-dependent evidence uses only opaque Authority receipt/generation IDs or Vault-keyed HMAC commitments, never a public raw secret hash. | `src/Prodbox/Test/Qualification/`, `DEVELOPMENT_PLAN/README.md` qualification ledger |
| Formal verification | Haskell TLA+ wrapper | `src/Prodbox/Tla.hs`, `documents/engineering/tla/` |
| Chart platform and retained state | Haskell chart registry and retained-storage renderer over pure operation-scoped capability requirements. It renders substrate-local Broker/Target-Agent/Gateway workloads; the retained home Lifecycle Authority plus separate Backup/TLS Adapters and Provider Worker; permit-created Credential/External-Material/Admin Jobs; role-specific config projections; distinct provider/Authority-backup/TLS-retention/Gateway-DNS/cert-manager/SES-SMTP Vault paths and ServiceAccounts; exact resources/queues/probes; and the application/platform surfaces. A readiness timeout preserves its release in the invocation that observes it. The target retry contract observes again before a later write, reconciles a terminal failed revision to exact absence, and refuses concurrent or unobservable state; Sprint `3.46` owns current-source adoption. No arbitrary readiness `IO`, shared operational AWS secret, generic secret export, gateway MinIO credential, or host-root-token writer exists in the target chart contract. | `src/Prodbox/CLI/Charts.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `src/Prodbox/Lifecycle/HelmRelease.hs`, `src/Prodbox/Lib/Storage.hs`, `src/Prodbox/PostgresPlatform.hs`, `src/Prodbox/ControlPlane/`, `charts/` |
| Public workload runtime | Haskell API/WebSocket runtime selected only by its mounted role-scoped Dhall config projection; owns `/api`, `/ws`, Redis-backed reconnect-safe state, one-connection-per-pod lifetime, and readiness-based drain | `src/Prodbox/Workload.hs` |
| Public-edge diagnostics | Haskell host diagnostic for the single-host Envoy edge and its path-routed application or admin surfaces | `src/Prodbox/Host.hs` |
| Onboarding and AWS administration | Haskell onboarding authors Tier-0 only. Explicit AWS administration submits a closed request to either the mode-indexed Credential Provisioner (identity setup/rotation/repair plus direct target/custody sealing) or Admin Action Runner (explicit destroy/migrate/compatibility/quota action). The ephemeral elevated credential enters only through authenticated process stdin after workload attestation and is never argv, environment, Kubernetes object, disk, log, Authority, Provider Worker, or Gateway data. The harness simulates only that prompt from `test-secrets.dhall`; ambient host AWS state is never a source. Non-AWS ACME EAB uses the separate schema-indexed External Material Ingress and never this prompt/session. | `src/Prodbox/Aws.hs`, `src/Prodbox/AwsEnvironment.hs`, `src/Prodbox/CLI/Parser.hs`, `src/Prodbox/Native.hs` |
| User invitation, listing, and revocation | Haskell `prodbox users invite|list|revoke`, Keycloak admin client, Vault SMTP/OIDC reads, and SES capture polling. `Prodbox.Ses.Readiness` builds one validated desired contract and classifies typed sender/DKIM, exact regional MX, active enabled exact recipient/S3 action, and capture-canary list/get observations as `Ready`, propagation `Pending`, terminal `Failed`, or `Unobservable`. Command success alone never opens the gate; explicit drift, disabled state, malformed output, denied access, and transport failure remain fail-closed. | `src/Prodbox/CLI/Users.hs`, `src/Prodbox/UsersAdmin.hs`, `src/Prodbox/Keycloak/`, `src/Prodbox/Ses/Readiness.hs`, `src/Prodbox/Ses/Capture.hs`, `src/Prodbox/Infra/AwsSesStack.hs`, `src/Prodbox/EffectInterpreter.hs` |
| Test harness and quality gate | **Target:** capability-bound preparation and receipt-backed cleanup registration as a client of the lifecycle kernel. The canonical lifecycle validation freezes the retained-bucket/tag-row counterexample and exercises API/Authority/Vault/MinIO/checkpoint/AWS/drain failure, response loss, cancellation, restart, and repeated-run recovery. Deployment qualification records exact revision/config identities and consecutive campaigns. **Current revision.** `TestRunner` selects exact registered keys and delegates graph compilation, durable registration, operation identity, closed dispatch, resumption, exact observation, and terminal node-state decisions to `Prodbox.Test.LifecycleCleanupClient` and lifecycle core. The client registers before the first AWS mutation; local config/RKE2/Vault desired-presence preparation necessarily precedes registration because it establishes the retained Authority. Operational credential deletion remains the legacy gated tail pending Sprint `6.5`. | `src/Prodbox/TestPlan.hs`, `src/Prodbox/TestRunner.hs`, `src/Prodbox/Test/LifecycleCleanupClient.hs`, `src/Prodbox/TestValidation.hs`, `test/` |
| Pulsar messaging and CBOR wire codec | Self-maintained native-protocol Pulsar client boundary, the non-optional CBOR-always payload codec for gateway/Orders, at-least-once envelopes, and Pulsar `Work*` payloads, the derived topic algebra, and the `Work*` envelope family; gateway `SignedAssertion`, cursor/delta, and repair request frames use canonical CBOR with `application/cbor`; Sprint `2.28` landed the shared `Prodbox.Cbor` payload type plus durable `StoredEvent` CBOR round trips; Sprint `3.21` landed `Prodbox.Pulsar.Codec`/`Topic`/`Envelope`, `Prodbox.Pulsar.Protocol`, the typed `Client` broker transport/framing boundary, `charts/pulsar`, and the live `pulsar-broker` produce/consume/ack proof. | `src/Prodbox/Pulsar/`, `charts/pulsar/`, `src/Prodbox/Cbor.hs`, `src/Prodbox/Gateway/Peer.hs`, `src/Prodbox/Gateway/State.hs`, `src/Prodbox/Daemon/Events.hs` |
| Resource scaling and capacity placement | Typed admission/runtime plans prove memory containment, CPU service rate, bounded queues, deadlines, and headroom. Bootstrap Broker, Lifecycle Authority, Backup and TLS Retention Adapters, Target Secret Agent, Provider Worker, Credential Provisioner, External Material Ingress, Admin Action Runner, and Gateway Runtime have independent envelopes/stability observations; the Decommission Runner has a separate offline envelope. The `host ≥ cluster ≥ Σworkloads` nesting is an opaque proof-carrying `AllocatedResourcePlan` built by the total `compileResourcePlan` (Sprint `1.68`, matching `ServiceCapacityPlan`/`RuntimeMemoryPlan`): a non-saturating budget subtraction makes over-commitment unrepresentable. Over-commit is unrepresentable at the **Haskell decode gate** — the proof is a required field of `ValidatedSettings` built over the *decoded* in-force plan (Sprint `1.69`); Dhall is a defense-in-depth cross-check — the Ring-1 `assertPlanValid` lean-emit shim (Sprint `1.72`) bakes an over-commit `assert` into the generated `prodbox.dhall` so an over-committed file fails to load — and the host is re-proved at reconcile against an `ObservedHostRoot` with durable and ephemeral observed on distinct devices (Sprint `4.52`) and pre-fitted at generate by `config generate` (Sprint `1.73`, deriving a host-fitting `host_capacity` and failing fast when the host is too small; `--portable` for host-agnostic generation). Each workload's `durable_storage_mib` is the *one* source that sizes the PVC, the `requests.storage` quota, and the fit proof (Sprint `3.29`); namespace `ResourceQuota`/`LimitRange` are derived projections of workload draws (Sprint `3.27`); one shared `Capacity.Render` emits every quota/limit spec (Sprint `3.28`); `GuaranteedEnvelope` is wired via `WorkloadQoS` (Sprint `1.70`). The distinct 3-axis `CapacityBudget` (durable-byte totals + placement) stays, owned by `Capacity.Storage`/`Scaling.Autoscaler`. | `dhall/capacity/Schema.dhall`, `src/Prodbox/Capacity/Config.hs`, `src/Prodbox/Capacity/Allocation.hs`, `src/Prodbox/Capacity/Render.hs`, `src/Prodbox/Capacity/Placement.hs`, `src/Prodbox/Capacity/ObservedHost.hs`, `src/Prodbox/Capacity/HostProbe.hs` (shared host-probe reader + host-fitting derivation), `src/Prodbox/Capacity/Storage.hs`, `src/Prodbox/Config/Tier0.hs` (Ring-1 over-commit shim in `renderProjectConfigDhall`), `src/Prodbox/Settings.hs` (`ValidatedSettings` proof), `src/Prodbox/ControlPlane/Capacity.hs` (bounded FIFO admission state machine), `src/Prodbox/Lib/ChartPlatform.hs`, `charts/`, temporal runtime oracle |
| Pulsar topic lifecycle | **Current correspondence:** `ManagedTopic`, three-valued `TopicResidueStatus`, callback-era ensure/delete, the `ResidueStatus` projection/registry adapter, and live broker-backed reconciliation. **Target:** the same opaque topic algebra and lifecycle-class facts project into the data-only lifecycle-indexed registry with an exact four-arm broker observation (`Absent`, `Present`, `Partial`, `Unobservable`), closed ensure/destroy program tags, and mandatory read-back. See [pulsar_topic_lifecycle_doctrine.md](../documents/engineering/pulsar_topic_lifecycle_doctrine.md); implementation ownership lives in the [plan status](README.md#resume-here). | `src/Prodbox/Pulsar/Topic*`, `src/Prodbox/Lifecycle/ResourceClass.hs`, `src/Prodbox/Lifecycle/ResourceRegistry.hs` |
| Tiered storage capacity DSL | A finite-budget capacity DSL for *how much* durable data prodbox may hold — storing more bytes than declared capacity, naming a sizeless durable claim, or calling a sink "unlimited" without an autoscaling witness are rejected before mutation; Sprint `1.51` landed the shared budget schema/config block and Sprint `4.36` landed the storage-specific Haskell planner, autoscaled-sink witness, AWS region-quota preflight binding, and mandatory ML JIT/model-cache budgets per [tiered_storage_capacity_doctrine.md](../documents/engineering/tiered_storage_capacity_doctrine.md) | `dhall/capacity/Schema.dhall`, `src/Prodbox/Capacity/Config.hs`, `src/Prodbox/Capacity/Storage.hs`, `src/Prodbox/Aws.hs` |
| Host platform provider (multi-OS) | A per-OS host-provider model, mirrored from the operator's `hostbootstrap` library, that classifies the host and canonically selects a Linux execution frame on every OS (host-native on Linux, a Lima VM on macOS, a WSL2 distro on Windows). Current source contains the detector, exported list-shaped lift, host-gated plans, runtime Docker host-frame refusal, provider-state decisions, and canonical Linux-frame dispatch. Because `LiftLayer (..)` and the arbitrary-list fold remain exported, a wrong lift is representable today; the closed `HostTool`/`AbsExe` and host-program boundary remains target-only, as [host_platform_doctrine.md](../documents/engineering/host_platform_doctrine.md) states explicitly. | `src/Prodbox/Host/Substrate.hs`, `src/Prodbox/Host/Lift.hs`, `src/Prodbox/Host/Lima.hs`, `src/Prodbox/Host/Wsl2.hs`, `src/Prodbox/Host/Ensure.hs`, `src/Prodbox/DockerConfig.hs`, `src/Prodbox/Prerequisite.hs` |
| Cluster topology and substrate-typed workers | The three explicit cluster types (`kind`/`rke2`/`eks`), the substrate-indexed one-compute-worker-per-machine rule, and type shapes that make an ill-formed topology unrepresentable. Sprint `1.53` landed the cluster-type/topology/worker Dhall schema, Haskell mirror, declared `cluster_topology` config field, and pure placement outcome ADT; Sprint `4.38` landed runtime substrate-typed placement, required hostname anti-affinity, `maxSurge = 0`, duplicate/mismatch refusal, and mixed-substrate-only-`rke2` enforcement per [cluster_topology_doctrine.md](../documents/engineering/cluster_topology_doctrine.md) | `dhall/cluster/Schema.dhall`, `src/Prodbox/Cluster/Substrate.hs`, `src/Prodbox/Cluster/Topology.hs`, `src/Prodbox/Cluster/Placement.hs`, `src/Prodbox/Settings.hs` |
| Validated deployment context | **Target:** every deployment-varying non-secret fact is authored in Tier-0 or explicitly derived from a test-run topology, narrowed once, and carried to all consumers with no compiled/environment fallback. Protocol-fixed and prodbox-chosen identities have one code declaration and no ignored Dhall field. **Current revision.** Tier-0 generation/validation, Gateway and chart consumers, host/lifecycle probes, and both harness generation lanes consume carried served-host/cluster/machine/Vault/MinIO projections; all state-store consumers import the one product-owned bucket identity. Only the AWS resource/storage profile remains under its own queue sprint; exact adoption ownership lives in the [plan status](README.md#resume-here). | `src/Prodbox/Settings.hs`, `src/Prodbox/Config/Tier0.hs`, `src/Prodbox/Cluster/Topology.hs`, `src/Prodbox/Aws.hs`, `src/Prodbox/TestRunner.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `src/Prodbox/PublicEdge.hs`, `src/Prodbox/CheckCode.hs` |
| Test topology | The executable-sibling `prodbox.test.dhall` is the explicit self-describing SSoT of one test run. Externally selected test-deployment values come from the generated `test-secrets.dhall` fixture schema; cluster shape plus Vault/MinIO endpoints come from the validated `RunVariant`; stable suite/variant identity derives the ephemeral cluster id and repository-relative `.test-data` root; protocol/product identities remain compiled once. The legacy aggregate carries equivalent explicit fixture inputs until its topology cutover. Both lanes share the production config builder, validate the complete config/context before one atomic disposable Tier-0 write, and never overwrite a complete operator sibling. Per-run AWS cleanup is a descriptor-bound Lifecycle Authority client with same-prefix nonterminal-run adoption; it is no longer a process-local callback graph. See [test_topology_doctrine.md](../documents/engineering/test_topology_doctrine.md); remaining cutover ownership lives in the [plan status](README.md#resume-here). | `dhall/TestTopologySchema.dhall`, `src/Prodbox/TestTopology.hs`, `src/Prodbox/TestRunner.hs`, `src/Prodbox/Test/LifecycleCleanupClient.hs`, `src/Prodbox/Aws.hs`, `src/Prodbox/Vault/Host.hs`, `src/Prodbox/Repo.hs`, `src/Prodbox/Lib/Storage.hs`, `src/Prodbox/TestValidation.hs` |
| Custodial-capability disposition | The indexed disposition, its four retire constructors and mandatory discharges, the single hold constructor, and the deliberate absence of a destroy constructor. The destructive boundary's argument type mentions no interpreter monad and the disposition multiset is a pure pre-dispatch projection, so every natural transformation of the interpreter observes identical arguments and has no arm through which to synthesise a disposition it was not handed. | `Prodbox.Lifecycle.Teardown.CapabilityCustody.Universe`, `Prodbox.Lifecycle.Teardown.CapabilityCustody.Internal`, over `Prodbox.Lifecycle.Teardown.CascadeEvidence.Internal` |
| Capability dependant derivation | The set of resources a capability reaches, computed from the registered ownership edges, the managed resource registry, and the managed AWS credential inventory rather than authored beside them. Where the registry declares no family the answer is underivable, never empty, because a set that defaulted to empty discharges trivially. A `prodbox dev check` rule fails the build on an undeclared answer or a derivation source that stopped contributing, and is measured against its own defect. | `Prodbox.Lifecycle.Teardown.CapabilityCustody.Universe` over `src/Prodbox/Lifecycle/Teardown/OwnershipManifest.hs`, `src/Prodbox/Lifecycle/Teardown/Registry.hs`, `src/Prodbox/Lifecycle/CredentialProvisioner/OperatorMaterial.hs`, and `src/Prodbox/CheckCode.hs` |
| Plan dependency and execution-order gate | The declaration proof over the plan's own dependency graph. `**Blocked by**` and `**Closure dependency**` are the only fields that record a dependency; `**Backward dependency**` admits a later one and is enforced as a two-way bijection against them, so neither an unadmitted backward dependency nor an orphan admission survives. Queue order is **derived, not authored**: the unique order obtained by repeatedly taking the numerically smallest open sprint whose declared dependencies are already above it — exactly ascending numerical order when none is declared, and a reported cycle when no complete order exists. A projection rule requires every declared dependency on an open sprint to appear in that sprint's `Resume Here` cell, so the derivation runs over the real graph rather than over whatever a reader restated. | `src/Prodbox/CheckCode.hs` (`sprintDependencyDirectionViolations`, `backwardDependencyAdmittedIds`, `executionOrderViolations`, `queueProjectionViolations`), over `DEVELOPMENT_PLAN/` |
| Surface-indexed cleanup entry protocol | The one entry protocol for a durable, descriptor-bound cleanup run, indexed by cleanup surface. One shared binding checks that the supplied run id is the compiled program's own, that the initial run carries that program's graph and digest, and captures the canonical program descriptor from both, so a second surface cannot be admitted by a weaker prefix of the cascade's checks. A present AWS DNS zone belongs to that compiled observation scope and is sealed into the operation/graph identity and descriptor wire; absence appends no identity field, preserving the pre-zone graph digest. The host cleanup record — the record that licenses uninstalling the retained local foundation — is cascade-only by construction rather than by convention: an ordinary per-run cleanup mutates registered targets and never the host, so it carries no record and has no store to pass. | `src/Prodbox/Lifecycle/CleanupRunEntry.hs`, `src/Prodbox/Lifecycle/Teardown/Graph.hs`, `src/Prodbox/Lifecycle/Teardown/CleanupProgramDescriptor/Internal.hs`, consumed by `src/Prodbox/Lifecycle/Teardown/CascadeCandidate/Internal.hs` |
| Custody-ending consumption sites | Every surface at which a run stops holding a custodial capability, each consuming a disposition rather than performing a deletion. Cascade readiness is a six-component composition in which custody is a component, so a run holding a lost capability has no value to hand the constructor and refuses before a report identity is committed or a one-shot permit requested. A stack's checkpoint retirement waits on the absence read-back of every resource the checkpoint reaches — the derived dependant set, not a second authored list — and consumes the discharge those read-backs supply. Pruning is a rotation onto the reference the Lifecycle Authority retains, not a deletion. The Authority registers a retirement permit only against a stated disposition and refuses a retirement for which none exists. Committing a revocation receipt consumes an inertness discharge, which claims nothing about the identity the credential family belongs to. | `src/Prodbox/Lifecycle/Teardown/CascadeEvidence/Internal.hs`, `src/Prodbox/Lifecycle/Teardown/PreUninstallReadiness.hs`, `src/Prodbox/Lifecycle/Teardown/Program.hs`, `src/Prodbox/Lifecycle/Teardown/Checkpoint.hs`, `src/Prodbox/Lifecycle/Teardown/AwsCheckpointInterpreter.hs`, `src/Prodbox/Lifecycle/LiveResidue.hs`, `src/Prodbox/Pulumi/EncryptedBackend.hs`, `src/Prodbox/Lifecycle/Authority/PulumiCheckpointRegistry.hs`, `src/Prodbox/Lifecycle/CredentialProvisioner/Execution.hs` |
| Disposition durable projection | The flat record a disposition travels as when it must cross a boundary the indexed value cannot. The indexed value exists so it cannot be built without a discharge and a decoder is a way to build one, so what crosses is evidence rather than authority and the receiving surface decides what it admits. The projection is total over the retire index and gains an arm or fails to compile. The Lifecycle Authority's aggregate records one per retirement operation and encodes the map **only when non-empty**, so each value keeps exactly one byte string and every retained aggregate that predates dispositions re-encodes byte-identically. | `Prodbox.Lifecycle.Teardown.CapabilityCustody.Universe`, `src/Prodbox/Lifecycle/Authority/PulumiCheckpointRegistry.hs`, `src/Prodbox/ControlPlane/PulumiCheckpointEndpoint.hs`, `src/Prodbox/ControlPlane/PulumiCheckpointClient.hs`, `src/Prodbox/ControlPlane/PulumiCheckpointRepository.hs` |
| Destructive delete entry contract | The no-install arm selected by the delete mode rather than by install presence alone, so only the local-only mode has a no-install success arm and the cascade mode has none; and the retained-state notice as a total function over the command's terminal arms, so no exit path silently reads as permission to retire the retained root. | `src/Prodbox/CLI/Rke2.hs`, `src/Prodbox/CLI/Spec.hs` |
| AWS substrate resource profile | The typed Tier-0 envelope for every non-secret AWS provisioning input: region, EKS node instance types and disk, node-group sizing sourced from `cluster_topology.Eks.node_group_size`, `aws-test` instance type and root-volume class and size, static retained/test EBS volume class, every VPC and subnet CIDR, and the operator CIDR. Smart constructors make an empty or ill-formed inhabitant unrepresentable and there is no compiled default envelope. Delivery is the existing `pulumi config set` transport the provider interpreter already drives plus the typed EBS lifecycle request. **Recorded negative**: the provider stack-configuration type's *existing constructors* may not be re-shaped — it derives `Serialise` generically into the retained authority's provider-operation map, whose dispatch digest is persisted and compared on every later read, so a re-shape makes retained objects undecodable and shifts every digest. Appending a constructor is byte-compatible. If the profile ends up outside the serialised intent, two dispatches under different profiles share a request digest and the profile is bound only by the Standard-P generated-config identity. | `src/Prodbox/Settings.hs`, `src/Prodbox/Cluster/Topology.hs`, `src/Prodbox/Lifecycle/ProviderWorker/ProviderWork.hs`, `src/Prodbox/ControlPlane/ProviderProduction.hs`, `src/Prodbox/Lifecycle/EbsVolume.hs`, `dhall/cluster/Schema.dhall` |
| Complete Haskell AWS-coordinate literal ban | `checkAwsCoordinateLiterals` in `src/Prodbox/CheckCode.hs` scans every repository-owned `.hs` string literal, including tests, and refuses every complete AWS-region-shaped coordinate. The production `awsCoordinateLiteralRegistry` is empty; its parameterized mechanics remain only so refusal is injection-tested in four directions. Deployment regions come from validated Tier-0 Dhall (`test-secrets.dhall` for automation), protocol-fixed roles are named in `Prodbox.Aws.Region`, and test-only synthetic regions come from `TestSupport`. No partition list is pinned. **Bound**: non-Haskell provisioning files such as `pulumi/*/Main.yaml` remain outside this gate and close through the typed AWS substrate profile (Sprint `7.37`), not by scan. | `src/Prodbox/CheckCode.hs`, `src/Prodbox/Aws/Region.hs`, `test/support/TestSupport.hs` |

## CLI Doctrine Components

The following inventory maps CLI doctrine components to their target contracts and current source
correspondence. Sprint state and implementation ownership remain in the
[plan status](README.md#resume-here).

| Component | Architecture/source correspondence | Paths |
|-----------|------------------------------------|-------|
| `CommandSpec` source-of-truth registry | `src/Prodbox/CLI/Spec.hs` now owns the typed command tree, and `src/Prodbox/CLI/Parser.hs` renders that registry into the public `optparse-applicative` parser while the same registry also drives help, tree, JSON, manpage, and completion generation | `src/Prodbox/CLI/Spec.hs`, `src/Prodbox/CLI/Parser.hs`, `src/Prodbox/CLI/Docs.hs`, `src/Prodbox/CLI/Tree.hs`, `src/Prodbox/CLI/Json.hs`, `src/Prodbox/App.hs` |
| `Plan` ADT and `Plan` / `apply` boundary | Shared `PlanOptions`, deterministic rendered plans, and the doctrine-owned `runPlanWithOptions` boundary now cover chart, RKE2, gateway-start, Pulumi, AWS setup or teardown, and config-setup flows, with golden coverage for every rendered plan plus unit coverage over dry-run vs apply behavior. Sprint `4.45` makes the home RKE2 payload compiled rather than re-derived at apply: `NativeInstallPayload` carries the validated component DAG and graph-derived step order, and invalid dependency/phase projections never produce a `Plan` | `src/Prodbox/CLI/Command.hs`, `src/Prodbox/CLI/Charts.hs`, `src/Prodbox/CLI/Rke2.hs`, `src/Prodbox/Gateway.hs`, `src/Prodbox/CLI/Pulumi.hs`, `src/Prodbox/Aws.hs`, `test/unit/Main.hs` |
| `Subprocess` typed-value ADT and interpreter | `src/Prodbox/Subprocess.hs` now exposes the doctrine-shaped `Subprocess` record, `renderSubprocess`, and the `runStreaming` / `capture` boundary backed internally by `typed-process`; validation paths that still consume the repository `Result` ADT use explicit `captureSubprocessResult` / `runSubprocessStreaming` adapters. `prodbox dev check` refuses direct `System.Process` / `System.Process.Typed` call sites and smart constructors in `src/Prodbox/` outside that interpreter. | `src/Prodbox/Subprocess.hs`, `src/Prodbox/TestRunner.hs`, `src/Prodbox/CLI/Charts.hs`, `src/Prodbox/CLI/Rke2.hs`, `src/Prodbox/Gateway.hs`, `test/unit/Main.hs` |
| `prerequisiteRegistry` with remedy-hint contract | `src/Prodbox/Prerequisite.hs` owns the authoritative prerequisite DAG, `src/Prodbox/EffectDAG.hs` rejects unknown node IDs during expansion, and prerequisite-aware commands fail with the required node-id, description, and remedy hint. Sprint `1.67` makes the generic Kubernetes branch substrate-neutral: `K8sClusterReachable -> ToolKubectl`, followed by authoritative `kubectl cluster-info`; `K8sReady` depends only on that reachability, while home RKE2 facts stay separately selectable. | `src/Prodbox/Prerequisite.hs`, `src/Prodbox/EffectDAG.hs`, `src/Prodbox/EffectInterpreter.hs`, `test/unit/Main.hs` |
| `GeneratedSectionRule` registry | Single source of truth for generated marker-delimited sections plus paired `docs check` / `docs generate` | `src/Prodbox/CheckCode.hs`, `src/Prodbox/CLI/Docs.hs`, repo-root `fourmolu.yaml` |
| `doctrineViolationsInPaths` negative-space decision | Refuses parallel-workflow surfaces such as `.github/`, `.husky/`, `.githooks/`, hook configs/scripts, and root `Makefile` / `justfile` / `Taskfile.yml`; current source uses one closed decision with local lists, not a separate forbidden-path registry | `src/Prodbox/CheckCode.hs` |
| `prodbox-haskell-style` test stanza | Cabal `test-suite` mirror of `prodbox dev lint haskell` | `prodbox.cabal`, `test/haskell-style/` |
| Capability classes plus `AsServiceError` and the shared transient-failure base | `src/Prodbox/Service.hs` defines the service-error inventory, IO-backed `HasMinIO` / `HasRedis` / `HasPg` capability instances, `retryServiceAction`, and Sprint `1.57`'s constructor-owned common transient groups exposed through `isRetryableTransientFailure`. The Phase-1 AWS-validation caller delegates to that base; Sprint `4.46` lands the three RKE2 callers and Sprint `7.32` lands the EKS image-mirror caller. `CheckCode` refuses a new standalone retry-substring table that bypasses the base, with no legacy classifier allowance. | `src/Prodbox/Service.hs`, `src/Prodbox/EffectInterpreter.hs`, `src/Prodbox/CheckCode.hs`, `src/Prodbox/CLI/Rke2.hs`, `src/Prodbox/Lib/EksImageMirror.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `src/Prodbox/Infra/MinioBackend.hs`, `test/unit/Main.hs` |
| `RetryPolicy` first-class values | Explicit `RetryPolicy` ADT plus pure `retryDelayMicros` are consumed by RKE2, chart-platform, service retry, and daemon-lifecycle retry paths. | `src/Prodbox/Retry.hs`, `src/Prodbox/Service.hs`, `src/Prodbox/CLI/Rke2.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `src/Prodbox/Gateway/Daemon.hs`, `test/unit/Main.hs` |
| `ErrorKind = Recoverable | Fatal` axis | `AppError` carries `errorKind`, `errorMsg`, and `errorCause`; the CLI boundary renders it through one shared output layer, and daemon worker retry decisions classify recoverable versus fatal failures before restart or propagation. | `src/Prodbox/Error.hs`, `src/Prodbox/Retry.hs`, `src/Prodbox/CLI/Output.hs`, `src/Prodbox/Gateway/Daemon.hs`, `test/unit/Main.hs` |
| `Prodbox.Naming` helpers | Shared `boundedResourceName`, `sanitizeResourceName`, and `hashSuffix` now own DNS-1123 or 63-character naming across storage, Patroni, and chart-platform related-resource surfaces | `src/Prodbox/Naming.hs`, `src/Prodbox/Lib/Storage.hs`, `src/Prodbox/PostgresPlatform.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `test/unit/Main.hs` |
| Paired-resource smart constructors | `storageBinding` plus the shared Patroni helper inventory now centralize the retained PV or PVC and related-name pairings used by the chart platform | `src/Prodbox/Lib/Storage.hs`, `src/Prodbox/PostgresPlatform.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `test/unit/Main.hs` |
| Retired GADT-indexed state-machine scaffold | The unadopted `Prodbox.StateMachine` scaffold and its typecheck-only test were deleted; the pure-FP doctrine now reserves GADT-indexed transitions for authoritative in-process state and permits flat exhaustive ADTs for externally-authoritative / log-reconciled projections. | `documents/engineering/pure_fp_standards.md`, `DEVELOPMENT_PLAN/legacy-tracking-for-deletion.md` |
| Explicit daemon lifecycle scaffold | `runGatewayDaemon` builds the daemon `Env`, installs signal handlers, tracks readiness, coordinates bounded drain, wraps workers with retry, and gates gateway daemon acquire through the prerequisite registry. | `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/Gateway.hs`, `src/Prodbox/Prerequisite.hs` |
| Daemon `/healthz`, `/readyz`, `/metrics` endpoints | Standard liveness, readiness, and Prometheus metrics endpoints alongside `/v1/state`, with daemon-lifecycle goldens and readiness or metrics negative-space rules | `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/CheckCode.hs`, `test/daemon-lifecycle/Main.hs`, `test/golden/daemon-health/` |
| `BootConfig` / `LiveConfig` split with mounted-Dhall file-watch reload | Hot-reloadable daemon fields live in `envLiveConfig :: TVar LiveConfig`. The daemon watches its `--config` Dhall path via filesystem-watch primitives and classifies each on-disk change: LiveConfig-only diffs atomic-swap via STM; any BootConfig diff drains within `liveDrainDeadlineSeconds` and exits with `ExitSuccess` so the kubelet restarts the Pod. See [config_doctrine.md](../documents/engineering/config_doctrine.md). | `src/Prodbox/Gateway/Types.hs`, `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/Gateway/Settings.hs` |
| `co-log` structured JSON logger | Gateway and workload daemon entrypoints emit JSON stderr logs through the co-log-backed typed field helpers; gateway log sites read `envLiveConfig` at emission time, and lifecycle/style coverage guards the JSON envelope, hot-reload filtering, dependency boundary, direct terminal writes, and inline log-object negative space | `src/Prodbox/Gateway/Logging.hs`, `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/Workload.hs`, `test/daemon-lifecycle/Main.hs` |
| Test hooks in `Env` | No-op production hook fields exist for gateway peer-commit and connection surfaces, and `prodbox dev check` enforces literal production `noopDaemonHooks` construction plus hook reads through `envHooks env`. | `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/Daemon/Events.hs`, `test/` |
| `prodbox-daemon-lifecycle` test stanza | Cabal `test-suite` spawns the built gateway daemon through the typed subprocess boundary, polls `/readyz` through the shared retry helper, checks `/healthz` and `/metrics`, captures endpoint response goldens, exercises first- and second-SIGTERM behavior, and `prodbox dev check` rejects direct `threadDelay` / raw `terminateProcess`. | `prodbox.cabal`, `test/daemon-lifecycle/` |
| `prodbox-pulumi` test stanza | Cabal `test-suite` validates retained Pulumi-program ownership plus the isolated ephemeral-stack harness locally, including typed-output round-trips and forced-failure cleanup; the live retained AWS flows are covered by the named integration validations and aggregate suite. | `prodbox.cabal`, `test/pulumi/`, `src/Prodbox/CLI/Pulumi.hs`, `src/Prodbox/TestValidation.hs` |
| `prodbox cluster reconcile` canonical entrypoint | Canonical reconcile command is implemented, documented, and validated locally; the completed one-cycle `install` alias and old `rke2` public command group have been removed from the parser, registry, generated docs, and current guidance | `src/Prodbox/CLI/Rke2.hs`, `src/Prodbox/CLI/Command.hs`, `src/Prodbox/CLI/Parser.hs`, `src/Prodbox/CLI/Spec.hs` |
| `--format` / `--color` output discipline | `OutputOptions`, `OutputFormat`, `ColorMode`, `--format`, `--color`, `--no-color`, and the stdout/stderr writer boundary are implemented and guarded by the output-boundary lint checks. | `src/Prodbox/CLI/Output.hs`, `src/Prodbox/CLI/Spec.hs`, `src/Prodbox/CheckCode.hs`, `test/unit/Main.hs`, `test/haskell-style/Main.hs` |
| One-shot `Env` and `newtype App` | Shared `Env` and `newtype App` backed by `ReaderT Env IO` exist for one-shot command dispatch, with environment access covered in the unit suite. | `src/Prodbox/App.hs`, `test/unit/Main.hs` |
| Style-tools sandbox plus `.hlint.yaml` nesting rules | `src/Prodbox/Lint.hs` bootstraps Fourmolu `0.19.0.1` and HLint `3.10` into `.build/prodbox-style-tools/bin/` through `ghcup run --install` on isolated formatter GHC `9.12.4`, while `prodbox dev lint haskell` runs the sandboxed binaries by absolute path and enforces the doctrine-owned nested-case marker set | `.hlint.yaml`, `src/Prodbox/Lint.hs`, `src/Prodbox/BuildSupport.hs`, `src/Prodbox/CheckCode.hs`, `test/haskell-style/Main.hs` |
| `prodbox test lint` alias plus lint-first ordering | Alias and ordering are implemented in the parser, registry, runner, and governed docs | `src/Prodbox/CLI/Command.hs`, `src/Prodbox/CLI/Parser.hs`, `src/Prodbox/CLI/Spec.hs`, `src/Prodbox/TestRunner.hs` |
| `trackingGeneratedPaths` registry plus renderer determinism property test | Registry for fully generated files alongside `GeneratedSectionRule`, plus repeated-render and synthetic forbidden-input determinism checks in the Haskell-style suite; negative-space path rejection is the separate `doctrineViolationsInPaths` decision | `src/Prodbox/CheckCode.hs`, `test/haskell-style/` |
| Standardized library set in `prodbox.cabal` | The dependency audit is closed: `typed-process` is isolated at the subprocess boundary, `co-log` / `co-log-core` are owned by Sprint 2.12, and retained project-specific libraries are documented in the dependency-management doctrine. | `prodbox.cabal`, `documents/engineering/dependency_management.md` |
| Daemon CLI: `--config <path>` is the only startup knob | Gateway and workload launch paths accept exactly one CLI flag, `--config <path>`. `--log-level`, `--port`, `--node-id`, and `PRODBOX_*` env-var precedence are forbidden per [config_doctrine.md](../documents/engineering/config_doctrine.md). | `src/Prodbox/CLI/Parser.hs`, `src/Prodbox/CLI/Spec.hs`, `src/Prodbox/Gateway.hs`, `src/Prodbox/Workload.hs` |
| `Prodbox.Daemon.Events` at-least-once event-processing module | Immutable `StoredEvent` records with `created_at`, idempotent `EventHandler` newtype, and the `recordEvent` / `markEventProcessed` / `fetchUnprocessedEvents` triad; the gateway remains the documented in-memory anti-entropy peer-gossip variant | `src/Prodbox/Daemon/Events.hs`, `documents/engineering/distributed_gateway_architecture.md`, `test/unit/Main.hs` |
| `prodbox dev lint chart` Helm-chart structural-invariants linter | `prodbox dev lint chart` validates `Chart.yaml` metadata, required chart-label helpers, and generated route-inventory drift through `src/Prodbox/CheckCode.hs` | `src/Prodbox/CheckCode.hs`, `src/Prodbox/CLI/Parser.hs`, `src/Prodbox/CLI/Spec.hs` |
| HLint negative-space symbol rules for daemon paths | The governed Haskell lint scan enforces the doctrine-owned daemon-path marker set for `forkIO`, `unsafePerformIO`, module-level `IORef`, forbidden subprocess primitives, and case-inside-lambda nesting before handing off to sandboxed HLint | `.hlint.yaml`, `src/Prodbox/Lint.hs`, `src/Prodbox/CheckCode.hs`, `test/haskell-style/Main.hs` |
| Marker-delimited route-inventory generation | Renders the `src/Prodbox/PublicEdge.hs` route catalog into the consuming Helm chart manifests via the generated-section registry, and `prodbox dev lint chart` validates drift | `src/Prodbox/PublicEdge.hs`, `src/Prodbox/CheckCode.hs`, `charts/` |
| Durable CLI documentation artifacts | Generated Markdown command reference, doctrine-owned manpages, shell completion scripts, and tracked-generated-path enforcement all derive from the command registry | `documents/cli/commands.md`, `share/man/man1/prodbox*.1`, `share/completion/{bash,zsh,fish}/`, `src/Prodbox/CLI/Docs.hs` |
| Parser-test category via `execParserPure` | Dedicated parser module now exercises leaf happy and unhappy paths through the pure optparse API and is guarded against subprocess regression | `test/unit/Parser.hs`, `prodbox-unit` stanza, `documents/engineering/unit_testing_policy.md` |
| `renderError` error-rendering boundary | `src/Prodbox/CLI/Output.hs` owns `renderError`, `writeError`, and terminal writer helpers; command failure paths route through the output layer, and `prodbox dev check` refuses `print`, `exitFailure`, `putStr`, `putStrLn`, and direct stderr writes outside the allowed output/logging boundaries | `src/Prodbox/CLI/Output.hs`, `src/Prodbox/Error.hs`, `src/Prodbox/App.hs`, `src/Prodbox/CheckCode.hs`, `test/unit/Main.hs`, `test/haskell-style/Main.hs` |
| `envMetrics :: MetricsRegistry` typed daemon `Env` field | Typed metrics registry is threaded through the daemon `Env`, `/metrics` reads counter values from it, and the negative-space guard refuses module-local mutable counter state under daemon paths | `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/CheckCode.hs`, `.hlint.yaml` |
| STM broadcast channel for `LiveConfig` subscribers | `TChan` notification surface is exposed through `Env` and successful reloads publish the refreshed `LiveConfig`. | `src/Prodbox/Gateway/Daemon.hs` |
| Prescribed daemon config shape | Gateway and workload daemons take their config from a mounted Dhall file at `--config`, decoded in-process via `Dhall.inputFile auto`. Credentials reach the Pod through Vault-backed materialization rather than a plaintext config section. Orders lives in its own ConfigMap-mounted Dhall and is imported by the main file. | `src/Prodbox/Gateway/Types.hs`, `src/Prodbox/Gateway/Settings.hs`, `prodbox.dhall`, `prodbox-config-types.dhall`, `charts/gateway/templates/configmap-config.yaml`, `charts/gateway/templates/configmap-orders.yaml` |
| Cabal-manifest toolchain pin declarations | `tested-with: ghc ==9.12.4` and `with-compiler: ghc-9.12.4` are present and the governed docs now cite the authoritative Cabal `3.16.1.0` pin | `prodbox.cabal`, `cabal.project`, `DEVELOPMENT_PLAN/` |
| Library-first / thin-`Main.hs` layout audit | `app/prodbox/Main.hs` is thin and `src/Prodbox/CheckCode.hs` enforces the doctrine rule | `app/prodbox/Main.hs`, `src/Prodbox/App.hs`, `src/Prodbox/CheckCode.hs` |
| `CommandSpec` / `OptionSpec` record-field bindings | The doctrine-named record fields are implemented in `src/Prodbox/CLI/Spec.hs`, and the registry now drives the public parser as well as the generated introspection and documentation surfaces | `src/Prodbox/CLI/Spec.hs` |
| Daemon-as-typed-`Command` dispatch | Gateway daemon entry is a `GatewayDaemonCommand DaemonLaunchOptions` constructor on the top-level `Command` ADT dispatched through ordinary pattern matching — bound per [Daemon as Command](../documents/engineering/distributed_gateway_architecture.md#the-daemon-as-command-pattern) | `src/Prodbox/CLI/Spec.hs`, `src/Prodbox/App.hs`, `src/Prodbox/Gateway/Daemon.hs` |
| Forbidden subprocess primitives | `prodbox dev lint files` now names `callProcess`, `readCreateProcess`, `readCreateProcessWithExitCode`, `createProcess`, `proc`, `shell`, and `import System.Process` in `src/Prodbox/` outside `src/Prodbox/Subprocess.hs`; the scanner strips string literals before token checks so Linux `/proc` paths do not false-positive, and `.hlint.yaml` carries the matching doctrine marker set | `src/Prodbox/CheckCode.hs`, `src/Prodbox/Subprocess.hs`, `.hlint.yaml` |
| `fourmolu.yaml` 13-setting list | The repo-root `fourmolu.yaml` carries the thirteen doctrine-named settings and the dedicated haskell-style suite enforces them | `fourmolu.yaml`, `test/haskell-style/Main.hs` |
| Canonical property-test invariants | Deterministic renderer and plan assertions exist in `test/unit/Main.hs` and `test/haskell-style/Main.hs`, the repo-root Dhall config plus persisted gateway and AWS snapshot shapes now round-trip through their supported decode paths, and parser examples now round-trip back to their registered command paths | `test/unit/Main.hs`, `test/unit/Parser.hs`, `test/haskell-style/Main.hs` |
| Service-error newtype inventory | `MinIOError`, `RedisError`, and `PgError` each wrap `ServiceError`, carry `AsServiceError` instances, and are consumed by the current MinIO, Redis, and PostgreSQL service boundaries. | `src/Prodbox/Service.hs`, `src/Prodbox/Infra/MinioBackend.hs`, `test/unit/Main.hs` |
| `AppError` record shape | `src/Prodbox/Error.hs` defines `AppError { errorKind, errorMsg, errorCause }`, the shared output layer consumes that record shape, and daemon worker retry classification uses it. | `src/Prodbox/Error.hs`, `src/Prodbox/CLI/Output.hs`, `src/Prodbox/Gateway/Daemon.hs`, `test/unit/Main.hs` |
| Naming-helper signatures with DNS-1123 / 63-character constraints | `boundedResourceName :: Text -> Text -> Text -> Text`, `sanitizeResourceName :: Text -> Text`, and `hashSuffix :: Text -> Text` are implemented in `src/Prodbox/Naming.hs` and now back storage, Patroni, and chart-platform related-resource naming on the supported path | `src/Prodbox/Naming.hs`, `src/Prodbox/Lib/Storage.hs`, `src/Prodbox/PostgresPlatform.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `test/unit/Main.hs` |
| Enumerated forbidden renderer inputs | Renderers in `GeneratedSectionRule` and `trackingGeneratedPaths` may not depend on timestamps, random IDs, locale-dependent ordering, terminal-width-dependent wrapping, or environment-dependent paths; the haskell-style suite injects synthetic violations for each class | `src/Prodbox/CheckCode.hs`, `test/haskell-style/` |
| Structured-concurrency primitive set | Gateway daemon workers now use `Control.Concurrent.Async.withAsync`, `race`, and `concurrently`, and the lint guard rejects daemon use of unrestricted Async primitives — bound per [Structured Concurrency](../documents/engineering/distributed_gateway_architecture.md#structured-concurrency) | `.hlint.yaml`, `src/Prodbox/CheckCode.hs`, daemon-path modules under `src/Prodbox/Gateway/` |
| File-watch reload trigger | The daemon watches its `--config` Dhall path via filesystem-watch primitives (the supported library is named by Sprint 2.21). The previously-supported SIGHUP signal handler and the `forbid-fsnotify` / `forbid-inotify` / `forbid-mtime` lint rules are removed; SIGHUP becomes an ordinary `drain + exit` terminate signal. Bound per [config_doctrine.md §7](../documents/engineering/config_doctrine.md#7-file-watch-reload-trigger). | `.hlint.yaml`, `src/Prodbox/CheckCode.hs`, `src/Prodbox/Gateway/Daemon.hs` |
| Typed `schemaVersion` field plus mismatch-as-parse-failure | The gateway daemon config parser treats structured `schemaVersion` mismatches as `config_schema_mismatch` and the reload path preserves the running config. | `src/Prodbox/Gateway/Types.hs`, `src/Prodbox/Gateway/Daemon.hs`, `prodbox-config-types.dhall` |
| Reload procedure | The reload worker runs in order: receive file-watch event, re-decode the Dhall via `Dhall.inputFile auto`, log `config_reload_decode_failed` on parse error and keep the in-memory config, classify diffs (any BootConfig change → drain + exit with `ExitSuccess`; LiveConfig-only change → atomic `writeTVar` + STM broadcast). Bound per [config_doctrine.md §8](../documents/engineering/config_doctrine.md#8-boot-vs-live-split-and-the-restart-contract). | `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/Gateway/Settings.hs` |
| Typed logging field helpers | `field :: (Aeson.ToJSON a) => Text -> a -> (Text, Aeson.Value)` plus `logStructured`, `logStructuredAt`, `logDebug`, `logInfo`, `logWarn`, and `logError` wrappers are backed by `co-log`; `prodbox dev check` and the Haskell style suite reject direct daemon terminal writes and inline log-object construction — bound per [Structured Logging Field Helpers](../documents/engineering/distributed_gateway_architecture.md#structured-logging-field-helpers) | `src/Prodbox/Gateway/Logging.hs`, `src/Prodbox/CheckCode.hs`, `test/haskell-style/Main.hs` |
| Production-no-op / test-injected hook contract | Daemon `Env` hook fields have no-op production defaults, and `prodbox dev check` enforces production startup through literal `noopDaemonHooks` plus hook reads through injected `envHooks env` — bound per [Test Hooks in Env](../documents/engineering/distributed_gateway_architecture.md#test-hooks-in-env) | `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/CheckCode.hs`, `test/integration/` |
| Health-endpoint response shapes as golden tests | The `prodbox-daemon-lifecycle` stanza captures `/healthz`, ready/draining `/readyz`, and normalized `/metrics` response-shape goldens under `test/golden/daemon-health/` | `test/daemon-lifecycle/Main.hs`, `test/golden/daemon-health/` |
| Forbidden reconciler flags and sister commands | `--force`, `--reinstall`, and sister commands `install`, `upgrade`, `repair`, `force-install` are rejected at parse time on the chart and lifecycle reconciler surfaces; no legacy lifecycle alias remains on the supported command surface | `src/Prodbox/CLI/Parser.hs`, `test/unit/Parser.hs` |
| Compiled gateway route registry and readiness projection | Closed `GatewayRoute` registry (Enum/Bounded) as the single place any daemon path string exists, with total dispatch and client/chart/wire-path projections; `GatewayChartStatics` feeding deployed values and generated port/identity sections; one pure constant-time `computeReadiness` projection over drain phase, emitter authority, and workers. The rollback topology latches validated continuity startup; the Sprint `2.32` target topology requires a current identity-bound journal lock, matching Lease witness, and completed recovery and fails closed on Lease loss. | `src/Prodbox/Gateway/Routes.hs`, `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/Gateway/Client.hs`, `src/Prodbox/Gateway/Probe.hs`, `src/Prodbox/Gateway/Readiness.hs`, `src/Prodbox/Gateway/ChartStatics.hs`, `src/Prodbox/Lib/ChartPlatform.hs` |
| Configurable certificate-scope algebra and derived edge projections | Pure `CertScope` algebra (`ScopeExact` / `ScopeWildcard` over smart-constructed `Fqdn`, a canonical deduped-ordered `CertScopeSet`, total `covers`, and the narrower-or-equal `impliedBy` coverage/admission order) with wildcard scopes anchored only at a config-declared delegated zone. `mkScopeSet` rejects undeclared-zone wildcards and `bindListener` rejects an uncovered served host; settings validate the explicit home and configured AWS served hosts. The Certificate `dnsNames` and path-safe `public-edge-tls/<substrate>/<canonical-scope-key>` coordinate are exact projections of the scope set, while Gateway listeners/routes/DNS remain projections of explicit bound served hosts. Retention restores only an identical canonical SAN set; `impliedBy` never aliases distinct issuance specifications. `edge status` observes `certificate-renew-due` / `certificate-expired` fail-closed from cert-manager `status.renewalTime` / `notAfter` with no repo-side renewal-window recompute. | `src/Prodbox/Tls/CertScope.hs`, `src/Prodbox/Settings.hs`, `src/Prodbox/PublicEdge.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `src/Prodbox/CLI/Charts.hs`, `src/Prodbox/Host.hs`, `charts/keycloak/templates/gateway.yaml`, `test/unit/CertScopeSuite.hs`, `test/unit/Main.hs` |

## Infrastructure Layer

| Component | Technology | Deployment | Authority | Durable State |
|-----------|------------|------------|-----------|---------------|
| Supported host runtime | `Ubuntu 24.04 LTS` with systemd | Bare metal | `prodbox` supported-host gate | Host filesystem |
| Host build root | `.build/` | Repository worktree | Canonical `cabal build --builddir=.build exe:prodbox` plus copy to `.build/prodbox`, which backs the supported `prodbox` command surface | Host filesystem |
| Container build root | `/opt/build` | Docker build stage | Dockerfiles under `docker/` | Container filesystem |
| Home local substrate (RKE2 on operator host) | RKE2 | Home local substrate (per [substrates.md](substrates.md)) | `prodbox cluster reconcile|delete --yes|delete --cascade|status|health|start|stop|restart|logs|wait|workload-logs`. Target reconcile deploys Broker, Target Agent, frozen Authority, private Backup/TLS Adapters and Provider Worker, and Gateway as separately admitted/resourced roles; it creates a mode-indexed Credential Provisioner, schema-indexed External Material Ingress, or Admin Action Runner only for the matching signed permit. Normal admission follows backup genesis. The Broker's intentionally no-wait Helm apply is followed by a finite 60-attempt exact Deployment-revision observation before bootstrap begins. | RKE2 data dirs, retained MinIO/Vault/backup/TLS references, and component-local journals |
| In-cluster registry (`registry:2`) pipeline | Single-binary `registry:2` plus idempotent public/custom-image reconcile and the bounded registry/storage bootstrap exception | RKE2/EKS workload plus host Docker runtime | The target graph requires an exact registry-publish capability whose observation, admission, blob upload/read-back, and subsequent image write share one service/storage identity and absolute deadline. The host-owned storage-bootstrap Job has no competing TTL collector: its exact 300-second waiter observes the terminal result before explicit deletion, retaining a failed Job for diagnosis. Front-door `/v2/` health and a separately injected MinIO probe are diagnostics, not authorization to publish. Historical deep-probe work remains evidence for its revision. | Cluster resources, registry data in the `prodbox-harbor-registry` MinIO bucket, host Docker cache, and runtime registry config |
| Root-accessor post-revocation observation | Vault token-accessor LIST through the bounded root auditor | Bootstrap Broker | A successful listing preserves its exact accessors; only Vault HTTP 404 after revoking the final root accessor denotes an empty collection. Connection, timeout, non-404 status, and decode failures remain closed refusals, and target presence still refuses baseline completion (Sprint `2.76`). | Retained root-session journal and protected payload-free diagnostic |
| Provisioner policy and Kubernetes-role repair authority | Fresh-generation policy write/read-back followed by ordinary baseline reconciliation in the root-session program | Bootstrap Broker | An accessor-free batch role alone has root-protected `sudo` on `sys/policies/acl/prodbox-bootstrap-provisioner`; it writes and immediately reads back that exact document, cannot edit its own policy or a wildcard, and the ordinary provisioner plan performs no ACL-policy writes. The repaired ordinary policy derives one exact `auth/kubernetes/role/<canonical-name>` coordinate per compiled reconcile role, with no role-path glob or `sudo`, before the ordinary plan writes and reads back those roles. Every semantic policy or role revision appends one exact baseline target; the credential-provisioner auditor lease-containment target follows the completion-principal target. A registered older completed target set is admissible only as restart input and drives the native orphan-cleanup/generated-root program before provisioner work; it is never current or valid in-progress state. Sprints `2.78`, `2.79`, `2.77`, and `2.80` close retained admission, advanced identity, ACL-policy repair, and exact ordinary role-path crossing. | Retained root-session journal and exact Vault policy/role observations |
| AWS substrate (EKS provisioning) | Amazon EKS in a dedicated, non-default VPC whose VPC/IGW/route-table/subnets carry `prodbox.io/managed-by=prodbox` for escape-sweep visibility | AWS substrate (provisioned per [substrates.md](substrates.md)) | `prodbox aws stack eks reconcile|destroy --yes`; the canonical-suite `aws-eks` validation in `src/Prodbox/TestValidation.hs` runs against it. The authored AWS profile supplies the operator ingress CIDR, instance/disk envelope, scaling bounds, VPC, and two subnets; positive `cluster_topology.Eks.node_group_size` supplies desired scale. **Per-run lifecycle class. Current revision:** the suite selects the registered EKS projection and durably registers the lifecycle-core desired-absence program before the first AWS mutation; completion requires exact terminal evidence. Explicit destroy and cascade still await Sprint `6.5`'s public single-writer cutover. See [substrates.md → Resource Lifecycle Classes](substrates.md#resource-lifecycle-classes). | EKS resources in AWS |
| AWS substrate (Route 53 subzone) | Amazon Route 53 | AWS substrate (provisioned per [substrates.md](substrates.md)) | `prodbox aws stack aws-subzone reconcile|destroy --yes`; provides the configured per-substrate subzone (for example, `aws.example.invalid`) that AWS-substrate public-edge proofs write into. Destroy deletes non-NS/SOA record sets before hosted-zone teardown so a failed run's A record cannot strand the subzone. **Per-run lifecycle class.** | Route 53 zone + NS delegation records |
| AWS substrate (HA RKE2 provisioning) | `Ubuntu 24.04 LTS` EC2 instances | AWS substrate (provisioned per [substrates.md](substrates.md)) | `prodbox aws stack test reconcile|destroy --yes` plus Haskell SSH orchestration; the canonical-suite `ha-rke2-aws` validation runs against it. The authored AWS profile supplies the operator ingress CIDR, each node's instance/root-volume envelope, the VPC, and three subnets. **Per-run lifecycle class.** | EC2, VPC, subnet, security-group, IAM, and Route 53 state |
| Cross-substrate shared SES provider infrastructure | AWS SES + Route 53 + S3 | AWS account plus retained Lifecycle Authority | A committed provider intent lets the fenced Provider Worker reconcile only the sending identity, DKIM, MX, receive rules, and capture bucket. Pulumi never declares or exports an SMTP IAM principal, policy, access key, or key material. Semantic polling starts from the committed provider revision and holds no provider fence. | SES identity/DKIM/MX/rules/capture, immutable checkpoint blobs, authority aggregate |
| Long-lived SES SMTP credential identity | AWS IAM plus retained-home Transit custody and per-substrate Vault KV | Credential Provisioner, retained Lifecycle Authority, and selected Target Secret Agents | A receipt-committed `OperatorMaterialPermit` selects one deterministic `LongLived` principal, least-privilege send policy, and finite access-key family. The Credential Provisioner creates/rotates/remints and performs repair-time key deletion, constructs the SMTP payload in bounded memory (username from access-key ID; password from one-time secret plus region), discards the raw AWS secret, and directly hands only the closed `SesSmtpSource` payload to the retained-home Agent. That Agent Transit-seals it; committed delivery rewraps only to an attested selected Agent, while Authority sees ciphertext/receipts. `DestroyAwsSes` proves consumers quiescent, waits for Provider-Worker provider-stack absence, invokes the Admin Action Runner to delete/read-back external IAM, then tombstones target/custody Vault state while Agents remain live. Ordinary postflight retains every node. | Registered IAM descriptor/key-family observation, schema-bound custody ciphertext/receipt, authority generation/outbox, target Vault generations |
| Pulumi test backend | Lifecycle Authority plus `Prodbox.Pulumi.EncryptedBackend` | Retained control-plane workloads | A fenced provider worker hydrates one immutable encrypted checkpoint into bounded scratch, runs the provider action, stores the new immutable blob, and conditionally advances the authority aggregate. The operation journal resolves lost responses by operation ID. Gateway and host-direct object APIs are pre-cutover compatibility paths scheduled for deletion. | Immutable Model-B blobs plus one versioned authority aggregate in `prodbox-state` |
| Load balancer IPs | MetalLB on self-managed local clusters with config-selected L2 or BGP advertisement, including `IPAddressPool`, `L2Advertisement`, `BGPPeer`, and `BGPAdvertisement` resources; the `IPAddressPool` is sized to a single LAN IP, matching the supported edge's one Envoy Gateway `LoadBalancer` Service | RKE2 workload | Haskell lifecycle plus Gateway API edge doctrine | Cluster resources |
| Public edge controller | Envoy Gateway plus Kubernetes Gateway API, with browser-facing auth and operational-dashboard access enforced at Envoy, request-carried JWTs validated locally at Envoy from Keycloak issuer metadata plus JWKS-backed signing keys and route claims, shared-host Keycloak integration on the substrate's validated served FQDN, public TLS termination at Envoy on port `443`, redirect-only HTTP handling on port `80`, and WebSocket-capable proxying on the same public edge | RKE2 workload | Haskell lifecycle plus single-host public-edge doctrine | Cluster resources |
| TLS issuance | cert-manager plus the one supported ZeroSSL ACME provider; its directory URL is a compiled protocol identity, while the account contact and typed EAB-generation references come from validated config. EAB enters through its own schema-indexed external-material permit, is Transit-custodied/rewrapped by the retained-home Agent, and reaches only role-scoped Target-Agent generations. It never reuses the AWS-admin identity plan and `config setup` remains Tier-0-only. DNS01 material follows its separate identity generation. One `ClusterIssuer`, `zerossl-dns01`, uses the DNS-01 Route 53 solver. Rebuild churn is handled by retained ciphertext/current-reference workflows, not by a separate test issuer. Host chart orchestration selects only the closed Authority retain/restore request; the retained Authority authenticates its own state fold and is the sole caller of the ciphertext Adapter and exact Target TLS routes. | RKE2 workload | Haskell lifecycle plus chart runtime, retained Lifecycle Authority, selected Target Agent, retained-home custody/TLS DEK lanes, and TLS Retention Adapter | Exact Kubernetes TLS Secret, LongLived ciphertext/wrapped-DEK object, Authority pending/current fold, and schema-bound EAB custody receipt |
| DNS control plane | Route 53 hosted zones with one canonical public record per active substrate, projected from that substrate's validated served FQDN | AWS | Home: exact Gateway-DNS reconcile/read-back capability. AWS: registered Lifecycle Authority provider intent reconciles the A record to Envoy NLB targets through the typed `DnsRecordProgram` — the Provider Worker holds a `DnsOwnerAuthority` minted from its own role/substrate pair, the coordinate's AWS account is observed from `sts get-caller-identity` and its ownership epoch from the retained Authority epoch, and the mutation is preceded by an observation and followed by a converging read-back (Sprint `4.72`). The SES verification/DKIM/MX writer runs through the same typed program under its own `AwsSesDnsOwner` — a `LongLived` lane distinct from the per-run provider lane, admitting exactly `{TXT, CNAME, MX}` — with one ensure per coordinate and one shared propagation barrier (Sprint `4.73`). `edge status` is read-only and EKS Gateway DNS is disabled. | Route 53 |
| Gateway workload | Mesh/DNS-only Haskell Gateway Runtime | Target: dedicated in-cluster StatefulSet identities, ServiceAccount, Services, resource envelope, bounded mailboxes, and encrypted identity-bound retained journals. Sprint `2.32` supplied the typed substrate-exact claim/Lease projection and completed Sprint `3.26` supplied chart/render consumption; the active production selection remains pre-cutover. | `prodbox charts reconcile gateway` plus `prodbox gateway status --config <path>`. One actor owns each target emitter's stage/fsync/publish/commit/fsync sequence. The target Gateway has no bootstrap, lifecycle, Pulumi, generic object-store, authority-clock/CAS, provider, or target-secret routes. `runGatewayDaemon` remains `LegacyModelBEmitter`; activation and removal ownership live only in the Development Plan/legacy ledger. | Bounded semantic mesh state, Vault-issued gateway credentials, and a gateway-local retained emitter journal |
| Cluster storage class | `manual` (`kubernetes.io/no-provisioner`, `Retain`) — the only StorageClass on **both** substrates; the PV volume source differs (`hostPath` on home, pre-created EBS via CSI `volumeHandle` on EKS), no dynamic provisioning | RKE2 + EKS cluster | `prodbox cluster reconcile` / `charts reconcile` | Cluster-scoped resource |
| Manual PV root (home substrate) | Configured host path, default `.data/`; on EKS the durable store is the pre-created EBS volume set (see the AWS retained block storage row) | Host filesystem / EBS | `prodbox.dhall` plus cluster and chart lifecycle commands | PV contents only |
| AWS retained block storage | Pre-created EBS volumes lifted in as static `Retain` PVs (CSI `volumeHandle`, `topology.ebs.csi.aws.com/zone` affinity), the EBS analog of `.data/`. **Current correspondence:** the registry has two distinct identities — `aws-ebs-volumes-per-run-test :: PerRun` and `aws-ebs-volumes-production-retained :: LongLived` — so lifecycle class is fixed statically and tags validate ownership/coordinate/spec after descriptor selection rather than determining class; the legacy reaper still accepts provider delete success without exact re-observation and can skip for unavailable credentials. **Target:** exact absence read-back is required for completion; partial or unobservable cleanup remains incomplete. The destructive AWS `volumeHandle` proof remains a non-blocking substrate-parity live-infra axis tracked in [substrates.md](substrates.md), and implementation ownership remains in the [plan status](README.md#resume-here). | EKS cluster | `charts reconcile` + the EBS managed-resource registry + `prodbox test integration eks-volume-rebind --substrate aws` | PV contents only; retained (production) or exact-cleanup-managed (test-scoped) |
| Master seed (gateway-owned) — **retired and removed** | None | None | Sprint `3.19` removed the HMAC-derivation model outright: there is no `master-seed` object in MinIO, no `Prodbox.Secret.{Derive,MasterSeed,Inventory}`, no `/v1/secret/*` gateway RPC, no daemon-only-seed lint, and no `selfBootstrapOwnSecrets`. Every previously-derived secret is now a Vault KV object fetched via Vault Kubernetes auth. See [secret_derivation_doctrine.md](../documents/engineering/secret_derivation_doctrine.md), [vault_doctrine.md](../documents/engineering/vault_doctrine.md), and the completed rows in [legacy-tracking-for-deletion.md](legacy-tracking-for-deletion.md) | Removed in Sprint `3.19` |
| Vault (sole per-cluster operational secret / KMS / PKI root) | In-cluster Vault with Transit, KV v2, PKI, Kubernetes auth, and root/child seal models | Dedicated durable Vault workload on both substrates. **Init-once / unseal-on-rebuild** remains invariant. | Broker owns bootstrap/signing trust; core Authority owns only state keys; Backup/TLS Adapters alone read their store identities; Provider Worker alone reads provider identity; Target Agent owns allowlisted target CAS/receipt commitments plus an exact TLS lane and, on home, TLS DEK Transit and the closed SES-SMTP/ACME-EAB custody family; Gateway reads only role-scoped mesh/home-DNS material. Credential/Admin/External-Material Jobs receive their disjoint active permits and ephemeral ingress; only the bounded first-reconcile AWS-admin provisioning session may accept a finite sequential identity-permit set. | Durable Vault PV; persists across rebuilds whenever retained state is preserved |
| Chart platform | Helm plus Haskell orchestration | RKE2 workloads | `prodbox charts ...` | Cluster resources plus retained roots |
| Cluster-wide Patroni operator platform | Percona operator for PostgreSQL installed by the canonical Helm lifecycle | RKE2 workload | `prodbox cluster reconcile` plus registry-backed steady-state image sourcing | Cluster resources |
| External application PostgreSQL HA | Percona-operator-backed Patroni PostgreSQL clusters with exactly three PostgreSQL replicas and synchronous replication, rendered from namespace-local application chart inputs and reconciled by the cluster-wide operator | RKE2 workloads | Haskell chart platform plus registry-backed steady-state image sourcing | Cluster resources plus retained roots |
| Namespace-local identity stack | `keycloak` behind Envoy on the shared public hostname, with the supported issuer, proxy-header, and shared-path contract for chart-managed workloads; new login, refresh, and later JWKS refresh depend on this workload while the steady-state JWT hot path remains local to Envoy | RKE2 workload | Haskell chart platform plus external Percona-operator-backed Patroni PostgreSQL dependency | Cluster resources plus retained roots |
| Namespace-local app stack | `vscode`, `api`, `websocket`, and supported operational dashboards behind Envoy on explicit paths under the substrate's validated shared public hostname | RKE2 workload | Haskell chart platform | Cluster resources plus retained roots |
| Shared realtime state | Redis backing supported WebSocket workloads today and any later explicit external rate-limit service, never Envoy JWT validation, and not yet shipping a standalone rate-limit-service workload on the current supported path | RKE2 workload | Haskell chart platform plus public workload doctrine | Cluster resources plus app state |
| AWS admin credential isolation | Elevated/admin bytes enter only a permit-selected attested Credential Provisioner/Admin Action Runner (or the post-export Decommission Runner) through authenticated prompt stdin and are discarded with that process. Lifecycle-provider, Authority-backup, TLS-retention, Gateway-DNS, cert-manager-DNS01, and SES-SMTP identities have disjoint policies, keys, Vault generations, consumers, and lifecycle-class-correct cleanup nodes; normal provider sessions cannot outlive their committed intent. | Prompt/TestPlaintext boundary | Permit-indexed Jobs, IAM harness, and durable operator-material workflow | Test fixture only for admin input; separate role-specific Vault paths and IAM resources for runtime identities |
| Pre-cutover AWS teardown residue policy | Historical `Prodbox.CLI.Command.PulumiResiduePolicy` flag/bypass ADT and harness postflight projections. It may orchestrate the explicit `prodbox aws stack <cli-verb> destroy --yes` surfaces, but its global residue answer is not exact stack evidence. The target cascade uses exact per-stack/family observers and adapters; LongLived resources remain outside ordinary teardown. Removal ownership lives in the [legacy ledger](legacy-tracking-for-deletion.md#pending-removal). | `prodbox aws teardown` flag surface + harness postflight | Haskell CLI + IAM validation harness | Current legacy behavior only; target checkpoint decisions use independent primary/backup observations and either a write-ahead manifest or a separately typed, plan-confirmed and read-back `ConfirmedLegacyAdoptionManifest` for the bounded pre-manifest stack families. |
| Durable cleanup kernel | Lifecycle-owned `CleanupRun` journal and exact desired-absence graph | Landed core: `Prodbox.Lifecycle.CleanupRun`, `CleanupRunRunner`, `Prodbox.Lifecycle.Teardown.{Model,Registry,Decision,Program,Graph,Execution,Report,CleanupProgramDescriptor,RecoveryCapability,RecoveryRequirement,RecoveryPlane}`, their hidden proof implementations, authenticated Lifecycle Authority cleanup/recovery routes, and the validation client in `Prodbox.Test.LifecycleCleanupClient`. `TestRunner` has cut over; the public destructive CLI remains pre-cutover. | Exact selected per-run stack, controller-family, DNS, EBS, IAM/key, and operation obligations are committed before their AWS mutation and resume under stable descriptor-bound operation IDs. Restart reconstructs the selected v5 descriptor program from wire v2, including its optional DNS hosted zone; retained zoneless wire-v1 descriptors admit only the superseded v3/v4 compiler identities and recompile before becoming committed evidence. Different zones produce different graph and descriptor identities, while a missing zone preserves the prior graph digest exactly. The ordinary dispatcher commits and reads back the Authority-owned report. Cascade proof joins are landed but do not become the public writer until Sprint `6.5`. | Cleanup obligations, ownership manifests, exact observations, durable node outcomes, pre-uninstall report/permit, local absence, completion receipt |
| Long-lived Pulumi state backend | Immutable encrypted `aws-ses` checkpoint blobs plus one versioned Lifecycle Authority aggregate. Primary bytes live in MinIO; the dedicated Backup Adapter must read back exact envelope/blob copies in the retained S3 backup coordinate before effects. Legacy first-touch input is a separate optional source. | `src/Prodbox/Lifecycle/Authority/`, target Backup Adapter, `src/Prodbox/Pulumi/EncryptedBackend.hs`, `src/Prodbox/Infra/AwsSesStack.hs`, `src/Prodbox/Infra/LongLivedPulumiBackend.hs` | Mandatory primary MinIO + independent S3 backup; optional legacy import only |
| Lifecycle reconciliation doctrine | A pure lifecycle-indexed registry prevents `LongLived` resources from entering cascade; flat exact-keyed external observations keep checkpoint usability, provider resource truth, and escape-audit evidence nominally separate; `Partial`/`Unobservable` remain complete observations but select typed refusal. Total desired-absence decisions choose verified checkpoint, backup restore, complete write-ahead or confirmed-legacy adoption manifest, already-absent, or refusal. Result-indexed programs feed one durable graph; pre-uninstall readiness and post-uninstall cascade completion are distinct private evidence types. | The `Prodbox.Lifecycle.Teardown.*` and lifecycle-owned `CleanupRun*` foundations now exist. Exact checkpoint/drain/ownership/recovery repositories and opaque public facades are present; the active public composition still uses legacy teardown. The credential-session-bound Provider operation and locked `Prepared -> Absent` host-completion protocol remain required before the replacement can prove completion. | Registry, observations, ownership/adoption manifests, cleanup runs, absence evidence, backed-up pre-uninstall report/permit, local completion receipt |

## CLI and Runtime Layer

| Surface | Command | Purpose |
|---------|---------|---------|
| CLI frontend | `prodbox <command>` | Parse the closed command surface and dispatch directly to native Haskell commands with no legacy delegation shim |
| Config management | `prodbox config setup|show|validate|schema|generate` | Author and validate the executable-sibling Tier-0 boot/proposal payload. Once Lifecycle Authority is available, observe or submit the exact `ConfigObserve`/`ConfigProposeCas` program under one capability reference; proposals carry expected epoch/generation, produce an immutable encrypted blob, require read-back, and CAS the aggregate reference. `show` consumes a role-scoped projection. Direct root-token, Gateway, and MinIO config transports are pre-cutover legacy owned by Sprint `4.50`. See [config_doctrine.md §0](../documents/engineering/config_doctrine.md#0-three-tier-config-model). |
| Host prerequisites and diagnostics | `prodbox host ensure-tools|check-ses-readiness|check-ports|info`, `prodbox host firewall gateway-restrict|gateway-unrestrict` | Verify local tools and diagnose host/network prerequisites. `host check-ses-readiness` runs the same read-only sender/DKIM, MX/rule, and operational capture-canary list/get prerequisite observations without reconciling AWS state; it prints Ready only when all three semantic scopes pass and otherwise preserves the structured fail-closed reason plus retry-the-same-validation remedy. |
| Cluster lifecycle | `prodbox cluster reconcile|delete --yes|delete --cascade|status|health|start|stop|restart|logs|wait|workload-logs` | Local-only delete preserves `.data` and makes no AWS claim. Target cascade starts/resumes recover-to-clean and uninstalls only from `ReadyToUninstallEvidence`; `CascadeCompleteEvidence` additionally requires exact `LocalUninstallEvidence` and its scoped read-back `LocalCompletionReceipt`. Incomplete runs return `CleanupRunId`, exact failures, and `RecoveryPlaneDisposition`, claiming a live plane only for `Established`. |
| Registry reconcile and image population | `prodbox cluster reconcile` | Allow direct public pulls only for the registry and the registry's storage backend before the registry is healthy and externally serving, require a stable `GET /v2/` external window (expect 200/401, six consecutive rounds), auto-create the `prodbox/` repositories on first anonymous push, and ensure required custom and public images are present in the registry before later Helm deployments, retrying alternate configured public-image candidates when a preferred source fails during registry publication, including `mirror.gcr.io` fallbacks for the Docker Hub-hosted Percona and Envoy images used by the supported lifecycle |
| AWS stack lifecycle | `prodbox aws stack eks|aws-subzone|test|aws-ses reconcile`, `prodbox aws stack eks|aws-subzone|test|aws-ses destroy --yes` | Provision and tear down every AWS substrate Pulumi stack (`aws-eks`, `aws-eks-subzone`, `aws-test`, `aws-ses`) per [substrates.md](substrates.md). EKS and test-stack desired-presence commands require the validated `aws_substrate.profile`; EKS additionally requires `cluster_topology.Eks.node_group_size`. The profile is carried inside the signed provider intent and projected onto each program's declared config keys. Per-run vs long-lived lifecycle classification lives in [substrates.md → Resource Lifecycle Classes](substrates.md#resource-lifecycle-classes). |
| Public-edge diagnostic | `prodbox edge status`, `prodbox edge reconcile` | Classify Route 53, Gateway API, TLS, port `80` redirect readiness, shared-host path routing, advertisement mode, and readiness for named external proof; on AWS it also reconciles the delegated-subzone A record to the Envoy NLB IPv4 targets |
| Gateway runtime | `prodbox gateway start --config <path>|status --config <path>|config-gen <output-path> --node-id <node-id>` | Run and inspect the bounded mesh/DNS daemon. Operational readiness is a cached actor/journal/admission projection; lifecycle/bootstrap/object-store/target-secret routes are not served. |
| Public workload runtime | `prodbox workload start --config <path>` | Run the internal API runtime or WebSocket runtime selected through the `workload.mode = Api \| Websocket` field of the mounted Dhall config per [config_doctrine.md](../documents/engineering/config_doctrine.md) (Sprint `3.14` removed the legacy `PRODBOX_WORKLOAD_MODE` path; the mounted config is the only supported selector) for the chart-managed workloads behind the shared public hostname, including the real `/ws` upgrade, one-connection-per-pod lifetime, and readiness-based drain contract |
| Chart runtime | `prodbox charts list|status <chart>|reconcile <chart> [--dry-run] [--plan-file <path>]|delete <chart> [--yes] [--dry-run] [--plan-file <path>]` | Manage the supported `gateway`, `keycloak`, `vscode`, `api`, and `websocket` chart surfaces, with `gateway` remaining the separate distributed gateway daemon path and the shared-host browser, API, WebSocket, and MinIO console routes staying behind Envoy with Keycloak-backed auth and RBAC |
| Vault lifecycle | `prodbox vault status|init|reset-ambiguous-initialization --yes|unseal|seal|reconcile|rotate-unlock-bundle|rotate-transit-key <key>|pki status|pki issue-test-cert` | Compile the request into a closed Bootstrap Broker program when Vault is sealed or uninitialized, or a least-privilege post-unseal Vault program otherwise. Ambiguous initialization can advance only through the confirmation-gated typed proven-pristine reset; the host cannot name or assert its reset proof or storage target. Physical reset failures and applied-initialization ambiguities cross the engine boundary only as closed payload-free algebras, are logged only for their respective routes, and retain generic public responses. The init response's redundant hex/base64 share projections must decode to equal encrypted bytes before the redundant projection is discarded. One operation-indexed `CapabilityRef` is observed, admitted, and executed under one absolute deadline; generic Gateway and host-root-token fallbacks are excluded from the target path. Implementation and cutover status live in [README.md](README.md#resume-here). |
| DNS check | `prodbox dns check` | Inspect DNS ownership state |
| Cluster utilities | `prodbox cluster health|wait|logs|workload-logs` | Inspect cluster readiness and collect infrastructure or workload logs |
| Interactive onboarding | `prodbox config setup` | Guided authoring and validation of non-secret Tier-0 boot/proposal coordinates only; credentialed effects use their explicit lifecycle command and permit path |
| AWS IAM, quota, and EBS maintenance | `prodbox aws policy|setup|teardown [--destroy-pulumi-residue | --allow-pulumi-residue]`, `prodbox aws quotas check`, `prodbox aws quotas request`, `prodbox aws ebs reap-test --yes` | Reconcile distinct Lifecycle-provider, long-lived Authority-backup/TLS-retention/home-DNS/SES-SMTP, and AWS-run DNS01 identities through the durable operator-material workflow. Identity create/rotate/remint and repair-time key deletion use a mode-indexed Credential Provisioner; `DestroyAwsSes` and migrate/compatibility/quota actions use the Admin Action Runner; normal EBS/provider work uses the fenced Provider Worker and can never own SES-SMTP IAM. Ordinary teardown removes only Operational provider/AWS-run DNS01 identities; SES-SMTP is retained until explicit `DestroyAwsSes`, TLS/home-DNS may leave through explicit consumer decommission, and exported-manifest `nuke` owns total retained teardown and the backup/shared-bucket tail. The legacy single-user/`aws.*` flow remains in the [removal ledger](legacy-tracking-for-deletion.md#pending-removal). |
| TLA+ validation | `prodbox dev tla-check` | Run formal verification |
| Test runner | `prodbox test lint|all|unit|integration ...` | Run named lint, unit, and integration suites on the Haskell stack, including the shared idempotent AWS IAM harness used by the named and aggregate IAM validation surfaces. A cluster-backed AWS harness establishes the retained Authority/config bootstrap floor before Lifecycle-provider credential repair, then re-enters ordinary local-only reconcile after repair so the Provider Service and strict deep readiness are proved before any Provider-backed prerequisite; the pre-credential floor does not admit the Provider, and pure IAM-only suites create no runtime. |
| Code quality | `prodbox dev check` | Run the required doctrine, formatting, lint, and type-check gate with governed doctrine-alignment enforcement |

## Validation Layer

The validation layer is one canonical test suite that runs against substrates (see
[substrates.md](substrates.md)). Validations are described as suite content with declared
prerequisites, not as substrate-specific concerns. The same validation runs on every substrate
that satisfies its declared prerequisites.

| Surface | Canonical Validation |
|---------|----------------------|
| Build artifact contract | Runnable `prodbox` binary at `.build/prodbox`, produced by the canonical build-plus-copy flow; container build proof closes when the canonical Dockerfiles under `docker/` emit artifacts under `/opt/build` through in-image `ghcup`-managed GHC `9.12.4` with no symlinked Haskell tool shims |
| CLI and env contract | `prodbox test integration cli` and `prodbox test integration env` run built-frontend Haskell suites against the direct-Dhall config contract without recreating `prodbox-config.json`. Fake Helm interpreters implement the same authoritative pre-upgrade status contract as production: exact not-found output means absent, and only explicit `.info.status` JSON means present; successful empty output is never a valid observation. The fake Docker interpreter likewise serves production's exact machine-formatted dangling-image inventory: empty output is observable absence, while an explicitly populated canonical managed row must be removed exactly once and read back absent through the production classifier. Its exact image-inspect format separates the plain local config/image identity from the JSON repository-qualified manifest inventory and returns distinct canonical digests for those two layers. The fake Kubernetes filename boundary stores a successful apply and returns those stored bytes on the independent exact get; absent storage refuses and malformed storage remains a production decode refusal. The fake Authority Backup forward accepts only production's exact Deployment target, derives both ports from its requested mapping, explicitly refuses the obsolete Service target, owns the fixture server as a child, proves exact loopback readiness before emitting the production acknowledgement, and retires and waits for that child on forward teardown. |
| Canonical test suite | Existing named validations plus stable `TEARDOWN-2026-08-15`: a mutation-sensitive exact-keyed reference oracle, constant causal profile, separate production profile, deterministic fault matrix, and a lifecycle-kernel client boundary. Standard P requires the complete revision/config/substrate/fault/cleanup evidence in [README.md](README.md); historical green component probes cannot qualify the refactor. |
| Substrate provision/teardown | Current home provision is `prodbox cluster reconcile` plus chart reconcile; local-only delete is `prodbox cluster delete --yes`. Target cascade is `prodbox cluster delete --cascade --yes`. Current AWS provision is `prodbox aws stack eks reconcile`, `prodbox aws stack aws-subzone reconcile`, and `prodbox aws stack test reconcile`, with corresponding explicit `destroy --yes` commands. Sprint `7.36` supplies the exact desired-absence adapters; activation of the single public route remains qualification-gated cutover work. The suite consumes these substrate owners but does not own them. |
| Substrate parity | Target parity is the substrate-local Broker/Target-Agent/Gateway set plus the same application/platform services, with AWS using an exact client reference to the single retained home Authority. Role-specific transports, credentials, config generations, static retained storage, registered DNS records, resource isolation, `LCPC-2026-07-11`, and always-run cleanup must be proven independently. Historical June/Sprint-`7.32` gateway-port-forward runs remain evidence only for the pre-cutover topology; current requirements and sole status ledger are [substrates.md](substrates.md) and [README.md](README.md#deployment-qualification). |
| In-cluster registry (`registry:2`) pipeline | `prodbox cluster reconcile`, bootstrap-source proof for the registry plus storage-backend direct-public exception before the registry is externally ready, registry inventory proof for required images after bootstrap, stable `GET /v2/` proof (expect 200/401) before image writes, native-host-architecture custom-image proof with no supported `docker buildx` or cross-arch emulation, and **host `docker` CLI auth isolation** (Sprint `1.47`, ephemeral hostbootstrap-`Registry` pattern): each host-docker flow runs inside `withEphemeralDockerConfig`, which discovers the host `docker.io` auth read-only and writes a throwaway `prodbox-docker-config` `DOCKER_CONFIG` carrying **only** that `docker.io` pull auth (pushes to the `127.0.0.1:30080` registry are anonymous, so no registry credential is materialised) `bracket`-scrubbed on exit — **no `docker login` anywhere**, the operator's global `~/.docker/config.json` is only read (public pulls keep the Docker Hub login), nothing persists in `~/prodbox`; the `docker.io` discovery/projection is the seam to swap onto `HostBootstrap.Registry` at the planned hostbootstrap refactor |
| Gateway runtime suite content | `gateway-daemon`, the Cabal `prodbox-daemon-lifecycle` process suite, `gateway-pods`, and `prodbox dev tla-check`. **Sprint `5.33` (2026-08-11)** moved `gateway-partition` off this row: it is an in-process property test over the `GatewayState` folds with no peer, cluster, or harness, and it now runs in `prodbox test unit` rather than as a named integration validation. It proves the fold composes; it never proved a deployed runtime survived a partition, and its former integration registration invited that reading. |
| Chart platform suite content | `charts-storage`, `charts-platform`, `charts-vscode`, `charts-api`, `charts-websocket`, and the admin-route validations, plus manifest and lifecycle proof that supported Patroni use flows through the Percona operator-backed platform, Keycloak-backed workloads preserve the shared-host proxy-aware issuer contract, Envoy enforces JWT auth and RBAC from explicit bearer-token and JWKS configuration, and the WebSocket path proves real upgrade behavior, Redis-backed state continuity, revocation-driven reconnect, and readiness-based drain |
| Public-host proof suite content | `prodbox edge status`, `public-dns`, `charts-api`, `charts-websocket`, and the admin-route validations on the single-host Gateway API target doctrine. `prodbox edge status` owns the cluster-side `ready-for-external-proof` classification on whichever substrate is active, while the named validations own HTTP-to-HTTPS redirect proof on port `80`, Keycloak shared-host issuer proof, route-level RBAC proof, real WebSocket upgrade behavior, one-connection-per-pod lifetime, revocation-driven reconnect, and readiness-based drain on the substrate that provisioned them. |
| Clean-room handoff | The qualification-only generic/home candidate carries a type-indexed cutover, exact completion receipts, and legacy-absence machinery. Two home candidate cycles are non-blocking Standard-P evidence. Only a proven current-revision home qualification may authorize public activation and legacy deletion; the resulting post-activation identity must be qualified again before the ledger row completes. AWS checkpoint-kubeconfig/adapters remain a separate plan-tracked residual; the three exact stack destroy commands and repeated cascade are non-blocking prerequisite evidence for final aggregate qualification. |
| AWS IAM lifecycle | **Target:** the harness simulates the operator prompt, idempotently reconciles/deletes every registered role-specific IAM user/key/role, delivers generations through the appropriate Target Secret Agent, validates each exact capability, and uses the durable cleanup DAG. IAM removal depends on all credential-consuming AWS/DNS/EBS cleanup; one role's credential can neither authorize nor stand in for another. **Current revision.** The generic Tier-0 `aws.*` projection and legacy host reader family remain named by the removal ledger, but per-run cleanup is descriptor-bound and its exact lifecycle node decision gates the legacy operational-credential teardown. Closing that final credential dispatcher is Sprint `6.5`; the former `secret/gateway/gateway/aws` coordinate is already removed. |
| Derived restore graph executor | `TestRunner` projects the typed restore plan through `runDerivedRestoreGraph`, whose total executor records every runnable/blocked result. Per-run cleanup separately uses the lifecycle-owned graph, closed dispatcher, stable operation IDs, and aggregate report; the validation layer retains neither a cleanup graph nor a callback executor. |
| Derived workload resource contracts | `Prodbox.Capacity.Derivation` compiles typed runtime-memory, service-demand/calibration, bounded ephemeral-scratch, durable-storage, replica, concurrency, and surge inputs into the only deployable `ResourceEnvelope`. `ResourcePlan` carries derivation inputs and proof output rather than an independently authored envelope (Sprint `1.71`); chart placement consumes the proof in Sprint `3.27`. |
| Measured calibration profiles | Committed artifacts under `dhall/capacity/measured/<profile>.dhall` certify empirical inputs used by the pure derivation algebra: service cost at a named reference CPU, runtime overhead, throttle observations, provenance, and staleness. The recorder accepts only a healthy run with at least a thirty-minute steady window (Sprints `1.65`/`5.21`). Measurements never become a parallel envelope-authoring surface. |
| Legacy escape registry | `src/Prodbox/Legacy/EscapeRegistry.hs` proves two independent things. The **bijection** requires a registry↔`LEGACY-ESCAPE[…]`-marker match over the declared set: today the `aws` CLI subprocess object-store path, the generic Tier-0 `aws.*` Lifecycle-provider aggregate and its two host readers, the host-direct MinIO transport, and the bespoke cascade executor. The **coverage layer** (Sprint `4.84`) binds each of those seams to the exact source symbol that names it and the complete set of files allowed to mention it, so a mention anywhere else fails `prodbox dev check` even when the bijection stays satisfied, a declared site whose symbol is gone fails too, and every coverage rule anchors on a registered marker entry of its own category. Each entry carries a typed category, an owning sprint, and a deletion condition. The check is now comprehensive over the ledger-named seams; it remains a legacy-*inventory* proof, not legacy-*absence* evidence, because those seams are still live and still Pending Removal. Removed gateway/operator-secret sites must not be reintroduced. |
| Controller-ownership derivation | Which registered stack's controllers own a registered resource family is computed from two registry coordinates rather than authored: `Prodbox.Lifecycle.Teardown.Model.coordinateControllerOwnerCluster` reads the owning cluster out of a family's `kubernetes.io/cluster/<name>=owned` tag (a `shared` value answers no owner, because a shared resource outlives the cluster by design), and `coordinateProvisionedClusterName` gives a stack's Pulumi stack name the `pulumiEksClusterNameSuffix` that `pulumi/aws-eks/Main.yaml` applies. `Prodbox.Lifecycle.Teardown.OwnershipManifest.registeredOwnershipEdges` is the join; the write-ahead manifest seeds from it and `Prodbox.Lifecycle.Teardown.Program` derives its controller-backstop and stack-destroy edges from it, so manifest membership and destroy order cannot disagree. It replaced a literal naming `aws-test` as owner of a family keyed on the EKS cluster's ownership tag, which had left the `aws-eks` write-ahead manifest unable to record the volumes its own cluster created. `prodbox dev check` fails on a controller-owned family whose cluster no registered stack provisions, because an absent edge otherwise reads as no owner. |
| Registered stack identity source | `Prodbox.Lifecycle.Teardown.Registry` is the compiled source for the three per-run Pulumi stack names, each stack's Pulumi project (`prodbox-<stack name>`), and the EKS cluster name (`<stack name>` plus `pulumiEksClusterNameSuffix`, the rule `pulumi/aws-eks/Main.yaml` applies). The per-run EBS family's cluster ownership tag is built from that derived cluster name, so its owning stack is true by construction. `Prodbox.Lifecycle.LiveResidue` and `Prodbox.Infra.AwsEksTestStack` are projections GHC keeps equal; the LongLived `aws-ses` stack remains on its explicit retained-stack observer rather than entering the per-run registry. `prodbox dev check` reads the `name:` field of every `pulumi/*/Pulumi.yaml` and fails when a registered stack names a project no program declares, or when a coordinate's project and stack names disagree with the derivation. |
| Provider admission fence | `Prodbox.Lifecycle.Authority.ProviderAdmissionEpoch.Internal` holds the retained Lifecycle-provider admission epoch and, since Sprint `4.85`, the two transitions into it: binding the serving credential generation, and the Cascade-audit freeze that fences fresh Provider submissions while reserving the terminal-audit submission. Both are package-private `AuthorityAdmissionCommand`s over the same retained aggregate the submission path reads, so the freeze's pending-work proof and its fence are one step and the proof is projected from the aggregate rather than supplied by the caller. The fence admits exactly the submission keys its binding reserved — refusing all of them would fence the audit the freeze exists to run. Freezing without a bound generation, over pending work, under a different binding, or after revocation is refused; an identical re-freeze is idempotent. Both commands are issued by the authenticated `authority/control` route through `AuthorityControlPayload`, whose projection onto the closed `AuthorityControlRoute` vocabulary is what the disposition join reads; the freeze binding and its smart constructor left the package-private facade for that route while every way to *apply* one stayed private. Revocation still has no transition. |
| Operational-credential disposition blockers | `Prodbox.Lifecycle.Teardown.OperationalCredentialInventory.dispositionBlockerEvidence` is total over the closed blocker universe and says how each blocker is established. Since Sprint `4.85` closed (2026-08-18) **every one of the eight is recomputed** by `Prodbox.Lifecycle.Teardown.OperationalCredentialCoverage` from a source that can stop being true: the compiled teardown programs and their emitted dependency graph, the credential-consumer classifier, the legacy identity's derived status, the closed Authority control-route vocabulary, the canonical revocation read-back decision table, and the teardown Provider dispatch key's cleanup ownership. The published list is empty, and `TypeLevelAbsence` — the kind that carried a "no constructor exists" fact no value could witness — was deleted with the last blocker that used it. `validateOperationalCredentialCoverage` compares published against measured in both directions, so a blocker that becomes true again is an unpublished-blocker failure rather than a silently lost reason. |
| Registered-target executor join | `src/Prodbox/Lifecycle/Teardown/RegisteredTargetExecutor.hs` names, as a total function of the closed registry key enumeration, which production executor runs a registered target's observe/reconcile/read-back operations. `Prodbox.Lifecycle.Teardown.AwsRegisteredTargetInterpreter` dispatches its three entry points on that function instead of ad-hoc `(kind, key)` guards, so the classifier and the interpreter cannot disagree, and its two unexecutable arms stay distinct: a key that should never reach this interpreter, and a key whose adapter is unbuilt. `prodbox dev check` fails when a descriptor with no executor projects onto a surface for which `cleanupSurfaceMintsCompletionEvidence` is true — because a registered descriptor compiles a mandatory absence read-back, and a surface that mints completion asserts every one of them succeeded. The gate is a derivation over one total function rather than an exemption list, so a stale exemption is not representable. As of 2026-08-21 there is no gap: Sprint `7.36` landed the `dns-aws` validation hosted-zone adapter and the retained-EBS adapter, so `registeredTargetExecutorFor` is total into `Right`. `RegisteredTargetAdapterGap` now has no constructors, which is what keeps the gate live rather than retiring it — declaring a gap is the only way to produce one, and adding a key without an executor is still an exhaustiveness failure. On 2026-08-22 the same sprint added `Dns01ChallengeRecordFamilyExecutor`, the first executor whose reconcile step is **not** a Provider mutation: its observation and mandatory absence read-back are Route 53 reads through the Provider, and its removal runs through the interpreter's separate Kubernetes-scoped execution arm. |
| Bounded legacy-adoption planner | The wall between discovery and authority for a stack whose checkpoints are unusable. The expected family is closed and registry-derived — the stack plus exactly the registered families whose controller-owner cluster is that stack's — so an observation outside it is an extra candidate that refuses the plan rather than widening it. Missing, extra, duplicated, unobservable, and cross-claimed-identity candidates are five distinct refusals and none produces a smaller plan; an observed-empty family is recorded as empty rather than omitted. The rendered document is exactly what the digest hashes, so an operator confirming a digest confirms those bytes; an admin permit naming that exact digest is the only thing that turns a plan into a confirmed one, and a confirmed plan is the only thing `bindConfirmedLegacyAdoptionManifestForCleanup` accepts. | `src/Prodbox/Lifecycle/Teardown/LegacyAdoptionPlan.hs` and the adoption binder in `src/Prodbox/Lifecycle/Teardown/OwnershipManifest.hs` |
| Registered-target Kubernetes execution arm | `Dns01ChallengeOwnerDeleteBoundary` on `Prodbox.Lifecycle.Teardown.AwsRegisteredTargetInterpreter`, beside its Provider boundary. Every other registered target reconciles through `dispatchRegisteredProviderMutation`, because every other registered target is an AWS resource this repository created; the DNS01 challenge TXT is written by cert-manager's DNS01 solver, which holds a finalizer over it and would rewrite a record deleted underneath it, and the Provider has no Kubernetes capability by construction. The arm is reachable only with an authorization a **present** observation of the family mints, it returns a mutation attempt rather than an absence, and a composition with no Kubernetes access installs `refusingDns01ChallengeOwnerDeleteBoundary` so the inability is a value the cleanup report can carry rather than an unset field. The production implementation lists cert-manager's challenges with their `spec.dnsName`, derives each owner's record name through the one prefix constant the registry coordinate and the creator already read, deletes exactly the owners the observation named, treats a malformed listing row as making the whole listing unusable, and reports an observed record no surviving owner claims as a refusal naming it. | `src/Prodbox/Lifecycle/Teardown/Dns01ChallengeOwnerDeleteInterpreter.hs` and `src/Prodbox/Lifecycle/Teardown/Dns01ChallengeRecordAdapter.hs`, composed by `Prodbox.Lifecycle.Teardown.CloudRuntimeProduction` |
| Ephemeral Kubernetes client | The one statement of the machinery both teardown paths run `kubectl` through: a kubeconfig written into a private temporary directory with `O_EXCL`/`O_NOFOLLOW` and owner-only mode, a bearer token served through a FIFO so the credential never lands on disk, an environment scrubbed of `KUBECONFIG` and every ambient AWS credential variable, and a client universally quantified by its continuation so it cannot outlive the directory backing it. Extracted from the drain interpreter by Sprint `7.36` when the DNS01 owner delete became the second caller: two statements of it would be two statements of a security property. | `src/Prodbox/Lifecycle/Teardown/EphemeralKubectl.hs` |
| Total-decommission program-tag parity | `src/Prodbox/Lifecycle/Decommission/ProgramTag.hs` names the semantic total-decommission operation universe and joins the two implementations that each cover part of it: the compiled `TotalDecommission` desired-absence program over the typed registry, and the signed `DecommissionNode` manifest with its graph, interpreter, and runner. Both classifiers are total and their result type is the tag enumeration, so the forward direction is a compile error. `validateDecommissionProgramTagParity` checks the authored implementation claim against both **measured** images — the operations the compiler emits and the manifest universe derived from the closed singleton and mandatory-choice enumerations — and gates `prodbox dev check`. The current measurement is twenty-two tags with four overlapping: the final no-retention escape audit, the home-substrate uninstall, the operator's explicit retained-local-data disposition, and the terminal receipt are the ordered terminal phase implemented on both sides. The twenty-second, added when Sprint `4.85` closed, is the operational-credential revocation the compiled program orders strictly between the terminal audit and the home uninstall; the signed manifest names resource families and has no node for it. The other eighteen are implemented by exactly one side, so neither implementation alone is yet a total decommission. |
| Explicit long-lived aggregate permit | `src/Prodbox/Lifecycle/Teardown/LongLivedAggregatePermit.hs` is the operator decision the `ExplicitLongLived` surface needs before it may report completion. Every other minting surface completes from its own read-backs; this one may not, because a long-lived family is infrastructure the rest of the system preserves, so its removal is a decision rather than a consequence of a run converging. A permit is admitted only when the aggregate it names equals `longLivedAggregateUniverse` — the registry's own `ExplicitLongLived` projection, derived rather than authored, so a newly registered long-lived descriptor widens the requirement and an older permit stops being admissible — under the `AdminActionRunner` audience, an unexpired window, and a nonce. `completeExplicitLongLivedDesiredAbsence` re-checks the permit's run and graph digest against the evidence, so a permit authorizes one destruction rather than standing open. Sprint `7.36`. |
| Code-created AWS resource tagging | `Prodbox.Lifecycle.OwnedResourceTags.CodeCreatedAwsResource` is the closed enumeration of AWS resources this repository creates from a direct AWS call in `src/` rather than from a `pulumi/` program or a Provider Worker intent — the third creation surface, which the disk-reading field-of-view gate cannot see because no provisioning program declares it. Writers take their tag set from it, so the writer, its read-back, and the audit's query catalog hold one value. `prodbox dev check` fails when a member authors no tag, or authors only families `terminalAuditQueryCatalog` does not cover. Its first member is the `dns-aws` validation hosted zone, which was created carrying no tag at all: billable, and returned by no audit query. The same module holds the zone-name prefix that bounds the registered validation hosted-zone family, so the creator, the harness sweep, and the teardown registry read one constant. |
| Terminal-audit field of view | `src/Prodbox/Lifecycle/Teardown/AuditFieldOfView.hs` joins the compiled terminal-audit query catalog to the resources the provisioning programs under `pulumi/` declare, and `prodbox dev check` fails when a resource whose provider type accepts tags authors none the audit queries for — the condition under which a leaked instance is returned by no query and a clean verdict says nothing about it. The program set is enumerated from disk rather than declared, an unrecognized provider type refuses instead of being assumed reachable or exempt, and a program the reader cannot read completely fails rather than contributing a subset. Reach is classified per type by `src/Prodbox/Lifecycle/Teardown/TaggingApiReach.hs`, the leaf module both this join and the audit-time verdict decide reach from; a tag on a §6a registry-owned IAM identity is a backstop, never an ownership authority. The second creation surface is joined the same way: `RetainedDiscoverability` is derived from the tag set a family's production writer authors rather than declared beside it, and `src/Prodbox/Lifecycle/OwnedResourceTags.hs` holds that set once for the writer, its read-back certification, and the audit. |
| Plan dependency direction | `Prodbox.CheckCode.sprintDependencyDirectionViolations` and `pendingRemovalPrerequisiteViolations` make [Standard N](development_plan_standards.md#n-phase-independence-and-execution-order) mechanical. `**Blocked by**` and `**Closure dependency**` are the only sprint fields that may record a dependency, `Pending Removal` rows declare a `**Prerequisite**`, and a live entry naming a later sprint fails the build — as does a dependency-shaped field name the gate cannot read. `compareSprintIds` orders by phase then numerically per segment, so `7.10` sorts after `7.5`. Prose is deliberately not scanned: disclaiming later work is the correct pattern, so Standard N instead requires dependencies to be declared. Reached through `runGovernedDocChecks`, so `prodbox dev lint docs`, `prodbox dev docs check`, and `prodbox dev check` all enforce it (Sprint `0.30`). |
| Static and doctrine gate | `prodbox dev check` — with its region stated, because it is not the repository: the gate formats and lints `app src test`, and since Sprint `5.30` its type-checking build is scoped `all --enable-tests`, which resolves to the library, the `prodbox` executable, **and the eight test suites**. Before `5.30` it was scoped `all` alone, so `test/` was linted here and type-checked nowhere routine. The widening closes the compile gap, not the behaviour gap: `dev check` still runs no test, so a sprint recording only `dev check` plus `prodbox test unit` has still not exercised the integration suite. Sprint `2.95` owns the current gate-local resource correction: the aggregate runs the four existing `dev lint files|docs|haskell|chart` leaves as sequential exact self-child processes, and each exit reclaims that family's working set before the next leaf or warning-clean build begins. Within the Haskell leaf, every file-reading style finding batch is fully forced before the next scan so empty lazy results cannot retain their source graphs. The leaf arms remain the non-recursive child targets. The general rule is "The region of Ring 2" in [resource_scaling_doctrine.md § 2C](../documents/engineering/resource_scaling_doctrine.md) |

## Authority and State Locations

| State Class | Authority | Durable Home | Notes |
|-------------|-----------|--------------|-------|
| Managed-resource definitions | Closed data-only `ManagedResourceDescriptor` values plus indexed `ManagedResourceObserve` / `ManagedResourceEnsure` / `ManagedResourceDestroy` / read-back programs | Pure desired-state planner and boundary interpreters selected by capability kind; descriptors contain no callbacks or `IO` | Target SSoT for everything prodbox can create. Lifecycle class and dependency facts are pure data; observation is flat/exhaustive, planning is total, mutation requires committed intent/fence, and exact read-back closes ensure/absence. Current callback/interpreter-bearing registry fields are pre-cutover residue; live replacement and removal ownership remain solely in the [plan status](README.md#resume-here) and [deletion ledger](legacy-tracking-for-deletion.md#pending-removal). |
| Repository configuration | Operator-authored binary-sibling Dhall | `prodbox.dhall` plus Lifecycle Authority config generation | Tier 0 contains non-secret boot coordinates and role-specific `SecretRef`s only. Deployment-varying values are authored/run-derived and fail closed; fixed product/protocol identities are absent from the schema. Post-unseal config is an immutable encrypted blob referenced by generation/digest in the Authority aggregate and served as role-scoped projections. Test admin plaintext remains only in `test-secrets.dhall`. |
| Test-only configuration fixtures | Test harness only | `test-secrets.dhall` (test-only) | Test-only plaintext (`TestPlaintext`) that simulates operator prompts plus explicit non-secret test-deployment choices consumed by the generated-run-config builder. Never imported by production config, never a production default, never in Vault, never in production. Ephemeral run coordinates derive from `prodbox.test.dhall` rather than being fixture secrets. |
| Host build artifacts | Canonical `cabal build --builddir=.build exe:prodbox` plus copy to `.build/prodbox` | `.build/prodbox` | Operator-facing binary that backs the canonical `prodbox` command surface |
| Container build artifacts | Dockerfiles under `docker/` | `/opt/build` | Canonical container build root |
| Registry image state | `registry:2` plus Haskell lifecycle reconcile | `prodbox/` repositories in the `prodbox-harbor-registry` MinIO/S3 bucket | Custom images and mirrored public images for the native host and cluster architecture only, including the Envoy Gateway target edge image set |
| CLI and doctrine source | Repository worktree | `app/`, `src/`, `documents/`, `DEVELOPMENT_PLAN/` | Version-controlled source of truth |
| Manual PV content root | Host filesystem | Configured path, default `.data/<namespace>/<StatefulSet>/<replica>`; Sprint `4.31` implements deterministic `prodbox-retained-<namespace>-<statefulset>-<ordinal>` PV names, the always-on MinIO/Vault storage inventory, and shared `storageBinding` topology for Patroni and `vscode` | PV contents only |
| Retained artifact store | Host filesystem | `.data/prodbox/artifacts`, with deliveries staged in `.data/prodbox/artifacts.staging` | Exactly the pinned substrate installer, release tarball, checksum, and system-images archive — the four files an offline install reads as one artifact directory — plus the control-plane runtime images the recovery closure loads, because that closure excludes the image Registry in every observed state. Membership is the validated inventory's, in both directions. |
| Chart secrets (derived) — **retired and removed** | None | None | Sprint `3.19` deleted the gateway `/v1/secret/ensure-namespace` path, retired the derivation inventory, and removed host pre-apply of derived Secrets. Gateway event-key consumption, Patroni role materialization, and host/admin helper reads use Vault KV through Vault Kubernetes auth or the host Vault helper. |
| Chart secrets (chart-generated) — **retired and removed** | None | None | Sprint `3.18` moved direct chart consumers to Vault; Sprint `3.19` removed the surrounding generated-secret assumptions and keeps no-regression coverage for chart-template generated secrets. MinIO root, Keycloak admin/OIDC, VS Code `SecurityPolicy`, gateway event keys, and Patroni role materialization are Vault-backed. |
| Vault secret root | Per-substrate in-cluster Vault | Durable Vault PV | Sole fail-closed per-cluster operational-secret/KMS/PKI backend. Bootstrap Broker owns bounded init/unseal/baseline/PKI administration; Lifecycle Authority owns only its config/lifecycle/object namespaces; Target Secret Agent owns allowlisted seal/generation/TLS lanes; Backup/TLS Adapters and Provider Worker each read only their exact role credential; Gateway and cert-manager read only role-specific material. The initial root token is PGP-encrypted to a burn recipient and never used; baseline uses a separately generated, accessor-audited short-lived root session that is revoked and observed absent. Generic host/gateway writers are pre-cutover legacy. See [vault_doctrine.md](../documents/engineering/vault_doctrine.md). |
| Vault capability and policy inventory | Typed operation inventory compiled to distinct Vault Kubernetes-auth roles and policies | Separate Bootstrap Broker, Lifecycle Authority, Backup/TLS Adapters, Provider Worker, Target Secret Agent, Gateway Runtime, permit-created Credential/External-Material/Admin Jobs, and workload identities; no shared generic operator or gateway policy | Every path is allowlisted by operation and authority scope. Target Agents own exact receipt/generation/TLS lanes plus the retained-home closed non-recoverable-material custody family; Lifecycle Authority owns state keys but no runtime AWS credential/plaintext; each Adapter/Worker owns only its role path; Gateway reads only mesh/home-DNS material; Broker has bounded bootstrap/signing trust. Existing shared daemon/host-root-token seams are pre-cutover legacy governed by the [removal ledger](legacy-tracking-for-deletion.md#pending-removal). |
| Cluster-federation custody | Parent Vault transit-seal trust tree, mediated by dedicated bootstrap and target capabilities | Parent and child durable Vault PVs | Parent/child initialization encrypts the initial root token to a pinned/audited burn public key whose private material existed only inside an isolated destructive ceremony, was never exported, was destroyed before adoption, is never accepted, retained, or available to prodbox, and has no known holder; prodbox never decrypts or uses the initial token. It durably acknowledges encrypted recovery-share receipts, then uses separately generated accessor-audited short-lived root sessions. Parent custody is delivered by an attested, generation-checked Target Secret Agent; Gateway peer membership confers no Vault custody or secret-write authority. See [cluster_federation_doctrine.md](../documents/engineering/cluster_federation_doctrine.md). |
| Vault prepared-init and unlock custody | Operator recovery material (Tier 1) | Durable bootstrap MinIO: password-AEAD `PreparedInitEnvelope`, encrypted init-response receipt, then final Argon2id/ChaCha20-Poly1305 unlock bundle; no host-disk copy | Broker read-backs the prepared PGP private-key envelope before init, persists the encrypted response, promotes/read-backs the final bundle, then deletes/read-backs preparation. Applied-without-response permits only proven-pristine reset; its one-shot result carries a closed payload-free failure class to the protected diagnostic boundary without changing the durable root journal or publishing Vault bodies. Its exact StatefulSet Scale observer treats an omitted `spec.replicas` as Kubernetes' protocol-defined zero while retaining exact object identity and resource-version validation. Only Broker may consume these objects; Gateway, Authority, and Agents never receive them or the static MinIO credential. See [vault_doctrine.md](../documents/engineering/vault_doctrine.md). |
| AWS substrate per-run state | Lifecycle Authority and fenced Pulumi worker | Primary immutable opaque blobs/aggregate in retained home `prodbox-state` plus receipt-committed encrypted copies at the independent S3 backup coordinate; first-touch migration is fenced | Pulumi state for `aws-eks`, `aws-eks-subzone`, and `aws-test`, recovered by `OperationId` after response loss or exact backup restore after primary loss. The stable identity of one created stack is a separate retained record at the run-invariant `authority/registered-stack-generations/<digest>` slot: its key names the registered resource, compiled coordinate digest, registry revision, local foundation, AWS account/region, and create/destroy cycle ordinal, and carries no fact about the run or surface that created it, so a later cleanup run addresses it without knowing the creating run's scope. Which cycle a create owns comes from one retained cursor per series at the distinct `authority/registered-stack-generation-series/<digest>` slot; reservation is idempotent in the admitted create operation, so a lost response cannot burn a second cycle. These records exist and are proven; production producers and consumers are not cut over to them (Sprint `4.84`). |
| Long-lived `aws-ses` checkpoint authority | Dedicated retained Lifecycle Authority; never Gateway Runtime | Primary immutable encrypted blobs/CAS aggregate in retained home MinIO plus receipt-committed exact envelope/blob copies at the independently credentialed long-lived S3 backup coordinate | Provider mutation holds only a narrow fence. No external effect runs before the backup commit receipt. Primary loss restores exact bytes and verifies every digest before a greater epoch opens; propagation and target delivery otherwise resume through the durable outbox. |
| Registered-stack lifecycle generation | Lifecycle Authority; minted only from an admitted create plus a Provider-proven AWS scope (`ProvenProviderAwsSession`) | `authority/registered-stack-generations/<digest>` for each cycle and `authority/registered-stack-generation-series/<digest>` for the series cursor, both in the retained `ClusterRetained` namespace | The run-invariant identity of one created stack: registered key, compiled coordinate digest, registry revision, local foundation, AWS account/region, and the ordinal distinguishing successive create/destroy cycles. Nothing about *who was running* enters the key; causal provenance is recorded and never consulted during selection. `Prodbox.ControlPlane.RegisteredStackCreationProducer` reserves the cycle and commits the generation before the run-scoped creation binding; `Prodbox.ControlPlane.RegisteredStackCleanupSelection` reaches the current cycle through the cursor and then the generation, refusing an unopened series rather than inferring one from residue. Cutover of the cascade and per-run cleanup callers is plan-tracked. |
| Terminal escape-audit retained set | Compiled catalog in `Prodbox.Lifecycle.Teardown.RetainedInventory`, surface-indexed and bound to the audited `AwsScope` | No durable home; derived from the compiled registry, the managed credential inventory, and a validated operator-name binding | Declares which resources a surface intends to keep, in four categories: S3 (long-lived Pulumi state and SES capture buckets), SES (sending identity and receipt rule set), shared identities (the operational IAM principal plus every managed credential family outliving the surface), and the one dynamic registered `LongLived` family. Every matcher is an exact ARN or a registry-declared family coordinate, so a runtime tag can witness family membership but can never mint, change, or recover a `LifecycleClass`. The retained-set and query digests are derived from the catalog. Discoverability is indexed by the audited region as well as by the writer's tags, and a would-be-clean verdict taken outside the global-service region lowers to `TerminalAuditUnobservable` naming each unqueried global service, because the Tagging API returns IAM and Route 53 only from that region; a discovered escapee is unaffected. The superseded tag classifier and executing sweep in `Prodbox.Lifecycle.TagSweep` remain the live cascade path, bind no region at all, and are Pending Removal. |
| Retained authority store (`ClusterRetained` Model-B namespace) | Durability-indexed `StoreLifetime` coordinates and adapters (`ChartLifetime` / `ClusterRetained` / `CrossClusterDurable`) partition the object namespace. **Current correspondence:** the interim host-direct adapter and generation-scoped operation replay exist. **Target:** the in-cluster Lifecycle Authority repository, dedicated MinIO identity, typed clients, and no host-direct/Gateway caller. | Same sealed Model-B envelopes in retained `prodbox-state`; host-direct is compatibility residue until the Authority role is constructible and callers are cut over. | The lifetime index and operation record exist. Production remains pre-cutover while the role server fails closed or a supported caller can select a legacy transport; status and removal ownership live in the [plan](README.md#resume-here) and [ledger](legacy-tracking-for-deletion.md#pending-removal). |
| Local RKE2 host state | Host lifecycle commands | RKE2 data dirs, kubeconfig files, systemd state | Deleted by `prodbox cluster delete --yes` except retained roots, with expected-absence cleanup rendered as normal delete disposition and Sprint `4.8` keeping successful uninstall runs hermetic through the lifecycle-local quiet path |
| Remote EKS AWS test stack | Pulumi plus Haskell orchestration | EKS resources in AWS | Created and destroyed only through named `prodbox aws stack eks` surfaces |
| Remote HA RKE2 AWS test stack | Pulumi plus Haskell orchestration | EC2 and supporting AWS resources | Exactly three Ubuntu 24.04 EC2 instances in separate AZs |
| Cluster resource state | Kubernetes | RKE2 datastore | Managed through canonical Haskell CLI flows |
| DNS ownership | Exact registered Route 53 capabilities | Hosted-zone A/TXT records | One public FQDN per substrate: the home A record is owned by Gateway-DNS, the AWS A record by a Lifecycle Authority provider intent, every cert-manager DNS01 TXT coordinate is registered before issuance, and the mutating hosted-zone canary is a registered short-lived resource. Wildcard and per-service public hostnames remain unsupported. |
| Pulumi backend state | Lifecycle Authority, separate Backup Adapter, and fenced provider worker | Primary retained `prodbox-state` plus exact receipt-committed encrypted envelope/blob copies at the independent S3 coordinate | Scratch hydration/provider execution are interpreters of a durable operation. Promotion requires primary and backup read-back; primary-loss restore verifies exact bytes before a greater epoch. |
| MinIO as a Model-B ciphertext store | Capability-scoped Bootstrap Broker and Lifecycle Authority interpreters over a shared pure object algebra | Retained `prodbox-state` namespace on the MinIO PV | Stored payloads use Vault Transit envelopes and opaque keyed names. The Broker can access only its bootstrap object; Lifecycle Authority can access only lifecycle blobs/aggregates. Gateway, host callers, and Target Secret Agents have no generic object-store capability after cutover. `ObjectStoreTypes` separately owns the exact `minio.prodbox.svc.cluster.local:9000` Pod-network Service coordinate; the authored Tier-0 endpoint remains a host-network coordinate and is never projected into a Pod. |
| Bootstrap Broker host connection | `Prodbox.Bootstrap.Broker.PortForward` | Exact `bootstrap-broker` Deployment and Service; host-local ephemeral port only | The Broker binds only Pod loopback and the host port-forward binds only `127.0.0.1`. Normal host access first observes the exact Deployment rollout through the selected kubectl environment, then mints the bounded TokenRequest credential and opens `service/bootstrap-broker` by its compiled named port. The ambiguity-reset client alone may skip Deployment readiness because ambiguity intentionally withdraws it; that closed path retains authenticated `/healthz`, the same exact Service/credential/loopback bounds, and bracketed cleanup. Sprints `2.53`/`2.61`. |
| Bootstrap Broker controller self-observation | Kubernetes PodList selected by the chart's exact controller labels | `app.kubernetes.io/name=prodbox-bootstrap-broker` plus `app.kubernetes.io/instance=bootstrap-broker`, then positive terminal-phase exclusion | Controller image attestation admits exactly one nonterminal matching Deployment Pod. One-shot workers lack the controller instance label, and retained `Succeeded`/`Failed` controller objects cannot mask the current Pod; multiple live, deleting, malformed, or unobservable candidates remain refusals. Sprints `2.55`/`2.58`. |
| Bootstrap Broker expired-owner worker cleanup | Generation-scoped observation of the sole fixed worker coordinate | Exact Pod UID, fence-generation annotation, terminal phase, UID-preconditioned delete, and positive 404 read-back | An expired fence may treat its worker as absent only after an exact same-generation `Succeeded`/`Failed` Pod is UID-deleted and the coordinate is observed absent. Pending, Running, Unknown, malformed, foreign, or unobservable Pods are never deleted on that fence's behalf; foreign-generation occupancy only proves the queried generation absent. Sprint `2.56`. |
| Bootstrap Broker worker termination wire | Kubernetes Pod `status.containerStatuses[].state.terminated` | Required exit code plus optional termination-log message | Kubernetes may omit `message` when a failed worker wrote no termination-log receipt. The Pod remains readable and terminal, but an empty decoded message cannot satisfy an operation's exact receipt binding; attestation independently refuses every non-Running Pod. Sprint `2.57`. |
| Bootstrap Broker worker fence revalidation RBAC | Worker ServiceAccount namespaced Role | `get` on Pod `bootstrap-secret-worker` plus `get` on Lease `bootstrap-broker-fence` | The worker reconstructs its exact Pod binding and revalidates the immutable fence Lease before each Vault/store effect. It cannot create/update Leases, create/delete Pods, attach/exec, issue TokenReviews, or read Secrets. Sprint `2.59`. |
| Root initialization pristine observation | Shared pure classifier consumed by the Bootstrap Broker controller and its attested secret worker | Exact `root-init-journal` observation in the retained bootstrap namespace | Absent admits the generation-derived pristine proof; a present `RootInitPristine` or `RootResetPristine` admits only the same exact proof. Progressed, ambiguous, foreign-generation, and foreign-proof states refuse. Sprint `2.54` removes the former split in which the controller admitted present pristine state and the worker rejected every present object. |
| Certificate material | Selected Target Agent + retained-home TLS DEK lane + TLS Retention Adapter | Exact Kubernetes TLS Secret plus LongLived S3 ciphertext/wrapped-DEK object | Selected Agent alone reads/materializes the Secret; retained-home Agent wraps/unwraps DEK to the selected Agent attestation; Adapter sees ciphertext only. Restore/read-back precedes issuance, including after AWS Vault/EBS replacement; explicit TLS-consumer decommission or nuke removes retained material/identity. |
| Gateway emitter continuity | One local actor per emitter plus bounded semantic gateway state | Encrypted identity-bound local retained journal, isolated from lifecycle authority storage | The actor alone owns stage/fsync/publish/commit/fsync. Crash recovery rewinds exact signed work to the durable-stage boundary, rebuilds local State from the authenticated journal floor and contiguous suffix, and republishes the same bytes before readiness. The durable projection retains the authenticated prior-Orders digest so an interrupted migration re-arms after restart. Missing/corrupt/unobservable journal state refuses emission. Peer checkpoint/suffix frames repair only a lagging remote replica; they never replace local continuity authority. The code-local target exists; production cutover and qualification remain governed by [README.md](README.md#deployment-qualification). |
| Ephemeral elevated/admin AWS credential | Interactive `SecretRef.Prompt` or harness-simulated prompt | Authenticated linear stdin and bounded memory of one attested permit-selected Job/runner only | Credential Provisioner creates identities, Admin Action Runner performs its closed action, or post-export Decommission Runner follows its manifest; bytes never enter config, Vault, Authority, Provider, Gateway, Kubernetes objects, disk, argv/environment, or logs |
| Lifecycle-provider AWS identity | Operator-material-mode Credential Provisioner; delivered by durable target-seal operation | `secret/aws/lifecycle-provider`; short operation sessions only in Provider-Worker memory | May assume only the provider role named by a committed intent; no Gateway DNS, credential-create, backup, or cert-manager use |
| Authority-backup AWS identity | Genesis/repair-mode Credential Provisioner; delivered through the home Target Secret Agent as a `LongLived` generation | `secret/aws/authority-backup-store` plus exact opaque S3 backup coordinate | Separately deployed home Authority Backup Adapter only; core Authority cannot read it. Closed prepare/read-back/restore/retire scope; removed only by external-receipt `nuke` after backup absence proof. |
| TLS-retention AWS identity | Permit-driven Credential Provisioner; LongLived generation delivered through home Agent | `secret/aws/tls-retention-store` plus exact public-edge TLS S3 prefix | TLS Retention Adapter only; no authority backup/provider/DNS power; explicit consumer decommission or nuke after TLS object/consumer cleanup, never ordinary teardown |
| Gateway-DNS AWS identity | Operator-material-mode Credential Provisioner; delivered to the home Target Secret Agent | Dedicated LongLived Gateway-DNS Vault path/generation | Exact home A record only; retained with live/restored home Gateway, removed by nuke/explicit decommission—not ordinary postflight |
| cert-manager DNS01 AWS identity | Operator-material-mode Credential Provisioner; delivered to the selected Target Agent | Home generation is LongLived; AWS generation is run-scoped | DNS01 TXT only. Home remains for background renewal; AWS is deleted only after EKS Challenge/TXT/cluster absence. Never shared with Gateway/provider. |
| SES SMTP AWS identity and custody | Operator-material-mode Credential Provisioner creates/rotates/remints the deterministic `LongLived` principal, least-privilege send policy, and finite key family; it also owns repair-time key deletion and in-memory region-bound SMTP derivation before raw-key disposal | Registered identity/key-family state in the Lifecycle Authority, retained-home Transit-sealed `SesSmtpSource`, and per-target `secret/keycloak/smtp` generations | Pulumi and the Provider Worker have no IAM descriptor or mutation constructor for it. The home Agent receives only the closed SMTP payload and owns schema-bound custody/attested rewrap; Authority sees ciphertext/receipts only. Ordinary postflight retains every node. `DestroyAwsSes` proves consumers quiescent, obtains Provider-Worker provider-stack absence, invokes the Admin Action Runner to delete/read-back external IAM, then tombstones/read-backs target generations and custody while Agents remain live, aggregating all failures. |
| Test-simulation admin credential (`aws_admin_for_test_simulation.*`) | Test harness only — suite-level IAM harness | `test-secrets.dhall` (test-only, TestPlaintext) | Test-harness-only fixture whose sole purpose is to simulate the operator's elevated-credential prompt so the harness can drive admin-credentialed flows non-interactively. Never imported by production config, never read by a production binary, never in Vault, never copied into generated cluster config or MinIO. SSoT: [aws_admin_credentials.md](../documents/engineering/aws_admin_credentials.md) (Sprint `7.16`) |
| Custodial capability custody | The disposition value, not the holder: possession is not authority to release | No durable home in this revision; derived per operation from the compiled registries | Custodial capabilities are checkpoint blobs, access-key families, sealed credential generations, and the contents of retained stores. Ending custody is a disposition and never a deletion; there is no destroy-alone constructor. A durable custody ledger is deliberately unscheduled until a disposition has more than one producer. |

## Artifact Locations

| Type | Location | Purpose |
|------|----------|---------|
| Haskell application entrypoint | `app/prodbox/Main.hs` | Main CLI binary entrypoint |
| Haskell source modules | `src/Prodbox/` | CLI, infra, gateway, settings, and library implementation. Subtrees: `CLI/` (parser, command ADT, per-command surfaces including `Nuke.hs` and `Users.hs`), `Daemon/` (long-running daemon helpers + at-least-once events), `Gateway/` (daemon, client, peer, types, logging), `Http/` (native HTTP client), `Infra/` (Pulumi-backed AWS stack orchestration including `AwsSesStack.hs`, `AwsEksSubzoneStack.hs`, `LongLivedPulumiBackend.hs`, `MinioBackend.hs`, `StackOutputs.hs` for encrypted stack list/output reads with legacy raw helpers retained for migration), `Pulumi/` (`EncryptedBackend.hs` for the decrypt-to-scratch Model-B stack wrapper), `Keycloak/` (admin client, email parser, credential-setup form parser), `Lib/` (chart platform plus substrate-aware helpers), `Lifecycle/` (`Preconditions.hs`, `K8sDrain.hs`, `TagSweep.hs`, `EbsVolume.hs`, `ResidueStatus.hs`, `LiveResidue.hs` reconciler-with-predicates building blocks, plus the `ResourceClass.hs` facts and `ResourceRegistry.hs` managed-resource registry + `reconcileAbsent` reconciler), `Secret/` (`VaultInventory.hs` for Vault KV path/policy/service-account inventory; the old derivation/master-seed/wire modules are removed), `Vault/` (Vault client, reconcile, host helper, unlock-bundle, Transit cipher, orchestration), `Ses/` (SMTP password derivation + capture-bucket polling). |
| Haskell tests | `test/` | Unit plus native CLI/env integration validation suites |
| Repository config artifacts | Operator-authored, git-ignored binary-sibling `prodbox.dhall` plus generated, git-ignored binary-sibling schemas. Repository config and secret-fixture values are not version-controlled. Tracked Dhall is limited to schema/golden artifacts under `dhall/` and `test/golden/`; see [config_doctrine.md §0](../documents/engineering/config_doctrine.md#0-three-tier-config-model). | Haskell-owned config source and shared schema |
| Cabal package definition | `prodbox.cabal` | Build, test, and dependency definition |
| Cabal project definition | `cabal.project` | Repository-wide Cabal package-set definition |
| Host build root | `.build/` | Canonical host-side Haskell build artifacts; operator binary at `.build/prodbox` |
| Container build root | `/opt/build` | Canonical container-side Haskell build artifacts |
| Canonical custom Dockerfiles | `docker/` | Authoritative home for repository-owned container builds |
| Union runtime container build | `docker/prodbox.Dockerfile` | One image (`prodbox/prodbox-runtime`) for every in-cluster role (gateway daemon + `api`/`websocket` workloads), owned by Phase `1` (consolidated from the former separate gateway image, Phase `2`); single-stage `ubuntu:24.04` with in-image `ghcup`, pinned GHC `9.12.4`, no symlinked Haskell tool shims, `tini` as PID 1, the official AWS CLI bundle keyed by the native Debian host architecture, and a bare `tini -- prodbox` ENTRYPOINT (role chosen by each chart's `args:`) |
| Pulumi definitions | `pulumi/aws-eks/{Pulumi,Main}.yaml`, `pulumi/aws-eks-subzone/{Pulumi,Main}.yaml`, `pulumi/aws-test/{Pulumi,Main}.yaml`, `pulumi/aws-ses/{Pulumi,Main}.yaml` | YAML Pulumi stacks (`runtime: yaml`) for the public AWS substrate resources. `aws-eks`, `aws-eks-subzone`, and `aws-test` are per-run; `aws-ses` is long-lived cross-substrate shared provider infrastructure and contains only sending identity/DKIM/MX/rules/capture resources—never the separately registered SMTP IAM principal/policy/key family (see [substrates.md → Resource Lifecycle Classes](substrates.md#resource-lifecycle-classes)). |
| Engineering doctrine | `documents/engineering/` (governed docs index at [documents/engineering/README.md](../documents/engineering/README.md)) | Architecture and operator docs: `cli_command_surface.md`, `pure_fp_standards.md`, `haskell_code_guide.md`, `code_quality.md`, `dependency_management.md`, `helm_chart_platform_doctrine.md`, `distributed_gateway_architecture.md`, `envoy_gateway_edge_doctrine.md`, `lifecycle_reconciliation_doctrine.md`, `vault_doctrine.md`, `cluster_federation_doctrine.md`, `secret_derivation_doctrine.md`, `storage_lifecycle_doctrine.md`, `prerequisite_doctrine.md`, `prerequisite_dag_system.md`, `effectful_dag_architecture.md`, `effect_interpreter.md`, `streaming_doctrine.md`, `integration_fixture_doctrine.md`, `unit_testing_policy.md`, `refactoring_patterns.md`, `tla_modelling_assumptions.md`, `local_registry_pipeline.md`, `aws_account_setup_guide.md`, `aws_admin_credentials.md`, `aws_integration_environment_doctrine.md`, `aws_test_environment.md`, `acme_provider_guide.md` |
| Development plan | `DEVELOPMENT_PLAN/` | Status, sequencing, and cleanup ownership |
| Manual PV content root | Configured path, default `.data/` | PV contents only |
| Master seed in MinIO — **retired and removed** | None | Sprint `3.19` removed the HMAC-derivation model outright: there is no `master-seed` object in MinIO and no code path reads or writes it. Sprint `4.30` adds the Model-B object-store foundation for remaining prodbox-owned state, with the in-force config read through an opaque `objects/<vault-keyed-HMAC>.enc` key in `prodbox-state`. Sprint `7.14` routes Pulumi stack state through the same encrypted object-store wrapper and imports legacy raw checkpoints on first touch. See [vault_doctrine.md § 9](../documents/engineering/vault_doctrine.md#9-minio-as-a-ciphertext-store) |
| Vault durable PV root | Configured host path under `.data/vault/vault/0` | Durable home for the in-cluster Vault backend, preserved across cluster wipes alongside the MinIO PV whenever `.data/` is preserved (Vault platform under Sprint `3.17`; lifecycle + PV preservation under Sprint `4.29`). See [vault_doctrine.md § 5](../documents/engineering/vault_doctrine.md#5-vault-deployment-model-and-durability) |
| Vault prepared-init/final unlock objects | Durable bootstrap MinIO only; host disk contains no recovery material | Password-AEAD `PreparedInitEnvelope` plus encrypted init-response receipt are read back before final unlock-bundle promotion, then preparation is deleted/read-back absent. Only Bootstrap Broker may consume them; applied-without-response permits only proven-pristine reset. Never exposed to Gateway Runtime or generic host/daemon transports. See [vault_doctrine.md](../documents/engineering/vault_doctrine.md). |
| AWS substrate per-run Pulumi state | MinIO bucket `prodbox-state`; Sprint `7.14` stores main-cycle checkpoints as opaque Model-B `objects/<id>.enc` objects via the decrypt-to-scratch Pulumi interposition and imports legacy raw checkpoints on first touch | Pulumi stack state for `aws-eks`, `aws-eks-subzone`, `aws-test`; HA-RKE2 SSH key fetched on demand from the encrypted scratch backend via `pulumi stack output --show-secrets` into `mktemp` |

## Related Documents

- [00-overview.md](00-overview.md)
- [substrates.md](substrates.md)
- [phase-1-runtime-cli-aws-foundations.md](phase-1-runtime-cli-aws-foundations.md)
- [phase-2-gateway-dns.md](phase-2-gateway-dns.md)
- [phase-3-chart-platform-vscode.md](phase-3-chart-platform-vscode.md)
- [phase-4-lifecycle-canonical-paths.md](phase-4-lifecycle-canonical-paths.md)
- [phase-5-canonical-test-suite.md](phase-5-canonical-test-suite.md)
- [phase-6-clean-room-handoff.md](phase-6-clean-room-handoff.md)
- [phase-7-aws-substrate-foundations.md](phase-7-aws-substrate-foundations.md)
- [phase-8-email-invite-auth.md](phase-8-email-invite-auth.md)
- [legacy-tracking-for-deletion.md](legacy-tracking-for-deletion.md)
- [vault_doctrine.md](../documents/engineering/vault_doctrine.md)
- [cluster_federation_doctrine.md](../documents/engineering/cluster_federation_doctrine.md)
