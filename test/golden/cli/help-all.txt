## prodbox aws policy
prodbox aws policy - Render IAM policy JSON

Render the doctrine-owned IAM policy document.
Options:
- --tier <TIER> - Operational IAM policy tier to render

Examples:
- prodbox aws policy --tier full  # Render the full IAM policy document.

## prodbox aws setup
prodbox aws setup - Create or refresh operational IAM user

Provision or refresh the operational IAM user.
Options:
- --tier <TIER> - Operational IAM policy tier to provision
- --dry-run - Render the IAM setup plan without mutating state
- --plan-file <PATH> - Write the rendered plan to a file

Examples:
- prodbox aws setup --tier full  # Create or refresh the operational IAM user.

## prodbox aws teardown
prodbox aws teardown - Delete operational IAM user

Delete the operational IAM user. Default mode opens with a Pulumi-residue check and refuses if any AWS substrate stack (`aws-eks`, `aws-eks-subzone`, `aws-test`, `aws-ses`) still owns live resources. `--destroy-pulumi-residue` orchestrates `prodbox aws stack <stack> destroy --yes` (the `<stack>` CLI verb is `eks`/`aws-subzone`/`test`/`aws-ses`, not the registry name) in canonical order before deleting the operational IAM user; destroying the long-lived `aws-ses` stack triggers a 5-30 min SES re-verification and ~24h S3 bucket-name cooldown. `--allow-pulumi-residue` is the operator-acknowledged recovery escape hatch that bypasses the residue check entirely (stacks become orphaned). `--destroy-pulumi-residue` and `--allow-pulumi-residue` are mutually exclusive at parse time.
Options:
- --dry-run - Render the IAM teardown plan without mutating state
- --plan-file <PATH> - Write the rendered plan to a file
- --destroy-pulumi-residue - Run `prodbox aws stack <stack> destroy --yes` for each live Pulumi-managed AWS stack in canonical order before deleting the operational IAM user (mutually exclusive with --allow-pulumi-residue)
- --allow-pulumi-residue - Bypass the Pulumi-residue refuse-path that prevents deleting the operational IAM user while AWS substrate stacks still have live resources (operator-acknowledged recovery only; mutually exclusive with --destroy-pulumi-residue)

Examples:
- prodbox aws teardown  # Delete the operational IAM user (refuses if any AWS substrate stack is live).
- prodbox aws teardown --destroy-pulumi-residue  # Destroy live AWS substrate stacks in canonical order, then delete the operational IAM user.

## prodbox aws quotas check
prodbox aws quotas check - Inspect supported AWS quotas

Inspect supported AWS quotas.
Examples:
- prodbox aws quotas check  # Inspect AWS quotas.

## prodbox aws quotas request
prodbox aws quotas request - Request supported AWS quotas

Request supported AWS quota increases.
Options:
- --tier <TIER> - Quota target tier to request

Examples:
- prodbox aws quotas request --tier core  # Request core-tier quota increases.

## prodbox aws ebs reap-test
prodbox aws ebs reap-test - Delete test-scoped EBS volumes

Delete only EBS volumes tagged as per-run test volumes for the canonical AWS EKS test cluster. Retained-production EBS volumes are never selected.
Options:
- --yes - Confirm deletion of test-scoped EBS volumes

Examples:
- prodbox aws ebs reap-test --yes  # Delete leaked test-scoped EBS volumes.

## prodbox aws stack eks reconcile
prodbox aws stack eks reconcile - Provision or inspect the eks stack

Reconcile the EKS validation stack.
Options:
- --dry-run - Render the Pulumi plan without mutating state
- --plan-file <PATH> - Write the rendered plan to a file

Examples:
- prodbox aws stack eks reconcile  # Reconcile the EKS validation stack.

## prodbox aws stack eks destroy
prodbox aws stack eks destroy - Destroy the eks stack

Destroy the EKS validation stack.
Options:
- --yes, -y - Skip confirmation prompts
- --dry-run - Render the destroy plan without mutating state
- --plan-file <PATH> - Write the rendered plan to a file

Examples:
- prodbox aws stack eks destroy --yes  # Destroy the EKS validation stack.

## prodbox aws stack eks prune-corrupt-checkpoint
prodbox aws stack eks prune-corrupt-checkpoint - Clear a corrupt eks per-run Pulumi checkpoint

Recovery: clear a genuinely-corrupt (or empty) per-run encrypted Pulumi checkpoint for the eks stack from the Model-B object store, so a cluster carrying stale corrupt checkpoints (truncated leftovers from an interrupted run) can converge. Observes the checkpoint first and refuses to prune a valid (present) checkpoint  use `destroy` for that. Fail-closed on an unobservable backend.
Options:
- --yes, -y - Skip confirmation prompts

Examples:
- prodbox aws stack eks prune-corrupt-checkpoint --yes  # Clear a corrupt eks per-run checkpoint.

## prodbox aws stack test reconcile
prodbox aws stack test reconcile - Provision or inspect the test stack

Reconcile the HA RKE2 validation stack.
Options:
- --dry-run - Render the Pulumi plan without mutating state
- --plan-file <PATH> - Write the rendered plan to a file

Examples:
- prodbox aws stack test reconcile  # Reconcile the HA RKE2 validation stack.

## prodbox aws stack test destroy
prodbox aws stack test destroy - Destroy the test stack

Destroy the HA RKE2 validation stack.
Options:
- --yes, -y - Skip confirmation prompts
- --dry-run - Render the destroy plan without mutating state
- --plan-file <PATH> - Write the rendered plan to a file

Examples:
- prodbox aws stack test destroy --yes  # Destroy the HA RKE2 validation stack.

## prodbox aws stack test prune-corrupt-checkpoint
prodbox aws stack test prune-corrupt-checkpoint - Clear a corrupt test per-run Pulumi checkpoint

Recovery: clear a genuinely-corrupt (or empty) per-run encrypted Pulumi checkpoint for the test stack from the Model-B object store, so a cluster carrying stale corrupt checkpoints (truncated leftovers from an interrupted run) can converge. Observes the checkpoint first and refuses to prune a valid (present) checkpoint  use `destroy` for that. Fail-closed on an unobservable backend.
Options:
- --yes, -y - Skip confirmation prompts

Examples:
- prodbox aws stack test prune-corrupt-checkpoint --yes  # Clear a corrupt test per-run checkpoint.

## prodbox aws stack aws-subzone reconcile
prodbox aws stack aws-subzone reconcile - Provision or inspect the aws-subzone stack

Reconcile the AWS-substrate Route 53 hosted subzone and NS delegation.
Options:
- --dry-run - Render the Pulumi plan without mutating state
- --plan-file <PATH> - Write the rendered plan to a file

Examples:
- prodbox aws stack aws-subzone reconcile  # Reconcile the AWS-substrate Route 53 hosted subzone and NS delegation.

## prodbox aws stack aws-subzone destroy
prodbox aws stack aws-subzone destroy - Destroy the aws-subzone stack

Destroy the AWS-substrate Route 53 hosted subzone and remove the parent NS delegation.
Options:
- --yes, -y - Skip confirmation prompts
- --dry-run - Render the destroy plan without mutating state
- --plan-file <PATH> - Write the rendered plan to a file

Examples:
- prodbox aws stack aws-subzone destroy --yes  # Destroy the AWS-substrate Route 53 hosted subzone and remove the parent NS delegation.

## prodbox aws stack aws-subzone prune-corrupt-checkpoint
prodbox aws stack aws-subzone prune-corrupt-checkpoint - Clear a corrupt aws-subzone per-run Pulumi checkpoint

Recovery: clear a genuinely-corrupt (or empty) per-run encrypted Pulumi checkpoint for the aws-subzone stack from the Model-B object store, so a cluster carrying stale corrupt checkpoints (truncated leftovers from an interrupted run) can converge. Observes the checkpoint first and refuses to prune a valid (present) checkpoint  use `destroy` for that. Fail-closed on an unobservable backend.
Options:
- --yes, -y - Skip confirmation prompts

Examples:
- prodbox aws stack aws-subzone prune-corrupt-checkpoint --yes  # Clear a corrupt aws-subzone per-run checkpoint.

## prodbox aws stack aws-ses reconcile
prodbox aws stack aws-ses reconcile - Provision or inspect the aws-ses stack

Reconcile the shared AWS SES sending identity, receive subdomain, receive rule set, S3 capture bucket, and SMTP IAM user used by Phase 8 operator-invited email auth.
Options:
- --dry-run - Render the Pulumi plan without mutating state
- --plan-file <PATH> - Write the rendered plan to a file

Examples:
- prodbox aws stack aws-ses reconcile  # Reconcile the shared AWS SES sending identity, receive subdomain, receive rule set, S3 capture bucket, and SMTP IAM user used by Phase 8 operator-invited email auth.

## prodbox aws stack aws-ses destroy
prodbox aws stack aws-ses destroy - Destroy the aws-ses stack

Destroy the shared AWS SES stack (sending identity, receive subdomain, receive rule set, S3 capture bucket, and SMTP IAM user).
Options:
- --yes, -y - Skip confirmation prompts
- --dry-run - Render the destroy plan without mutating state
- --plan-file <PATH> - Write the rendered plan to a file

Examples:
- prodbox aws stack aws-ses destroy --yes  # Destroy the shared AWS SES stack (sending identity, receive subdomain, receive rule set, S3 capture bucket, and SMTP IAM user).

## prodbox aws stack aws-ses migrate-backend
prodbox aws stack aws-ses migrate-backend - Migrate aws-ses Pulumi state onto the long-lived S3 backend

Operator-interactive command: migrate the `aws-ses` stack's Pulumi state from the in-cluster MinIO backend onto the dedicated long-lived S3 bucket named by `pulumi_state_backend` in `prodbox.dhall`. Idempotent; no-op when the stack already lives in the long-lived backend. TTY-only; refuses non-interactive contexts.
Options:
- --dry-run - Render the migration plan without mutating state
- --plan-file <PATH> - Write the rendered plan to a file

Examples:
- prodbox aws stack aws-ses migrate-backend  # Migrate aws-ses state onto the long-lived S3 backend.

## prodbox charts list
prodbox charts list - List supported charts

List supported root charts.
Examples:
- prodbox charts list  # List supported root charts.

## prodbox charts status
prodbox charts status - Show detailed chart status

Inspect the current state of a root chart.
Examples:
- prodbox charts status vscode  # Inspect the vscode chart status.

## prodbox charts reconcile
prodbox charts reconcile - Reconcile a root chart stack

Reconcile a root chart to the supported state.
Options:
- --dry-run - Render the deployment plan without mutating state
- --plan-file <PATH> - Write the rendered plan to a file
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox charts reconcile vscode  # Reconcile the vscode stack.
- prodbox charts reconcile --dry-run vscode  # Render the chart deployment plan.

## prodbox charts delete
prodbox charts delete - Delete a root chart stack

Delete a root chart stack.
Options:
- --yes, -y - Skip confirmation prompt
- --dry-run - Render the deletion plan without mutating state
- --plan-file <PATH> - Write the rendered plan to a file
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox charts delete vscode --yes  # Delete the vscode stack.
- prodbox charts delete --dry-run vscode  # Render the chart deletion plan.

## prodbox cluster status
prodbox cluster status - Check cluster and Vault status

Inspect the local cluster service status and the in-cluster Vault seal state.
Examples:
- prodbox cluster status  # Inspect local cluster and Vault status.

## prodbox cluster health
prodbox cluster health - Check Kubernetes health

Inspect Kubernetes cluster health.
Examples:
- prodbox cluster health  # Inspect Kubernetes health.

## prodbox cluster start
prodbox cluster start - Start the cluster service

Start the local cluster service.
Examples:
- prodbox cluster start  # Start the local cluster.

## prodbox cluster stop
prodbox cluster stop - Stop the cluster service

Stop the local cluster service.
Examples:
- prodbox cluster stop  # Stop the local cluster.

## prodbox cluster restart
prodbox cluster restart - Restart the cluster service

Restart the local cluster service.
Examples:
- prodbox cluster restart  # Restart the local cluster.

## prodbox cluster reconcile
prodbox cluster reconcile - Reconcile the local cluster

Reconcile the supported local cluster state. Local-only by default (no AWS); pass --with-edge to also reconcile the public edge (or run `prodbox edge reconcile` separately).
Options:
- --dry-run - Render the lifecycle plan without mutating state
- --plan-file <PATH> - Write the rendered plan to a file
- --with-edge - Also reconcile the AWS-gated public edge (Route 53 DNS + ZeroSSL TLS); requires operational aws.*

Examples:
- prodbox cluster reconcile  # Reconcile the supported local cluster (no AWS needed).
- prodbox cluster reconcile --dry-run  # Render the lifecycle plan.
- prodbox cluster reconcile --with-edge  # Reconcile the cluster and attach the public edge.

## prodbox cluster delete
prodbox cluster delete - Delete the local cluster

Delete the local cluster. Default mode is a PURE LOCAL UNINSTALL: it uninstalls RKE2 and preserves `.data/` (including the MinIO-backed per-run Pulumi state and the durable Vault PV) without querying, gating on, or destroying the per-run AWS Pulumi backend  so per-run AWS stacks (if any) are left untouched and remain destroyable afterward via `prodbox cluster delete --cascade` or `prodbox aws stack <name> destroy --yes`. `--cascade` orchestrates the full teardown (K8s drain + per-run Pulumi destroys + cluster uninstall + postflight tag sweep) as one atomic operator action; the K8s drain phase skips gracefully when no cluster is reachable.
Options:
- --yes, -y - Confirm full cluster deletion
- --cascade - Orchestrate the full teardown (K8s drain + per-run Pulumi destroys + cluster uninstall + postflight tag sweep) as one atomic operator action
- --dry-run - Render the delete plan without mutating state
- --plan-file <PATH> - Write the rendered plan to a file

Examples:
- prodbox cluster delete --yes  # Uninstall the local cluster (pure local uninstall; leaves per-run AWS stacks untouched).
- prodbox cluster delete --yes --cascade  # Orchestrate the full teardown including per-run AWS destroys.

## prodbox cluster logs
prodbox cluster logs - Show cluster service logs

Show the local cluster service (RKE2) journal logs.
Options:
- --lines, -n <INTEGER> - Number of log lines to show

Examples:
- prodbox cluster logs --lines 50  # Show recent cluster service logs.

## prodbox cluster federation register
prodbox cluster federation register - Register a downstream cluster

Register parent-owned custody for a downstream cluster: child metadata, child init-key custody path, opaque Vault namespace, Transit key name, and the child bootstrap token Secret.
Options:
- --dry-run - Render the registration plan without mutating state
- --plan-file <PATH> - Write the rendered plan to a file
- --child-vault-address <URL> - Host-reachable Vault API address for the child cluster
- --child-kubeconfig <PATH> - Kubeconfig for applying the child transit-seal token Secret
- --child-endpoint <NAME=URL> - Child endpoint inventory entry to custody in the parent Vault; repeatable
- --child-kubeconfig-reference <REF> - Parent-custodied kubeconfig reference for the child cluster
- --child-account-id <ACCOUNT_ID> - Parent-custodied cloud/account identifier for the child cluster
- --child-pulumi-stack <NAME=REF> - Child Pulumi stack reference to custody in the parent Vault; repeatable

Examples:
- prodbox cluster federation register child-a --dry-run  # Render the downstream custody plan.
- prodbox cluster federation register child-a --child-vault-address http://child-vault.example:8200 --child-kubeconfig /secure/child-a.kubeconfig --child-kubeconfig-reference vault:secret/clusters/child-a/kubeconfig --child-account-id 123456789012 --child-endpoint api=https://api.child-a.example --child-pulumi-stack aws-eks=org/prodbox-child-a/aws-eks  # Register child-a and apply its transit-seal token Secret.

## prodbox cluster wait
prodbox cluster wait - Wait for deployments to be ready

Wait for named namespaces to become ready.
Options:
- --timeout, -t <INTEGER> - Timeout in seconds
- --namespace, -n <VALUE> - Namespace to wait for

Examples:
- prodbox cluster wait --timeout 300  # Wait for infrastructure workloads.

## prodbox cluster workload-logs
prodbox cluster workload-logs - Show recent workload logs

Show recent logs from infrastructure/workload namespaces.
Options:
- --namespace, -n <VALUE> - Namespace to get logs from
- --tail <INTEGER> - Number of log lines per container

Examples:
- prodbox cluster workload-logs --tail 25  # Show recent workload logs.

## prodbox commands
prodbox commands - Render the command registry

Render the command registry in plain text, tree, or JSON form.
Options:
- --tree - Render the command registry as a tree
- --json - Render the command registry as JSON

Examples:
- prodbox commands  # Render the command registry in plain-text form.
- prodbox commands --tree  # Render the command registry as a tree.
- prodbox commands --json  # Render the command registry as JSON.

## prodbox config setup
prodbox config setup - Interactively author config

Write the supported prodbox Dhall config.
Options:
- --dry-run - Render the config-setup plan without mutating state
- --plan-file <PATH> - Write the rendered plan to a file

Examples:
- prodbox config setup  # Create or refresh the config interactively.

## prodbox config show
prodbox config show - Display current config

Render the decoded config with secrets masked by default.
Options:
- --show-secrets - Show full secret values

Examples:
- prodbox config show  # Render the current config with secrets masked.

## prodbox config validate
prodbox config validate - Validate current config

Validate the repository-root config file.
Examples:
- prodbox config validate  # Validate the current config.

## prodbox config schema
prodbox config schema - Regenerate Dhall schema files

Regenerate prodbox-config-types.dhall + test-secrets-types.dhall from the Haskell source of truth.
Examples:
- prodbox config schema  # Regenerate the committed Dhall schema files.

## prodbox config generate
prodbox config generate - Generate the default non-secret config

Non-interactively write a default, non-secret prodbox.dhall from the Haskell source of truth when it is absent; leaves an existing file unchanged (idempotent).
Examples:
- prodbox config generate  # Generate prodbox.dhall from defaults for a headless bring-up.

## prodbox dev check
prodbox dev check - Run policy, lint, and type checks

Run the canonical quality gate.
Examples:
- prodbox dev check  # Run the canonical quality gate.

## prodbox dev lint all
prodbox dev lint all - Run every lint surface

Run every doctrine-owned lint surface.
Examples:
- prodbox dev lint all  # Run every doctrine-owned lint surface.

## prodbox dev lint files
prodbox dev lint files - Run repository-policy lint checks

Check forbidden paths and library-first policy invariants.
Options:
- --write - Rewrite the target surface instead of only checking for drift

Examples:
- prodbox dev lint files  # Run repository-policy lint checks.

## prodbox dev lint docs
prodbox dev lint docs - Check generated documentation sections

Check or rewrite marker-delimited documentation sections.
Options:
- --write - Rewrite the generated documentation sections

Examples:
- prodbox dev lint docs  # Check generated documentation sections for drift.

## prodbox dev lint haskell
prodbox dev lint haskell - Run Haskell formatter and lint checks

Run the formatter, hlint, and cabal-format consistency checks.
Options:
- --write - Rewrite Haskell formatting surfaces in place

Examples:
- prodbox dev lint haskell  # Run Haskell formatter and lint checks.

## prodbox dev lint chart
prodbox dev lint chart - Run Helm chart structural lint checks

Run the Helm chart structural invariants linter.
Examples:
- prodbox dev lint chart  # Run the Helm chart structural invariants linter.

## prodbox dev docs check
prodbox dev docs check - Check generated docs for drift

Check marker-delimited documentation sections for drift.
Examples:
- prodbox dev docs check  # Fail when generated documentation has drifted.

## prodbox dev docs generate
prodbox dev docs generate - Regenerate generated docs

Rewrite marker-delimited documentation sections from their renderers.
Examples:
- prodbox dev docs generate  # Regenerate marker-delimited documentation sections.

## prodbox dev tla-check
prodbox dev tla-check - Run TLA+ checks

Run the TLA+ model checks.
Examples:
- prodbox dev tla-check  # Run the TLA+ model checks.

## prodbox dns check
prodbox dns check - Inspect Route 53 state

Inspect Route 53 ownership state.
Examples:
- prodbox dns check  # Inspect Route 53 state.

## prodbox edge reconcile
prodbox edge reconcile - Reconcile the public edge

Reconcile the public edge: the ZeroSSL DNS-01 ClusterIssuer and the Route 53 bootstrap record. Requires operational aws.*; fails fast naming `prodbox aws setup` when it is empty.
Options:
- --dry-run - Render the edge plan without mutating state
- --plan-file <PATH> - Write the rendered plan to a file

Examples:
- prodbox edge reconcile  # Reconcile Route 53 DNS + ZeroSSL TLS for the public edge.
- prodbox edge reconcile --dry-run  # Render the edge plan.

## prodbox edge status
prodbox edge status - Check public DNS/TLS edge state

Inspect Route 53, certificate, shared-host readiness, and the in-cluster Vault seal state.
Options:
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox edge status  # Inspect public-edge readiness (home substrate).
- prodbox edge status --substrate aws  # Inspect public-edge readiness on the AWS substrate.

## prodbox gateway start
prodbox gateway start - Start gateway daemon

Start the distributed gateway daemon.
Options:
- --config <PATH> - Gateway config path
- --dry-run - Render the daemon-start plan without mutating state
- --plan-file <PATH> - Write the rendered plan to a file

Examples:
- prodbox gateway start --config gateway.dhall  # Start the gateway daemon.

## prodbox gateway status
prodbox gateway status - Query gateway daemon status

Query the gateway daemon status surface.
Options:
- --config <PATH> - Gateway config path

Examples:
- prodbox gateway status --config gateway.dhall  # Inspect the gateway daemon state.

## prodbox gateway config-gen
prodbox gateway config-gen - Generate gateway config

Generate a gateway config template.
Options:
- --node-id <NODE_ID> - Node ID for the generated config

Examples:
- prodbox gateway config-gen gateway.json --node-id node-a  # Generate a gateway config template.

## prodbox help
prodbox help - Render help for a command path

Render detailed help for a registered command path.
Examples:
- prodbox help charts reconcile  # Render detailed help for `prodbox charts reconcile`.

## prodbox host ensure-tools
prodbox host ensure-tools - Verify required host tools

Check the supported host toolchain.
Examples:
- prodbox host ensure-tools  # Verify required host tools are installed.

## prodbox host check-ports
prodbox host check-ports - Check required ports

Check required host ports.
Examples:
- prodbox host check-ports  # Inspect required host ports.

## prodbox host info
prodbox host info - Display host diagnostics

Show host diagnostics.
Examples:
- prodbox host info  # Render host diagnostics.

## prodbox host firewall gateway-restrict
prodbox host firewall gateway-restrict - Restrict the gateway NodePort to 127.0.0.1

Install an idempotent iptables INPUT-DROP rule restricting the gateway NodePort to loopback ingress.
Options:
- --port <PORT> - Gateway-service NodePort to restrict (default: 30443)

Examples:
- prodbox host firewall gateway-restrict  # Install the loopback-only restriction on the default NodePort.
- prodbox host firewall gateway-restrict --port 30443  # Install the restriction on an explicit NodePort.

## prodbox host firewall gateway-unrestrict
prodbox host firewall gateway-unrestrict - Remove the gateway NodePort loopback restriction

Remove the idempotent iptables INPUT-DROP rule that restricts the gateway NodePort to loopback ingress. Safe to call when the rule is absent (reports `not-present` and exits 0).
Options:
- --port <PORT> - Gateway-service NodePort to unrestrict (default: 30443)

Examples:
- prodbox host firewall gateway-unrestrict  # Remove the loopback-only restriction on the default NodePort.
- prodbox host firewall gateway-unrestrict --port 30443  # Remove the restriction on an explicit NodePort.

## prodbox nuke
prodbox nuke - Total teardown of every prodbox-owned AWS resource (operator-only)

The only sanctioned path to destroy long-lived shared infrastructure (`aws-ses`, the long-lived `pulumi_state_backend` bucket) transitively. TTY-only; refuses non-interactive contexts. Requires the typed confirmation literal `NUKE EVERYTHING`. No `--yes` shorthand by design. `--dry-run` renders the plan without mutating any state.
Options:
- --dry-run - Render the teardown plan without mutating state
- --plan-file <PATH> - Write the rendered plan to a file

Examples:
- prodbox nuke --dry-run  # Render the total-teardown plan.

## prodbox test init
prodbox test init - Create prodbox.test.dhall

Create the executable-sibling prodbox.test.dhall test-topology document.
Options:
- --force - Overwrite an existing prodbox.test.dhall

Examples:
- prodbox test init  # Create the test-topology document.

## prodbox test run
prodbox test run - Run a topology-declared suite

Run one topology-declared suite, or every suite with `all`, using .test-data isolation.
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test run unit  # Run the topology-declared unit suite.

## prodbox test all
prodbox test all - Run the full test suite

Run lint and then the full test surface.
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test all  # Run lint and the full test surface.

## prodbox test lint
prodbox test lint - Run lint and build checks

Run the full lint surface plus a build.
Examples:
- prodbox test lint  # Run lint and build checks.

## prodbox test unit
prodbox test unit - Run unit tests

Run unit tests.
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test unit  # Run unit tests.

## prodbox test integration all
prodbox test integration all - Run all integration suites

Run all integration suites
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test integration all  # Run the `all` integration suite.

## prodbox test integration cli
prodbox test integration cli - Run CLI integration tests

Run CLI integration tests
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test integration cli  # Run the `cli` integration suite.

## prodbox test integration aws-iam
prodbox test integration aws-iam - Run AWS IAM integration tests

Run AWS IAM integration tests
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test integration aws-iam  # Run the `aws-iam` integration suite.

## prodbox test integration dns-aws
prodbox test integration dns-aws - Run Route 53 integration tests

Run Route 53 integration tests
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test integration dns-aws  # Run the `dns-aws` integration suite.

## prodbox test integration aws-eks
prodbox test integration aws-eks - Run EKS integration tests

Run EKS integration tests
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test integration aws-eks  # Run the `aws-eks` integration suite.

## prodbox test integration env
prodbox test integration env - Run environment integration tests

Run environment integration tests
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test integration env  # Run the `env` integration suite.

## prodbox test integration gateway-daemon
prodbox test integration gateway-daemon - Run gateway-daemon integration tests

Run gateway-daemon integration tests
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test integration gateway-daemon  # Run the `gateway-daemon` integration suite.

## prodbox test integration gateway-pods
prodbox test integration gateway-pods - Run gateway pod integration tests

Run gateway pod integration tests
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test integration gateway-pods  # Run the `gateway-pods` integration suite.

## prodbox test integration gateway-partition
prodbox test integration gateway-partition - Run gateway partition integration tests

Run gateway partition integration tests
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test integration gateway-partition  # Run the `gateway-partition` integration suite.

## prodbox test integration ha-rke2-aws
prodbox test integration ha-rke2-aws - Run HA RKE2 AWS integration tests

Run HA RKE2 AWS integration tests
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test integration ha-rke2-aws  # Run the `ha-rke2-aws` integration suite.

## prodbox test integration lifecycle
prodbox test integration lifecycle - Run lifecycle integration tests

Run lifecycle integration tests
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test integration lifecycle  # Run the `lifecycle` integration suite.

## prodbox test integration pulumi
prodbox test integration pulumi - Run Pulumi integration tests

Run Pulumi integration tests
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test integration pulumi  # Run the `pulumi` integration suite.

## prodbox test integration eks-volume-rebind
prodbox test integration eks-volume-rebind - Run retained-volume rebinding integration tests

Run retained-volume rebinding integration tests
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test integration eks-volume-rebind  # Run the `eks-volume-rebind` integration suite.

## prodbox test integration charts-storage
prodbox test integration charts-storage - Run chart-storage integration tests

Run chart-storage integration tests
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test integration charts-storage  # Run the `charts-storage` integration suite.

## prodbox test integration charts-platform
prodbox test integration charts-platform - Run chart-platform integration tests

Run chart-platform integration tests
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test integration charts-platform  # Run the `charts-platform` integration suite.

## prodbox test integration charts-vscode
prodbox test integration charts-vscode - Run vscode stack integration tests

Run vscode stack integration tests
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test integration charts-vscode  # Run the `charts-vscode` integration suite.

## prodbox test integration charts-api
prodbox test integration charts-api - Run API stack integration tests

Run API stack integration tests
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test integration charts-api  # Run the `charts-api` integration suite.

## prodbox test integration charts-websocket
prodbox test integration charts-websocket - Run WebSocket stack integration tests

Run WebSocket stack integration tests
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test integration charts-websocket  # Run the `charts-websocket` integration suite.

## prodbox test integration admin-routes
prodbox test integration admin-routes - Run shared-host admin-route integration tests

Run shared-host admin-route integration tests
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test integration admin-routes  # Run the `admin-routes` integration suite.

## prodbox test integration public-dns
prodbox test integration public-dns - Run public DNS integration tests

Run public DNS integration tests
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test integration public-dns  # Run the `public-dns` integration suite.

## prodbox test integration keycloak-invite
prodbox test integration keycloak-invite - Run Keycloak operator-invite integration tests

Run Keycloak operator-invite integration tests
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test integration keycloak-invite  # Run the `keycloak-invite` integration suite.

## prodbox test integration sealed-vault
prodbox test integration sealed-vault - Run sealed-Vault fail-closed integration tests

Run sealed-Vault fail-closed integration tests
Options:
- --coverage - Enable coverage reporting for the selected test scope
- --cov-fail-under <INTEGER> - Require a minimum coverage percentage
- --substrate <SUBSTRATE> - Target substrate (home-local, aws); default home-local

Examples:
- prodbox test integration sealed-vault  # Run the `sealed-vault` integration suite.

## prodbox users invite
prodbox users invite - Invite an operator-owned user by email

Create a Keycloak user with emailVerified=false and trigger the SES-backed invite email.
Options:
- --role <ROLE> - Operator-defined role to assign on invite
- --dry-run - Render the invite plan without mutating state
- --plan-file <PATH> - Write the rendered plan to a file

Examples:
- prodbox users invite operator@example.invalid  # Invite operator@example.invalid to set up an account.
- prodbox users invite operator@example.invalid --role admin  # Invite an operator with the admin role assignment.

## prodbox users list
prodbox users list - List operator-managed users

List Keycloak users with their email-verification status and last-login time.
Options:
- --status, -s - Filter the listing by status (default all; --status alone selects verified)
- --status-unverified - Restrict the listing to users whose email is not yet verified

Examples:
- prodbox users list  # List all operator-managed users.
- prodbox users list --status  # List only email-verified users.
- prodbox users list --status-unverified  # List users awaiting invite activation.

## prodbox users revoke
prodbox users revoke - Disable or delete an operator-managed user

Revoke an operator-managed user. Disables the user by default; pass --delete to fully remove the user.
Options:
- --delete - Fully delete the user instead of disabling
- --dry-run - Render the revoke plan without mutating state
- --plan-file <PATH> - Write the rendered plan to a file

Examples:
- prodbox users revoke operator@example.invalid  # Disable the operator account.
- prodbox users revoke operator@example.invalid --delete  # Fully delete the operator account.

## prodbox vault status
prodbox vault status - Report Vault seal state

Probe the in-cluster Vault and report initialized / sealed / unseal-progress, or that it is unreachable.
Examples:
- prodbox vault status  # Probe the in-cluster Vault seal state.

## prodbox vault init
prodbox vault init - Initialize Vault

Initialize an uninitialized Vault, capturing the unseal/recovery keys and root token into the encrypted unlock bundle exactly once.
Examples:
- prodbox vault init  # Initialize Vault and write the encrypted unlock bundle.

## prodbox vault unseal
prodbox vault unseal - Unseal Vault

Decrypt the host-side unlock bundle and submit the unseal keys until Vault is unsealed.
Examples:
- prodbox vault unseal  # Unseal Vault from the encrypted unlock bundle.

## prodbox vault seal
prodbox vault seal - Seal Vault

Seal Vault, returning the cluster to the fail-closed state.
Examples:
- prodbox vault seal  # Seal Vault.

## prodbox vault reconcile
prodbox vault reconcile - Reconcile Vault policy

Idempotently reconcile Vault auth mounts, policies, roles, KV mounts, Transit keys, PKI issuers, and Kubernetes auth roles.
Examples:
- prodbox vault reconcile  # Reconcile Vault mounts, policies, and keys.

## prodbox vault rotate-unlock-bundle
prodbox vault rotate-unlock-bundle - Re-encrypt the unlock bundle

Re-encrypt the host-side unlock bundle under a new password.
Examples:
- prodbox vault rotate-unlock-bundle  # Re-encrypt the unlock bundle under a new password.

## prodbox vault rotate-transit-key
prodbox vault rotate-transit-key - Rotate a Transit key

Rotate a named Vault Transit envelope key to a new version.
Examples:
- prodbox vault rotate-transit-key prodbox-minio-envelope  # Rotate the named Transit key.

## prodbox vault pki status
prodbox vault pki status - Report Vault PKI state

Report the Vault PKI issuer and certificate state.
Examples:
- prodbox vault pki status  # Report Vault PKI issuer state.

## prodbox vault pki issue-test-cert
prodbox vault pki issue-test-cert - Issue a throwaway PKI cert

Issue a throwaway certificate from Vault PKI to validate the issuer.
Examples:
- prodbox vault pki issue-test-cert  # Issue a throwaway Vault PKI certificate.

## prodbox workload start
prodbox workload start - Start internal workload runtime

Start the internal workload daemon.
Options:
- --config <PATH> - Workload Dhall config path (e.g. /etc/workload/config.dhall)

Examples:
- prodbox workload start  # Start the internal workload runtime.

-- end --
