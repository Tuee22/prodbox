global:
  namespace: default
  rootChart: websocket

replicaCount: 2

podAntiAffinity:
  enabled: true

image:
  repository: 127.0.0.1:30080/prodbox/prodbox-public-edge-workload
  tag: latest

gateway:
  name: public-edge
  namespace: vscode
  listenerName: websocket-https
  host: ws.example.com

jwt:
  securityPolicyName: websocket-jwt
  providerName: keycloak
  issuer: https://auth.example.com/realms/prodbox
  audience: prodbox-websocket
  jwksUri: https://auth.example.com/realms/prodbox/protocol/openid-connect/certs
  routeClaimName: prodbox_route
  routeClaimValue: websocket

oidc:
  issuer: https://auth.example.com/realms/prodbox
  tokenEndpoint: http://keycloak.vscode.svc.cluster.local:8080/realms/prodbox/protocol/openid-connect/token
  clientId: prodbox-websocket
  clientSecret: change-me
  publicBaseUrl: https://ws.example.com

redis:
  host: redis
  port: 6379

websocket:
  port: 8080
  path: /ws
