# System Components

**Status**: Authoritative source
**Supersedes**: N/A
**Referenced by**: [README.md](README.md), [development_plan_standards.md](development_plan_standards.md), [00-overview.md](00-overview.md), [substrates.md](substrates.md), [phase-1-runtime-cli-aws-foundations.md](phase-1-runtime-cli-aws-foundations.md), [phase-2-gateway-dns.md](phase-2-gateway-dns.md), [phase-3-chart-platform-vscode.md](phase-3-chart-platform-vscode.md), [phase-4-lifecycle-canonical-paths.md](phase-4-lifecycle-canonical-paths.md), [phase-5-canonical-test-suite.md](phase-5-canonical-test-suite.md), [phase-7-aws-substrate-foundations.md](phase-7-aws-substrate-foundations.md), [phase-8-email-invite-auth.md](phase-8-email-invite-auth.md), [../HASKELL_CLI_TOOL.md](../HASKELL_CLI_TOOL.md)

> **Purpose**: Document the authoritative target component inventory for infrastructure, runtime
> control surfaces, validation surfaces, and state or authority boundaries in the Haskell rewrite.

The inventory documents the authoritative Haskell-only implementation end state. The supported
public edge closes on one public hostname, shared-host path routing, and native-host-architecture
container publication. [legacy-tracking-for-deletion.md](legacy-tracking-for-deletion.md)
preserves cleanup history separately from this canonical inventory. The earlier alignment follow-
up on native `gateway-partition` validation, peer-transport trust-material runtime closure,
root-chart-only public chart commands, and the Harbor-plus-storage-backend bootstrap contract is
now closed in code and governed docs.

## Haskell-Only Architecture

| Surface | Owner | Paths |
|---------|-------|-------|
| CLI frontend and command surface | Haskell parser, native-only request ADT, and native command entrypoints for the full supported command matrix | `app/prodbox/Main.hs`, `src/Prodbox/CLI/Command.hs`, `src/Prodbox/CLI/Parser.hs`, `src/Prodbox/Native.hs`, `prodbox.cabal`, `cabal.project` |
| Configuration and settings | Haskell-owned Dhall-to-JSON decode bridge, repo-root config-path resolution, typed settings model, masked display, and validation over the operator-authored repo-root config | `src/Prodbox/Settings.hs`, `src/Prodbox/Repo.hs`, `prodbox-config.dhall`, `prodbox-config-types.dhall` |
| Settings and command runtime | Haskell build-support, effect, DAG, interpreter, prerequisite, result, subprocess, isolated AWS environment projection, and domain modules | `src/Prodbox/AwsEnvironment.hs`, `src/Prodbox/BuildSupport.hs`, `src/Prodbox/Effect.hs`, `src/Prodbox/EffectDAG.hs`, `src/Prodbox/EffectInterpreter.hs`, `src/Prodbox/Prerequisite.hs`, `src/Prodbox/Result.hs`, `src/Prodbox/Subprocess.hs`, `src/Prodbox/TestPlan.hs` |
| Host and Kubernetes helpers | Haskell host and k8s modules | `src/Prodbox/Host.hs`, `src/Prodbox/K8s.hs` |
| Container packaging and registry doctrine | Dockerfiles under `docker/`, Haskell-build containers that stay single-stage `ubuntu:24.04`, install `ghcup` in-image, pin GHC `9.14.1`, avoid symlinked Haskell tool shims, Harbor-first steady-state image sourcing with a Harbor-plus-storage-backend bootstrap exception only, ordered public-image candidate retry during Harbor mirror publish including `mirror.gcr.io` fallbacks for the Docker Hub-hosted Percona and Envoy images used on the supported path, and native-host-architecture image publication only with no supported `docker buildx`, cross-arch emulation, or mixed-arch cluster closure | `docker/`, `src/Prodbox/ContainerImage.hs`, `src/Prodbox/CLI/Rke2.hs`, `src/Prodbox/Lib/ChartPlatform.hs` |
| Pulumi orchestration and YAML stack programs | Haskell Pulumi orchestration over YAML Pulumi definitions for the public AWS substrate stacks, including stack-config synchronization for AWS substrate inputs, generated stack snapshots under `.prodbox-state/aws-test/` and `.prodbox-state/aws-eks-test/`, refresh-before-retry destroy hardening for both retained AWS stacks, one destroy-and-recreate repair when HA-RKE2 SSH validation finds stale retained EC2 nodes after a successful stack reconcile, and the generated HA-RKE2 validation SSH key under `.prodbox-state/aws-test/`. Local-lifecycle bootstrap DNS reconcile and ACME `ClusterIssuer` projection stay outside the public `prodbox pulumi ...` stack-program surface. | `src/Prodbox/CLI/Pulumi.hs`, `src/Prodbox/Infra/MinioBackend.hs`, `src/Prodbox/Infra/AwsTestStack.hs`, `src/Prodbox/Infra/AwsEksTestStack.hs`, `pulumi/aws-eks/Pulumi.yaml`, `pulumi/aws-eks/Main.yaml`, `pulumi/aws-test/Pulumi.yaml`, `pulumi/aws-test/Main.yaml`, generated state under `.prodbox-state/` |
| DNS inspection | Haskell DNS check module | `src/Prodbox/Dns.hs` |
| Shared public-edge route catalog | Haskell-owned single-host route catalog, path-prefix constants, canonical public-route URL construction, and Keycloak issuer derivation consumed by lifecycle, DNS, chart, host-diagnostic, and native validation surfaces | `src/Prodbox/PublicEdge.hs` |
| Gateway runtime | Haskell daemon runtime with bounded HTTP `/v1/state` observability, heartbeat, in-memory ownership, DNS write loops, Orders-backed gateway-interval validation, HMAC signing, peer-transport gossip with commit-log replication via `Prodbox.Gateway.Peer`, runtime claim/yield emission under the `canWriteDns` predicate, bounded-clock-skew enforcement keyed off `daemonMaxClockSkewSeconds` plus a `prodbox host info` NTP gate, monotonic Orders-version coordination across the mesh, and single-stage `ubuntu:24.04` packaging with in-image `ghcup`, pinned GHC `9.14.1`, no symlinked Haskell tool shims, and an official AWS CLI bundle per target architecture. | `src/Prodbox/Gateway.hs`, `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/Gateway/Peer.hs`, `src/Prodbox/Gateway/Types.hs`, `docker/gateway.Dockerfile` |
| Formal verification | Haskell TLA+ wrapper | `src/Prodbox/Tla.hs`, `documents/engineering/tla/` |
| Chart platform and retained state | Haskell chart registry, PostgreSQL platform constants, retained-storage reconciler, CLI runtime, the Percona-operator-backed application-database contract, path-routed browser and supported admin delivery through Envoy on `test.resolvefintech.com`, explicit bearer-token and browser-return-path auth-carrier doctrine, Keycloak JWT-backed Envoy auth and RBAC, JWT-only API delivery from Keycloak JWKS-backed Envoy validation, and the Redis-backed WebSocket chart surface with real `/ws` upgrade plus any later explicit external rate-limit-service surface | `src/Prodbox/CLI/Charts.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `src/Prodbox/Lib/Storage.hs`, `src/Prodbox/PostgresPlatform.hs`, `charts/`, plus generated retained non-PV state under `.prodbox-state/` |
| Public workload runtime | Haskell internal runtime for the supported API workload plus the WebSocket workload selected through environment rather than repo-root config; the current runtime owns the shared-host `/api` and `/ws` surfaces, real `/ws` upgrade, Redis-backed reconnect-safe state, one-connection-per-pod lifetime, and readiness-based drain | `src/Prodbox/Workload.hs` |
| Public-edge diagnostics | Haskell host diagnostic for the single-host Envoy edge and its path-routed application or admin surfaces | `src/Prodbox/Host.hs` |
| Onboarding and AWS administration | Haskell interactive onboarding plus AWS CLI subprocess orchestration for prompt-driven temporary elevated flows, with `aws_admin_for_test_simulation.*` reserved only for test-suite simulation of that ephemeral prompt input and supported AWS subprocess auth isolated from ambient host AWS state | `src/Prodbox/Aws.hs`, `src/Prodbox/AwsEnvironment.hs`, `src/Prodbox/CLI/Parser.hs`, `src/Prodbox/Native.hs` |
| Test harness and quality gate | Haskell build-support, governed-doctrine enforcement through `prodbox check-code`, aggregate suite ordering, supported-runtime bootstrap, and named real-world validation harness, including one shared suite-level AWS IAM harness that provisions temporary operational `aws.*` before aggregate AWS validation and clears those credentials again before suite return | `src/Prodbox/BuildSupport.hs`, `src/Prodbox/CheckCode.hs`, `src/Prodbox/TestPlan.hs`, `src/Prodbox/TestRunner.hs`, `src/Prodbox/TestValidation.hs`, `test/` |

## CLI Doctrine Components

Components introduced by the doctrine adoption sprints scheduled in
[phase-1-runtime-cli-aws-foundations.md](phase-1-runtime-cli-aws-foundations.md),
[phase-2-gateway-dns.md](phase-2-gateway-dns.md),
[phase-3-chart-platform-vscode.md](phase-3-chart-platform-vscode.md), and
[phase-4-lifecycle-canonical-paths.md](phase-4-lifecycle-canonical-paths.md).

| Component | Owner | Status | Paths |
|-----------|-------|--------|-------|
| `CommandSpec` source-of-truth registry | `src/Prodbox/CLI/Spec.hs` now owns the typed command tree, and `src/Prodbox/CLI/Parser.hs` renders that registry into the public `optparse-applicative` parser while the same registry also drives help, tree, JSON, manpage, and completion generation | ✅ Done (Sprint 1.6) | `src/Prodbox/CLI/Spec.hs`, `src/Prodbox/CLI/Parser.hs`, `src/Prodbox/CLI/Docs.hs`, `src/Prodbox/CLI/Tree.hs`, `src/Prodbox/CLI/Json.hs`, `src/Prodbox/App.hs` |
| `Plan` ADT and `Plan` / `apply` boundary | Shared `PlanOptions`, deterministic rendered plans, and the doctrine-owned `runPlanWithOptions` boundary now cover chart, RKE2, gateway-start, Pulumi, AWS setup or teardown, and config-setup flows, with golden coverage for every rendered plan plus unit coverage over dry-run vs apply behavior | ✅ Done (Sprint 1.7) | `src/Prodbox/CLI/Command.hs`, `src/Prodbox/CLI/Charts.hs`, `src/Prodbox/CLI/Rke2.hs`, `src/Prodbox/Gateway.hs`, `src/Prodbox/CLI/Pulumi.hs`, `src/Prodbox/Aws.hs`, `test/unit/Main.hs` |
| `Subprocess` typed-value ADT and interpreter | `src/Prodbox/Subprocess.hs` now exposes the doctrine-shaped `Subprocess` record, `renderSubprocess`, and the `runStreaming` / `capture` boundary backed internally by `typed-process`; validation paths that still consume the repository `Result` ADT use explicit `captureSubprocessResult` / `runSubprocessStreaming` adapters. `prodbox check-code` refuses direct `System.Process` / `System.Process.Typed` call sites and smart constructors in `src/Prodbox/` outside that interpreter. | ✅ Done (Sprint 1.8) | `src/Prodbox/Subprocess.hs`, `src/Prodbox/TestRunner.hs`, `src/Prodbox/CLI/Charts.hs`, `src/Prodbox/CLI/Rke2.hs`, `src/Prodbox/Gateway.hs`, `test/unit/Main.hs` |
| `prerequisiteRegistry` with remedy-hint contract | `src/Prodbox/Prerequisite.hs` now owns the authoritative prerequisite DAG, `src/Prodbox/EffectDAG.hs` rejects unknown node IDs during expansion, and prerequisite-aware command flows on the supported path now fail with the required node-id, description, and remedy-hint contract | ✅ Done (Sprint 1.9) | `src/Prodbox/Prerequisite.hs`, `src/Prodbox/EffectDAG.hs`, `src/Prodbox/EffectInterpreter.hs`, `test/unit/Main.hs` |
| `GeneratedSectionRule` registry | Single source of truth for generated marker-delimited sections plus paired `docs check` / `docs generate` | ✅ Done (Sprint 1.10) | `src/Prodbox/CheckCode.hs`, `src/Prodbox/CLI/Docs.hs`, repo-root `fourmolu.yaml` |
| `forbiddenPathRegistry` (negative-space lint) | Refuses parallel-workflow surfaces such as `.github/workflows/`, `.husky/`, `.githooks/`, root `Makefile` / `justfile` / `Taskfile.yml` | ✅ Done (Sprint 1.10) | `src/Prodbox/CheckCode.hs` |
| `prodbox-haskell-style` test stanza | Cabal `test-suite` mirror of `prodbox lint haskell` | ✅ Done (Sprint 1.11 component closure) | `prodbox.cabal`, `test/haskell-style/` |
| Capability classes plus `AsServiceError` | `src/Prodbox/Service.hs` now defines the service-error inventory, IO-backed `HasMinIO` / `HasRedis` / `HasPg` capability instances, and `retryServiceAction`; Patroni chart-platform calls classify transient PostgreSQL failures through `PgError`, and MinIO backend bucket calls route through `runMinIOWithEnv` instead of direct `aws` subprocess construction. | ✅ Done (Sprint 1.12; chart Redis/Postgres closed by Sprint 3.9) | `src/Prodbox/Service.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `src/Prodbox/Infra/MinioBackend.hs`, `test/unit/Main.hs` |
| `RetryPolicy` first-class values | Explicit `RetryPolicy` ADT plus pure `retryDelayMicros` are consumed by RKE2, chart-platform, service retry, and daemon-lifecycle retry paths. | ✅ Done (Sprint 1.13) | `src/Prodbox/Retry.hs`, `src/Prodbox/Service.hs`, `src/Prodbox/CLI/Rke2.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `src/Prodbox/Gateway/Daemon.hs`, `test/unit/Main.hs` |
| `ErrorKind = Recoverable | Fatal` axis | `AppError` carries `errorKind`, `errorMsg`, and `errorCause`; the CLI boundary renders it through one shared output layer, and daemon worker retry decisions classify recoverable versus fatal failures before restart or propagation. | ✅ Done (Sprint 1.14) | `src/Prodbox/Error.hs`, `src/Prodbox/Retry.hs`, `src/Prodbox/CLI/Output.hs`, `src/Prodbox/Gateway/Daemon.hs`, `test/unit/Main.hs` |
| `Prodbox.Naming` helpers | Shared `boundedResourceName`, `sanitizeResourceName`, and `hashSuffix` now own DNS-1123 or 63-character naming across storage, Patroni, and chart-platform related-resource surfaces | ✅ Done (Sprint 1.15) | `src/Prodbox/Naming.hs`, `src/Prodbox/Lib/Storage.hs`, `src/Prodbox/PostgresPlatform.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `test/unit/Main.hs` |
| Paired-resource smart constructors | `storageBinding` plus the shared Patroni helper inventory now centralize the retained PV or PVC and related-name pairings used by the chart platform | ✅ Done (Sprint 3.8) | `src/Prodbox/Lib/Storage.hs`, `src/Prodbox/PostgresPlatform.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `test/unit/Main.hs` |
| GADT-indexed state machines | `src/Prodbox/StateMachine.hs` defines phantom-indexed transition surfaces for gateway ownership, Pulumi stack lifecycle, and chart deploy phases, with valid transition paths typechecked in the unit suite. | ✅ Done (Sprint 1.16) | `src/Prodbox/StateMachine.hs`, `test/unit/Main.hs` |
| Explicit daemon lifecycle scaffold | `runGatewayDaemon` builds the daemon `Env`, installs signal handlers, tracks readiness, coordinates bounded drain, wraps workers with retry, and gates gateway daemon acquire through the prerequisite registry. | ✅ Done (Sprint 2.9) | `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/Gateway.hs`, `src/Prodbox/Prerequisite.hs` |
| Daemon `/healthz`, `/readyz`, `/metrics` endpoints | Standard liveness, readiness, and Prometheus metrics endpoints alongside `/v1/state`, with daemon-lifecycle goldens and readiness or metrics negative-space rules | ✅ Done (Sprint 2.10) | `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/CheckCode.hs`, `test/daemon-lifecycle/Main.hs`, `test/golden/daemon-health/` |
| `BootConfig` / `LiveConfig` split with `SIGHUP` hot reload | Hot-reloadable daemon fields live in `envLiveConfig :: TVar LiveConfig`; reloads validate the structured `schemaVersion` / `boot` / `live` shape, atomically swap live config, and publish through STM. | ✅ Done (Sprint 2.11) | `src/Prodbox/Gateway/Types.hs`, `src/Prodbox/Gateway/Daemon.hs` |
| `co-log` structured JSON logger | Gateway and workload daemon entrypoints emit JSON stderr logs through the co-log-backed typed field helpers; gateway log sites read `envLiveConfig` at emission time, and lifecycle/style coverage guards the JSON envelope, hot-reload filtering, dependency boundary, direct terminal writes, and inline log-object negative space | ✅ Done (Sprint 2.12) | `src/Prodbox/Gateway/Logging.hs`, `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/Workload.hs`, `test/daemon-lifecycle/Main.hs` |
| Test hooks in `Env` | No-op production hook fields exist for gateway peer-commit and connection surfaces, and `prodbox check-code` enforces literal production `noopDaemonHooks` construction plus hook reads through `envHooks env`. | ✅ Done (Sprint 2.13) | `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/Daemon/Events.hs`, `test/` |
| `prodbox-daemon-lifecycle` test stanza | Cabal `test-suite` spawns the built gateway daemon through the typed subprocess boundary, polls `/readyz` through the shared retry helper, checks `/healthz` and `/metrics`, captures endpoint response goldens, exercises first- and second-SIGTERM behavior, and `prodbox check-code` rejects direct `threadDelay` / raw `terminateProcess`. | ✅ Done (Sprint 2.14) | `prodbox.cabal`, `test/daemon-lifecycle/` |
| `prodbox-pulumi` test stanza | Cabal `test-suite` validates retained Pulumi-program ownership plus the isolated ephemeral-stack harness locally, including typed-output round-trips and forced-failure cleanup; the live retained AWS flows are covered by the named integration validations and aggregate suite. | ✅ Done (Sprint 4.7) | `prodbox.cabal`, `test/pulumi/`, `src/Prodbox/CLI/Pulumi.hs`, `src/Prodbox/TestValidation.hs` |
| `prodbox rke2 reconcile` canonical entrypoint | Canonical reconcile command is implemented, documented, and validated locally; the completed one-cycle `install` alias has been removed from the parser, registry, generated docs, and current guidance | ✅ Done (Sprint 4.5) | `src/Prodbox/CLI/Rke2.hs`, `src/Prodbox/CLI/Command.hs`, `src/Prodbox/CLI/Parser.hs`, `src/Prodbox/CLI/Spec.hs` |
| `--format` / `--color` output discipline | `OutputOptions`, `OutputFormat`, `ColorMode`, `--format`, `--color`, `--no-color`, and the stdout/stderr writer boundary are implemented and guarded by the output-boundary lint checks. | ✅ Done (Sprint 1.17) | `src/Prodbox/CLI/Output.hs`, `src/Prodbox/CLI/Spec.hs`, `src/Prodbox/CheckCode.hs`, `test/unit/Main.hs`, `test/haskell-style/Main.hs` |
| One-shot `Env` and `newtype App` | Shared `Env` and `newtype App` backed by `ReaderT Env IO` exist for one-shot command dispatch, with environment access covered in the unit suite. | ✅ Done (Sprint 1.18) | `src/Prodbox/App.hs`, `test/unit/Main.hs` |
| Style-tools sandbox plus `.hlint.yaml` nesting rules | `src/Prodbox/Lint.hs` bootstraps Fourmolu `0.19.0.1` and HLint `3.10` into `.build/prodbox-style-tools/bin/` through `ghcup run --install` on isolated formatter GHC `9.12.4`, while `prodbox lint haskell` runs the sandboxed binaries by absolute path and enforces the doctrine-owned nested-case marker set | ✅ Done (Sprint 1.19) | `.hlint.yaml`, `src/Prodbox/Lint.hs`, `src/Prodbox/BuildSupport.hs`, `src/Prodbox/CheckCode.hs`, `test/haskell-style/Main.hs` |
| `prodbox test lint` alias plus lint-first ordering | Alias and ordering are implemented in the parser, registry, runner, and governed docs | ✅ Done (Sprint 1.20) | `src/Prodbox/CLI/Command.hs`, `src/Prodbox/CLI/Parser.hs`, `src/Prodbox/CLI/Spec.hs`, `src/Prodbox/TestRunner.hs` |
| `trackingGeneratedPaths` registry plus renderer determinism property test | Third registry alongside `GeneratedSectionRule` and `forbiddenPathRegistry` for fully-generated files, plus repeated-render and synthetic forbidden-input determinism checks in the haskell-style suite | ✅ Done (Sprint 1.21) | `src/Prodbox/CheckCode.hs`, `test/haskell-style/` |
| Standardized library set in `prodbox.cabal` | The dependency audit is closed: `typed-process` is isolated at the subprocess boundary, `co-log` / `co-log-core` are owned by Sprint 2.12, and retained project-specific libraries are documented in the dependency-management doctrine. | ✅ Done (Sprint 1.22) | `prodbox.cabal`, `documents/engineering/dependency_management.md` |
| Daemon CLI plumbing plus `PRODBOX_*` env-var precedence | Gateway and workload launch paths now honor CLI flag > env var > config/default precedence for `--config`, `--log-level`, and `--port` | ✅ Done (Sprint 2.15) | `src/Prodbox/CLI/Parser.hs`, `src/Prodbox/CLI/Spec.hs`, `src/Prodbox/Gateway.hs`, `src/Prodbox/Workload.hs` |
| `Prodbox.Daemon.Events` at-least-once event-processing module | Immutable `StoredEvent` records with `created_at`, idempotent `EventHandler` newtype, and the `recordEvent` / `markEventProcessed` / `fetchUnprocessedEvents` triad; the gateway remains the documented in-memory anti-entropy peer-gossip variant | ✅ Done (Sprint 2.16) | `src/Prodbox/Daemon/Events.hs`, `documents/engineering/distributed_gateway_architecture.md`, `test/unit/Main.hs` |
| `prodbox lint chart` Helm-chart structural-invariants linter | `prodbox lint chart` validates `Chart.yaml` metadata, required chart-label helpers, and generated route-inventory drift through `src/Prodbox/CheckCode.hs` | ✅ Done (Sprint 3.12) | `src/Prodbox/CheckCode.hs`, `src/Prodbox/CLI/Parser.hs`, `src/Prodbox/CLI/Spec.hs` |
| Dhall freeze discipline on the committed repo-root config path | Hash-pinned committed Dhall imports on the supported repo-root config path; `prodbox check-code` refuses unfrozen imports and the supported config-writing paths refreeze after rewrites | ✅ Done (Sprint 1.23) | `prodbox-config.dhall`, `src/Prodbox/Aws.hs`, `src/Prodbox/CheckCode.hs` |
| HLint negative-space symbol rules for daemon paths | The governed Haskell lint scan enforces the doctrine-owned daemon-path marker set for `forkIO`, `unsafePerformIO`, module-level `IORef`, forbidden subprocess primitives, and case-inside-lambda nesting before handing off to sandboxed HLint | ✅ Done (Sprint 1.19) | `.hlint.yaml`, `src/Prodbox/Lint.hs`, `src/Prodbox/CheckCode.hs`, `test/haskell-style/Main.hs` |
| Marker-delimited route-inventory generation | Renders the `src/Prodbox/PublicEdge.hs` route catalog into the consuming Helm chart manifests via the generated-section registry, and `prodbox lint chart` validates drift | ✅ Done (Sprint 3.12) | `src/Prodbox/PublicEdge.hs`, `src/Prodbox/CheckCode.hs`, `charts/` |
| Durable CLI documentation artifacts | Generated Markdown command reference, doctrine-owned manpages, shell completion scripts, and tracked-generated-path enforcement all derive from the command registry | ✅ Done (Sprint 1.24) | `documents/cli/commands.md`, `share/man/man1/prodbox*.1`, `share/completion/{bash,zsh,fish}/`, `src/Prodbox/CLI/Docs.hs` |
| Parser-test category via `execParserPure` | Dedicated parser module now exercises leaf happy and unhappy paths through the pure optparse API and is guarded against subprocess regression | ✅ Done (Sprint 1.25) | `test/unit/Parser.hs`, `prodbox-unit` stanza, `documents/engineering/unit_testing_policy.md` |
| `renderError` error-rendering boundary | `src/Prodbox/CLI/Output.hs` owns `renderError`, `writeError`, and terminal writer helpers; command failure paths route through the output layer, and `prodbox check-code` refuses `print`, `exitFailure`, `putStr`, `putStrLn`, and direct stderr writes outside the allowed output/logging boundaries | ✅ Done (Sprint 1.26) | `src/Prodbox/CLI/Output.hs`, `src/Prodbox/Error.hs`, `src/Prodbox/App.hs`, `src/Prodbox/CheckCode.hs`, `test/unit/Main.hs`, `test/haskell-style/Main.hs` |
| `envMetrics :: MetricsRegistry` typed daemon `Env` field | Typed metrics registry is threaded through the daemon `Env`, `/metrics` reads counter values from it, and the negative-space guard refuses module-local mutable counter state under daemon paths | ✅ Done (Sprint 2.10) | `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/CheckCode.hs`, `.hlint.yaml` |
| STM broadcast channel for `LiveConfig` subscribers | `TChan` notification surface is exposed through `Env` and successful reloads publish the refreshed `LiveConfig`. | ✅ Done (Sprint 2.11) | `src/Prodbox/Gateway/Daemon.hs` |
| Prescribed daemon config shape | Gateway config parsing and template generation accept the structured `schemaVersion` / `boot` / `live` shape and keep flat JSON compatibility; repository-root Dhall import freezing closed under Sprint 1.23. | ✅ Done (Sprint 2.11) | `src/Prodbox/Gateway/Types.hs`, `src/Prodbox/Gateway.hs`, `prodbox-config.dhall`, `prodbox-config-types.dhall` |
| Cabal-manifest toolchain pin declarations | `tested-with: ghc ==9.14.1` and `with-compiler: ghc-9.14.1` are present and the governed docs now cite the authoritative Cabal `3.16.1.0` pin | ✅ Done (Sprint 1.27) | `prodbox.cabal`, `cabal.project`, `DEVELOPMENT_PLAN/` |
| Library-first / thin-`Main.hs` layout audit | `app/prodbox/Main.hs` is thin and `src/Prodbox/CheckCode.hs` enforces the doctrine rule | ✅ Done (Sprint 1.27) | `app/prodbox/Main.hs`, `src/Prodbox/App.hs`, `src/Prodbox/CheckCode.hs` |
| `CommandSpec` / `OptionSpec` record-field bindings | The doctrine-named record fields are implemented in `src/Prodbox/CLI/Spec.hs`, and the registry now drives the public parser as well as the generated introspection and documentation surfaces | ✅ Done (Sprint 1.6, extended by Sprint 0.4) | `src/Prodbox/CLI/Spec.hs` |
| Daemon-as-typed-`Command` dispatch | Gateway daemon entry is a `GatewayDaemonCommand DaemonLaunchOptions` constructor on the top-level `Command` ADT dispatched through ordinary pattern matching — bound per [HASKELL_CLI_TOOL.md → Long-Running Daemons → Daemon as Command](../HASKELL_CLI_TOOL.md) §1156–1196 | ✅ Done (Sprint 1.6, extended by Sprint 0.4) | `src/Prodbox/CLI/Spec.hs`, `src/Prodbox/App.hs`, `src/Prodbox/Gateway/Daemon.hs` |
| Forbidden subprocess primitives | `prodbox lint files` now names `callProcess`, `readCreateProcess`, `readCreateProcessWithExitCode`, `createProcess`, `proc`, `shell`, and `import System.Process` in `src/Prodbox/` outside `src/Prodbox/Subprocess.hs`; the scanner strips string literals before token checks so Linux `/proc` paths do not false-positive, and `.hlint.yaml` carries the matching doctrine marker set | ✅ Done (Sprint 1.8, extended by Sprint 0.4) | `src/Prodbox/CheckCode.hs`, `src/Prodbox/Subprocess.hs`, `.hlint.yaml` |
| `fourmolu.yaml` 12-setting list | The repo-root `fourmolu.yaml` carries the twelve doctrine-named settings and the dedicated haskell-style suite enforces them | ✅ Done (Sprint 1.10, extended by Sprint 0.4) | `fourmolu.yaml`, `test/haskell-style/Main.hs` |
| Canonical property-test invariants | Deterministic renderer and plan assertions exist in `test/unit/Main.hs` and `test/haskell-style/Main.hs`, the repo-root Dhall config plus persisted gateway and AWS snapshot shapes now round-trip through their supported decode paths, and parser examples now round-trip back to their registered command paths | ✅ Done (Sprint 1.11, extended by Sprint 0.4) | `test/unit/Main.hs`, `test/unit/Parser.hs`, `test/haskell-style/Main.hs` |
| Service-error newtype inventory | `MinIOError`, `RedisError`, and `PgError` each wrap `ServiceError`, carry `AsServiceError` instances, and are consumed by the current MinIO, Redis, and PostgreSQL service boundaries. | ✅ Done (Sprint 1.12, extended by Sprint 0.4) | `src/Prodbox/Service.hs`, `src/Prodbox/Infra/MinioBackend.hs`, `test/unit/Main.hs` |
| `AppError` record shape | `src/Prodbox/Error.hs` defines `AppError { errorKind, errorMsg, errorCause }`, the shared output layer consumes that record shape, and daemon worker retry classification uses it. | ✅ Done (Sprint 1.14, extended by Sprint 0.4) | `src/Prodbox/Error.hs`, `src/Prodbox/CLI/Output.hs`, `src/Prodbox/Gateway/Daemon.hs`, `test/unit/Main.hs` |
| Naming-helper signatures with DNS-1123 / 63-character constraints | `boundedResourceName :: Text -> Text -> Text -> Text`, `sanitizeResourceName :: Text -> Text`, and `hashSuffix :: Text -> Text` are implemented in `src/Prodbox/Naming.hs` and now back storage, Patroni, and chart-platform related-resource naming on the supported path | ✅ Done (Sprint 1.15, extended by Sprint 0.4) | `src/Prodbox/Naming.hs`, `src/Prodbox/Lib/Storage.hs`, `src/Prodbox/PostgresPlatform.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `test/unit/Main.hs` |
| Enumerated forbidden renderer inputs | Renderers in `GeneratedSectionRule` and `trackingGeneratedPaths` may not depend on timestamps, random IDs, locale-dependent ordering, terminal-width-dependent wrapping, or environment-dependent paths; the haskell-style suite injects synthetic violations for each class | ✅ Done (Sprint 1.21, extended by Sprint 0.4) | `src/Prodbox/CheckCode.hs`, `test/haskell-style/` |
| Structured-concurrency primitive set | Gateway daemon workers now use `Control.Concurrent.Async.withAsync`, `race`, and `concurrently`, and the lint guard rejects daemon use of unrestricted Async primitives — bound per [HASKELL_CLI_TOOL.md → Long-Running Daemons → Structured Concurrency](../HASKELL_CLI_TOOL.md) §1313–1324 | ✅ Done (Sprint 2.9, extended by Sprint 0.4) | `.hlint.yaml`, `src/Prodbox/CheckCode.hs`, daemon-path modules under `src/Prodbox/Gateway/` |
| Forbidden reload triggers | `fsnotify`, `inotify`, and `mtime` polling are named forbidden in the lint stack; SIGHUP via the dedicated `TBQueue ()` worker is the only sanctioned trigger — bound per [HASKELL_CLI_TOOL.md → Long-Running Daemons → Configuration → Reload Trigger](../HASKELL_CLI_TOOL.md) §1491–1500 | ✅ Done (Sprint 2.11, extended by Sprint 0.4) | `.hlint.yaml`, `src/Prodbox/CheckCode.hs`, `src/Prodbox/Gateway/Daemon.hs` |
| Typed `schemaVersion` field plus mismatch-as-parse-failure | The gateway daemon config parser treats structured `schemaVersion` mismatches as `config_schema_mismatch` and the reload path preserves the running config. | ✅ Done (Sprint 2.11, extended by Sprint 0.4) | `src/Prodbox/Gateway/Types.hs`, `src/Prodbox/Gateway/Daemon.hs`, `prodbox-config-types.dhall` |
| Eight-step reload procedure | The reload worker runs the doctrine-named steps in order: read path, parse/decode, log warn on failure, reject boot-field changes, validate `schemaVersion`, atomic `writeTVar`, emit `config_reloaded` with changed fields, and publish on STM broadcast channel. | ✅ Done (Sprint 2.11, extended by Sprint 0.4) | `src/Prodbox/Gateway/Daemon.hs` |
| Typed logging field helpers | `field :: (Aeson.ToJSON a) => Text -> a -> (Text, Aeson.Value)` plus `logStructured`, `logStructuredAt`, `logDebug`, `logInfo`, `logWarn`, and `logError` wrappers are backed by `co-log`; `prodbox check-code` and the Haskell style suite reject direct daemon terminal writes and inline log-object construction — bound per [HASKELL_CLI_TOOL.md → Long-Running Daemons → Logging](../HASKELL_CLI_TOOL.md) §1370–1410 | ✅ Done (Sprint 2.12, extended by Sprint 0.4) | `src/Prodbox/Gateway/Logging.hs`, `src/Prodbox/CheckCode.hs`, `test/haskell-style/Main.hs` |
| Production-no-op / test-injected hook contract | Daemon `Env` hook fields have no-op production defaults, and `prodbox check-code` enforces production startup through literal `noopDaemonHooks` plus hook reads through injected `envHooks env` — bound per [HASKELL_CLI_TOOL.md → Long-Running Daemons → Test Hooks](../HASKELL_CLI_TOOL.md) §1284–1300 | ✅ Done (Sprint 2.13, extended by Sprint 0.4) | `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/CheckCode.hs`, `test/integration/` |
| Health-endpoint response shapes as golden tests | The `prodbox-daemon-lifecycle` stanza captures `/healthz`, ready/draining `/readyz`, and normalized `/metrics` response-shape goldens under `test/golden/daemon-health/` | ✅ Done (Sprint 2.14, extended by Sprint 0.4) | `test/daemon-lifecycle/Main.hs`, `test/golden/daemon-health/` |
| Forbidden reconciler flags and sister commands | `--force`, `--reinstall`, and sister commands `install`, `upgrade`, `repair`, `force-install` are rejected at parse time on the chart and lifecycle reconciler surfaces; no legacy lifecycle alias remains on the supported command surface | ✅ Done (Sprints 3.10 / 4.5, extended by Sprint 0.4) | `src/Prodbox/CLI/Parser.hs`, `test/unit/Parser.hs` |

## Infrastructure Layer

| Component | Technology | Deployment | Authority | Durable State |
|-----------|------------|------------|-----------|---------------|
| Supported host runtime | `Ubuntu 24.04 LTS` with systemd | Bare metal | `prodbox` supported-host gate | Host filesystem |
| Host build root | `.build/` | Repository worktree | Canonical `cabal build --builddir=.build exe:prodbox` plus copy to `.build/prodbox`, which backs the supported `prodbox` command surface | Host filesystem |
| Container build root | `/opt/build` | Docker build stage | Dockerfiles under `docker/` | Container filesystem |
| Home local substrate (RKE2 on operator host) | RKE2 | Home local substrate (per [substrates.md](substrates.md)) | `prodbox rke2 reconcile|delete --yes|status|start|stop|restart|logs`, including hermetic delete reporting on success and actionable failure summaries on non-zero uninstall, closed by Sprint `4.8`. Provides the home substrate against which the canonical test suite runs. | RKE2 data dirs and systemd units |
| Harbor-first registry pipeline | Harbor plus Docker CLI image reconcile, Harbor-plus-storage-backend public-registry bootstrap exception until Harbor is externally ready, post-bootstrap Harbor populate with ordered candidate retry on publish-time failure, and native-host-architecture-only custom-image publish | RKE2 workload plus host Docker runtime | `prodbox rke2 reconcile` plus Harbor-first steady-state workload-image doctrine | Cluster resources, Harbor registry data, host Docker cache, and RKE2 registry config |
| AWS substrate (EKS provisioning) | Amazon EKS | AWS substrate (provisioned per [substrates.md](substrates.md)) | `prodbox pulumi eks-resources|eks-destroy --yes`; the canonical-suite `aws-eks` validation in `src/Prodbox/TestValidation.hs` runs against it | EKS resources in AWS |
| AWS substrate (HA RKE2 provisioning) | `Ubuntu 24.04 LTS` EC2 instances | AWS substrate (provisioned per [substrates.md](substrates.md)) | `prodbox pulumi test-resources|test-destroy --yes` plus Haskell SSH orchestration; the canonical-suite `ha-rke2-aws` validation runs against it | EC2, VPC, subnet, security-group, IAM, and Route 53 state |
| Pulumi test backend | MinIO | Local RKE2 workload | Local-cluster-first bootstrap plus Pulumi backend configuration, bounded backend login, and deleted-export-mount repair | S3-compatible objects in `prodbox-test-pulumi-backends` |
| Load balancer IPs | MetalLB on self-managed local clusters with config-selected L2 or BGP advertisement, including `IPAddressPool`, `L2Advertisement`, `BGPPeer`, and `BGPAdvertisement` resources | RKE2 workload | Haskell lifecycle plus Gateway API edge doctrine | Cluster resources |
| Public edge controller | Envoy Gateway plus Kubernetes Gateway API, with browser-facing auth and operational-dashboard access enforced at Envoy, request-carried JWTs validated locally at Envoy from Keycloak issuer metadata plus JWKS-backed signing keys and route claims, shared-host Keycloak integration on `test.resolvefintech.com`, public TLS termination at Envoy on port `443`, redirect-only HTTP handling on port `80`, and WebSocket-capable proxying on the same public edge | RKE2 workload | Haskell lifecycle plus single-host public-edge doctrine | Cluster resources |
| TLS issuance | cert-manager plus ACME provider selected from config, with ZeroSSL EAB projection when configured | RKE2 workload | Haskell lifecycle plus chart runtime, including lifecycle-owned `ClusterIssuer` projection | Kubernetes secrets |
| DNS control plane | Route 53 hosted zone with one canonical public record for `test.resolvefintech.com` | AWS | Haskell AWS orchestration plus in-cluster gateway `dns_write_gate` | Route 53 |
| Gateway workload | Haskell gateway daemon | In-cluster Kubernetes workload | `prodbox charts deploy gateway` plus `prodbox gateway status --config <path>` | Cluster resources, `gateway-aws-credentials`, and Route 53 |
| Cluster storage class | `manual` (`kubernetes.io/no-provisioner`) | RKE2 cluster | `prodbox rke2 reconcile` | Cluster-scoped resource |
| Manual PV root | Configured host path, default `.data/` | Host filesystem | `prodbox-config.dhall` plus cluster and chart lifecycle commands | PV contents only |
| Retained repo-local state root | `.prodbox-state/` | Host filesystem | `prodbox charts`, `prodbox pulumi ...`, AWS substrate helpers, and `prodbox rke2 delete --yes` preservation contract | Generated chart secrets, gateway event keys, AWS substrate stack snapshots, and the HA-RKE2 validation SSH key |
| Chart platform | Helm plus Haskell orchestration | RKE2 workloads | `prodbox charts ...` | Cluster resources plus retained roots |
| Cluster-wide Patroni operator platform | Percona operator for PostgreSQL installed by the canonical Helm lifecycle | RKE2 workload | `prodbox rke2 reconcile` plus Harbor-backed steady-state image sourcing | Cluster resources |
| External application PostgreSQL HA | Percona-operator-backed Patroni PostgreSQL clusters with exactly three PostgreSQL replicas and synchronous replication, rendered from namespace-local application chart inputs and reconciled by the cluster-wide operator | RKE2 workloads | Haskell chart platform plus Harbor-backed steady-state image sourcing | Cluster resources plus retained roots |
| Namespace-local identity stack | `keycloak` behind Envoy on the shared public hostname, with the supported issuer, proxy-header, and shared-path contract for chart-managed workloads; new login, refresh, and later JWKS refresh depend on this workload while the steady-state JWT hot path remains local to Envoy | RKE2 workload | Haskell chart platform plus external Percona-operator-backed Patroni PostgreSQL dependency | Cluster resources plus retained roots |
| Namespace-local app stack | `vscode`, `api`, `websocket`, and supported operational dashboards behind Envoy on explicit shared-host paths under `test.resolvefintech.com` | RKE2 workload | Haskell chart platform | Cluster resources plus retained roots |
| Shared realtime state | Redis backing supported WebSocket workloads today and any later explicit external rate-limit service, never Envoy JWT validation, and not yet shipping a standalone rate-limit-service workload on the current supported path | RKE2 workload | Haskell chart platform plus public workload doctrine | Cluster resources plus app state |
| AWS admin credential isolation | Dhall config `aws_admin_for_test_simulation` section that stores only test-suite simulation of the ephemeral elevated prompt input; the native IAM validation harness is the only supported runtime consumer, and the shared named-and-aggregate IAM harness materializes operational `aws.*` from it only for the duration of a validation run | Repository config plus integration tests | Haskell config and IAM validation harness | Repository root |

## CLI and Runtime Layer

| Surface | Command | Purpose |
|---------|---------|---------|
| CLI frontend | `prodbox <command>` | Parse the closed command surface and dispatch directly to native Haskell commands with no legacy delegation shim |
| Config management | `prodbox config setup|show|validate` | Interactively author, display, and validate operator-authored repository-root Dhall configuration decoded directly into Haskell types, with temporary elevated AWS input provided by prompts on the supported public path |
| Host prerequisites and diagnostics | `prodbox host ensure-tools|check-ports|info|firewall` | Verify local tools and diagnose host-networking prerequisites |
| RKE2 lifecycle | `prodbox rke2 reconcile|delete --yes|status|start|stop|restart|logs` | Reconcile, inspect, control, and remove the local cluster with a hermetic delete summary on success, including the cluster-wide Percona operator lifecycle and the Sprint `4.8` lifecycle-local quiet path that suppresses benign upstream uninstall chatter |
| Harbor reconcile and image population | `prodbox rke2 reconcile` | Allow direct public pulls only for Harbor and Harbor's storage backend before Harbor is healthy and externally serving, require a stable `/readyz` plus `/v2/` external window, reconcile the `prodbox` project, and ensure required custom and public images are present in Harbor before later Helm deployments, retrying alternate configured public-image candidates when a preferred source fails during Harbor publication, including `mirror.gcr.io` fallbacks for the Docker Hub-hosted Percona and Envoy images used by the supported lifecycle |
| Pulumi lifecycle | `prodbox pulumi eks-resources|eks-destroy --yes|test-resources|test-destroy --yes` | Provision and tear down the AWS substrate's Pulumi stacks (`aws-eks-test`, `aws-test`) per [substrates.md](substrates.md) |
| Public-edge diagnostic | `prodbox host public-edge` | Classify Route 53, Gateway API, TLS, port `80` redirect readiness, shared-host path routing, advertisement mode, and readiness for named external proof |
| Gateway runtime | `prodbox gateway start --config <path>|status --config <path>|config-gen <output-path> --node-id <node-id>` | Start the in-pod gateway entrypoint, inspect gateway state over the governed bounded HTTP `/v1/state` observability surface, serve `/healthz`, `/readyz`, and `/metrics`, and generate config |
| Public workload runtime | `prodbox workload start` | Run the internal API runtime or WebSocket runtime selected through `PRODBOX_WORKLOAD_MODE=api|websocket` for the chart-managed workloads behind the shared public hostname, including the real `/ws` upgrade, one-connection-per-pod lifetime, and readiness-based drain contract |
| Chart runtime | `prodbox charts list|status <chart>|deploy <chart> [--dry-run] [--plan-file <path>]|delete <chart> [--yes] [--dry-run] [--plan-file <path>]` | Manage the supported `gateway`, `keycloak`, `vscode`, `api`, and `websocket` chart surfaces, with `gateway` remaining the separate distributed gateway daemon path and the shared-host browser, API, WebSocket, Harbor, and MinIO routes staying behind Envoy with Keycloak-backed auth and RBAC |
| DNS check | `prodbox dns check` | Inspect DNS ownership state |
| Kubernetes utilities | `prodbox k8s health|wait|logs` | Inspect cluster readiness and collect infrastructure pod logs |
| Interactive onboarding | `prodbox config setup` | Guided Dhall authoring plus live AWS and operator prompts for one temporary elevated credential set |
| AWS IAM and quota management | `prodbox aws policy|setup|teardown|check-quotas|request-quotas` | Generate policies, manage IAM users, and manage AWS quotas through prompt-driven public admin flows |
| TLA+ validation | `prodbox tla-check` | Run formal verification |
| Test runner | `prodbox test lint|all|unit|integration ...` | Run named lint, unit, and integration suites on the Haskell stack, including the shared idempotent AWS IAM harness used by the named and aggregate IAM validation surfaces |
| Code quality | `prodbox check-code` | Run the required doctrine, formatting, lint, and type-check gate with governed doctrine-alignment enforcement |

## Validation Layer

The validation layer is one canonical test suite that runs against substrates (see
[substrates.md](substrates.md)). Validations are described as suite content with declared
prerequisites, not as substrate-specific concerns. The same validation runs on every substrate
that satisfies its declared prerequisites.

| Surface | Canonical Validation |
|---------|----------------------|
| Build artifact contract | Runnable `prodbox` binary at `.build/prodbox`, produced by the canonical build-plus-copy flow; container build proof closes when the canonical Dockerfiles under `docker/` emit artifacts under `/opt/build` through in-image `ghcup`-managed GHC `9.14.1` with no symlinked Haskell tool shims |
| CLI and env contract | `prodbox test integration cli` and `prodbox test integration env` run built-frontend Haskell suites against the direct-Dhall config contract without recreating `prodbox-config.json` |
| Canonical test suite | `prodbox test integration public-dns`, `dns-aws`, `aws-iam`, `gateway-daemon`, `gateway-pods`, `gateway-partition`, `lifecycle`, `pulumi`, `aws-eks`, `ha-rke2-aws`, `charts-platform`, `charts-storage`, `charts-vscode`, `charts-api`, `charts-websocket`, `admin-routes`, and the phase-8 `keycloak-invite` dispatch native Haskell validation flows through `src/Prodbox/TestValidation.hs`, planned by `src/Prodbox/TestPlan.hs`. The phase 1/2 prerequisite DAG validates AWS credentials, Route 53 access, bounded MinIO-backed Pulumi login, native IAM harness readiness, and (per [phase-8-email-invite-auth.md](phase-8-email-invite-auth.md)) SES sending identity and receive-bucket readiness before suite execution proceeds. |
| Substrate provision/teardown | Home substrate: `prodbox rke2 reconcile`, `prodbox rke2 delete --yes` (hermetic delete reporting per Sprint `4.8`). AWS substrate: `prodbox pulumi eks-resources|eks-destroy --yes`, `prodbox pulumi test-resources|test-destroy --yes`. Provision/teardown are substrate ownership; the canonical suite is substrate-agnostic. |
| Substrate parity | Home substrate runs the full canonical suite today. The AWS substrate runs only the `aws-iam`, `aws-eks`, `ha-rke2-aws`, `pulumi`, and `dns-aws` validations today; AWS-substrate parity for the chart-platform and public-edge validations is tracked in [phase-7-aws-substrate-foundations.md](phase-7-aws-substrate-foundations.md). |
| Harbor-first registry pipeline | `prodbox rke2 reconcile`, bootstrap-source proof for the Harbor plus storage-backend direct-public exception before Harbor is externally ready, Harbor inventory proof for required images after bootstrap, stable `/readyz` plus `/v2/` proof before image writes, and native-host-architecture custom-image proof with no supported `docker buildx` or cross-arch emulation |
| Gateway runtime suite content | `gateway-daemon`, the Cabal `prodbox-daemon-lifecycle` process suite, `gateway-pods`, `gateway-partition`, and `prodbox tla-check` |
| Chart platform suite content | `charts-storage`, `charts-platform`, `charts-vscode`, `charts-api`, `charts-websocket`, and the admin-route validations, plus manifest and lifecycle proof that supported Patroni use flows through the Percona operator-backed platform, Keycloak-backed workloads preserve the shared-host proxy-aware issuer contract, Envoy enforces JWT auth and RBAC from explicit bearer-token and JWKS configuration, and the WebSocket path proves real upgrade behavior, Redis-backed state continuity, revocation-driven reconnect, and readiness-based drain |
| Public-host proof suite content | `prodbox host public-edge`, `public-dns`, `charts-api`, `charts-websocket`, and the admin-route validations on the single-host Gateway API target doctrine. `prodbox host public-edge` owns the cluster-side `ready-for-external-proof` classification on whichever substrate is active, while the named validations own HTTP-to-HTTPS redirect proof on port `80`, Keycloak shared-host issuer proof, route-level RBAC proof, real WebSocket upgrade behavior, one-connection-per-pod lifetime, revocation-driven reconnect, and readiness-based drain on the substrate that provisioned them. |
| Clean-room handoff | `prodbox rke2 delete --yes` for the home substrate plus `prodbox pulumi {eks,test}-destroy --yes` for the AWS substrate, a rerun that starts and finishes with no supported-path `prodbox-config.json` artifact, `prodbox rke2 reconcile`, `prodbox config show`, `prodbox config validate`, the canonical suite reruns, `prodbox host public-edge`, and repository review gates that confirm no supported-path placeholder-domain or Python residue survives |
| AWS IAM lifecycle | `prodbox test integration aws-iam`, `prodbox test integration all`, and `prodbox test all` use one shared idempotent harness driven by the test-suite-only `aws_admin_for_test_simulation.*` section in `prodbox-config.dhall`; that harness deletes any pre-existing dedicated `prodbox` IAM user and keys before provisioning, uses any pre-existing `aws.*` only to discover and delete the IAM user associated with those credentials, proves STS-federated operational credentials with a compact AWS-substrate session policy, waits for the dedicated IAM-user credentials to pass STS and repeated Route 53 hosted-zone probes, materializes IAM-user operational `aws.*` because cert-manager Route 53 DNS01 credentials do not support an STS session-token field, and clears operational `aws.*` before returning |
| Static and doctrine gate | `prodbox check-code` |

## Authority and State Locations

| State Class | Authority | Durable Home | Notes |
|-------------|-----------|--------------|-------|
| Repository configuration | Operator-authored repository root | `prodbox-config.dhall` | Written by `prodbox config setup`, ignored from VCS, decoded directly into Haskell types, and temporarily materialized plus cleared by the shared IAM validation harness when it simulates the public interactive flow |
| Host build artifacts | Canonical `cabal build --builddir=.build exe:prodbox` plus copy to `.build/prodbox` | `.build/prodbox` | Operator-facing binary that backs the canonical `prodbox` command surface |
| Container build artifacts | Dockerfiles under `docker/` | `/opt/build` | Canonical container build root |
| Harbor image state | Harbor plus Haskell lifecycle reconcile | Harbor project storage | Custom images and mirrored public images for the native host and cluster architecture only, including the Envoy Gateway target edge image set |
| CLI and doctrine source | Repository worktree | `app/`, `src/`, `documents/`, `DEVELOPMENT_PLAN/` | Version-controlled source of truth |
| Manual PV content root | Host filesystem | Configured path, default `.data/<namespace>/<release>/<workload>/<ordinal>/<claim>` | PV contents only |
| Retained non-PV chart state | Chart platform helpers | `.prodbox-state/<namespace>/` | Generated secrets and gateway event keys |
| AWS substrate local state | Haskell Pulumi orchestration and AWS substrate helpers | Generated under `.prodbox-state/aws-test/` and `.prodbox-state/aws-eks-test/` | Pulumi stack snapshots plus the HA-RKE2 validation SSH key |
| Local RKE2 host state | Host lifecycle commands | RKE2 data dirs, kubeconfig files, systemd state | Deleted by `prodbox rke2 delete --yes` except retained roots, with expected-absence cleanup rendered as normal delete disposition and Sprint `4.8` keeping successful uninstall runs hermetic through the lifecycle-local quiet path |
| Remote EKS AWS test stack | Pulumi plus Haskell orchestration | EKS resources in AWS | Created and destroyed only through named `prodbox pulumi` surfaces |
| Remote HA RKE2 AWS test stack | Pulumi plus Haskell orchestration | EC2 and supporting AWS resources | Exactly three Ubuntu 24.04 EC2 instances in separate AZs |
| Cluster resource state | Kubernetes | RKE2 datastore | Managed through canonical Haskell CLI flows |
| DNS ownership | AWS Route 53 | Hosted zone records | One canonical `test.resolvefintech.com` record only; wildcard public DNS and per-service public hostnames are unsupported |
| Pulumi backend state | Local-cluster MinIO | `prodbox-test-pulumi-backends` bucket | The local cluster must exist before remote AWS resources are created; backend validation recreates a deleted MinIO export host path and restarts `deployment/minio` before retrying login |
| Certificate material | Kubernetes | Secrets issued by cert-manager | ACME server URL comes from repository config, ZeroSSL EAB material is projected into `cert-manager/acme-eab-credentials` when configured, and the target public edge attaches those secrets to Gateway listeners |
| Gateway continuity state | Kubernetes plus retained chart state | Cluster resources plus `.prodbox-state/` | Used for ownership projection, DNS-write state, and event-key continuity |
| AWS admin credentials | `prodbox-config.dhall` `aws_admin_for_test_simulation` section | Operator-authored repository root | Test-suite-only stored simulation of the ephemeral elevated prompt input; the native IAM harness is the only supported runtime consumer, and the public command surface never reads it |

## Artifact Locations

| Type | Location | Purpose |
|------|----------|---------|
| Haskell application entrypoint | `app/prodbox/Main.hs` | Main CLI binary entrypoint |
| Haskell source modules | `src/Prodbox/` | CLI, infra, gateway, settings, and library implementation |
| Haskell tests | `test/` | Unit plus native CLI/env integration validation suites |
| Repository config artifacts | Operator-authored `prodbox-config.dhall` plus tracked `prodbox-config-types.dhall` | Haskell-owned config source and shared schema |
| Cabal package definition | `prodbox.cabal` | Build, test, and dependency definition |
| Cabal project definition | `cabal.project` | Repository-wide Cabal package-set definition |
| Host build root | `.build/` | Canonical host-side Haskell build artifacts; operator binary at `.build/prodbox` |
| Container build root | `/opt/build` | Canonical container-side Haskell build artifacts |
| Canonical custom Dockerfiles | `docker/` | Authoritative home for repository-owned container builds |
| Frontend container build | `docker/prodbox.Dockerfile` | Frontend image build owned by Phase `1`; single-stage `ubuntu:24.04` with in-image `ghcup`, pinned GHC `9.14.1`, and no symlinked Haskell tool shims |
| Gateway container build | `docker/gateway.Dockerfile` | Gateway image build owned by Phase `2`; single-stage `ubuntu:24.04` with in-image `ghcup`, pinned GHC `9.14.1`, no symlinked Haskell tool shims, and the official AWS CLI bundle keyed by the native Debian host architecture |
| Pulumi definitions | `pulumi/aws-eks/Pulumi.yaml`, `pulumi/aws-eks/Main.yaml`, `pulumi/aws-test/Pulumi.yaml`, `pulumi/aws-test/Main.yaml` | YAML Pulumi stacks (`runtime: yaml`) for the public AWS substrate resources |
| Engineering doctrine | `documents/engineering/` | Architecture and operator docs |
| Development plan | `DEVELOPMENT_PLAN/` | Status, sequencing, and cleanup ownership |
| Manual PV content root | Configured path, default `.data/` | PV contents only |
| Retained repo-local state root | `.prodbox-state/` | Generated namespace-local chart state plus AWS validation snapshots and HA-RKE2 SSH key material |
| AWS substrate local state | Generated under `.prodbox-state/aws-test/` and `.prodbox-state/aws-eks-test/` | Pulumi stack snapshots plus the HA-RKE2 validation SSH key |

## Related Documents

- [00-overview.md](00-overview.md)
- [substrates.md](substrates.md)
- [phase-1-runtime-cli-aws-foundations.md](phase-1-runtime-cli-aws-foundations.md)
- [phase-2-gateway-dns.md](phase-2-gateway-dns.md)
- [phase-3-chart-platform-vscode.md](phase-3-chart-platform-vscode.md)
- [phase-4-lifecycle-canonical-paths.md](phase-4-lifecycle-canonical-paths.md)
- [phase-5-canonical-test-suite.md](phase-5-canonical-test-suite.md)
- [phase-7-aws-substrate-foundations.md](phase-7-aws-substrate-foundations.md)
- [phase-8-email-invite-auth.md](phase-8-email-invite-auth.md)
