{{- /*
Sprint 3.18: intentionally renders nothing. Keycloak no longer asks the
gateway daemon to materialize `keycloak-runtime` / `keycloak-oidc-clients`
Kubernetes Secrets. The Pod authenticates to Vault with its ServiceAccount in
`deployment.yaml`, materializes an env file in tmpfs, and fails closed before
startup if Vault cannot supply the admin, DB, OIDC, demo-user, or SMTP fields.
*/ -}}
