{{- /*
Sprint 3.18: intentionally renders nothing. MinIO root credentials live at
Vault KV `secret/data/minio/root`; the StatefulSet's Vault-login init
container writes tmpfs files and the MinIO container reads them through
MINIO_ROOT_USER_FILE / MINIO_ROOT_PASSWORD_FILE. No Helm lookup or
Kubernetes Secret fallback remains on the supported path.
*/ -}}
