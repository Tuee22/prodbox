# System Components

**Status**: Authoritative source
**Supersedes**: N/A
**Referenced by**: [README.md](README.md), [development_plan_standards.md](development_plan_standards.md), [00-overview.md](00-overview.md), [substrates.md](substrates.md), [phase-1-runtime-cli-aws-foundations.md](phase-1-runtime-cli-aws-foundations.md), [phase-2-gateway-dns.md](phase-2-gateway-dns.md), [phase-3-chart-platform-vscode.md](phase-3-chart-platform-vscode.md), [phase-4-lifecycle-canonical-paths.md](phase-4-lifecycle-canonical-paths.md), [phase-5-canonical-test-suite.md](phase-5-canonical-test-suite.md), [phase-7-aws-substrate-foundations.md](phase-7-aws-substrate-foundations.md), [phase-8-email-invite-auth.md](phase-8-email-invite-auth.md), [vault_doctrine.md](../documents/engineering/vault_doctrine.md)
**Generated sections**: none

> **Purpose**: Document the authoritative target component inventory for infrastructure, runtime
> control surfaces, validation surfaces, and state or authority boundaries in the Haskell rewrite.

The inventory documents the authoritative Haskell-only implementation end state. The supported
public edge closes on one public hostname, shared-host path routing, and native-host-architecture
container publication. Component status here follows the phase-independence model adopted by
Sprint `0.15`: a component is Done on its code-owned surface once it builds and passes local
validation, a proof needing live infrastructure is a non-blocking Live-proof: pending note, and
AWS-substrate coverage is orthogonal parity tracked in [substrates.md](substrates.md) — never a
backward block on an earlier phase. SSoT: [development_plan_standards.md](development_plan_standards.md)
Standards N (Phase Independence) and O (Code-Local vs Live-Infra Proof).
[legacy-tracking-for-deletion.md](legacy-tracking-for-deletion.md)
preserves cleanup history separately from this canonical inventory. The earlier alignment follow-
up on native `gateway-partition` validation, peer-transport trust-material runtime closure,
root-chart-only public chart commands, and the registry-plus-storage-backend bootstrap contract is
now closed in code and governed docs.

## Haskell-Only Architecture

| Surface | Owner | Paths |
|---------|-------|-------|
| CLI frontend and command surface | Haskell parser, native-only request ADT, native command entrypoints for the full supported command matrix, and the operator-interactive non-TTY guard module (refuses to prompt when stdin is not a TTY and emits structured guidance naming the automation equivalent) | `app/prodbox/Main.hs`, `src/Prodbox/CLI/Command.hs`, `src/Prodbox/CLI/Parser.hs`, `src/Prodbox/CLI/Interactive.hs`, `src/Prodbox/Native.hs`, `prodbox.cabal`, `cabal.project` |
| Configuration and settings | Haskell-owned native `dhall`-library decode of the operator-authored binary-sibling `prodbox.dhall`, typed settings model, masked display, and validation | `src/Prodbox/Settings.hs`, `src/Prodbox/Repo.hs`, `prodbox.dhall`, `prodbox-config-types.dhall` |
| Settings and command runtime | Haskell build-support, effect/DAG/interpreter, typed prerequisites, subprocess isolation, readiness observations, and the landed Sprint `1.60` runtime-memory planner. `RuntimeMemoryPlan` is opaque; the matching workload profile supplies its sole cgroup limit, its nested proof counts the heap cap once, its child term requires finite permit/peak/deadline evidence, and its typed scratch/high-water projections feed later gateway/test work. ChartPlatform generates the gateway's exact RTS argv and the executable enables only `-rtsopts`; Cabal, Docker, and Helm author no heap cap. | `src/Prodbox/Capacity/RuntimeMemory.hs`, `src/Prodbox/Capacity/Config.hs`, `src/Prodbox/Config/ComponentGraph.hs`, `src/Prodbox/Effect.hs`, `src/Prodbox/EffectDAG.hs`, `src/Prodbox/EffectInterpreter.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `src/Prodbox/Lifecycle/ReadinessObservation.hs`, `src/Prodbox/Prerequisite.hs`, `src/Prodbox/Service.hs`, `src/Prodbox/Subprocess.hs`, `src/Prodbox/TestPlan.hs`, `src/Prodbox/TestRestore.hs`, `charts/gateway/templates/deployments.yaml`, `prodbox.cabal` |
| Host and Kubernetes helpers | Haskell host and k8s modules | `src/Prodbox/Host.hs`, `src/Prodbox/K8s.hs` |
| Home RKE2 compiled reconcile plan | Sprint `4.45` validates the component DAG, derives `nativeInstallStepOrder` as `concatMap stepsForComponent (componentReconcileOrder dag)`, and appends edge reconciliation as a separate explicit tail. The graph declares the registry/post-Vault dependencies consumed by cert-manager, the pre-Vault gateway, MetalLB, Envoy Gateway, and Percona. `buildNativeInstallExecutionPlan` rejects dependency or phase regressions with a structured error before apply and carries the validated DAG/order in `NativeInstallPayload`. Every native step has a total anchor and explicit phase action; MetalLB, Envoy Gateway, and Percona are first-class steps. Production readiness targets poll caller-injected one-shot observations within bounded fail-closed waits and require the final component barrier before dependants execute. The plan goldens intentionally show those three steps and omit redundant home MinIO steady-state narration. Sprint `4.46` delegates the Route 53, Helm, and Harbor publication retry classifiers to `isRetryableTransientFailure`; Helm inherits the shared DNS/transport cases, and `legacyInlineRetryClassifier` retains no RKE2 entry. Historical code-local evidence for that landed surface is unit 1276/1276 and `dev check` exit 0; current phase status remains solely in `README.md`. | `src/Prodbox/CLI/Rke2.hs`, `src/Prodbox/Config/ComponentGraph.hs`, `src/Prodbox/Lifecycle/ReadinessObservation.hs`, `src/Prodbox/Service.hs`, `src/Prodbox/CheckCode.hs`, `test/unit/Main.hs`, `test/golden/plans/rke2-reconcile.txt`, `test/golden/plans/rke2-reconcile-with-edge.txt` |
| Container packaging and registry doctrine | Dockerfiles under `docker/`, Haskell-build containers that stay single-stage `ubuntu:24.04`, install `ghcup` in-image, pin GHC `9.12.4`, avoid symlinked Haskell tool shims, registry-backed steady-state image sourcing with a registry-plus-storage-backend bootstrap exception only, ordered public-image candidate retry during registry mirror publish including `mirror.gcr.io` fallbacks for the Docker Hub-hosted Percona and Envoy images used on the supported path, and native-host-architecture image publication only with basic `docker build`/`docker push` on the daemon's default builder — no supported `docker buildx`, `docker-container` builder, BuildKit-only Dockerfile features (`# syntax=` frontend pin or `RUN --mount` cache/bind mounts), cross-arch emulation, or mixed-arch cluster closure. Sprint `4.44` makes the registry S3 input a `RegistryStorageBackend` with required `RedirectPolicy`; the canonical MinIO-backed value is `RedirectDisabled`. `registryConfigYaml` remains an `unlines` renderer over the typed value, preserving the golden output and existing resource ownership. | `docker/`, `src/Prodbox/ContainerImage.hs`, `src/Prodbox/CLI/Rke2.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `test/golden/config/registry-config.yaml` |
| Pulumi orchestration and YAML stack programs | Haskell Pulumi orchestration over YAML Pulumi definitions for the public AWS substrate stacks, exposed through `prodbox aws stack ...` commands, including stack-config synchronization for AWS substrate inputs, decrypt-to-scratch stack execution through `Prodbox.Pulumi.EncryptedBackend`, first-touch legacy checkpoint import/delete, Vault-only AWS provider credential resolution through `Prodbox.Infra.AwsProviderCredentials`, production residue/output reads through encrypted checkpoint helpers, refresh-before-retry destroy hardening for retained AWS stacks, one destroy-and-recreate repair when HA-RKE2 SSH validation finds stale retained EC2 nodes after a successful stack reconcile, and the HA-RKE2 validation SSH key fetched on demand via `pulumi stack output --show-secrets` into an ephemeral `mktemp` file (Sprint `4.18`). The generic bucket name landed in Sprint `4.30`; Sprint `7.14` landed the wrapper/read/migration/provider path and the generated operational `prodbox` `aws.*` credential is minted into Vault KV (`secret/gateway/gateway/aws`) after Vault is unsealed, with production config carrying only a `SecretRef.Vault` reference to it. Sprint `7.30` routes the supported per-run Model-B Pulumi checkpoint load/store/delete/read paths through the daemon object-store API, so the host no longer opens a MinIO port-forward for encrypted backend hydration, persistence, residue, stack-output, or prune operations. Elevated/admin power enters every supported stack flow only through the interactive `SecretRef.Prompt`; the test harness simulates that prompt from the `aws_admin_for_test_simulation.*` test fixture in `test-secrets.dhall` (Sprint `7.16`). Local-lifecycle bootstrap DNS reconcile and ACME `ClusterIssuer` projection stay outside the public `prodbox aws stack ...` surface. | `src/Prodbox/CLI/Spec.hs`, `src/Prodbox/Aws.hs`, `src/Prodbox/Pulumi/EncryptedBackend.hs`, `src/Prodbox/Gateway/ObjectStore.hs`, `src/Prodbox/Gateway/Client.hs`, `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/Infra/AwsProviderCredentials.hs`, `src/Prodbox/Infra/StackOutputs.hs`, `src/Prodbox/Lifecycle/LiveResidue.hs`, `src/Prodbox/Infra/AwsTestStack.hs`, `src/Prodbox/Infra/AwsEksTestStack.hs`, `src/Prodbox/Infra/AwsEksSubzoneStack.hs`, `src/Prodbox/Infra/AwsSesStack.hs`, `pulumi/aws-eks/Pulumi.yaml`, `pulumi/aws-eks-subzone/Pulumi.yaml`, `pulumi/aws-ses/Pulumi.yaml`, `pulumi/aws-test/Pulumi.yaml`, MinIO `prodbox-state` bucket |
| DNS inspection | Haskell DNS check module | `src/Prodbox/Dns.hs` |
| Shared public-edge route catalog | Haskell-owned single-host route catalog, path-prefix constants, canonical public-route URL construction, and Keycloak issuer derivation consumed by lifecycle, DNS, chart, host-diagnostic, and native validation surfaces | `src/Prodbox/PublicEdge.hs` |
| Gateway runtime | Bounded Haskell daemon runtime: finite latest-heartbeat/ownership semantic state admitted only from bounded Orders (`max_members` plus raw/per-member byte limits); fixed-width per-emitter epoch/sequence and previous-hash anchors; vector-cursor delta gossip with signed semantic checkpoint/suffix repair; bounded frame/parser/process-wide in-flight/rejection allocations; a capacity-one serialized child schedule; credential-gated DNS effects with no ambient AWS fallback; constant-time `/healthz` and `/readyz`; and bounded `/v1/state` diagnostics. Before every local assertion publication or epoch rotation, the daemon conditionally stages and durably re-observes the exact signed assertion plus next local-emitter anchor through `GatewayContinuityAuthority`. The per-emitter Model-B record preserves safe continuity across total peer restart but does not claim to persist the complete semantic projection; peer repair repopulates current heartbeat/ownership evidence. A separate Vault admission marker makes missing previously-admitted state fail closed. | `src/Prodbox/Gateway.hs`, `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/Gateway/Bounds.hs`, `src/Prodbox/Gateway/State.hs`, `src/Prodbox/Gateway/Orders.hs`, `src/Prodbox/Gateway/Continuity.hs`, `src/Prodbox/Gateway/ContinuityStore.hs`, `src/Prodbox/Gateway/DnsAuthority.hs`, `src/Prodbox/Gateway/ChildSchedule.hs`, `src/Prodbox/Gateway/ObjectStore.hs`, `src/Prodbox/Gateway/Client.hs`, `src/Prodbox/Gateway/Peer.hs`, `src/Prodbox/Gateway/Types.hs`, `src/Prodbox/Gateway/Settings.hs` |
| Formal verification | Haskell TLA+ wrapper | `src/Prodbox/Tla.hs`, `documents/engineering/tla/` |
| Chart platform and retained state | Haskell chart registry, PostgreSQL platform constants, retained-storage reconciler, CLI runtime, the Percona-operator-backed application-database contract, path-routed browser and supported admin delivery through Envoy on `test.resolvefintech.com`, explicit bearer-token and browser-return-path auth-carrier doctrine, Keycloak JWT-backed Envoy auth and RBAC, Keycloak NetworkPolicy egress for DNS/PostgreSQL/internal traffic plus HTTPS and configured SES SMTP, JWT-only API delivery from Keycloak JWKS-backed Envoy validation, and the Redis-backed WebSocket chart surface with real `/ws` upgrade plus any later explicit external rate-limit-service surface. Sprint `3.24` routes graph-projected operator gates through the exhaustive `operatorAvailableTarget` registry and `ReadinessObservation`: the Percona adapter observes its CRD and Deployment `Available=True` once, and only `ReadyObserved` opens chart mutation. New `ComponentId` constructors require an explicit compile-time registry decision; existing config-driven IDs without a registered operator target fail closed at runtime. Chart rendering consumes the validated resource plan: every container/init container has explicit cpu/memory/ephemeral-storage requests and limits, every namespace has quota/limit-range manifests, and every PVC capacity draws from the durable-storage budget. Chart-secret authority has moved to Vault KV via Kubernetes auth: websocket consumes its OIDC client secret app-side through `SecretRef.Vault`, Keycloak / MinIO materialize their covered runtime fields through Vault-login init containers, the VS Code Envoy `SecurityPolicy` client Secret is materialized from Vault by a chart Job, gateway event keys plus Route 53 AWS and gateway MinIO credentials resolve through Vault Kubernetes auth, Patroni role Secrets are materialized from Vault by the `keycloak-postgres` pre-install hook, and host/admin helper paths plus the AWS SES SMTP sync read/write their remaining Keycloak admin, OIDC, demo-user, and SMTP material through Vault KV. Sprint `3.18` pins the sealed-startup structural proof; Sprint `3.19` removed the old derivation and generated-secret machinery. The public-edge **production** TLS certificate is retained as a `LongLived` registry-managed resource in the long-lived `pulumi_state_backend` S3 bucket (Sprints `4.24`/`7.11`/`8.7`) and restored into `vscode/public-edge-tls` before every issuance on all rebuild paths so the high-churn canonical loop never re-orders it; this supersedes the earlier in-cluster `prodbox/public-edge-tls-retained` backup copy. | `src/Prodbox/CLI/Charts.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `src/Prodbox/Lib/Storage.hs`, `src/Prodbox/PostgresPlatform.hs`, `src/Prodbox/Secret/VaultInventory.hs`, `src/Prodbox/Vault/Reconcile.hs`, `src/Prodbox/Vault/Host.hs`, `src/Prodbox/Lifecycle/ReadinessObservation.hs`, `charts/` |
| Public workload runtime | Haskell internal runtime for the supported API workload plus the WebSocket workload selected through environment rather than repo-root config; the current runtime owns the shared-host `/api` and `/ws` surfaces, real `/ws` upgrade, Redis-backed reconnect-safe state, one-connection-per-pod lifetime, and readiness-based drain | `src/Prodbox/Workload.hs` |
| Public-edge diagnostics | Haskell host diagnostic for the single-host Envoy edge and its path-routed application or admin surfaces | `src/Prodbox/Host.hs` |
| Onboarding and AWS administration | Haskell interactive onboarding plus AWS CLI subprocess orchestration for prompt-driven elevated/admin flows: the ephemeral elevated credential enters prodbox only through the interactive `SecretRef.Prompt`, is held in memory for one command to mint the dedicated least-privilege `prodbox` IAM identity, then discarded. The test harness simulates that prompt non-interactively from the `aws_admin_for_test_simulation.*` test fixture (TestPlaintext) in `test-secrets.dhall`, not a production config section (Sprint `7.16`); supported AWS subprocess auth is isolated from ambient host AWS state | `src/Prodbox/Aws.hs`, `src/Prodbox/AwsEnvironment.hs`, `src/Prodbox/CLI/Parser.hs`, `src/Prodbox/Native.hs` |
| User invitation, listing, and revocation | Haskell `prodbox users invite|list|revoke`, Keycloak admin client, Vault SMTP/OIDC reads, and SES capture polling. `Prodbox.Ses.Readiness` builds one validated desired contract and classifies typed sender/DKIM, exact regional MX, active enabled exact recipient/S3 action, and capture-canary list/get observations as `Ready`, propagation `Pending`, terminal `Failed`, or `Unobservable`. Command success alone never opens the gate; explicit drift, disabled state, malformed output, denied access, and transport failure remain fail-closed. | `src/Prodbox/CLI/Users.hs`, `src/Prodbox/UsersAdmin.hs`, `src/Prodbox/Keycloak/`, `src/Prodbox/Ses/Readiness.hs`, `src/Prodbox/Ses/Capture.hs`, `src/Prodbox/Infra/AwsSesStack.hs`, `src/Prodbox/EffectInterpreter.hs` |
| Test harness and quality gate | Haskell aggregate/named-validation runtime with explicit typed preparation and external observation. Sprint `5.16` supplies `gateway-pods` with a run-scoped absorbing restart/OOM/failure-high-water/unobservable fold plus a separately restartable three-sample healthy window; instantaneous Deployment readiness remains a shallow dependency gate and logs are diagnostic-only. The recorder spans compiled home/AWS gateway restore, lifecycle, and postflight boundaries. Sprint `5.17` adds capability-derived retained-SES preparation, and Sprint `8.10` completes its visible await-ready stage: every poll first proves the full provider inventory, including the Pulumi-owned S3 readiness canary, then runs the complete semantic classifier. Control-plane observations use the lease-scoped role while capture list/get uses the operational credential consumed by invite polling; only `Pending` retries inside the bounded propagation window, while `Failed` and `Unobservable` terminate before SMTP KV materialization or dependent charts. Per-run cleanup remains automatic; `aws-ses` remains retained. | `src/Prodbox/BuildSupport.hs`, `src/Prodbox/CheckCode.hs`, `src/Prodbox/Test/GatewayRuntimeStability.hs`, `src/Prodbox/TestPlan.hs`, `src/Prodbox/TestRestore.hs`, `src/Prodbox/TestRunner.hs`, `src/Prodbox/TestValidation.hs`, `src/Prodbox/Ses/Readiness.hs`, `src/Prodbox/Infra/AwsSesStack.hs`, `test/` |
| Pulsar messaging and CBOR wire codec | Self-maintained native-protocol Pulsar client boundary, the non-optional CBOR-always payload codec for gateway/Orders, at-least-once envelopes, and Pulsar `Work*` payloads, the derived topic algebra, and the `Work*` envelope family; gateway `SignedAssertion`, cursor/delta, and repair request frames use canonical CBOR with `application/cbor`; Sprint `2.28` landed the shared `Prodbox.Cbor` payload type plus durable `StoredEvent` CBOR round trips; Sprint `3.21` landed `Prodbox.Pulsar.Codec`/`Topic`/`Envelope`, `Prodbox.Pulsar.Protocol`, the typed `Client` broker transport/framing boundary, `charts/pulsar`, and the live `pulsar-broker` produce/consume/ack proof. | `src/Prodbox/Pulsar/`, `charts/pulsar/`, `src/Prodbox/Cbor.hs`, `src/Prodbox/Gateway/Peer.hs`, `src/Prodbox/Gateway/State.hs`, `src/Prodbox/Daemon/Events.hs` |
| Resource scaling and capacity placement | Typed authored capacity/admission plan plus the landed opaque runtime-memory decomposition. Existing `fitsWithin` lemmas reject over-reserved hosts, quotas, declared workload limits, regions, and uncapped containers; cgroups contain an over-demanding runtime but do not prove its demand. Sprint `1.60` validates nested heap/cgroup lemmas against the matching workload envelope, finite child-schedule evidence, and a safety-derived high-water projection, then generates the gateway RTS policy. Sprint `2.31` consumes that plan in `Prodbox.Gateway.Bounds`, charging bounded Orders/state/replay/checkpoint evidence, the `max_members²` peer-cursor term, continuity authority, aggregate frame scratch, and the enforced capacity-one child permit. Sprint `5.16` consumes the plan's outer-demand, high-water, and limit projections for external warning/failure thresholds and restart/OOM evidence. | `dhall/capacity/Schema.dhall`, `src/Prodbox/Capacity/RuntimeMemory.hs`, `src/Prodbox/Gateway/Bounds.hs`, `src/Prodbox/Gateway/ChildSchedule.hs`, `src/Prodbox/Capacity/Config.hs`, `src/Prodbox/Capacity/Storage.hs`, `src/Prodbox/Scaling/Autoscaler.hs`, `src/Prodbox/Scaling/Spot.hs`, `src/Prodbox/Substrate.hs`, `src/Prodbox/Settings.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `src/Prodbox/CLI/Rke2.hs`, `charts/`, `src/Prodbox/Test/GatewayRuntimeStability.hs`, `src/Prodbox/TestValidation.hs` |
| Pulsar topic lifecycle | A Pulsar topic as a first-class managed resource — typed three-valued broker discover, typed ensure/delete, a lifecycle class, and a name only the topic algebra can produce — reconciled present/absent idempotently through the same managed-resource registry every other resource uses; Sprint `4.35` landed the topic lifecycle surface and live broker-backed topic reconciliation proof per [pulsar_topic_lifecycle_doctrine.md](../documents/engineering/pulsar_topic_lifecycle_doctrine.md). | `src/Prodbox/Pulsar/Topic*`, `src/Prodbox/Lifecycle/ResourceClass.hs`, `src/Prodbox/Lifecycle/ResourceRegistry.hs` |
| Tiered storage capacity DSL | A finite-budget capacity DSL for *how much* durable data prodbox may hold — storing more bytes than declared capacity, naming a sizeless durable claim, or calling a sink "unlimited" without an autoscaling witness are rejected before mutation; Sprint `1.51` landed the shared budget schema/config block and Sprint `4.36` landed the storage-specific Haskell planner, autoscaled-sink witness, AWS region-quota preflight binding, and mandatory ML JIT/model-cache budgets per [tiered_storage_capacity_doctrine.md](../documents/engineering/tiered_storage_capacity_doctrine.md) | `dhall/capacity/Schema.dhall`, `src/Prodbox/Capacity/Config.hs`, `src/Prodbox/Capacity/Storage.hs`, `src/Prodbox/Aws.hs` |
| Host platform provider (multi-OS) | A per-OS host-provider model, mirrored from the operator's `hostbootstrap` library, that classifies the host and descends into a Linux execution frame on every OS (host-native on Linux, a Lima VM on macOS, a WSL2 distro on Windows), making "run a Linux cluster tool on a non-Linux host without a VM" unrepresentable. Sprint `1.52` landed the `HostSubstrate` detector, closed `HostTool`/`AbsExe` surface, `LiftLayer` fold, host-gated reconciler plans, Docker host-frame gate, and `host_substrate_supported` prerequisite root; Sprint `4.37` landed provider reconciler selection, ready/missing/reboot decisions, wrong-provider fail-fast refusal, and Docker Linux-frame dispatch for native-arch builds per [host_platform_doctrine.md](../documents/engineering/host_platform_doctrine.md) | `src/Prodbox/Host/Substrate.hs`, `src/Prodbox/Host/Tool.hs`, `src/Prodbox/Host/Lift.hs`, `src/Prodbox/Host/Lima.hs`, `src/Prodbox/Host/Wsl2.hs`, `src/Prodbox/Host/Ensure.hs`, `src/Prodbox/DockerConfig.hs`, `src/Prodbox/Prerequisite.hs` |
| Cluster topology and substrate-typed workers | The three explicit cluster types (`kind`/`rke2`/`eks`), the substrate-indexed one-compute-worker-per-machine rule, and type shapes that make an ill-formed topology unrepresentable. Sprint `1.53` landed the cluster-type/topology/worker Dhall schema, Haskell mirror, declared `cluster_topology` config field, and pure placement outcome ADT; Sprint `4.38` landed runtime substrate-typed placement, required hostname anti-affinity, `maxSurge = 0`, duplicate/mismatch refusal, and mixed-substrate-only-`rke2` enforcement per [cluster_topology_doctrine.md](../documents/engineering/cluster_topology_doctrine.md) | `dhall/cluster/Schema.dhall`, `src/Prodbox/Cluster/Substrate.hs`, `src/Prodbox/Cluster/Topology.hs`, `src/Prodbox/Cluster/Placement.hs`, `src/Prodbox/Settings.hs` |
| Test topology | The executable-sibling `prodbox.test.dhall` as the explicit self-describing SSoT of one test run, plus the `test init` / `test run` surface that stands up each declared cluster variant, asserts it, and always tears it down without touching production config, production `.data/`, or any long-lived resource. Sprint `1.54` landed the schema, Haskell mirror, executable-sibling decoder, and topology-mode production-config preflight; Sprint `5.11` landed the command surface, per-variant generated Tier-0 run config, `.test-data/` isolation through `storage.manual_pv_host_root`, sealed-Vault audit-root repointing, and finally-guaranteed cleanup per [test_topology_doctrine.md](../documents/engineering/test_topology_doctrine.md) | `dhall/TestTopologySchema.dhall`, `src/Prodbox/TestTopology.hs`, `src/Prodbox/TestRunner.hs`, `src/Prodbox/Repo.hs`, `src/Prodbox/Lib/Storage.hs`, `src/Prodbox/TestValidation.hs` |

## CLI Doctrine Components

Components introduced by the doctrine adoption sprints scheduled in
[phase-1-runtime-cli-aws-foundations.md](phase-1-runtime-cli-aws-foundations.md),
[phase-2-gateway-dns.md](phase-2-gateway-dns.md),
[phase-3-chart-platform-vscode.md](phase-3-chart-platform-vscode.md), and
[phase-4-lifecycle-canonical-paths.md](phase-4-lifecycle-canonical-paths.md).

| Component | Owner | Status | Paths |
|-----------|-------|--------|-------|
| `CommandSpec` source-of-truth registry | `src/Prodbox/CLI/Spec.hs` now owns the typed command tree, and `src/Prodbox/CLI/Parser.hs` renders that registry into the public `optparse-applicative` parser while the same registry also drives help, tree, JSON, manpage, and completion generation | ✅ Done (Sprint 1.6) | `src/Prodbox/CLI/Spec.hs`, `src/Prodbox/CLI/Parser.hs`, `src/Prodbox/CLI/Docs.hs`, `src/Prodbox/CLI/Tree.hs`, `src/Prodbox/CLI/Json.hs`, `src/Prodbox/App.hs` |
| `Plan` ADT and `Plan` / `apply` boundary | Shared `PlanOptions`, deterministic rendered plans, and the doctrine-owned `runPlanWithOptions` boundary now cover chart, RKE2, gateway-start, Pulumi, AWS setup or teardown, and config-setup flows, with golden coverage for every rendered plan plus unit coverage over dry-run vs apply behavior. Sprint `4.45` makes the home RKE2 payload compiled rather than re-derived at apply: `NativeInstallPayload` carries the validated component DAG and graph-derived step order, and invalid dependency/phase projections never produce a `Plan` | ✅ Done (Sprint 1.7) | `src/Prodbox/CLI/Command.hs`, `src/Prodbox/CLI/Charts.hs`, `src/Prodbox/CLI/Rke2.hs`, `src/Prodbox/Gateway.hs`, `src/Prodbox/CLI/Pulumi.hs`, `src/Prodbox/Aws.hs`, `test/unit/Main.hs` |
| `Subprocess` typed-value ADT and interpreter | `src/Prodbox/Subprocess.hs` now exposes the doctrine-shaped `Subprocess` record, `renderSubprocess`, and the `runStreaming` / `capture` boundary backed internally by `typed-process`; validation paths that still consume the repository `Result` ADT use explicit `captureSubprocessResult` / `runSubprocessStreaming` adapters. `prodbox dev check` refuses direct `System.Process` / `System.Process.Typed` call sites and smart constructors in `src/Prodbox/` outside that interpreter. | ✅ Done (Sprint 1.8) | `src/Prodbox/Subprocess.hs`, `src/Prodbox/TestRunner.hs`, `src/Prodbox/CLI/Charts.hs`, `src/Prodbox/CLI/Rke2.hs`, `src/Prodbox/Gateway.hs`, `test/unit/Main.hs` |
| `prerequisiteRegistry` with remedy-hint contract | `src/Prodbox/Prerequisite.hs` now owns the authoritative prerequisite DAG, `src/Prodbox/EffectDAG.hs` rejects unknown node IDs during expansion, and prerequisite-aware command flows on the supported path now fail with the required node-id, description, and remedy-hint contract | ✅ Done (Sprint 1.9) | `src/Prodbox/Prerequisite.hs`, `src/Prodbox/EffectDAG.hs`, `src/Prodbox/EffectInterpreter.hs`, `test/unit/Main.hs` |
| `GeneratedSectionRule` registry | Single source of truth for generated marker-delimited sections plus paired `docs check` / `docs generate` | ✅ Done (Sprint 1.10) | `src/Prodbox/CheckCode.hs`, `src/Prodbox/CLI/Docs.hs`, repo-root `fourmolu.yaml` |
| `forbiddenPathRegistry` (negative-space lint) | Refuses parallel-workflow surfaces such as `.github/workflows/`, `.husky/`, `.githooks/`, root `Makefile` / `justfile` / `Taskfile.yml` | ✅ Done (Sprint 1.10) | `src/Prodbox/CheckCode.hs` |
| `prodbox-haskell-style` test stanza | Cabal `test-suite` mirror of `prodbox dev lint haskell` | ✅ Done (Sprint 1.11 component closure) | `prodbox.cabal`, `test/haskell-style/` |
| Capability classes plus `AsServiceError` and the shared transient-failure base | `src/Prodbox/Service.hs` defines the service-error inventory, IO-backed `HasMinIO` / `HasRedis` / `HasPg` capability instances, `retryServiceAction`, and Sprint `1.57`'s constructor-owned common transient groups exposed through `isRetryableTransientFailure`. The Phase-1 AWS-validation caller delegates to that base; Sprint `4.46` lands the three RKE2 callers and Sprint `7.32` lands the EKS image-mirror caller. `CheckCode` refuses a new standalone retry-substring table that bypasses the base, with no legacy classifier allowance. | ✅ Done (Sprint 1.12; chart Redis/Postgres closed by Sprint 3.9; shared retry base + lint closed by Sprint 1.57; RKE2 delegation closed by Sprint 4.46; EKS delegation closed by Sprint 7.32) | `src/Prodbox/Service.hs`, `src/Prodbox/EffectInterpreter.hs`, `src/Prodbox/CheckCode.hs`, `src/Prodbox/CLI/Rke2.hs`, `src/Prodbox/Lib/EksImageMirror.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `src/Prodbox/Infra/MinioBackend.hs`, `test/unit/Main.hs` |
| `RetryPolicy` first-class values | Explicit `RetryPolicy` ADT plus pure `retryDelayMicros` are consumed by RKE2, chart-platform, service retry, and daemon-lifecycle retry paths. | ✅ Done (Sprint 1.13) | `src/Prodbox/Retry.hs`, `src/Prodbox/Service.hs`, `src/Prodbox/CLI/Rke2.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `src/Prodbox/Gateway/Daemon.hs`, `test/unit/Main.hs` |
| `ErrorKind = Recoverable | Fatal` axis | `AppError` carries `errorKind`, `errorMsg`, and `errorCause`; the CLI boundary renders it through one shared output layer, and daemon worker retry decisions classify recoverable versus fatal failures before restart or propagation. | ✅ Done (Sprint 1.14) | `src/Prodbox/Error.hs`, `src/Prodbox/Retry.hs`, `src/Prodbox/CLI/Output.hs`, `src/Prodbox/Gateway/Daemon.hs`, `test/unit/Main.hs` |
| `Prodbox.Naming` helpers | Shared `boundedResourceName`, `sanitizeResourceName`, and `hashSuffix` now own DNS-1123 or 63-character naming across storage, Patroni, and chart-platform related-resource surfaces | ✅ Done (Sprint 1.15) | `src/Prodbox/Naming.hs`, `src/Prodbox/Lib/Storage.hs`, `src/Prodbox/PostgresPlatform.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `test/unit/Main.hs` |
| Paired-resource smart constructors | `storageBinding` plus the shared Patroni helper inventory now centralize the retained PV or PVC and related-name pairings used by the chart platform | ✅ Done (Sprint 3.8) | `src/Prodbox/Lib/Storage.hs`, `src/Prodbox/PostgresPlatform.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `test/unit/Main.hs` |
| Retired GADT-indexed state-machine scaffold | The unadopted `Prodbox.StateMachine` scaffold and its typecheck-only test were deleted; the pure-FP doctrine now reserves GADT-indexed transitions for authoritative in-process state and permits flat exhaustive ADTs for externally-authoritative / log-reconciled projections. | ✅ Done (Sprint 1.32; supersedes the Sprint 1.16 scaffold) | `documents/engineering/pure_fp_standards.md`, `DEVELOPMENT_PLAN/legacy-tracking-for-deletion.md` |
| Explicit daemon lifecycle scaffold | `runGatewayDaemon` builds the daemon `Env`, installs signal handlers, tracks readiness, coordinates bounded drain, wraps workers with retry, and gates gateway daemon acquire through the prerequisite registry. | ✅ Done (Sprint 2.9) | `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/Gateway.hs`, `src/Prodbox/Prerequisite.hs` |
| Daemon `/healthz`, `/readyz`, `/metrics` endpoints | Standard liveness, readiness, and Prometheus metrics endpoints alongside `/v1/state`, with daemon-lifecycle goldens and readiness or metrics negative-space rules | ✅ Done (Sprint 2.10) | `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/CheckCode.hs`, `test/daemon-lifecycle/Main.hs`, `test/golden/daemon-health/` |
| `BootConfig` / `LiveConfig` split with mounted-Dhall file-watch reload | Hot-reloadable daemon fields live in `envLiveConfig :: TVar LiveConfig`. The daemon watches its `--config` Dhall path via filesystem-watch primitives and classifies each on-disk change: LiveConfig-only diffs atomic-swap via STM; any BootConfig diff drains within `liveDrainDeadlineSeconds` and exits with `ExitSuccess` so the kubelet restarts the Pod. See [config_doctrine.md](../documents/engineering/config_doctrine.md). | ✅ Done (Sprint 2.11, implemented through Sprints 2.20/2.21) | `src/Prodbox/Gateway/Types.hs`, `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/Gateway/Settings.hs` |
| `co-log` structured JSON logger | Gateway and workload daemon entrypoints emit JSON stderr logs through the co-log-backed typed field helpers; gateway log sites read `envLiveConfig` at emission time, and lifecycle/style coverage guards the JSON envelope, hot-reload filtering, dependency boundary, direct terminal writes, and inline log-object negative space | ✅ Done (Sprint 2.12) | `src/Prodbox/Gateway/Logging.hs`, `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/Workload.hs`, `test/daemon-lifecycle/Main.hs` |
| Test hooks in `Env` | No-op production hook fields exist for gateway peer-commit and connection surfaces, and `prodbox dev check` enforces literal production `noopDaemonHooks` construction plus hook reads through `envHooks env`. | ✅ Done (Sprint 2.13) | `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/Daemon/Events.hs`, `test/` |
| `prodbox-daemon-lifecycle` test stanza | Cabal `test-suite` spawns the built gateway daemon through the typed subprocess boundary, polls `/readyz` through the shared retry helper, checks `/healthz` and `/metrics`, captures endpoint response goldens, exercises first- and second-SIGTERM behavior, and `prodbox dev check` rejects direct `threadDelay` / raw `terminateProcess`. | ✅ Done (Sprint 2.14) | `prodbox.cabal`, `test/daemon-lifecycle/` |
| `prodbox-pulumi` test stanza | Cabal `test-suite` validates retained Pulumi-program ownership plus the isolated ephemeral-stack harness locally, including typed-output round-trips and forced-failure cleanup; the live retained AWS flows are covered by the named integration validations and aggregate suite. | ✅ Done (Sprint 4.7) | `prodbox.cabal`, `test/pulumi/`, `src/Prodbox/CLI/Pulumi.hs`, `src/Prodbox/TestValidation.hs` |
| `prodbox cluster reconcile` canonical entrypoint | Canonical reconcile command is implemented, documented, and validated locally; the completed one-cycle `install` alias and old `rke2` public command group have been removed from the parser, registry, generated docs, and current guidance | ✅ Done (Sprint 4.5) | `src/Prodbox/CLI/Rke2.hs`, `src/Prodbox/CLI/Command.hs`, `src/Prodbox/CLI/Parser.hs`, `src/Prodbox/CLI/Spec.hs` |
| `--format` / `--color` output discipline | `OutputOptions`, `OutputFormat`, `ColorMode`, `--format`, `--color`, `--no-color`, and the stdout/stderr writer boundary are implemented and guarded by the output-boundary lint checks. | ✅ Done (Sprint 1.17) | `src/Prodbox/CLI/Output.hs`, `src/Prodbox/CLI/Spec.hs`, `src/Prodbox/CheckCode.hs`, `test/unit/Main.hs`, `test/haskell-style/Main.hs` |
| One-shot `Env` and `newtype App` | Shared `Env` and `newtype App` backed by `ReaderT Env IO` exist for one-shot command dispatch, with environment access covered in the unit suite. | ✅ Done (Sprint 1.18) | `src/Prodbox/App.hs`, `test/unit/Main.hs` |
| Style-tools sandbox plus `.hlint.yaml` nesting rules | `src/Prodbox/Lint.hs` bootstraps Fourmolu `0.19.0.1` and HLint `3.10` into `.build/prodbox-style-tools/bin/` through `ghcup run --install` on isolated formatter GHC `9.12.4`, while `prodbox dev lint haskell` runs the sandboxed binaries by absolute path and enforces the doctrine-owned nested-case marker set | ✅ Done (Sprint 1.19) | `.hlint.yaml`, `src/Prodbox/Lint.hs`, `src/Prodbox/BuildSupport.hs`, `src/Prodbox/CheckCode.hs`, `test/haskell-style/Main.hs` |
| `prodbox test lint` alias plus lint-first ordering | Alias and ordering are implemented in the parser, registry, runner, and governed docs | ✅ Done (Sprint 1.20) | `src/Prodbox/CLI/Command.hs`, `src/Prodbox/CLI/Parser.hs`, `src/Prodbox/CLI/Spec.hs`, `src/Prodbox/TestRunner.hs` |
| `trackingGeneratedPaths` registry plus renderer determinism property test | Third registry alongside `GeneratedSectionRule` and `forbiddenPathRegistry` for fully-generated files, plus repeated-render and synthetic forbidden-input determinism checks in the haskell-style suite | ✅ Done (Sprint 1.21) | `src/Prodbox/CheckCode.hs`, `test/haskell-style/` |
| Standardized library set in `prodbox.cabal` | The dependency audit is closed: `typed-process` is isolated at the subprocess boundary, `co-log` / `co-log-core` are owned by Sprint 2.12, and retained project-specific libraries are documented in the dependency-management doctrine. | ✅ Done (Sprint 1.22) | `prodbox.cabal`, `documents/engineering/dependency_management.md` |
| Daemon CLI: `--config <path>` is the only startup knob | Gateway and workload launch paths accept exactly one CLI flag, `--config <path>`. `--log-level`, `--port`, `--node-id`, and `PRODBOX_*` env-var precedence are forbidden per [config_doctrine.md](../documents/engineering/config_doctrine.md). | ✅ Done (Sprint 2.15, closed through Sprint 2.24 and Phase 3 Sprint 3.15 workload cleanup) | `src/Prodbox/CLI/Parser.hs`, `src/Prodbox/CLI/Spec.hs`, `src/Prodbox/Gateway.hs`, `src/Prodbox/Workload.hs` |
| `Prodbox.Daemon.Events` at-least-once event-processing module | Immutable `StoredEvent` records with `created_at`, idempotent `EventHandler` newtype, and the `recordEvent` / `markEventProcessed` / `fetchUnprocessedEvents` triad; the gateway remains the documented in-memory anti-entropy peer-gossip variant | ✅ Done (Sprint 2.16) | `src/Prodbox/Daemon/Events.hs`, `documents/engineering/distributed_gateway_architecture.md`, `test/unit/Main.hs` |
| `prodbox dev lint chart` Helm-chart structural-invariants linter | `prodbox dev lint chart` validates `Chart.yaml` metadata, required chart-label helpers, and generated route-inventory drift through `src/Prodbox/CheckCode.hs` | ✅ Done (Sprint 3.12) | `src/Prodbox/CheckCode.hs`, `src/Prodbox/CLI/Parser.hs`, `src/Prodbox/CLI/Spec.hs` |
| HLint negative-space symbol rules for daemon paths | The governed Haskell lint scan enforces the doctrine-owned daemon-path marker set for `forkIO`, `unsafePerformIO`, module-level `IORef`, forbidden subprocess primitives, and case-inside-lambda nesting before handing off to sandboxed HLint | ✅ Done (Sprint 1.19) | `.hlint.yaml`, `src/Prodbox/Lint.hs`, `src/Prodbox/CheckCode.hs`, `test/haskell-style/Main.hs` |
| Marker-delimited route-inventory generation | Renders the `src/Prodbox/PublicEdge.hs` route catalog into the consuming Helm chart manifests via the generated-section registry, and `prodbox dev lint chart` validates drift | ✅ Done (Sprint 3.12) | `src/Prodbox/PublicEdge.hs`, `src/Prodbox/CheckCode.hs`, `charts/` |
| Durable CLI documentation artifacts | Generated Markdown command reference, doctrine-owned manpages, shell completion scripts, and tracked-generated-path enforcement all derive from the command registry | ✅ Done (Sprint 1.24) | `documents/cli/commands.md`, `share/man/man1/prodbox*.1`, `share/completion/{bash,zsh,fish}/`, `src/Prodbox/CLI/Docs.hs` |
| Parser-test category via `execParserPure` | Dedicated parser module now exercises leaf happy and unhappy paths through the pure optparse API and is guarded against subprocess regression | ✅ Done (Sprint 1.25) | `test/unit/Parser.hs`, `prodbox-unit` stanza, `documents/engineering/unit_testing_policy.md` |
| `renderError` error-rendering boundary | `src/Prodbox/CLI/Output.hs` owns `renderError`, `writeError`, and terminal writer helpers; command failure paths route through the output layer, and `prodbox dev check` refuses `print`, `exitFailure`, `putStr`, `putStrLn`, and direct stderr writes outside the allowed output/logging boundaries | ✅ Done (Sprint 1.26) | `src/Prodbox/CLI/Output.hs`, `src/Prodbox/Error.hs`, `src/Prodbox/App.hs`, `src/Prodbox/CheckCode.hs`, `test/unit/Main.hs`, `test/haskell-style/Main.hs` |
| `envMetrics :: MetricsRegistry` typed daemon `Env` field | Typed metrics registry is threaded through the daemon `Env`, `/metrics` reads counter values from it, and the negative-space guard refuses module-local mutable counter state under daemon paths | ✅ Done (Sprint 2.10) | `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/CheckCode.hs`, `.hlint.yaml` |
| STM broadcast channel for `LiveConfig` subscribers | `TChan` notification surface is exposed through `Env` and successful reloads publish the refreshed `LiveConfig`. | ✅ Done (Sprint 2.11) | `src/Prodbox/Gateway/Daemon.hs` |
| Prescribed daemon config shape | Gateway and workload daemons take their config from a mounted Dhall file at `--config`, decoded in-process via `Dhall.inputFile auto`. Credentials reach the Pod through Vault-backed materialization rather than a plaintext config section. Orders lives in its own ConfigMap-mounted Dhall and is imported by the main file. | ✅ Done (Sprint 2.11, implemented through Sprints 2.20/2.22) | `src/Prodbox/Gateway/Types.hs`, `src/Prodbox/Gateway/Settings.hs`, `prodbox.dhall`, `prodbox-config-types.dhall`, `charts/gateway/templates/configmap-config.yaml`, `charts/gateway/templates/configmap-orders.yaml` |
| Cabal-manifest toolchain pin declarations | `tested-with: ghc ==9.12.4` and `with-compiler: ghc-9.12.4` are present and the governed docs now cite the authoritative Cabal `3.16.1.0` pin | ✅ Done (Sprint 1.27) | `prodbox.cabal`, `cabal.project`, `DEVELOPMENT_PLAN/` |
| Library-first / thin-`Main.hs` layout audit | `app/prodbox/Main.hs` is thin and `src/Prodbox/CheckCode.hs` enforces the doctrine rule | ✅ Done (Sprint 1.27) | `app/prodbox/Main.hs`, `src/Prodbox/App.hs`, `src/Prodbox/CheckCode.hs` |
| `CommandSpec` / `OptionSpec` record-field bindings | The doctrine-named record fields are implemented in `src/Prodbox/CLI/Spec.hs`, and the registry now drives the public parser as well as the generated introspection and documentation surfaces | ✅ Done (Sprint 1.6, extended by Sprint 0.4) | `src/Prodbox/CLI/Spec.hs` |
| Daemon-as-typed-`Command` dispatch | Gateway daemon entry is a `GatewayDaemonCommand DaemonLaunchOptions` constructor on the top-level `Command` ADT dispatched through ordinary pattern matching — bound per [engineering doctrine: Long-Running Daemons → Daemon as Command](../documents/engineering/README.md)| ✅ Done (Sprint 1.6, extended by Sprint 0.4) | `src/Prodbox/CLI/Spec.hs`, `src/Prodbox/App.hs`, `src/Prodbox/Gateway/Daemon.hs` |
| Forbidden subprocess primitives | `prodbox dev lint files` now names `callProcess`, `readCreateProcess`, `readCreateProcessWithExitCode`, `createProcess`, `proc`, `shell`, and `import System.Process` in `src/Prodbox/` outside `src/Prodbox/Subprocess.hs`; the scanner strips string literals before token checks so Linux `/proc` paths do not false-positive, and `.hlint.yaml` carries the matching doctrine marker set | ✅ Done (Sprint 1.8, extended by Sprint 0.4) | `src/Prodbox/CheckCode.hs`, `src/Prodbox/Subprocess.hs`, `.hlint.yaml` |
| `fourmolu.yaml` 13-setting list | The repo-root `fourmolu.yaml` carries the thirteen doctrine-named settings and the dedicated haskell-style suite enforces them | ✅ Done (Sprint 1.10, extended by Sprint 0.4) | `fourmolu.yaml`, `test/haskell-style/Main.hs` |
| Canonical property-test invariants | Deterministic renderer and plan assertions exist in `test/unit/Main.hs` and `test/haskell-style/Main.hs`, the repo-root Dhall config plus persisted gateway and AWS snapshot shapes now round-trip through their supported decode paths, and parser examples now round-trip back to their registered command paths | ✅ Done (Sprint 1.11, extended by Sprint 0.4) | `test/unit/Main.hs`, `test/unit/Parser.hs`, `test/haskell-style/Main.hs` |
| Service-error newtype inventory | `MinIOError`, `RedisError`, and `PgError` each wrap `ServiceError`, carry `AsServiceError` instances, and are consumed by the current MinIO, Redis, and PostgreSQL service boundaries. | ✅ Done (Sprint 1.12, extended by Sprint 0.4) | `src/Prodbox/Service.hs`, `src/Prodbox/Infra/MinioBackend.hs`, `test/unit/Main.hs` |
| `AppError` record shape | `src/Prodbox/Error.hs` defines `AppError { errorKind, errorMsg, errorCause }`, the shared output layer consumes that record shape, and daemon worker retry classification uses it. | ✅ Done (Sprint 1.14, extended by Sprint 0.4) | `src/Prodbox/Error.hs`, `src/Prodbox/CLI/Output.hs`, `src/Prodbox/Gateway/Daemon.hs`, `test/unit/Main.hs` |
| Naming-helper signatures with DNS-1123 / 63-character constraints | `boundedResourceName :: Text -> Text -> Text -> Text`, `sanitizeResourceName :: Text -> Text`, and `hashSuffix :: Text -> Text` are implemented in `src/Prodbox/Naming.hs` and now back storage, Patroni, and chart-platform related-resource naming on the supported path | ✅ Done (Sprint 1.15, extended by Sprint 0.4) | `src/Prodbox/Naming.hs`, `src/Prodbox/Lib/Storage.hs`, `src/Prodbox/PostgresPlatform.hs`, `src/Prodbox/Lib/ChartPlatform.hs`, `test/unit/Main.hs` |
| Enumerated forbidden renderer inputs | Renderers in `GeneratedSectionRule` and `trackingGeneratedPaths` may not depend on timestamps, random IDs, locale-dependent ordering, terminal-width-dependent wrapping, or environment-dependent paths; the haskell-style suite injects synthetic violations for each class | ✅ Done (Sprint 1.21, extended by Sprint 0.4) | `src/Prodbox/CheckCode.hs`, `test/haskell-style/` |
| Structured-concurrency primitive set | Gateway daemon workers now use `Control.Concurrent.Async.withAsync`, `race`, and `concurrently`, and the lint guard rejects daemon use of unrestricted Async primitives — bound per [engineering doctrine: Long-Running Daemons → Structured Concurrency](../documents/engineering/README.md)| ✅ Done (Sprint 2.9, extended by Sprint 0.4) | `.hlint.yaml`, `src/Prodbox/CheckCode.hs`, daemon-path modules under `src/Prodbox/Gateway/` |
| File-watch reload trigger | The daemon watches its `--config` Dhall path via filesystem-watch primitives (the supported library is named by Sprint 2.21). The previously-supported SIGHUP signal handler and the `forbid-fsnotify` / `forbid-inotify` / `forbid-mtime` lint rules are removed; SIGHUP becomes an ordinary `drain + exit` terminate signal. Bound per [config_doctrine.md §7](../documents/engineering/config_doctrine.md#7-file-watch-reload-trigger). | ✅ Done (Sprint 2.21, closing the Sprint 2.11 file-watch surface) | `.hlint.yaml`, `src/Prodbox/CheckCode.hs`, `src/Prodbox/Gateway/Daemon.hs` |
| Typed `schemaVersion` field plus mismatch-as-parse-failure | The gateway daemon config parser treats structured `schemaVersion` mismatches as `config_schema_mismatch` and the reload path preserves the running config. | ✅ Done (Sprint 2.11, extended by Sprint 0.4) | `src/Prodbox/Gateway/Types.hs`, `src/Prodbox/Gateway/Daemon.hs`, `prodbox-config-types.dhall` |
| Reload procedure | The reload worker runs in order: receive file-watch event, re-decode the Dhall via `Dhall.inputFile auto`, log `config_reload_decode_failed` on parse error and keep the in-memory config, classify diffs (any BootConfig change → drain + exit with `ExitSuccess`; LiveConfig-only change → atomic `writeTVar` + STM broadcast). Bound per [config_doctrine.md §8](../documents/engineering/config_doctrine.md#8-boot-vs-live-split-and-the-restart-contract). | ✅ Done (Sprint 2.11, implemented through Sprints 2.20/2.21) | `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/Gateway/Settings.hs` |
| Typed logging field helpers | `field :: (Aeson.ToJSON a) => Text -> a -> (Text, Aeson.Value)` plus `logStructured`, `logStructuredAt`, `logDebug`, `logInfo`, `logWarn`, and `logError` wrappers are backed by `co-log`; `prodbox dev check` and the Haskell style suite reject direct daemon terminal writes and inline log-object construction — bound per [engineering doctrine: Long-Running Daemons → Logging](../documents/engineering/README.md)| ✅ Done (Sprint 2.12, extended by Sprint 0.4) | `src/Prodbox/Gateway/Logging.hs`, `src/Prodbox/CheckCode.hs`, `test/haskell-style/Main.hs` |
| Production-no-op / test-injected hook contract | Daemon `Env` hook fields have no-op production defaults, and `prodbox dev check` enforces production startup through literal `noopDaemonHooks` plus hook reads through injected `envHooks env` — bound per [engineering doctrine: Long-Running Daemons → Test Hooks](../documents/engineering/README.md)| ✅ Done (Sprint 2.13, extended by Sprint 0.4) | `src/Prodbox/Gateway/Daemon.hs`, `src/Prodbox/CheckCode.hs`, `test/integration/` |
| Health-endpoint response shapes as golden tests | The `prodbox-daemon-lifecycle` stanza captures `/healthz`, ready/draining `/readyz`, and normalized `/metrics` response-shape goldens under `test/golden/daemon-health/` | ✅ Done (Sprint 2.14, extended by Sprint 0.4) | `test/daemon-lifecycle/Main.hs`, `test/golden/daemon-health/` |
| Forbidden reconciler flags and sister commands | `--force`, `--reinstall`, and sister commands `install`, `upgrade`, `repair`, `force-install` are rejected at parse time on the chart and lifecycle reconciler surfaces; no legacy lifecycle alias remains on the supported command surface | ✅ Done (Sprints 3.10 / 4.5, extended by Sprint 0.4) | `src/Prodbox/CLI/Parser.hs`, `test/unit/Parser.hs` |

## Infrastructure Layer

| Component | Technology | Deployment | Authority | Durable State |
|-----------|------------|------------|-----------|---------------|
| Supported host runtime | `Ubuntu 24.04 LTS` with systemd | Bare metal | `prodbox` supported-host gate | Host filesystem |
| Host build root | `.build/` | Repository worktree | Canonical `cabal build --builddir=.build exe:prodbox` plus copy to `.build/prodbox`, which backs the supported `prodbox` command surface | Host filesystem |
| Container build root | `/opt/build` | Docker build stage | Dockerfiles under `docker/` | Container filesystem |
| Home local substrate (RKE2 on operator host) | RKE2 | Home local substrate (per [substrates.md](substrates.md)) | `prodbox cluster reconcile|delete --yes|delete --cascade|status|health|start|stop|restart|logs|wait|workload-logs`, including Sprint `4.45`'s graph-compiled reconcile order, separate edge tail, total step anchors/actions, fail-closed compiled payload, and bounded final-component readiness barriers; delete retains hermetic success reporting and actionable failure summaries. Provides the home substrate against which the canonical test suite runs. | RKE2 data dirs and systemd units |
| In-cluster registry (`registry:2`) pipeline | Single-binary `registry:2` (applied via `kubectl apply`, no Helm) plus Docker CLI image reconcile, a registry-plus-storage-backend public-registry bootstrap exception until the registry is externally ready, anonymous HTTP push with repos auto-created on first push (no projects API), post-bootstrap registry populate with ordered candidate retry on publish-time failure, and native-host-architecture-only custom-image publish. Sprints `4.43` (home) / `7.31` (EKS) (✅ Done 2026-07-06): the pre-publish readiness gate is a **deep** registry→MinIO S3 edge probe (`ensureRegistryStorageBackendEdgeReady`, a blob-upload round-trip) ahead of the front-door `/v2/` pre-check, and the mirror retry classifiers treat transient name-resolution failures as retryable, per [bootstrap_readiness_doctrine.md](../documents/engineering/bootstrap_readiness_doctrine.md). | RKE2 workload plus host Docker runtime | `prodbox cluster reconcile` plus registry-backed steady-state workload-image doctrine | Cluster resources, registry data in the `prodbox-harbor-registry` MinIO bucket, host Docker cache, and RKE2 registry config |
| AWS substrate (EKS provisioning) | Amazon EKS in a dedicated, non-default VPC whose VPC/IGW/route-table/subnets carry `prodbox.io/managed-by=prodbox` for postflight sweep visibility | AWS substrate (provisioned per [substrates.md](substrates.md)) | `prodbox aws stack eks reconcile|destroy --yes`; the canonical-suite `aws-eks` validation in `src/Prodbox/TestValidation.hs` runs against it. **Per-run lifecycle class** — auto-managed by the test harness with destroy-before-ensure fresh-VPC purge, see [substrates.md → Resource Lifecycle Classes](substrates.md#resource-lifecycle-classes). | EKS resources in AWS |
| AWS substrate (Route 53 subzone) | Amazon Route 53 | AWS substrate (provisioned per [substrates.md](substrates.md)) | `prodbox aws stack aws-subzone reconcile|destroy --yes`; provides the per-substrate subzone (e.g. `aws.test.resolvefintech.com`) that AWS-substrate public-edge proofs write into. Destroy deletes non-NS/SOA record sets before hosted-zone teardown so a failed run's A record cannot strand the subzone. **Per-run lifecycle class.** | Route 53 zone + NS delegation records |
| AWS substrate (HA RKE2 provisioning) | `Ubuntu 24.04 LTS` EC2 instances | AWS substrate (provisioned per [substrates.md](substrates.md)) | `prodbox aws stack test reconcile|destroy --yes` plus Haskell SSH orchestration; the canonical-suite `ha-rke2-aws` validation runs against it. **Per-run lifecycle class.** | EC2, VPC, subnet, security-group, IAM, and Route 53 state |
| Cross-substrate shared SES infrastructure | AWS SES + S3 + IAM | AWS account (per [substrates.md → Cross-Substrate Shared Resources](substrates.md#cross-substrate-shared-resources)) | `prodbox aws stack aws-ses reconcile|destroy --yes`; provides the sending identity, DKIM, exact regional MX record, receive rule set, S3 capture bucket plus harness-owned readiness canary, and SMTP IAM user shared by both substrates' `keycloak-invite` validation. Pulumi owns the SMTP IAM user/policy and canary but no access-key resource or key outputs; `Prodbox.Lifecycle.SmtpKeyRepairInterpreter` is the sole access-key creator and guarded Model-B projection writer. Sprint `4.47` supplies the registered idempotent desired-present action, explicit retained-home `LongLivedCheckpointAuthority`, separate selected-cluster `TargetClusterSecretSink`, bounded non-renewable lease, fixed one-hour same-account STS role, fenced checkpoint writeback, and global target-intent protocol. Sprint `5.17` supplies capability-derived invite-plan composition; Sprint `8.10` binds `Prodbox.Ses.Readiness` after provider presence and before SMTP materialization, with operational-credential capture proof kept distinct from lease-role control-plane reconnaissance. Setup/teardown owns the role as a registered `Operational` resource. The main checkpoint is the opaque Model-B object in retained `prodbox-state`; long-lived S3 retains public-edge TLS and optional legacy first-touch input. `LongLived` prevents ordinary postflight destruction; it does not exempt desired-present preparation. | SES sending identity, S3 capture bucket/readiness canary, SMTP IAM user/policy, Haskell-managed SMTP access key, and operational SES lease role |
| Pulumi test backend | MinIO plus `Prodbox.Pulumi.EncryptedBackend` | Local RKE2 workload | Local-cluster-first bootstrap plus Pulumi backend configuration, bounded backend login, deleted-export-mount repair, the Sprint `1.37` sealed-Vault apply-path gate (`runPulumiCommandWithGate`), the Sprint `4.30` generic `prodbox-state` bucket, and the Sprint `7.14` decrypt-to-scratch wrapper: dry-runs render without probing Vault, while real apply/destroy/migrate actions refuse before touching stack state when Vault is unreachable, uninitialized, or sealed. First-touch raw checkpoint migration imports old backend state into the encrypted object-store before supported writes continue; raw backend env is confined to `LegacyPulumiBackend`, supported Pulumi action env is provider-only before the scratch `file://` rewrite, and Sprint `7.30` removes the remaining post-bootstrap host MinIO port-forward dependency by routing object-store reads/writes through the daemon API. | Opaque Model-B objects in `prodbox-state` |
| Load balancer IPs | MetalLB on self-managed local clusters with config-selected L2 or BGP advertisement, including `IPAddressPool`, `L2Advertisement`, `BGPPeer`, and `BGPAdvertisement` resources; the `IPAddressPool` is sized to a single LAN IP, matching the supported edge's one Envoy Gateway `LoadBalancer` Service | RKE2 workload | Haskell lifecycle plus Gateway API edge doctrine | Cluster resources |
| Public edge controller | Envoy Gateway plus Kubernetes Gateway API, with browser-facing auth and operational-dashboard access enforced at Envoy, request-carried JWTs validated locally at Envoy from Keycloak issuer metadata plus JWKS-backed signing keys and route claims, shared-host Keycloak integration on `test.resolvefintech.com`, public TLS termination at Envoy on port `443`, redirect-only HTTP handling on port `80`, and WebSocket-capable proxying on the same public edge | RKE2 workload | Haskell lifecycle plus single-host public-edge doctrine | Cluster resources |
| TLS issuance | cert-manager plus the ZeroSSL ACME provider (EAB-authenticated from config). One `ClusterIssuer` is rendered: `zerossl-dns01` (`acme.server`) with a DNS-01 Route 53 solver. Rebuild churn is handled by retaining and restoring the issued certificate from the long-lived S3 bucket (Sprints `4.24`/`7.11`/`8.7`), not by a separate test issuer | RKE2 workload | Haskell lifecycle plus chart runtime, including lifecycle-owned `ClusterIssuer` projection | Kubernetes secrets |
| DNS control plane | Route 53 hosted zones with one canonical public record per active substrate (`test.resolvefintech.com` on home, the delegated AWS subzone name on AWS) | AWS | Home local: Haskell gateway daemon `dns_write_gate`; AWS: `prodbox edge status --substrate aws` reconciles the A record to the resolved Envoy NLB targets | Route 53 |
| Gateway workload | Haskell gateway daemon | In-cluster Kubernetes workload | `prodbox charts reconcile gateway` plus `prodbox gateway status --config <path>`; the daemon `dns_write_gate` is enabled on home local and disabled on AWS where the host-side public-edge reconciler owns the subzone A record. Sprint `2.29` added the pre-Vault bootstrap loader/listener and loopback-restricted NodePort endpoint that lets the host hand off Vault initialization/unseal/reconcile after the daemon is deployed. | Cluster resources plus Vault KV paths for gateway event, AWS, and MinIO credentials |
| Cluster storage class | `manual` (`kubernetes.io/no-provisioner`, `Retain`) — the only StorageClass on **both** substrates; the PV volume source differs (`hostPath` on home, pre-created EBS via CSI `volumeHandle` on EKS), no dynamic provisioning | RKE2 + EKS cluster | `prodbox cluster reconcile` / `charts reconcile` | Cluster-scoped resource |
| Manual PV root (home substrate) | Configured host path, default `.data/`; on EKS the durable store is the pre-created EBS volume set (see the AWS retained block storage row) | Host filesystem / EBS | `prodbox.dhall` plus cluster and chart lifecycle commands | PV contents only |
| AWS retained block storage | Pre-created EBS volumes lifted in as static `Retain` PVs (CSI `volumeHandle`, `topology.ebs.csi.aws.com/zone` affinity); the EBS analog of `.data/` — retained on teardown, only test-scoped volumes deleted by the harness postflight reaper, cascade hook, or `aws ebs reap-test --yes` (Sprints `7.28`/`4.39`/`4.40`). Sprint `4.39` has registered `aws-ebs-volumes`, the typed EC2 discover/destroy boundary, retain/test-scoped tags, and deterministic retained-inventory parity; Sprint `4.40` has landed the test-scoped reaper and `Delete`-only drain guard; Sprint `5.12` adds the named `eks-volume-rebind` validation surface that proves same-PV/PVC rebinding and sentinel preservation. The destructive AWS `volumeHandle` proof remains a non-blocking substrate-parity live-infra axis tracked in [substrates.md](substrates.md). | EKS cluster | `charts reconcile` + the EBS managed-resource registry + `prodbox test integration eks-volume-rebind --substrate aws` | PV contents only; retained (prod) or test-scoped (harness-deleted) |
| Master seed (gateway-owned) — **retired and removed** | None | None | Sprint `3.19` removed the HMAC-derivation model outright: there is no `master-seed` object in MinIO, no `Prodbox.Secret.{Derive,MasterSeed,Inventory}`, no `/v1/secret/*` gateway RPC, no daemon-only-seed lint, and no `selfBootstrapOwnSecrets`. Every previously-derived secret is now a Vault KV object fetched via Vault Kubernetes auth. See [secret_derivation_doctrine.md](../documents/engineering/secret_derivation_doctrine.md), [vault_doctrine.md](../documents/engineering/vault_doctrine.md), and the completed rows in [legacy-tracking-for-deletion.md](legacy-tracking-for-deletion.md) | Removed in Sprint `3.19` |
| Vault (sole per-cluster secret / KMS / PKI root) | In-cluster HashiCorp-Vault-style server (Vault Transit, KV v2, PKI, Kubernetes auth, seal/unseal) | In-cluster Kubernetes workload on a durable Vault PVC under `.data/vault/vault/0`, standing up on both home-local and AWS substrates identically (substrate equivalence). Landed structure — the root `prodbox vault` command group + encrypted unlock bundle under Sprint `1.36`, Vault platform component under Sprint `3.17`, chart-secret Vault adoption under Sprint `3.18`, master-seed retirement under Sprint `3.19`, the root Shamir / child Transit seal model under Sprint `3.20`, lifecycle integration + durable Vault PV preservation under Sprint `4.29`, the Sprint `4.32` federated child lifecycle / fail-closed parent-readiness cascade, Sprint `4.42` daemon-mediated root lifecycle boundary, and Sprint `7.30` daemon-mediated Pulumi object-store boundary. **Init-once / unseal-on-rebuild**: `vault init` runs exactly once ever (first time the PV is empty); every later `cluster reconcile` only unseals. | `prodbox vault status|init|unseal|seal|reconcile|rotate-unlock-bundle|rotate-transit-key <key>|pki status|pki issue-test-cert` (Sprint `1.36`); `vault reconcile` applies the baseline Vault mount/auth/policy/key plan after Vault is already initialized and unsealed, and `cluster reconcile` brings MinIO up BEFORE Vault (Sprint `7.25`) then deploys/rebinds Vault, deploys the pre-Vault gateway daemon, and runs init/unseal/reconcile through the daemon NodePort while preserving the Vault PV alongside the MinIO PV. The **sole, finalized** fail-closed secret/KMS/PKI root for the cluster: a sealed (or unreachable/uninitialized) Vault **bricks the cluster** — no secret resolves and TLS, Keycloak, Pulumi, and AWS-credential paths all fail closed. A root cluster is Shamir-sealed (operator-unsealed via the password-AEAD unlock bundle in MinIO, Sprint `7.25`); child clusters `seal "transit"` against the parent and refuse secret-dependent reconcile when the parent is sealed or unreachable. Post-bootstrap root lifecycle and supported Pulumi object-store operations reach Vault/MinIO through the loopback-restricted daemon NodePort; surviving direct host Vault/MinIO transports are explicit legacy/config/test seams tracked in [legacy-tracking-for-deletion.md](legacy-tracking-for-deletion.md). See [vault_doctrine.md § 5](../documents/engineering/vault_doctrine.md#5-vault-deployment-model) and [cluster_federation_doctrine.md](../documents/engineering/cluster_federation_doctrine.md) | Vault KV / Transit / PKI data on the durable Vault PV under `.data/vault/vault/0`; persists across cluster wipes whenever `.data/` is preserved, exactly like the MinIO PV |
| Chart platform | Helm plus Haskell orchestration | RKE2 workloads | `prodbox charts ...` | Cluster resources plus retained roots |
| Cluster-wide Patroni operator platform | Percona operator for PostgreSQL installed by the canonical Helm lifecycle | RKE2 workload | `prodbox cluster reconcile` plus registry-backed steady-state image sourcing | Cluster resources |
| External application PostgreSQL HA | Percona-operator-backed Patroni PostgreSQL clusters with exactly three PostgreSQL replicas and synchronous replication, rendered from namespace-local application chart inputs and reconciled by the cluster-wide operator | RKE2 workloads | Haskell chart platform plus registry-backed steady-state image sourcing | Cluster resources plus retained roots |
| Namespace-local identity stack | `keycloak` behind Envoy on the shared public hostname, with the supported issuer, proxy-header, and shared-path contract for chart-managed workloads; new login, refresh, and later JWKS refresh depend on this workload while the steady-state JWT hot path remains local to Envoy | RKE2 workload | Haskell chart platform plus external Percona-operator-backed Patroni PostgreSQL dependency | Cluster resources plus retained roots |
| Namespace-local app stack | `vscode`, `api`, `websocket`, and supported operational dashboards behind Envoy on explicit shared-host paths under `test.resolvefintech.com` | RKE2 workload | Haskell chart platform | Cluster resources plus retained roots |
| Shared realtime state | Redis backing supported WebSocket workloads today and any later explicit external rate-limit service, never Envoy JWT validation, and not yet shipping a standalone rate-limit-service workload on the current supported path | RKE2 workload | Haskell chart platform plus public workload doctrine | Cluster resources plus app state |
| AWS admin credential isolation | Elevated/admin AWS power enters prodbox only through the interactive `SecretRef.Prompt`, used by setup to mint the dedicated `prodbox` IAM identity and its exact same-account SES lease role, by explicit long-lived destroy/migration, and by `prodbox nuke`; it is never used by the supported `aws-ses` reconcile transaction. The `aws_admin_for_test_simulation.*` block is a test-harness-only fixture (TestPlaintext) in `test-secrets.dhall` whose sole purpose is to simulate that operator prompt so the suite-level IAM harness can drive these flows non-interactively; it is never read by any production binary, never in Vault, never in production config (Sprint `7.16`). The minted operational `prodbox` `aws.*` credential is written into Vault KV and may assume only the fixed bounded SES role; the role session expires no later than its authority lease grant. SSoT: [aws_admin_credentials.md](../documents/engineering/aws_admin_credentials.md), [vault_doctrine.md § 4](../documents/engineering/vault_doctrine.md#4-config-split-production-references-vs-test-plaintext). | Test fixture (`test-secrets.dhall`) drives the prompt; production prompts the operator | Haskell config, IAM validation harness, SES lease-role owner, and long-lived Pulumi backend | `test-secrets.dhall` (test-only); Vault KV for the operational credential; fixed IAM role for bounded SES work |
| AWS teardown residue policy | `Prodbox.CLI.Command.PulumiResiduePolicy` ADT (Sprint `7.7`, extended by Sprint `7.5.c.v.c`) with five constructors: `RefuseOnAnyResidue` (default, operator path), `DestroyPulumiResidueFirst` (operator `--destroy-pulumi-residue` — dispatches `prodbox aws stack <cli-verb> destroy --yes` in canonical order before IAM teardown), `AcceptOrphanResidue` (operator `--allow-pulumi-residue` — Sprint `7.6` escape hatch), `BypassPerRunResidueOnly` (refuses on long-lived `aws-ses` residue while bypassing per-run stacks; was the harness preflight+postflight policy at Sprint 7.7 but **has no production caller after Sprint `7.9`** — it remains a valid ADT member tracked for eventual removal in [legacy-tracking-for-deletion.md](legacy-tracking-for-deletion.md)), `BypassAllResidueForHarnessRefresh` (harness-internal only; used by both harness preflight and postflight). Sprint `4.47` makes the SES lease role and trusted user/config registered `Operational` resources: teardown deletes and re-observes the role before its user, while a later invite-capable run recreates both through harness setup before retained reconcile. Explicit long-lived destroy still uses the admin path. Per-run vs long-lived partition fixed by `Prodbox.Aws.perRunStackNames` / `longLivedResourceNames` and must match [substrates.md → Resource Lifecycle Classes](substrates.md#resource-lifecycle-classes) verbatim. | `prodbox aws teardown` flag surface + harness postflight | Haskell CLI + IAM validation harness | Encrypted Model-B checkpoint observations for both classes through their explicit authorities; legacy long-lived S3 is optional first-touch input only. See [lifecycle_reconciliation_doctrine.md §3](../documents/engineering/lifecycle_reconciliation_doctrine.md). |
| AWS harness orphan-safety | Sprint `7.6` guards: `prodbox aws teardown` refuses to delete the operational IAM user while any Pulumi-managed stack reports live resources (file-based check via `<stack>HasLiveResources` predicates, replaced by Sprint `4.16` `<stack>ResidueStatus` queries and Sprint `7.14` encrypted checkpoint reads); test-runner postflight (`runWithAwsHarnessCleanup`) auto-destroys per-run stacks on success / failure / Ctrl-C, then — as of Sprint `7.10` — clears operational `aws.*` + deletes the operational `prodbox` IAM user **only when the per-run destroy succeeded** (pure decision `clearOperationalCredsAfterPostflight :: ExitCode -> Bool`, `True` iff `ExitSuccess`). On a per-run destroy failure the operational-credential teardown is **held** so the orphaned per-run stacks keep the operational creds needed to destroy them on retry; a diagnostic names the recovery path (`prodbox aws stack <cli-verb> destroy --yes` then `prodbox aws teardown`). The per-run EKS destroy itself drains the cluster first (Sprint `4.23`, [lifecycle_reconciliation_doctrine.md §5c](../documents/engineering/lifecycle_reconciliation_doctrine.md)). `aws-ses` explicitly excluded from auto-destroy per the long-lived class. | Encrypted Pulumi checkpoint residue queries + harness teardown postflight | `src/Prodbox/Aws.hs`, `src/Prodbox/TestRunner.hs`, `src/Prodbox/Infra/Aws*Stack.hs`, `src/Prodbox/Lifecycle/LiveResidue.hs` | Opaque Model-B checkpoint objects plus AWS Resource Tagging API |
| Long-lived Pulumi state backend | Sprint `7.14` is Done: `aws-ses` uses the same encrypted Model-B object-store wrapper as per-run stacks for main reconcile/destroy/read paths and for the `aws-ses migrate-backend` compatibility command. Sprint `4.47` binds the non-interchangeable retained home/control-plane authority and selected target-cluster SMTP sink types to the supported transaction: reconcile persists only through a fresh owner/fence permit and the gateway-backed opaque CAS adapter. The `pulumi_state_backend` S3 bucket remains retained public-edge TLS storage and an optional first-touch source for old checkpoints. | `src/Prodbox/Pulumi/EncryptedBackend.hs`, `src/Prodbox/Lifecycle/CheckpointAuthority.hs`, `src/Prodbox/Lifecycle/CheckpointAuthorityStore.hs`, `src/Prodbox/Infra/AwsSesStack.hs`, `src/Prodbox/Infra/LongLivedPulumiBackend.hs`, `src/Prodbox/Settings.hs::PulumiStateBackendSection` | Opaque Model-B object for `aws-ses`; AWS S3 bucket named by `pulumi_state_backend.bucket_name` for retained TLS and first-touch legacy checkpoint import |
| Lifecycle reconciliation doctrine | Reconciler-with-predicates pattern (no global state machine) that every destructive lifecycle command composes. Its bring-up inverse-polarity twin is `ReadinessObservation`: only `ReadyObserved` opens a gate; pending and unreachable observations remain bounded and fail closed, while an incompatible target/probe pair refuses before polling. Sprint `4.45` binds this seam to home RKE2 production targets: the graph-derived, fully anchored plan polls caller-injected one-shot observations within bounds after each component's final step, then permits dependants; invalid dependency/phase projections return a structured error before apply. SSoT: [../documents/engineering/lifecycle_reconciliation_doctrine.md](../documents/engineering/lifecycle_reconciliation_doctrine.md). | `src/Prodbox/Lifecycle/ReadinessObservation.hs`, `src/Prodbox/Lifecycle/ResidueStatus.hs`, `src/Prodbox/Lifecycle/Preconditions.hs`, `src/Prodbox/Lifecycle/ResourceRegistry.hs`, `src/Prodbox/CLI/Rke2.hs`, `src/Prodbox/CLI/Nuke.hs` | Observed external readiness, compiled home reconcile order, and encrypted Pulumi/AWS residue state |

## CLI and Runtime Layer

| Surface | Command | Purpose |
|---------|---------|---------|
| CLI frontend | `prodbox <command>` | Parse the closed command surface and dispatch directly to native Haskell commands with no legacy delegation shim |
| Config management | `prodbox config setup|show|validate|schema|generate` | Author, display, validate, regenerate schema for, and initialize the executable-sibling Tier-0 `prodbox.dhall`. Sprint `1.38` lands the in-force-config foundation and loader switch: Dhall payload decode through `decodeConfigDhallBytes`, injected `fetchInForceConfigWith` / `storeInForceConfigWith`, and `loadConfigForSettingsWith` switching host settings loads from direct Dhall-as-live-config to basics → ready Vault root token → Vault KV MinIO credentials → MinIO envelope fetch/decrypt/decode once basics exist. Sprint `4.30` routes the production MinIO read through `Prodbox.Minio.EncryptedObject` / `ObjectStore` using the Vault-owned `secret/object-store/hmac` key and removes the literal `active-config/in-force-config.prodbox-envelope-v1` backend helpers. Sprint `1.41` consolidates the sealed-Vault floor onto the self-contained Tier-0 `prodbox.dhall` read directly via `projectBasics` — eliminating the separate JSON floor (`prodbox-basics.json` + the legacy `.data/prodbox/unencrypted-basics.json`) — and Sprint `1.42` seeds the in-force config into the encrypted MinIO SSoT (`storeInForceConfigWith`) so the cluster reads config from the SSoT rather than the Sprint `1.39` `inForceConfigObjectAbsent` seed-fallback. The retired standalone `prodbox-config.dhall` name survives only as a legacy payload/schema/temp-decode shape. See [config_doctrine.md §0](../documents/engineering/config_doctrine.md#0-three-tier-config-model). |
| Host prerequisites and diagnostics | `prodbox host ensure-tools|check-ses-readiness|check-ports|info`, `prodbox host firewall gateway-restrict|gateway-unrestrict` | Verify local tools and diagnose host/network prerequisites. `host check-ses-readiness` runs the same read-only sender/DKIM, MX/rule, and operational capture-canary list/get prerequisite observations without reconciling AWS state; it prints Ready only when all three semantic scopes pass and otherwise preserves the structured fail-closed reason plus retry-the-same-validation remedy. |
| Cluster lifecycle | `prodbox cluster reconcile|delete --yes|delete --cascade|status|health|start|stop|restart|logs|wait|workload-logs` | Reconcile through the validated component DAG's component-owned steps plus explicit edge tail, polling each final readiness barrier within bounds before dependants; inspect, control, and remove the local cluster with a hermetic delete summary on success, including the cluster-wide Percona operator lifecycle and the lifecycle-local quiet path that suppresses benign upstream uninstall chatter |
| Registry reconcile and image population | `prodbox cluster reconcile` | Allow direct public pulls only for the registry and the registry's storage backend before the registry is healthy and externally serving, require a stable `GET /v2/` external window (expect 200/401, six consecutive rounds), auto-create the `prodbox/` repositories on first anonymous push, and ensure required custom and public images are present in the registry before later Helm deployments, retrying alternate configured public-image candidates when a preferred source fails during registry publication, including `mirror.gcr.io` fallbacks for the Docker Hub-hosted Percona and Envoy images used by the supported lifecycle |
| AWS stack lifecycle | `prodbox aws stack eks|aws-subzone|test|aws-ses reconcile`, `prodbox aws stack eks|aws-subzone|test|aws-ses destroy --yes` | Provision and tear down every AWS substrate Pulumi stack (`aws-eks`, `aws-eks-subzone`, `aws-test`, `aws-ses`) per [substrates.md](substrates.md). Per-run vs long-lived lifecycle classification lives in [substrates.md → Resource Lifecycle Classes](substrates.md#resource-lifecycle-classes). |
| Public-edge diagnostic | `prodbox edge status`, `prodbox edge reconcile` | Classify Route 53, Gateway API, TLS, port `80` redirect readiness, shared-host path routing, advertisement mode, and readiness for named external proof; on AWS it also reconciles the delegated-subzone A record to the Envoy NLB IPv4 targets |
| Gateway runtime | `prodbox gateway start --config <path>|status --config <path>|config-gen <output-path> --node-id <node-id>` | Start the in-pod gateway entrypoint, inspect bounded state and the already-observed continuity disposition over operator-only `/v1/state`, serve constant-time `/healthz` and `/readyz` endpoints plus `/metrics`, and generate Dhall config. Bounded Orders/state/transport, retained local-emitter continuity, and persistence-before-emission are enforced; unavailable/unobservable continuity may serve allowed diagnostic/pre-Vault bootstrap routes but cannot enable emitter/claim/DNS capabilities. The Sprint `3.25` typed/generated chart surface binds kubelet liveness to `/healthz` and readiness to `/readyz`, with chart lint forbidding `/v1/state` in either probe. |
| Public workload runtime | `prodbox workload start --config <path>` | Run the internal API runtime or WebSocket runtime selected through the `workload.mode = Api \| Websocket` field of the mounted Dhall config per [config_doctrine.md](../documents/engineering/config_doctrine.md) (Sprint `3.14` removed the legacy `PRODBOX_WORKLOAD_MODE` path; the mounted config is the only supported selector) for the chart-managed workloads behind the shared public hostname, including the real `/ws` upgrade, one-connection-per-pod lifetime, and readiness-based drain contract |
| Chart runtime | `prodbox charts list|status <chart>|reconcile <chart> [--dry-run] [--plan-file <path>]|delete <chart> [--yes] [--dry-run] [--plan-file <path>]` | Manage the supported `gateway`, `keycloak`, `vscode`, `api`, and `websocket` chart surfaces, with `gateway` remaining the separate distributed gateway daemon path and the shared-host browser, API, WebSocket, and MinIO console routes staying behind Envoy with Keycloak-backed auth and RBAC |
| Vault lifecycle | `prodbox vault status|init|unseal|seal|reconcile|rotate-unlock-bundle|rotate-transit-key <key>|pki status|pki issue-test-cert` | Active structure (Sprints `1.36`, `3.17`–`3.20`, `4.29`–`4.33`, `4.42`, plus the Sprint `5.8` and `5.14` code-owned validation surfaces): manage the per-cluster Vault secret / KMS / PKI backend — report seal state, initialize, unseal from the MinIO-only unlock bundle (Sprint `7.25`) or parent Transit seal, seal, reconcile (deploy + unseal), rotate the encrypted unlock bundle, rotate a named Transit key, and inspect or test-issue from the Vault PKI mount. Landed pieces: `Prodbox.Vault.Client` (bootstrap + authenticated KV/Transit/seal/reconcile/rotate/PKI HTTP surface), `Prodbox.Vault.UnlockBundle`, `Prodbox.Vault.Orchestration`, `Prodbox.Vault.Reconcile` (base `secret` KV v2 / Transit / PKI mount, Kubernetes auth, per-domain Transit keys, policies, and roles), `Prodbox.CLI.Vault` handlers for every vault leaf (init/unseal/seal/reconcile, unlock-bundle re-encryption, Transit-key rotation, PKI status, and PKI test issue against the later `prodbox-test` role), `Prodbox.Vault.Status`, `Prodbox.Settings.SecretRef` (FileSecret-free union + Vault KV resolver seam), `Prodbox.Crypto.Envelope` (`prodbox-envelope-v2` writes with v1 decode compatibility), `Prodbox.Vault.TransitCipher` (production Vault-Transit `DekCipher`), the shared Vault platform install, root Shamir / child Transit seal rendering, cluster lifecycle deploy/init-if-empty/unseal/reconcile wiring, the Model-B object-store foundation, the federated child lifecycle fail-closed cascade, the Haskell-side sealed-state gate/redaction audit from Sprint `4.33`, the `sealed-vault` native validation plus pure forbidden-pattern oracle, generated Dhall/config SecretRef sweep, live home proof from Sprint `5.8`, the Pulumi decrypt-to-scratch wrapper/read/migration path from Sprint `7.14`, the Sprint `2.29` pre-Vault daemon bootstrap endpoint, the Sprint `4.42` daemon routing for `cluster reconcile` root bootstrap plus post-bootstrap `prodbox vault ...` lifecycle leaves, the Sprint `5.14` `daemon-bootstrap` no-legacy-transport regression proof, and the Sprint `7.30` daemon object-store API for supported Pulumi checkpoint reads/writes/deletes. Remaining axes are live AWS-substrate sealed-Vault red-team closure and AWS/EKS daemon object-store parity (non-blocking items tracked in [substrates.md](substrates.md), orthogonal to the passed home proof under Standards N/O). Sprint `8.9` chart Vault-auth adoption and Sprint `7.15` Vault-sourced ACME material are landed; optional native Vault-PKI issuance is outside the current cert-manager contract. See [vault_doctrine.md](../documents/engineering/vault_doctrine.md#7-vault-lifecycle-commands) |
| DNS check | `prodbox dns check` | Inspect DNS ownership state |
| Cluster utilities | `prodbox cluster health|wait|logs|workload-logs` | Inspect cluster readiness and collect infrastructure or workload logs |
| Interactive onboarding | `prodbox config setup` | Guided Dhall authoring plus live AWS and operator prompts for one temporary admin credential set |
| AWS IAM, quota, and EBS maintenance | `prodbox aws policy|setup|teardown [--destroy-pulumi-residue | --allow-pulumi-residue]`, `prodbox aws quotas check`, `prodbox aws quotas request`, `prodbox aws ebs reap-test --yes` | Generate policies, manage IAM users, manage AWS quotas, and delete leaked test-scoped EBS volumes through typed native flows. `aws teardown` Sprint `7.6`/`7.7` policy: default refuses on Pulumi residue, `--destroy-pulumi-residue` destroys live stacks first, `--allow-pulumi-residue` is the operator-acknowledged orphan escape hatch (mutually exclusive at parse time). Residue check runs before the credential prompt; "Nothing to do." short-circuits when residue is empty and operational `aws.*` is empty. `aws setup` prompt auto-detects `AKIA…` vs `ASIA…` access keys to conditionally prompt for the session token. `aws ebs reap-test --yes` loads operational `aws.*` from Vault/config and deletes only volumes tagged `prodbox.io/lifecycle=per-run-test` plus `kubernetes.io/cluster/<cluster-name>=owned`; retained-production EBS is never selected. |
| TLA+ validation | `prodbox dev tla-check` | Run formal verification |
| Test runner | `prodbox test lint|all|unit|integration ...` | Run named lint, unit, and integration suites on the Haskell stack, including the shared idempotent AWS IAM harness used by the named and aggregate IAM validation surfaces |
| Code quality | `prodbox dev check` | Run the required doctrine, formatting, lint, and type-check gate with governed doctrine-alignment enforcement |

## Validation Layer

The validation layer is one canonical test suite that runs against substrates (see
[substrates.md](substrates.md)). Validations are described as suite content with declared
prerequisites, not as substrate-specific concerns. The same validation runs on every substrate
that satisfies its declared prerequisites.

| Surface | Canonical Validation |
|---------|----------------------|
| Build artifact contract | Runnable `prodbox` binary at `.build/prodbox`, produced by the canonical build-plus-copy flow; container build proof closes when the canonical Dockerfiles under `docker/` emit artifacts under `/opt/build` through in-image `ghcup`-managed GHC `9.12.4` with no symlinked Haskell tool shims |
| CLI and env contract | `prodbox test integration cli` and `prodbox test integration env` run built-frontend Haskell suites against the direct-Dhall config contract without recreating `prodbox-config.json` |
| Canonical test suite | `prodbox test integration charts-vscode`, `charts-api`, `charts-websocket`, `admin-routes`, `public-dns`, `dns-aws`, `aws-iam`, `aws-eks`, `pulumi`, `ha-rke2-aws`, `gateway-daemon`, `gateway-pods`, `gateway-partition`, `charts-platform`, `resource-guardrails`, `daemon-bootstrap`, `pulsar-broker`, `keycloak-invite`, `charts-storage`, `eks-volume-rebind`, `sealed-vault`, and `lifecycle` dispatch native Haskell validation flows through `src/Prodbox/TestValidation.hs`, planned by `src/Prodbox/TestPlan.hs`. The phase 1/2 prerequisite DAG validates AWS credentials, Route 53 access, bounded MinIO-backed Pulumi login, native IAM harness readiness, cluster readiness for `sealed-vault`, and (per [phase-8-email-invite-auth.md](phase-8-email-invite-auth.md)) read-only semantic SES sender/DKIM, exact MX/rule, and operational capture-canary list/get readiness before invite execution proceeds. Sprint `5.8` owns the pure `sealedVaultAuditReport` oracle used by the sealed-state cross-surface audit and generated Dhall/config SecretRef sweep; Sprint `5.14` owns `daemonBootstrapAuditReport`, the no-legacy-transport/redaction oracle for daemon-mediated bootstrap and lifecycle traces. |
| Substrate provision/teardown | Home substrate: `prodbox cluster reconcile`, `prodbox cluster delete --yes` (hermetic delete reporting per Sprint `4.8`). AWS substrate: `prodbox aws stack eks reconcile|destroy --yes`, `prodbox aws stack test reconcile|destroy --yes`, and `prodbox aws stack aws-subzone reconcile|destroy --yes`. Provision/teardown are substrate ownership; the canonical suite is substrate-agnostic. |
| Substrate parity | The AWS substrate's substrate-platform install (`Prodbox.Lib.AwsSubstratePlatform.ensureAwsSubstratePlatformRuntime`) is **code-complete** as of Sprints `7.5.a`/`7.5.b.*`/`7.5.b.iii`/`7.5.c.i`–`7.5.c.iv`/`7.5.c.v.b`, the June 5 admin-route fix, Sprint `7.28`, and Sprint `7.32`: AWS LB Controller, AWS-specific Envoy Gateway runtime, cert-manager + ACME DNS01, EKS containerd registry-mirror DaemonSet, retained EBS PV bootstrap, MinIO, the in-cluster registry (`registry:2`), image mirroring/custom-image push, Percona PostgreSQL operator, gateway MinIO bootstrap, and substrate-aware MinIO console admin route installation. Sprint `7.32` replaces the former hand-maintained action list with a configured-DAG projection through the shared anchored-order engine, final EKS-owned readiness targets, and a scoped gateway Service port-forward across the Vault transition; it also removes the redundant MinIO reinstall and derives AWS restore from the shared builder. The June 5-9, 2026 AWS runs confirmed the then-canonical public-edge/chart/keycloak/lifecycle slice on EKS, including `keycloak-invite` OIDC claim verification and clean per-run teardown. Current full-suite membership is defined by `src/Prodbox/TestPlan.hs`; live-infra axes added later, such as AWS `sealed-vault` and `eks-volume-rebind`, are tracked as non-blocking substrate-parity rows in [substrates.md](substrates.md). |
| In-cluster registry (`registry:2`) pipeline | `prodbox cluster reconcile`, bootstrap-source proof for the registry plus storage-backend direct-public exception before the registry is externally ready, registry inventory proof for required images after bootstrap, stable `GET /v2/` proof (expect 200/401) before image writes, native-host-architecture custom-image proof with no supported `docker buildx` or cross-arch emulation, and **host `docker` CLI auth isolation** (Sprint `1.47`, ephemeral hostbootstrap-`Registry` pattern): each host-docker flow runs inside `withEphemeralDockerConfig`, which discovers the host `docker.io` auth read-only and writes a throwaway `prodbox-docker-config` `DOCKER_CONFIG` carrying **only** that `docker.io` pull auth (pushes to the `127.0.0.1:30080` registry are anonymous, so no registry credential is materialised) `bracket`-scrubbed on exit — **no `docker login` anywhere**, the operator's global `~/.docker/config.json` is only read (public pulls keep the Docker Hub login), nothing persists in `~/prodbox`; the `docker.io` discovery/projection is the seam to swap onto `HostBootstrap.Registry` at the planned hostbootstrap refactor |
| Gateway runtime suite content | `gateway-daemon`, the Cabal `prodbox-daemon-lifecycle` process suite, `gateway-pods`, `gateway-partition`, and `prodbox dev tla-check` |
| Chart platform suite content | `charts-storage`, `charts-platform`, `charts-vscode`, `charts-api`, `charts-websocket`, and the admin-route validations, plus manifest and lifecycle proof that supported Patroni use flows through the Percona operator-backed platform, Keycloak-backed workloads preserve the shared-host proxy-aware issuer contract, Envoy enforces JWT auth and RBAC from explicit bearer-token and JWKS configuration, and the WebSocket path proves real upgrade behavior, Redis-backed state continuity, revocation-driven reconnect, and readiness-based drain |
| Public-host proof suite content | `prodbox edge status`, `public-dns`, `charts-api`, `charts-websocket`, and the admin-route validations on the single-host Gateway API target doctrine. `prodbox edge status` owns the cluster-side `ready-for-external-proof` classification on whichever substrate is active, while the named validations own HTTP-to-HTTPS redirect proof on port `80`, Keycloak shared-host issuer proof, route-level RBAC proof, real WebSocket upgrade behavior, one-connection-per-pod lifetime, revocation-driven reconnect, and readiness-based drain on the substrate that provisioned them. |
| Clean-room handoff | `prodbox cluster delete --yes` for the home substrate plus `prodbox aws stack eks destroy --yes`, `prodbox aws stack aws-subzone destroy --yes`, and `prodbox aws stack test destroy --yes` for the AWS substrate, a rerun that starts and finishes with no supported-path `prodbox-config.json` artifact, `prodbox cluster reconcile`, `prodbox config show`, `prodbox config validate`, the canonical suite reruns, `prodbox edge status`, and repository review gates that confirm no supported-path placeholder-domain or Python residue survives |
| AWS IAM lifecycle | `prodbox test integration aws-iam`, targeted `prodbox test integration <name> --substrate aws` validations, `prodbox test integration all`, and `prodbox test all` use one shared idempotent harness that simulates the operator's elevated-credential prompt by reading the `aws_admin_for_test_simulation.*` test fixture (TestPlaintext) from `test-secrets.dhall` (never from production config, never from Vault; Sprint `7.16`); that harness deletes any pre-existing dedicated `prodbox` IAM user and keys before provisioning, uses any pre-existing `aws.*` only to discover and delete the IAM user associated with those credentials, proves STS-federated operational credentials with a compact AWS-substrate session policy, waits for the dedicated IAM-user credentials to pass STS and repeated Route 53 hosted-zone probes, materializes IAM-user operational `aws.*` because cert-manager Route 53 DNS01 credentials do not support an STS session-token field, destroys validation-owned per-run stacks when the targeted suite may provision them, and clears operational `aws.*` before returning |
| Static and doctrine gate | `prodbox dev check` |

## Authority and State Locations

| State Class | Authority | Durable Home | Notes |
|-------------|-----------|--------------|-------|
| Managed-resource definitions | `Prodbox.Lifecycle.ResourceClass`, `Prodbox.Lifecycle.ResourceRegistry`, stack descriptors, and typed resource modules | In-code lifecycle-class and resource facts plus external observation/reconcile boundaries | SSoT for everything prodbox can create: lifecycle class controls cleanup independently from preparation. `ResourceRegistry` preserves `reconcileAbsent` teardown and exposes optional `resourceEnsureCommand` / `resourceEnsurePresent` fields plus `desiredPresentManagedResources = [awsSesPulumiResource]`. `ResidueStatus` and `DesiredPresence` implement separate flat presence/checkpoint observations, exhaustive fail-closed planning, enactment, and mandatory re-observation. The fixed `operational-aws-ses-lease-role` is registered before its trusted IAM user in teardown order, with typed observe/ensure/delete and authoritative postcondition checks. |
| Repository configuration | Operator-authored binary-sibling Dhall | `prodbox.dhall` | Written by `prodbox config setup`, ignored from VCS, decoded directly into Haskell types; production-safe topology, the Tier-0 basics, and `SecretRef` references (Vault/TransitKey/Prompt) only — no plaintext secrets and no `aws_admin_for_test_simulation` block. The shared IAM validation harness simulates the operator's elevated-credential prompt from the `aws_admin_for_test_simulation.*` test fixture in `test-secrets.dhall` and mints the operational `prodbox` `aws.*` credential into Vault KV, clearing it again on suite return (Sprint `7.16`). SSoT: [vault_doctrine.md § 4](../documents/engineering/vault_doctrine.md#4-config-split-production-references-vs-test-plaintext) |
| Test-only configuration fixtures | Test harness only | `test-secrets.dhall` (test-only) | All test-only plaintext (TestPlaintext) that simulates operator prompts and seeds fixtures: the Vault unlock-bundle password, `aws_admin_for_test_simulation.*`, fake ACME/EAB values, fake MinIO/Keycloak bootstrap, and Vault seed fixtures. Never imported by production config, never in Vault, never in production (Sprint `7.16`) |
| Host build artifacts | Canonical `cabal build --builddir=.build exe:prodbox` plus copy to `.build/prodbox` | `.build/prodbox` | Operator-facing binary that backs the canonical `prodbox` command surface |
| Container build artifacts | Dockerfiles under `docker/` | `/opt/build` | Canonical container build root |
| Registry image state | `registry:2` plus Haskell lifecycle reconcile | `prodbox/` repositories in the `prodbox-harbor-registry` MinIO/S3 bucket | Custom images and mirrored public images for the native host and cluster architecture only, including the Envoy Gateway target edge image set |
| CLI and doctrine source | Repository worktree | `app/`, `src/`, `documents/`, `DEVELOPMENT_PLAN/` | Version-controlled source of truth |
| Manual PV content root | Host filesystem | Configured path, default `.data/<namespace>/<StatefulSet>/<replica>`; Sprint `4.31` implements deterministic `prodbox-retained-<namespace>-<statefulset>-<ordinal>` PV names, the always-on MinIO/Vault storage inventory, and shared `storageBinding` topology for Patroni and `vscode` | PV contents only |
| Chart secrets (derived) — **retired and removed** | None | None | Sprint `3.19` deleted the gateway `/v1/secret/ensure-namespace` path, retired the derivation inventory, and removed host pre-apply of derived Secrets. Gateway event-key consumption, Patroni role materialization, and host/admin helper reads use Vault KV through Vault Kubernetes auth or the host Vault helper. |
| Chart secrets (chart-generated) — **retired and removed** | None | None | Sprint `3.18` moved direct chart consumers to Vault; Sprint `3.19` removed the surrounding generated-secret assumptions and keeps no-regression coverage for chart-template generated secrets. MinIO root, Keycloak admin/OIDC, VS Code `SecurityPolicy`, gateway event keys, and Patroni role materialization are Vault-backed. |
| Vault secret root (sole, finalized; active foundation) | In-cluster Vault (`prodbox vault ...`, Sprints `1.36`, `3.17`, `3.18`) | Durable Vault PV under `.data/vault/vault/0` | Active foundation: the **sole, finalized** fail-closed per-cluster root authority for secrets / KMS (Vault Transit) / PKI. Every secret/credential/key/cert is a Vault object — KV v2, Transit, or PKI — with no second store and no plaintext fallback; the master-seed derivation model is retired (Sprint `3.19`) and `FileSecret` / Secret-mounted Dhall is removed. Production config carries only typed `Prodbox.Settings.SecretRef.Vault` values (never plaintext; no `FileSecret` arm). Landed pieces include the Vault command/client/reconcile surface, shared Vault chart platform install on both substrates, the Vault-Transit `DekCipher`, the Sprint `3.18` typed chart-secret policy/role inventory, Kubernetes-auth backend config, Vault TokenReview RBAC, live generated/static seed bootstrap in `Prodbox.Secret.VaultInventory`, direct websocket OIDC `SecretRef.Vault` app-side consumption, Keycloak / MinIO chart Vault-login init-container consumption for their covered runtime fields, MinIO admin bootstrap Job root credentials through Vault-login init containers, VS Code Envoy `SecurityPolicy` client-Secret materialization from Vault, gateway event/AWS/MinIO credentials resolved through Vault Kubernetes auth, Patroni role Secret materialization from Vault, host/admin helper plus AWS SES SMTP Vault reads/writes, structural sealed-startup fail-closed proof for the migrated materializers, the Sprint `3.20` transit-seal hierarchy, the Sprint `4.29` lifecycle + durable PV preservation, the Sprint `4.33` sealed-state gate/redaction audit, the Sprint `5.8` `sealed-vault` named validation plus pure forbidden-pattern oracle and generated Dhall/config SecretRef sweep, and the Sprint `7.14` encrypted Pulumi wrapper/read/migration/provider path. The home live sealed-Vault proof passed in Sprint `5.8` with the encrypted wrapper/provider path in place. AWS-substrate and parent/child federation variants remain non-blocking proof axes under Standards N/O and are tracked in [substrates.md](substrates.md). See [vault_doctrine.md § 2](../documents/engineering/vault_doctrine.md#2-the-fail-closed-invariant) and [vault_doctrine.md § 3](../documents/engineering/vault_doctrine.md#3-the-secretref-model) |
| Vault chart-secret auth inventory (active foundation) | `Prodbox.Secret.VaultInventory` + `Prodbox.Vault.Reconcile` (Sprint `3.18`) | Vault Kubernetes-auth config, policy/role definitions, generated/static seed KV objects reconciled into the in-cluster Vault, daemon-mediated operator-owned writes when the `prodbox-operator-write` JWT is mintable, and explicit host-root-token seams only before that service account exists or in test fixtures; service accounts rendered by supported charts | Active foundation: the chart-secret inventory enumerates KV v2 paths, least-privilege policies, Kubernetes service accounts, and Vault Kubernetes-auth roles for Keycloak, Patroni, OIDC, SMTP, gateway event keys, gateway AWS/MinIO credentials, and MinIO root credentials. It also defines the read-before-write seed-object plan and wires the automatically managed generated/static objects into `prodbox vault reconcile` with a real 32-byte random, base64url-unpadded generator, while excluding externally-owned material such as SMTP credentials from automatic seeding. `prodbox vault reconcile` configures `auth/kubernetes/config` against `https://kubernetes.default.svc:443`, writes the config-owned `secret/gateway/gateway/aws` object when operational `aws.*` is present, and the Vault chart binds its service account to `system:auth-delegator` for TokenReview. The `api`, `keycloak`, `minio`, `vault`, `vscode`, and `websocket` charts render explicit service accounts; `charts/websocket` renders `oidc.client_secret` as `SecretRef.Vault` for `secret/data/vscode/oidc/prodbox-websocket`, and `Prodbox.Workload` logs in through Vault Kubernetes auth before starting the WebSocket runtime. The `keycloak` chart materializes admin, Patroni app-role, OIDC, demo-user, and SMTP fields through a Vault-login init container; the `minio` chart materializes root credential files through the same pattern; the MinIO admin bootstrap Jobs use the `minio` service account plus a Vault-login init container for root credential files; the `vscode` chart materializes Envoy's required `SecurityPolicy` client Secret from Vault with a dedicated materializer ServiceAccount and namespace-local Secret RBAC; the `gateway` chart renders event keys plus AWS/MinIO credentials as `SecretRef.Vault` while the gateway MinIO bootstrap Job logs into Vault as `gateway-minio-bootstrap`; the `keycloak-postgres` chart materializes Percona-watched Patroni role Secrets from Vault with the `prodbox-<namespace>-pg` pre-install hook; remaining host/admin helper reads for admin, OIDC, demo-user, and SMTP Vault fields are tracked for daemon-mediated replacement in [legacy-tracking-for-deletion.md](legacy-tracking-for-deletion.md); unit tests pin the fail-closed startup structure for sealed/unreachable Vaults; and Sprint `3.19` removed the legacy derivation/RPC machinery around it. |
| Cluster-federation custody (active gateway surface) | Parent cluster's in-cluster Vault KV (`prodbox` federation surface, Sprint `2.26`) | Parent cluster's durable Vault PV under `.data/vault/vault/0` | Intended structure: prodbox manages a hierarchy of clusters as a Vault transit-seal trust tree — a root cluster (Shamir seal, operator-unsealed via the password-AEAD unlock bundle in durable MinIO, with target host handoff through the daemon NodePort) and zero or more child clusters (`seal "transit"` against the parent). Each parent's Vault KV owns its children's init keys (recovery keys + initial root token); a cluster's downstream-cluster inventory (identities, endpoints, kubeconfigs, account IDs, Pulumi stacks) is secret data legible only behind an unsealed Vault, so the fail-closed brick cascades down the tree from the root. Sprint `2.26` has the typed custody foundation, native `prodbox cluster federation register <child>` surface, full downstream-inventory metadata, parent child-index/bootstrap KV objects, and Vault-backed gateway child-listing / bootstrap-reference endpoints; Sprint `3.20` has the root Shamir / child Transit seal model, chart `seal "transit"` rendering, and child init-custody field shape; Sprint `4.32` has the direct parent-side live registration writer and child lifecycle interpreter; Sprint `4.33` has the Haskell-side opaque namespace audit, sealed-state readiness gate, and token-bearing redaction surface. Live sealed-federation validation remains Sprint `5.8`. See [cluster_federation_doctrine.md](../documents/engineering/cluster_federation_doctrine.md) |
| Vault unlock bundle | Operator recovery material (Tier 1; MinIO-only, Sprint `7.25`) | In the durable MinIO bucket ONLY (Sprint `7.25`; host disk holds no unseal material — a non-secret `.cluster-established` marker is the only on-disk artifact) | Implemented by Sprint `1.36`: Argon2id/ChaCha20-Poly1305 authenticated-encryption bundle holding the unseal/recovery material consumed by `prodbox vault unseal` through the daemon-mediated path; rotated by `prodbox vault rotate-unlock-bundle`. Never a raw SHA-256 artifact. This is the **Tier 1** bootstrap secret ([config_doctrine.md §0](../documents/engineering/config_doctrine.md#0-three-tier-config-model)); Sprint `7.25` relocated it off host disk **entirely** into the durable MinIO bucket (MinIO-only, disk-free), read via the STATIC MinIO root credential; MinIO is reordered ahead of Vault to serve it pre-unseal. See [vault_doctrine.md § 6](../documents/engineering/vault_doctrine.md#6-the-unlock-bundle-root-cluster) and [§ 6.1](../documents/engineering/vault_doctrine.md#61-bootstrap-minio-credential) |
| AWS substrate per-run state | Haskell Pulumi orchestration via `Prodbox.Pulumi.EncryptedBackend` | Opaque Model-B objects in the MinIO `prodbox-state` bucket on MinIO's PV under `.data/prodbox/minio/0`; first-touch migration imports any legacy raw checkpoint before supported writes continue encrypted | Pulumi stack state for `aws-eks`, `aws-eks-subzone`, `aws-test` |
| Long-lived `aws-ses` checkpoint authority | `Prodbox.Lifecycle.CheckpointAuthority.LongLivedCheckpointAuthority`, rooted at the retained home/control-plane gateway, `prodbox-state`, and its Vault Transit/HMAC keyspace; `CheckpointAuthorityStore.gatewayModelBCasAdapter` binds it to the gateway opaque-CAS API | Opaque Model-B object in the retained control-plane MinIO store; the configured long-lived S3 store is retained public-edge TLS storage and an optional legacy first-touch SES source, not the primary checkpoint | The authority boundary is non-interchangeable with `TargetClusterSecretSink`, the selected-substrate Vault KV destination. It provides missing/observed/corrupt/unobservable observation and initialize/replace CAS for leases, checkpoints, and target intents. The canonical `aws-ses` flow uses this authority, retains released-predecessor recovery context, resolves late same- or cross-sink writes after bounded grace/read-back, and refuses an unsafe legacy released projection. |
| Local RKE2 host state | Host lifecycle commands | RKE2 data dirs, kubeconfig files, systemd state | Deleted by `prodbox cluster delete --yes` except retained roots, with expected-absence cleanup rendered as normal delete disposition and Sprint `4.8` keeping successful uninstall runs hermetic through the lifecycle-local quiet path |
| Remote EKS AWS test stack | Pulumi plus Haskell orchestration | EKS resources in AWS | Created and destroyed only through named `prodbox aws stack eks` surfaces |
| Remote HA RKE2 AWS test stack | Pulumi plus Haskell orchestration | EC2 and supporting AWS resources | Exactly three Ubuntu 24.04 EC2 instances in separate AZs |
| Cluster resource state | Kubernetes | RKE2 datastore | Managed through canonical Haskell CLI flows |
| DNS ownership | AWS Route 53 | Hosted zone records | One canonical `test.resolvefintech.com` record only; wildcard public DNS and per-service public hostnames are unsupported |
| Pulumi backend state | Local-cluster MinIO plus Vault Transit object-store wrapper | One generically-named bucket, `prodbox-state` (the role-revealing `prodbox-test-pulumi-backends` name is retired, Sprint `4.30`) | The local cluster must exist before remote AWS resources are created; backend validation recreates a deleted MinIO export host path and restarts `statefulset/minio` before retrying login. Sprint `7.14` hydrates each stack via a decrypt-to-scratch RAM-tmpfs Pulumi interposition, imports legacy raw checkpoint state on first touch when needed, and re-envelopes stack state through the Model-B object-store. See [vault_doctrine.md § 9](../documents/engineering/vault_doctrine.md#9-minio-as-a-ciphertext-store) and [vault_doctrine.md § 10](../documents/engineering/vault_doctrine.md#10-pulumi-backend-under-vault) |
| MinIO as a Model-B ciphertext store | In-cluster Vault Transit, bound by host and daemon accessors through a shared pure object layer | One generically-named bucket on the MinIO PV under `.data/prodbox/minio/0`: `prodbox-state` | Sprint `4.30` implements the shared object-store foundation: logical objects envelope via Vault Transit (`prodbox-envelope-v2`, hashed stored AAD), names are `objects/<vault-keyed-HMAC>.enc`, the encrypted index payload shape and decoy key pool are unit-tested, the object-store HMAC key is Vault-owned at `secret/object-store/hmac`, and the production in-force-config read goes through this opaque key. Sprint `4.33` gates Haskell residue/listing translators and the retained-object `NoSuchKey` classifier behind Vault readiness and redacts token-bearing debug rendering. Sprint `7.14` routes Pulumi stack cycles and production stack reads through the same object-store wrapper; the home whole-system sealed-state validation passed in Sprint `5.8`, with AWS/federation variants tracked separately. See [vault_doctrine.md § 8](../documents/engineering/vault_doctrine.md#8-envelope-encryption-with-vault-transit) and [vault_doctrine.md § 9](../documents/engineering/vault_doctrine.md#9-minio-as-a-ciphertext-store) |
| Certificate material | Kubernetes plus long-lived S3 retention store | Secrets issued by cert-manager; the public-edge certificate is a `LongLived` registry-managed resource retained in the long-lived `pulumi_state_backend` S3 bucket | The ACME server URL comes from repository config (`acme.server`, the ZeroSSL directory), ZeroSSL EAB material is projected into `cert-manager/acme-eab-credentials`, and the target public edge attaches the issued Secret to Gateway listeners. The public-edge certificate is issued once and (Sprints `4.24`/`7.11`/`8.7`) retained under a substrate-scoped key (`public-edge-tls/<substrate>/<fqdn>`) in the long-lived S3 bucket via `src/Prodbox/Infra/LongLivedPulumiBackend.hs`, restored before every issuance on all rebuild paths so the high-churn loop never re-orders it, and registered as a `LongLived` managed resource removed only by `prodbox nuke` (superseding the in-cluster `prodbox/public-edge-tls-retained` copy) |
| Gateway continuity state | `GatewayContinuityAuthority` plus bounded semantic gateway state, Vault KV event keys/admission markers, and explicit credential capability | One opaque Model-B object per emitter at logical `continuity/<emitter>` in retained `prodbox-state`; each authenticated record binds the Orders/emitter scope, one committed fixed-width epoch/sequence/digest anchor, and at most one exact staged signed assertion plus next anchor. The hot semantic projection, bounded replay/checkpoint evidence, and fixed 64-hash diagnostic tail are separate and no logical audit history is retained. | Every local publication/rotation conditionally stages and durably re-observes the exact assertion/next anchor before emission; `Missing` after durable admission, `Corrupt`, or `Unobservable` refuses emitter/claim/DNS authority. `secret/prodbox/gateway/continuity-admission/<node>` distinguishes one-time initialization from lost state. A simultaneous peer restart recovers safe emitter anchors from retained authority, then repairs current heartbeat/ownership evidence through bounded peer snapshots when peers reconnect; the authority is not misrepresented as a full semantic checkpoint set. No gateway continuity state is derived from a master seed. |
| Ephemeral elevated/admin AWS credential | Interactive `SecretRef.Prompt` (operator) or harness-simulated prompt | In memory for one command only — never persisted | The privileged AWS credential a human operator holds (temporary IAM admin key). Enters prodbox only through the prompt, used once to mint the dedicated least-privilege `prodbox` IAM identity, then discarded. Never written to production config, never stored in Vault, never on disk. Every elevated flow — `config setup`, `aws setup`, the native IAM harness, `aws-ses` stack ops, `prodbox nuke` — unifies on this prompt (Sprint `7.16`) |
| Generated operational `prodbox` `aws.*` credential | `prodbox vault reconcile` mints it after Vault is unsealed | Vault KV (`secret/gateway/gateway/aws`) | The least-privilege dedicated identity minted using the ephemeral elevated credential; written straight into Vault KV the instant it is created, never in cleartext. Production config carries only a `SecretRef.Vault` reference to it, never the plaintext key (Sprint `7.16`) |
| Test-simulation admin credential (`aws_admin_for_test_simulation.*`) | Test harness only — suite-level IAM harness | `test-secrets.dhall` (test-only, TestPlaintext) | Test-harness-only fixture whose sole purpose is to simulate the operator's elevated-credential prompt so the harness can drive admin-credentialed flows non-interactively. Never imported by production config, never read by a production binary, never in Vault, never copied into generated cluster config or MinIO. SSoT: [aws_admin_credentials.md](../documents/engineering/aws_admin_credentials.md) (Sprint `7.16`) |

## Artifact Locations

| Type | Location | Purpose |
|------|----------|---------|
| Haskell application entrypoint | `app/prodbox/Main.hs` | Main CLI binary entrypoint |
| Haskell source modules | `src/Prodbox/` | CLI, infra, gateway, settings, and library implementation. Subtrees: `CLI/` (parser, command ADT, per-command surfaces including `Nuke.hs` and `Users.hs`), `Daemon/` (long-running daemon helpers + at-least-once events), `Gateway/` (daemon, client, peer, types, logging), `Http/` (native HTTP client), `Infra/` (Pulumi-backed AWS stack orchestration including `AwsSesStack.hs`, `AwsEksSubzoneStack.hs`, `LongLivedPulumiBackend.hs`, `MinioBackend.hs`, `StackOutputs.hs` for encrypted stack list/output reads with legacy raw helpers retained for migration), `Pulumi/` (`EncryptedBackend.hs` for the decrypt-to-scratch Model-B stack wrapper), `Keycloak/` (admin client, email parser, credential-setup form parser), `Lib/` (chart platform plus substrate-aware helpers), `Lifecycle/` (`Preconditions.hs`, `K8sDrain.hs`, `TagSweep.hs`, `EbsVolume.hs`, `ResidueStatus.hs`, `LiveResidue.hs` reconciler-with-predicates building blocks, plus the `ResourceClass.hs` facts and `ResourceRegistry.hs` managed-resource registry + `reconcileAbsent` reconciler), `Secret/` (`VaultInventory.hs` for Vault KV path/policy/service-account inventory; the old derivation/master-seed/wire modules are removed), `Vault/` (Vault client, reconcile, host helper, unlock-bundle, Transit cipher, orchestration), `Ses/` (SMTP password derivation + capture-bucket polling). |
| Haskell tests | `test/` | Unit plus native CLI/env integration validation suites |
| Repository config artifacts | Operator-authored, git-ignored binary-sibling `prodbox.dhall` plus the generated, git-ignored `prodbox-config-types.dhall` / `test-secrets-types.dhall` schemas. Sprints `1.58`/`1.59` regenerate `prodbox-config-types.dhall` after the component-ID/probe split and final `ProbeServiceActive`/edge corrections; the file remains ignored and uncommitted. All `.dhall` is generated or locally authored and none is version-controlled. See [config_doctrine.md §0](../documents/engineering/config_doctrine.md#0-three-tier-config-model) | Haskell-owned config source and shared schema |
| Cabal package definition | `prodbox.cabal` | Build, test, and dependency definition |
| Cabal project definition | `cabal.project` | Repository-wide Cabal package-set definition |
| Host build root | `.build/` | Canonical host-side Haskell build artifacts; operator binary at `.build/prodbox` |
| Container build root | `/opt/build` | Canonical container-side Haskell build artifacts |
| Canonical custom Dockerfiles | `docker/` | Authoritative home for repository-owned container builds |
| Union runtime container build | `docker/prodbox.Dockerfile` | One image (`prodbox/prodbox-runtime`) for every in-cluster role (gateway daemon + `api`/`websocket` workloads), owned by Phase `1` (consolidated from the former separate gateway image, Phase `2`); single-stage `ubuntu:24.04` with in-image `ghcup`, pinned GHC `9.12.4`, no symlinked Haskell tool shims, `tini` as PID 1, the official AWS CLI bundle keyed by the native Debian host architecture, and a bare `tini -- prodbox` ENTRYPOINT (role chosen by each chart's `args:`) |
| Pulumi definitions | `pulumi/aws-eks/{Pulumi,Main}.yaml`, `pulumi/aws-eks-subzone/{Pulumi,Main}.yaml`, `pulumi/aws-test/{Pulumi,Main}.yaml`, `pulumi/aws-ses/{Pulumi,Main}.yaml` | YAML Pulumi stacks (`runtime: yaml`) for the public AWS substrate resources. `aws-eks`, `aws-eks-subzone`, and `aws-test` are per-run; `aws-ses` is long-lived cross-substrate shared infrastructure (see [substrates.md → Resource Lifecycle Classes](substrates.md#resource-lifecycle-classes)). |
| Engineering doctrine | `documents/engineering/` (governed docs index at [documents/engineering/README.md](../documents/engineering/README.md)) | Architecture and operator docs: `cli_command_surface.md`, `pure_fp_standards.md`, `haskell_code_guide.md`, `code_quality.md`, `dependency_management.md`, `helm_chart_platform_doctrine.md`, `distributed_gateway_architecture.md`, `envoy_gateway_edge_doctrine.md`, `lifecycle_reconciliation_doctrine.md`, `vault_doctrine.md`, `cluster_federation_doctrine.md`, `secret_derivation_doctrine.md`, `storage_lifecycle_doctrine.md`, `prerequisite_doctrine.md`, `prerequisite_dag_system.md`, `effectful_dag_architecture.md`, `effect_interpreter.md`, `streaming_doctrine.md`, `integration_fixture_doctrine.md`, `unit_testing_policy.md`, `refactoring_patterns.md`, `tla_modelling_assumptions.md`, `local_registry_pipeline.md`, `aws_account_setup_guide.md`, `aws_admin_credentials.md`, `aws_integration_environment_doctrine.md`, `aws_test_environment.md`, `acme_provider_guide.md` |
| Development plan | `DEVELOPMENT_PLAN/` | Status, sequencing, and cleanup ownership |
| Manual PV content root | Configured path, default `.data/` | PV contents only |
| Master seed in MinIO — **retired and removed** | None | Sprint `3.19` removed the HMAC-derivation model outright: there is no `master-seed` object in MinIO and no code path reads or writes it. Sprint `4.30` adds the Model-B object-store foundation for remaining prodbox-owned state, with the in-force config read through an opaque `objects/<vault-keyed-HMAC>.enc` key in `prodbox-state`. Sprint `7.14` routes Pulumi stack state through the same encrypted object-store wrapper and imports legacy raw checkpoints on first touch. See [vault_doctrine.md § 9](../documents/engineering/vault_doctrine.md#9-minio-as-a-ciphertext-store) |
| Vault durable PV root | Configured host path under `.data/vault/vault/0` | Durable home for the in-cluster Vault backend, preserved across cluster wipes alongside the MinIO PV whenever `.data/` is preserved (Vault platform under Sprint `3.17`; lifecycle + PV preservation under Sprint `4.29`). See [vault_doctrine.md § 5](../documents/engineering/vault_doctrine.md#5-vault-deployment-model) |
| Vault unlock bundle | In the durable MinIO bucket ONLY (Sprint `7.25`; host disk holds no unseal material — a non-secret `.cluster-established` marker is the only on-disk artifact) | Implemented by Sprint `1.36`: operator-gated Argon2id/ChaCha20-Poly1305 authenticated-encryption recovery material consumed by `prodbox vault unseal` through the daemon-mediated path and rotated by `prodbox vault rotate-unlock-bundle`. This is the **Tier 1** bootstrap secret ([config_doctrine.md §0](../documents/engineering/config_doctrine.md#0-three-tier-config-model)); Sprint `7.25` relocated it off host disk **entirely** into the durable MinIO bucket (MinIO-only, disk-free), read via the STATIC MinIO root credential; MinIO is reordered ahead of Vault to serve it pre-unseal. See [vault_doctrine.md § 6](../documents/engineering/vault_doctrine.md#6-the-unlock-bundle-root-cluster) and [§ 6.1](../documents/engineering/vault_doctrine.md#61-bootstrap-minio-credential) |
| AWS substrate per-run Pulumi state | MinIO bucket `prodbox-state`; Sprint `7.14` stores main-cycle checkpoints as opaque Model-B `objects/<id>.enc` objects via the decrypt-to-scratch Pulumi interposition and imports legacy raw checkpoints on first touch | Pulumi stack state for `aws-eks`, `aws-eks-subzone`, `aws-test`; HA-RKE2 SSH key fetched on demand from the encrypted scratch backend via `pulumi stack output --show-secrets` into `mktemp` |

## Related Documents

- [00-overview.md](00-overview.md)
- [substrates.md](substrates.md)
- [phase-1-runtime-cli-aws-foundations.md](phase-1-runtime-cli-aws-foundations.md)
- [phase-2-gateway-dns.md](phase-2-gateway-dns.md)
- [phase-3-chart-platform-vscode.md](phase-3-chart-platform-vscode.md)
- [phase-4-lifecycle-canonical-paths.md](phase-4-lifecycle-canonical-paths.md)
- [phase-5-canonical-test-suite.md](phase-5-canonical-test-suite.md)
- [phase-6-clean-room-handoff.md](phase-6-clean-room-handoff.md)
- [phase-7-aws-substrate-foundations.md](phase-7-aws-substrate-foundations.md)
- [phase-8-email-invite-auth.md](phase-8-email-invite-auth.md)
- [legacy-tracking-for-deletion.md](legacy-tracking-for-deletion.md)
- [vault_doctrine.md](../documents/engineering/vault_doctrine.md)
- [cluster_federation_doctrine.md](../documents/engineering/cluster_federation_doctrine.md)
