{{- /*
Sprint 3.18: this template intentionally renders nothing.

Keycloak's admin password, DB password, OIDC client secrets, demo-user
password, and SMTP settings are read from Vault KV by the Pod's Vault-login
init container. The chart does not render a Kubernetes Secret or use Helm
`lookup` as a fallback.
*/ -}}
